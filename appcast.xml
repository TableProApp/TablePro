<?xml version="1.0" standalone="yes"?>
<rss xmlns:sparkle="http://www.andymatuschak.org/xml-namespaces/sparkle" version="2.0">
    <channel>
        <title>TablePro</title>
        <item>
            <title>0.71.0</title>
            <pubDate>Wed, 02 Sep 2026 08:12:36 +0000</pubDate>
            <sparkle:version>127</sparkle:version>
            <sparkle:shortVersionString>0.71.0</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <sparkle:hardwareRequirements>arm64</sparkle:hardwareRequirements>
            <description><![CDATA[<h3>Added</h3>
<ul>
<li>Comparisons button in the Compare & Sync toolbar, listing every saved comparison.</li>
<li>Save Comparison… in the Compare & Sync toolbar and under Database > Compare.</li>
<li>Select > All and Select > None in the structure results pane.</li>
<li>Whole-schema index and table metadata reads on the driver protocol.</li>
<li>Script that checks the SQLite whole-schema reads against the per-table ones.</li>
<li>Middle-click on a tab to close it. (#2595)</li>
<li>Text in Display As for binary columns. (#2599)</li>
<li>Smart value detection for binary columns holding UTF-8 text. (#2599)</li>
<li>Find covering binary columns that a display format renders as text. (#2599)</li>
<li>Launch trace in Instruments' Points of Interest, and `TABLEPRO_LAUNCH_TRACE=1` for the same table on standard error.</li>
<li>Identity File on SSH Agent connections, and `IdentitiesOnly` from `~/.ssh/config`. (#2601)</li>
</ul>
<h3>Changed</h3>
<ul>
<li>Compare & Sync reads a whole schema in a few queries, and reads both sides at once.</li>
<li>Data mode lists the tables both sides share as soon as a pair is chosen.</li>
<li>Apply… builds the script when there is not one.</li>
<li>Saved comparisons set both endpoints, and list whatever pair is on screen.</li>
<li>Compare & Sync reopens on the source, target, mode and options it last held.</li>
<li>Table collation on MySQL, previously never read.</li>
<li>PluginKit ABI 20. Every registry plugin needs rebuilding before or with this release.</li>
<li>Cold launch to a usable window, 470ms down to 260ms.</li>
<li>Half-second grace before a connect, schema or query progress indicator appears, and a minimum time on screen.</li>
<li>Window chrome stays put through a connect that finishes inside that grace, instead of collapsing and reopening.</li>
<li>Plugin signature checks run after the first window rather than on the launch thread, at 13ms each.</li>
<li>One gate in front of every path that loads a plugin's executable, enabling one included.</li>
<li>Stale `cloudflared` and `cloud-sql-proxy` cleanup waits for the process to exit before a connection reuses its port.</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>Generated column expressions missing from MySQL's whole-schema column read.</li>
<li>Generated columns missing entirely from SQLite's whole-schema column read.</li>
<li>PostgreSQL index reads matching a table name in every schema rather than the one asked for.</li>
<li>Unreachable hazard allowances in the Apply sheet.</li>
<li>Compare & Sync toolbar naming the old pair after a saved comparison was loaded from Options.</li>
<li>A failed whole-schema trigger read counting as a schema with no triggers.</li>
<li>Compare & Sync publishing one pair's results after the pickers moved to another.</li>
<li>Clicks and hovers in the scrolled tab strip's edge padding landing on a tab clipped off the edge.</li>
<li>SSH Agent auth offering every key the agent holds, exhausting the server's `MaxAuthTries`. (#2601)</li>
<li>Crash when opening CSV and TSV files on macOS 27. (#2600)</li>
</ul>
]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.71.0/TablePro-0.71.0-arm64.zip" length="26576647" type="application/octet-stream" sparkle:edSignature="9edfU/v2/S/X+E5NdDNVrE9TyzKxtlKBBwlmsW4zD4fm6QC34zQ/8lO3phNBgzLeL5P8+Kg85s/AAeG+Fl09CQ=="/>
        </item>
        <item>
            <title>0.71.0</title>
            <pubDate>Wed, 02 Sep 2026 08:12:37 +0000</pubDate>
            <sparkle:version>127</sparkle:version>
            <sparkle:shortVersionString>0.71.0</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <description><![CDATA[<h3>Added</h3>
<ul>
<li>Comparisons button in the Compare & Sync toolbar, listing every saved comparison.</li>
<li>Save Comparison… in the Compare & Sync toolbar and under Database > Compare.</li>
<li>Select > All and Select > None in the structure results pane.</li>
<li>Whole-schema index and table metadata reads on the driver protocol.</li>
<li>Script that checks the SQLite whole-schema reads against the per-table ones.</li>
<li>Middle-click on a tab to close it. (#2595)</li>
<li>Text in Display As for binary columns. (#2599)</li>
<li>Smart value detection for binary columns holding UTF-8 text. (#2599)</li>
<li>Find covering binary columns that a display format renders as text. (#2599)</li>
<li>Launch trace in Instruments' Points of Interest, and `TABLEPRO_LAUNCH_TRACE=1` for the same table on standard error.</li>
<li>Identity File on SSH Agent connections, and `IdentitiesOnly` from `~/.ssh/config`. (#2601)</li>
</ul>
<h3>Changed</h3>
<ul>
<li>Compare & Sync reads a whole schema in a few queries, and reads both sides at once.</li>
<li>Data mode lists the tables both sides share as soon as a pair is chosen.</li>
<li>Apply… builds the script when there is not one.</li>
<li>Saved comparisons set both endpoints, and list whatever pair is on screen.</li>
<li>Compare & Sync reopens on the source, target, mode and options it last held.</li>
<li>Table collation on MySQL, previously never read.</li>
<li>PluginKit ABI 20. Every registry plugin needs rebuilding before or with this release.</li>
<li>Cold launch to a usable window, 470ms down to 260ms.</li>
<li>Half-second grace before a connect, schema or query progress indicator appears, and a minimum time on screen.</li>
<li>Window chrome stays put through a connect that finishes inside that grace, instead of collapsing and reopening.</li>
<li>Plugin signature checks run after the first window rather than on the launch thread, at 13ms each.</li>
<li>One gate in front of every path that loads a plugin's executable, enabling one included.</li>
<li>Stale `cloudflared` and `cloud-sql-proxy` cleanup waits for the process to exit before a connection reuses its port.</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>Generated column expressions missing from MySQL's whole-schema column read.</li>
<li>Generated columns missing entirely from SQLite's whole-schema column read.</li>
<li>PostgreSQL index reads matching a table name in every schema rather than the one asked for.</li>
<li>Unreachable hazard allowances in the Apply sheet.</li>
<li>Compare & Sync toolbar naming the old pair after a saved comparison was loaded from Options.</li>
<li>A failed whole-schema trigger read counting as a schema with no triggers.</li>
<li>Compare & Sync publishing one pair's results after the pickers moved to another.</li>
<li>Clicks and hovers in the scrolled tab strip's edge padding landing on a tab clipped off the edge.</li>
<li>SSH Agent auth offering every key the agent holds, exhausting the server's `MaxAuthTries`. (#2601)</li>
<li>Crash when opening CSV and TSV files on macOS 27. (#2600)</li>
</ul>
]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.71.0/TablePro-0.71.0-x86_64.zip" length="28345573" type="application/octet-stream" sparkle:edSignature="5J3dB3iolOMQ7DGdOXq14pYlvKUgzv/Ug3ZdBI0AFXY6dob28j8j5MawZfo1sQKxRl28kEvoIK2xIcMrcALuAQ=="/>
        </item>
        <item>
            <title>0.70.0</title>
            <pubDate>Tue, 01 Sep 2026 17:01:09 +0000</pubDate>
            <sparkle:version>126</sparkle:version>
            <sparkle:shortVersionString>0.70.0</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <sparkle:hardwareRequirements>arm64</sparkle:hardwareRequirements>
            <description><![CDATA[<h3>Added</h3>
<ul>
<li>JSON tab in the inspector, showing the selected row as JSON, with Show Row as JSON on a row's right-click menu.</li>
<li>Foreign key expansion in the JSON tab, fetching the referenced row on click, five levels deep.</li>
<li>Filter field in the JSON tab, taking text or a regular expression in slashes.</li>
<li>Always Expand Foreign Keys in the JSON tab, off until turned on.</li>
<li>JavaScript shell for MongoDB queries, with mongosh's `db` API, cursors, variables, functions and `print`.</li>
<li>Per-connection MongoDB shell state, carried from one statement to the next.</li>
<li>Cursor method autocomplete after `find()` and `aggregate()`.</li>
<li>Copy To and Duplicate Database, carrying structure, data or both to any connection. (#2487)</li>
<li>Search in the connection, database and schema picker that Copy To and Compare & Sync share. (#2487)</li>
<li>Move Column Up and Move Column Down on a column's right-click menu, with the reason where the engine cannot. (#2479)</li>
<li>Copy on a column's right-click menu in Structure, for the cell under the pointer.</li>
<li>`Up` and `Down` while editing a cell, moving the editor to the same column of the row above or below. (#2569)</li>
<li>Tab rows in Settings > General > Tabs, wrapping the strip instead of scrolling it. (#2438)</li>
<li>Autoscrolling while dragging a tab past the end of the strip. (#2438)</li>
<li>Move Tab to New Window on a tab's right-click menu, and by dragging a tab out of the strip. (#2438)</li>
<li>Column reorder by dragging on ClickHouse and Oracle. (#2479)</li>
<li>Column reorder on PostgreSQL, SQLite, libSQL, Turso and Cloudflare D1, through a previewed table rebuild. (#2479)</li>
<li>Recognition of SQLite and DuckDB databases by their contents, whatever they are named. (#2476)</li>
<li>`.parquet` files in Finder's Open With, read through DuckDB. (#2476)</li>
<li>Prompt to install the driver a file needs, before the file opens. (#2476)</li>
<li>Korean, Turkish, Vietnamese and Chinese for the 742 untranslated strings, and for the document kinds Finder shows.</li>
</ul>
<h3>Changed</h3>
<ul>
<li>MongoDB statements split as JavaScript rather than at every semicolon.</li>
<li>MongoDB editor diagnostics report JavaScript syntax errors rather than unsupported method names.</li>
<li>Editor tab presses handled by AppKit rather than SwiftUI gestures. (#2438)</li>
<li>Connection-first labels with the database or schema on a second line in the connections strip. (#2550)</li>
<li>Column reorder withheld, with the reason on the row number, where the engine cannot change column order. (#2479)</li>
<li>File > Open File… as an app command over every file TablePro reads. (#2476)</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>Row inspector lag on hover, on a tab switch and on every keystroke, from re-parsing each field's value.</li>
<li>The data grid's row commands on a column's right-click menu in Structure, when the column was already selected.</li>
<li>Wrong keyboard shortcuts shown beside Copy Name and Duplicate in the Structure right-click menu.</li>
<li>Plugin download reporting no progress at all when a connection or a file needs a driver installed.</li>
<li>Parse error on any MongoDB filter written in shell syntax, such as `db.orders.find({status: 1})`.</li>
<li>MongoDB `.sort()` and `.projection()` silently ignored when written with unquoted keys.</li>
<li>Tab drag doing nothing, about one drag in seven. (#2438)</li>
<li>Tab drag released on a neighbour's exact centre leaving the order unchanged. (#2438)</li>
<li>Compare & Sync unable to drop an overloaded PostgreSQL routine, or any trigger.</li>
<li>PostgreSQL sequence DDL naming the schema it was read from, in SQL export and the structure editor.</li>
<li>Half-composed input method text saved and left behind when `Tab` moved the cell editor.</li>
<li>Cell editor opening off screen when `Tab` wrapped onto a row below the visible ones.</li>
<li>Cell cursor left on the old column after `Tab` carried the editor to the next one.</li>
<li>Every data grid switching to its accessibility layout after one `Tab` press, with no assistive app attached.</li>
<li>Crash loop on every launch after resizing a column on a database with a long file path, with iCloud sync on. (#2575)</li>
<li>SSH Agent auth prompting for a private key passphrase instead of reporting that the agent was never reached. (#2583)</li>
<li>"SSH password rejected" on an SSH connection that has no password, when the server offers no keyboard-interactive.</li>
<li>NULL pre-filled into an identity column on Add Row, failing the insert on PostgreSQL. (#2588)</li>
<li>No "Default" in a cell's Set Value menu for an identity column. (#2588)</li>
<li>Duplicate Row copying an identity column that is not the primary key. (#2588)</li>
<li>Editing a `GENERATED ALWAYS AS IDENTITY` cell, which the server rejects on save. (#2588)</li>
<li>Generated and identity columns editable again after a tab switch or a refresh that reused cached metadata.</li>
<li>A new row of nothing but server-assigned columns silently dropped from the save.</li>
<li>`OVERRIDING SYSTEM VALUE` and `setval` in a SQL export of a SQL Server database.</li>
<li>PGlite `$$` bodies split at their inner semicolons, from treating PGlite as a generic SQL dialect.</li>
<li>Paste, Fill Column and the row inspector staging an edit to a column the server owns. (#2588)</li>
<li>A rerun answered from cache adopting another pinned result's column metadata. (#2588)</li>
<li>Add Row and Duplicate Row offered before a table's schema has loaded. (#2588)</li>
<li>Missing `SET IDENTITY_INSERT` around a SQL Server table's rows in a SQL export. (#2588)</li>
<li>Row inspector editing a column the driver marks immutable, such as MongoDB's `_id`.</li>
<li>Add Row and Duplicate Row inert for good on a result the user switched away from while its schema was loading. (#2588)</li>
</ul>
]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.70.0/TablePro-0.70.0-arm64.zip" length="26465725" type="application/octet-stream" sparkle:edSignature="Od7CGtgzRvl96EngRHct5xjNYQnPW/sOdRintNdWYGJgVsfEFgUVAPhhdB6GhxRyXUvdLwb1VEJJchr0jQZZAQ=="/>
        </item>
        <item>
            <title>0.70.0</title>
            <pubDate>Tue, 01 Sep 2026 17:01:10 +0000</pubDate>
            <sparkle:version>126</sparkle:version>
            <sparkle:shortVersionString>0.70.0</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <description><![CDATA[<h3>Added</h3>
<ul>
<li>JSON tab in the inspector, showing the selected row as JSON, with Show Row as JSON on a row's right-click menu.</li>
<li>Foreign key expansion in the JSON tab, fetching the referenced row on click, five levels deep.</li>
<li>Filter field in the JSON tab, taking text or a regular expression in slashes.</li>
<li>Always Expand Foreign Keys in the JSON tab, off until turned on.</li>
<li>JavaScript shell for MongoDB queries, with mongosh's `db` API, cursors, variables, functions and `print`.</li>
<li>Per-connection MongoDB shell state, carried from one statement to the next.</li>
<li>Cursor method autocomplete after `find()` and `aggregate()`.</li>
<li>Copy To and Duplicate Database, carrying structure, data or both to any connection. (#2487)</li>
<li>Search in the connection, database and schema picker that Copy To and Compare & Sync share. (#2487)</li>
<li>Move Column Up and Move Column Down on a column's right-click menu, with the reason where the engine cannot. (#2479)</li>
<li>Copy on a column's right-click menu in Structure, for the cell under the pointer.</li>
<li>`Up` and `Down` while editing a cell, moving the editor to the same column of the row above or below. (#2569)</li>
<li>Tab rows in Settings > General > Tabs, wrapping the strip instead of scrolling it. (#2438)</li>
<li>Autoscrolling while dragging a tab past the end of the strip. (#2438)</li>
<li>Move Tab to New Window on a tab's right-click menu, and by dragging a tab out of the strip. (#2438)</li>
<li>Column reorder by dragging on ClickHouse and Oracle. (#2479)</li>
<li>Column reorder on PostgreSQL, SQLite, libSQL, Turso and Cloudflare D1, through a previewed table rebuild. (#2479)</li>
<li>Recognition of SQLite and DuckDB databases by their contents, whatever they are named. (#2476)</li>
<li>`.parquet` files in Finder's Open With, read through DuckDB. (#2476)</li>
<li>Prompt to install the driver a file needs, before the file opens. (#2476)</li>
<li>Korean, Turkish, Vietnamese and Chinese for the 742 untranslated strings, and for the document kinds Finder shows.</li>
</ul>
<h3>Changed</h3>
<ul>
<li>MongoDB statements split as JavaScript rather than at every semicolon.</li>
<li>MongoDB editor diagnostics report JavaScript syntax errors rather than unsupported method names.</li>
<li>Editor tab presses handled by AppKit rather than SwiftUI gestures. (#2438)</li>
<li>Connection-first labels with the database or schema on a second line in the connections strip. (#2550)</li>
<li>Column reorder withheld, with the reason on the row number, where the engine cannot change column order. (#2479)</li>
<li>File > Open File… as an app command over every file TablePro reads. (#2476)</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>Row inspector lag on hover, on a tab switch and on every keystroke, from re-parsing each field's value.</li>
<li>The data grid's row commands on a column's right-click menu in Structure, when the column was already selected.</li>
<li>Wrong keyboard shortcuts shown beside Copy Name and Duplicate in the Structure right-click menu.</li>
<li>Plugin download reporting no progress at all when a connection or a file needs a driver installed.</li>
<li>Parse error on any MongoDB filter written in shell syntax, such as `db.orders.find({status: 1})`.</li>
<li>MongoDB `.sort()` and `.projection()` silently ignored when written with unquoted keys.</li>
<li>Tab drag doing nothing, about one drag in seven. (#2438)</li>
<li>Tab drag released on a neighbour's exact centre leaving the order unchanged. (#2438)</li>
<li>Compare & Sync unable to drop an overloaded PostgreSQL routine, or any trigger.</li>
<li>PostgreSQL sequence DDL naming the schema it was read from, in SQL export and the structure editor.</li>
<li>Half-composed input method text saved and left behind when `Tab` moved the cell editor.</li>
<li>Cell editor opening off screen when `Tab` wrapped onto a row below the visible ones.</li>
<li>Cell cursor left on the old column after `Tab` carried the editor to the next one.</li>
<li>Every data grid switching to its accessibility layout after one `Tab` press, with no assistive app attached.</li>
<li>Crash loop on every launch after resizing a column on a database with a long file path, with iCloud sync on. (#2575)</li>
<li>SSH Agent auth prompting for a private key passphrase instead of reporting that the agent was never reached. (#2583)</li>
<li>"SSH password rejected" on an SSH connection that has no password, when the server offers no keyboard-interactive.</li>
<li>NULL pre-filled into an identity column on Add Row, failing the insert on PostgreSQL. (#2588)</li>
<li>No "Default" in a cell's Set Value menu for an identity column. (#2588)</li>
<li>Duplicate Row copying an identity column that is not the primary key. (#2588)</li>
<li>Editing a `GENERATED ALWAYS AS IDENTITY` cell, which the server rejects on save. (#2588)</li>
<li>Generated and identity columns editable again after a tab switch or a refresh that reused cached metadata.</li>
<li>A new row of nothing but server-assigned columns silently dropped from the save.</li>
<li>`OVERRIDING SYSTEM VALUE` and `setval` in a SQL export of a SQL Server database.</li>
<li>PGlite `$$` bodies split at their inner semicolons, from treating PGlite as a generic SQL dialect.</li>
<li>Paste, Fill Column and the row inspector staging an edit to a column the server owns. (#2588)</li>
<li>A rerun answered from cache adopting another pinned result's column metadata. (#2588)</li>
<li>Add Row and Duplicate Row offered before a table's schema has loaded. (#2588)</li>
<li>Missing `SET IDENTITY_INSERT` around a SQL Server table's rows in a SQL export. (#2588)</li>
<li>Row inspector editing a column the driver marks immutable, such as MongoDB's `_id`.</li>
<li>Add Row and Duplicate Row inert for good on a result the user switched away from while its schema was loading. (#2588)</li>
</ul>
]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.70.0/TablePro-0.70.0-x86_64.zip" length="28218963" type="application/octet-stream" sparkle:edSignature="MSoufySbNwmSq8hfBe85bYzSRBeVG68TiYZKXe5cmYMlolpVhfQL2Z76QSavxWfdb4/Qe79uH2aHDYNDsyDFAA=="/>
        </item>
        <item>
            <title>0.69.0</title>
            <pubDate>Thu, 27 Aug 2026 16:03:07 +0000</pubDate>
            <sparkle:version>125</sparkle:version>
            <sparkle:shortVersionString>0.69.0</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <sparkle:hardwareRequirements>arm64</sparkle:hardwareRequirements>
            <description><![CDATA[<h3>Added</h3>
<ul>
<li>Restore Previous Values in the Edit menu, taking back a save that already committed. Starter license. (#2107)</li>
<li>Data Rewind settings in Settings > Data & Results, with an off switch and Clear Saved Changes.</li>
<li>Restore Previous Values in the toolbar's Table Actions group.</li>
<li>Rebindable Find shortcut in Settings > Keyboard, for giving `Cmd+F` to the filter bar instead.</li>
<li>Constraints tab in the structure editor, listing check constraints with their expression. (#2478)</li>
<li>Generated and Expression fields on the column row, with a stored or virtual choice. (#2478)</li>
<li>`check_constraints` and `generation_expression` in the MCP `describe_table` response.</li>
<li>Remote File pane for SQLite, opening a read-only copy of a database that lives on an SSH server. (#2474)</li>
<li>Rename on a table's right-click menu, editing the row's label in place. (#2482)</li>
<li>Rename Database and Rename Schema on the sidebar's container rows, where the engine has them. (#2482)</li>
<li>Turso in the New Connection picker as an engine of its own, offered before the libSQL plugin installs.</li>
</ul>
<h3>Changed</h3>
<ul>
<li>Editor tabs drawn as a segmented tab picker rather than in Liquid Glass, on every macOS version. (#2439)</li>
<li>A connection opens full screen on iPhone and iPad, with its four sections in a sidebar on iPad. (#2544)</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>Column charset and collation lost when reordering a MySQL column by dragging its row.</li>
<li>Free-text cells for foreign key actions and index types in Create Table, where Structure shows a menu.</li>
<li>A generated column comparing equal to a plain one in Compare & Sync.</li>
<li>Empty Indexes, Foreign Keys or DDL tab, with no error, when the structure read fails.</li>
<li>Connections strip absent from a window whose selected connection has nothing to show, when Show Connections is off.</li>
<li>Switch Connection disabled, in the Database menu and the toolbar, while the selected connection is disconnected.</li>
<li>One floating switcher panel per connection in a window, each centred on the same point.</li>
<li>Leaked socket on every SSH Test Connection against a server reached without jump hosts.</li>
<li>Leaked listening port, socket and session each time an SSH tunnel died from sleep or a dropped network.</li>
<li>Selection highlight missing from part of a long selection after scrolling back up to it.</li>
<li>Editor not scrolling to follow a selection extended past the edge of the viewport.</li>
<li>Find highlight and the run band covering only the first line of a match that spans several lines.</li>
<li>Nothing selected when double-clicking `=`, `<`, `>` or any other SQL operator.</li>
<li>Shift+Arrow extending the wrong end of a selection made by dragging.</li>
<li>Selection starting a few characters away from the press point on a quick drag.</li>
<li>A pause before the pointer responds when pressing inside selected text.</li>
<li>Shift+double-click and Shift+triple-click doing nothing.</li>
<li>Drag-select scrolling faster on a mouse than on a trackpad, and stalling mid-drag.</li>
<li>Statement selection with `Option+Shift+Down` leaving the highlight behind.</li>
<li>No arrow-key movement or Shift+Arrow selection in the JSON, DDL and SQL preview editors.</li>
<li>Explain with AI acting on the previous selection after a right-click somewhere else.</li>
<li>Selection painted in the accent colour in a window that is not the active one.</li>
<li>Selection bounds reported to VoiceOver covering only the first line, and no announcement when the selection moved.</li>
<li>An abandoned editor tab drag reordering the strip anyway, and keeping that order across relaunch.</li>
<li>An editor tab reorder stopping partway when the pointer strayed a couple of points off the track.</li>
<li>Text dropped on the editor tab strip reported as accepted and then discarded.</li>
<li>An editor tab left faded and the strip's separators hidden after a cancelled drag.</li>
<li>Size All Columns to Fit slowing down with the square of the column count.</li>
<li>Column-resize cursor over the leading edge of the data grid header, where there is no divider to drag.</li>
<li>A data grid shortcut bound to `Cmd+G` silently taking the key from Find Next, with no conflict warning.</li>
<li>Stale cells after fitting, hiding or reordering a data grid column on a result narrower than the window. (#2446)</li>
<li>A shortcut recorded with only Option or Shift beeping with no reason given, against docs that said it would work.</li>
<li>A rebound shortcut silently killing Quit, Minimize, Hide, Settings, Show Toolbar or Enter Full Screen.</li>
<li>`Ctrl+Cmd+J` accepted in Settings > Keyboard while the SQL editor kept it for Jump to Definition.</li>
<li>Autocomplete keeping an earlier prefix's ordering after the typed word becomes an exact match. (#2444)</li>
<li>Whole MySQL and MariaDB result set fetched before a capped query returned its first rows. (#2427)</li>
<li>KILL sent to a different server when a MySQL or MariaDB connection's host is spelled `localhost`.</li>
<li>Stop on a MySQL or MariaDB connection freezing the app for up to five seconds when the server stopped answering.</li>
<li>A MySQL or MariaDB read re-run after a dropped connection, advancing a sequence or taking a lock twice.</li>
<li>Save reporting the number of statements it ran as the number of rows it changed.</li>
<li>An edit or a delete on a table with no primary key changing every identical row. (#2107)</li>
<li>The same statements committed twice when Cmd+S is pressed again during a slow save.</li>
<li>Switching tabs during a save clearing the edits of the tab switched to, and re-running its query.</li>
<li>Placeholder SQL with no values in the Safe Mode confirmation and the authorization prompt.</li>
<li>Deleting one row of a pasted batch dropping the other rows, or saving them with another row's values.</li>
<li>Undoing the deletion of a new row putting its values in the wrong columns.</li>
<li>Undoing a cell edit reverting the wrong row while a column filter is active.</li>
<li>A save that deletes a row and reuses its unique value failing on the constraint.</li>
<li>"Ignore foreign key checks" doing nothing on SQLite, libSQL and Cloudflare D1.</li>
<li>A save reported as failed on an engine without transactions after some statements had been written.</li>
<li>Keep Open for a preview tab, by double-clicking it in the tab strip or from its contextual menu. (#2436)</li>
<li>Kafka driver plugin: topics in the grid, consumer group lag, and KafkaQL for seeking and producing. (#2419)</li>
<li>Active editor tab indistinguishable from the inactive ones in light appearance. (#2439)</li>
<li>Active editor tab drawn darker than its track on macOS 27, and inverting when the window lost focus.</li>
<li>Editor tab selection changing with the desktop picture behind the window.</li>
<li>Editor tab strip tests reporting four appearances while running plain Aqua and Dark Aqua twice.</li>
<li>Icon cut off the entry at the top of a scrolled connections strip. (#2452)</li>
<li>Connections strip not scrolling to the entry you switch to.</li>
<li>Background connections stuck on the session preparation screen after connecting. (#2545)</li>
<li>Blank sidebar, grid and inspector on a connection opened into a window that was already on screen.</li>
<li>Connections strip hidden, and its switch commands disabled, whenever the connection on screen was disconnected.</li>
<li>Rows, tabs and an enabled toolbar over a connection the connections strip already showed as failed.</li>
<li>Reconnect doing nothing on a connection whose automatic reconnect had already given up.</li>
<li>Health monitor waking every 30 seconds for the life of the app after it stopped trying to reconnect.</li>
<li>Choosing a connection from the connection list re-fronting its window without switching to it.</li>
<li>Grid cells left at their old column positions until the next click, after any column geometry change. (#2449, #2446)</li>
<li>The row-number column draggable out of first place, which walked it to the far right on the next refresh.</li>
<li>A time entered into a date cell discarded when the stored value carried no time.</li>
<li>A timestamp with an offset showing one day in the grid and another in its date picker.</li>
<li>Opening a date picker and confirming without changing anything rewriting the cell.</li>
<li>Cell overlay appearance test failing at random when its window was released before the appearance changed.</li>
<li>A reordered column snapping back on the next refresh in the Structure tab and in query results.</li>
<li>Double-clicking a cell editing the wrong row, after deleting one of several rows added in the same session.</li>
<li>VoiceOver reading a cell's value under a different row's number after such a delete.</li>
<li>Snowflake `DATE`, `TIME` and `TIMESTAMP_*` cells showing the raw epoch number the server sends. (#2454)</li>
<li>Snowflake `BINARY` cells showing the hex of their hex, which the hex editor then wrote back.</li>
<li>A date cell the app cannot read opening a picker set to today, which overwrote the value on OK. (#2454)</li>
<li>Snowflake reporting no rows changed for an `UPDATE` or `MERGE`, and misreading a `number of rows` column.</li>
<li>A database switch moving both windows, when two saved Snowflake connections to one account shared a session.</li>
<li>Stop on a Snowflake query cancelling every other query on the same connection, including a sidebar refresh or a save.</li>
<li>`tablepro://` deep links ignored on iPhone and iPad, from the widget, a Live Activity or a shortcut.</li>
<li>iOS PostgreSQL and Redshift reading another schema's table of the same name for columns, indexes and foreign keys.</li>
<li>iOS SQL Server using the login's default schema rather than the one the toolbar showed, Truncate and Drop included.</li>
<li>Computed SQL Server columns offered as editable and written into the `INSERT`, on both Mac and iOS.</li>
<li>A CSV export writing a real NULL and the text `NULL` identically.</li>
<li>A JSON export emitting a leading-zero string such as `01234` as an unquoted number, which no JSON parser accepts.</li>
<li>A SQL `INSERT` export quoting identifiers for ANSI on every engine, which MySQL and MariaDB reject outright.</li>
<li>Insert Row on iPhone and iPad writing every column, blocking a `NOT NULL` column with a default. (#2543)</li>
<li>NULL offered on a `NOT NULL` column in Insert Row and the row editor on iPhone and iPad.</li>
<li>Insert Row on iPhone and iPad writing a generated column, which every engine refuses.</li>
<li>Both halves of a composite integer primary key left out of the insert on iPhone and iPad.</li>
<li>Typed values discarded when Insert Row opened before the column list had loaded on iPhone and iPad.</li>
<li>Insert Row offered on Redis connections on iPhone and iPad, where it can only fail.</li>
<li>Missing search field in a connection's Tables tab on iPhone and iPad. (#2544)</li>
<li>A search from one connection or table still filtering another's list on iPhone and iPad.</li>
<li>A staged drop or truncate running against the database in front at Save time rather than the one it was staged in.</li>
<li>Unqualified `DROP TABLE` for every object on Oracle, Dameng, Trino, Snowflake and BigQuery, whatever its kind.</li>
<li>Dropping a table closing the tab on a same-named table in another schema or database.</li>
</ul>
<h3>Security</h3>
<ul>
<li>SQL injection through a Snowflake schema or routine name containing a backslash.</li>
<li>Safe Mode on iPhone and iPad letting a write run when it followed a comment or a CTE.</li>
</ul>
]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.69.0/TablePro-0.69.0-arm64.zip" length="25835052" type="application/octet-stream" sparkle:edSignature="I4jeUNcZHYxdEoy2r27A8c6d/j5sxBy/tYM4XlsCNyVMsV3Z2boA9FvxLbK73BG9cOordsXhBoWjSgiY+nvmDg=="/>
        </item>
        <item>
            <title>0.69.0</title>
            <pubDate>Thu, 27 Aug 2026 16:03:08 +0000</pubDate>
            <sparkle:version>125</sparkle:version>
            <sparkle:shortVersionString>0.69.0</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <description><![CDATA[<h3>Added</h3>
<ul>
<li>Restore Previous Values in the Edit menu, taking back a save that already committed. Starter license. (#2107)</li>
<li>Data Rewind settings in Settings > Data & Results, with an off switch and Clear Saved Changes.</li>
<li>Restore Previous Values in the toolbar's Table Actions group.</li>
<li>Rebindable Find shortcut in Settings > Keyboard, for giving `Cmd+F` to the filter bar instead.</li>
<li>Constraints tab in the structure editor, listing check constraints with their expression. (#2478)</li>
<li>Generated and Expression fields on the column row, with a stored or virtual choice. (#2478)</li>
<li>`check_constraints` and `generation_expression` in the MCP `describe_table` response.</li>
<li>Remote File pane for SQLite, opening a read-only copy of a database that lives on an SSH server. (#2474)</li>
<li>Rename on a table's right-click menu, editing the row's label in place. (#2482)</li>
<li>Rename Database and Rename Schema on the sidebar's container rows, where the engine has them. (#2482)</li>
<li>Turso in the New Connection picker as an engine of its own, offered before the libSQL plugin installs.</li>
</ul>
<h3>Changed</h3>
<ul>
<li>Editor tabs drawn as a segmented tab picker rather than in Liquid Glass, on every macOS version. (#2439)</li>
<li>A connection opens full screen on iPhone and iPad, with its four sections in a sidebar on iPad. (#2544)</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>Column charset and collation lost when reordering a MySQL column by dragging its row.</li>
<li>Free-text cells for foreign key actions and index types in Create Table, where Structure shows a menu.</li>
<li>A generated column comparing equal to a plain one in Compare & Sync.</li>
<li>Empty Indexes, Foreign Keys or DDL tab, with no error, when the structure read fails.</li>
<li>Connections strip absent from a window whose selected connection has nothing to show, when Show Connections is off.</li>
<li>Switch Connection disabled, in the Database menu and the toolbar, while the selected connection is disconnected.</li>
<li>One floating switcher panel per connection in a window, each centred on the same point.</li>
<li>Leaked socket on every SSH Test Connection against a server reached without jump hosts.</li>
<li>Leaked listening port, socket and session each time an SSH tunnel died from sleep or a dropped network.</li>
<li>Selection highlight missing from part of a long selection after scrolling back up to it.</li>
<li>Editor not scrolling to follow a selection extended past the edge of the viewport.</li>
<li>Find highlight and the run band covering only the first line of a match that spans several lines.</li>
<li>Nothing selected when double-clicking `=`, `<`, `>` or any other SQL operator.</li>
<li>Shift+Arrow extending the wrong end of a selection made by dragging.</li>
<li>Selection starting a few characters away from the press point on a quick drag.</li>
<li>A pause before the pointer responds when pressing inside selected text.</li>
<li>Shift+double-click and Shift+triple-click doing nothing.</li>
<li>Drag-select scrolling faster on a mouse than on a trackpad, and stalling mid-drag.</li>
<li>Statement selection with `Option+Shift+Down` leaving the highlight behind.</li>
<li>No arrow-key movement or Shift+Arrow selection in the JSON, DDL and SQL preview editors.</li>
<li>Explain with AI acting on the previous selection after a right-click somewhere else.</li>
<li>Selection painted in the accent colour in a window that is not the active one.</li>
<li>Selection bounds reported to VoiceOver covering only the first line, and no announcement when the selection moved.</li>
<li>An abandoned editor tab drag reordering the strip anyway, and keeping that order across relaunch.</li>
<li>An editor tab reorder stopping partway when the pointer strayed a couple of points off the track.</li>
<li>Text dropped on the editor tab strip reported as accepted and then discarded.</li>
<li>An editor tab left faded and the strip's separators hidden after a cancelled drag.</li>
<li>Size All Columns to Fit slowing down with the square of the column count.</li>
<li>Column-resize cursor over the leading edge of the data grid header, where there is no divider to drag.</li>
<li>A data grid shortcut bound to `Cmd+G` silently taking the key from Find Next, with no conflict warning.</li>
<li>Stale cells after fitting, hiding or reordering a data grid column on a result narrower than the window. (#2446)</li>
<li>A shortcut recorded with only Option or Shift beeping with no reason given, against docs that said it would work.</li>
<li>A rebound shortcut silently killing Quit, Minimize, Hide, Settings, Show Toolbar or Enter Full Screen.</li>
<li>`Ctrl+Cmd+J` accepted in Settings > Keyboard while the SQL editor kept it for Jump to Definition.</li>
<li>Autocomplete keeping an earlier prefix's ordering after the typed word becomes an exact match. (#2444)</li>
<li>Whole MySQL and MariaDB result set fetched before a capped query returned its first rows. (#2427)</li>
<li>KILL sent to a different server when a MySQL or MariaDB connection's host is spelled `localhost`.</li>
<li>Stop on a MySQL or MariaDB connection freezing the app for up to five seconds when the server stopped answering.</li>
<li>A MySQL or MariaDB read re-run after a dropped connection, advancing a sequence or taking a lock twice.</li>
<li>Save reporting the number of statements it ran as the number of rows it changed.</li>
<li>An edit or a delete on a table with no primary key changing every identical row. (#2107)</li>
<li>The same statements committed twice when Cmd+S is pressed again during a slow save.</li>
<li>Switching tabs during a save clearing the edits of the tab switched to, and re-running its query.</li>
<li>Placeholder SQL with no values in the Safe Mode confirmation and the authorization prompt.</li>
<li>Deleting one row of a pasted batch dropping the other rows, or saving them with another row's values.</li>
<li>Undoing the deletion of a new row putting its values in the wrong columns.</li>
<li>Undoing a cell edit reverting the wrong row while a column filter is active.</li>
<li>A save that deletes a row and reuses its unique value failing on the constraint.</li>
<li>"Ignore foreign key checks" doing nothing on SQLite, libSQL and Cloudflare D1.</li>
<li>A save reported as failed on an engine without transactions after some statements had been written.</li>
<li>Keep Open for a preview tab, by double-clicking it in the tab strip or from its contextual menu. (#2436)</li>
<li>Kafka driver plugin: topics in the grid, consumer group lag, and KafkaQL for seeking and producing. (#2419)</li>
<li>Active editor tab indistinguishable from the inactive ones in light appearance. (#2439)</li>
<li>Active editor tab drawn darker than its track on macOS 27, and inverting when the window lost focus.</li>
<li>Editor tab selection changing with the desktop picture behind the window.</li>
<li>Editor tab strip tests reporting four appearances while running plain Aqua and Dark Aqua twice.</li>
<li>Icon cut off the entry at the top of a scrolled connections strip. (#2452)</li>
<li>Connections strip not scrolling to the entry you switch to.</li>
<li>Background connections stuck on the session preparation screen after connecting. (#2545)</li>
<li>Blank sidebar, grid and inspector on a connection opened into a window that was already on screen.</li>
<li>Connections strip hidden, and its switch commands disabled, whenever the connection on screen was disconnected.</li>
<li>Rows, tabs and an enabled toolbar over a connection the connections strip already showed as failed.</li>
<li>Reconnect doing nothing on a connection whose automatic reconnect had already given up.</li>
<li>Health monitor waking every 30 seconds for the life of the app after it stopped trying to reconnect.</li>
<li>Choosing a connection from the connection list re-fronting its window without switching to it.</li>
<li>Grid cells left at their old column positions until the next click, after any column geometry change. (#2449, #2446)</li>
<li>The row-number column draggable out of first place, which walked it to the far right on the next refresh.</li>
<li>A time entered into a date cell discarded when the stored value carried no time.</li>
<li>A timestamp with an offset showing one day in the grid and another in its date picker.</li>
<li>Opening a date picker and confirming without changing anything rewriting the cell.</li>
<li>Cell overlay appearance test failing at random when its window was released before the appearance changed.</li>
<li>A reordered column snapping back on the next refresh in the Structure tab and in query results.</li>
<li>Double-clicking a cell editing the wrong row, after deleting one of several rows added in the same session.</li>
<li>VoiceOver reading a cell's value under a different row's number after such a delete.</li>
<li>Snowflake `DATE`, `TIME` and `TIMESTAMP_*` cells showing the raw epoch number the server sends. (#2454)</li>
<li>Snowflake `BINARY` cells showing the hex of their hex, which the hex editor then wrote back.</li>
<li>A date cell the app cannot read opening a picker set to today, which overwrote the value on OK. (#2454)</li>
<li>Snowflake reporting no rows changed for an `UPDATE` or `MERGE`, and misreading a `number of rows` column.</li>
<li>A database switch moving both windows, when two saved Snowflake connections to one account shared a session.</li>
<li>Stop on a Snowflake query cancelling every other query on the same connection, including a sidebar refresh or a save.</li>
<li>`tablepro://` deep links ignored on iPhone and iPad, from the widget, a Live Activity or a shortcut.</li>
<li>iOS PostgreSQL and Redshift reading another schema's table of the same name for columns, indexes and foreign keys.</li>
<li>iOS SQL Server using the login's default schema rather than the one the toolbar showed, Truncate and Drop included.</li>
<li>Computed SQL Server columns offered as editable and written into the `INSERT`, on both Mac and iOS.</li>
<li>A CSV export writing a real NULL and the text `NULL` identically.</li>
<li>A JSON export emitting a leading-zero string such as `01234` as an unquoted number, which no JSON parser accepts.</li>
<li>A SQL `INSERT` export quoting identifiers for ANSI on every engine, which MySQL and MariaDB reject outright.</li>
<li>Insert Row on iPhone and iPad writing every column, blocking a `NOT NULL` column with a default. (#2543)</li>
<li>NULL offered on a `NOT NULL` column in Insert Row and the row editor on iPhone and iPad.</li>
<li>Insert Row on iPhone and iPad writing a generated column, which every engine refuses.</li>
<li>Both halves of a composite integer primary key left out of the insert on iPhone and iPad.</li>
<li>Typed values discarded when Insert Row opened before the column list had loaded on iPhone and iPad.</li>
<li>Insert Row offered on Redis connections on iPhone and iPad, where it can only fail.</li>
<li>Missing search field in a connection's Tables tab on iPhone and iPad. (#2544)</li>
<li>A search from one connection or table still filtering another's list on iPhone and iPad.</li>
<li>A staged drop or truncate running against the database in front at Save time rather than the one it was staged in.</li>
<li>Unqualified `DROP TABLE` for every object on Oracle, Dameng, Trino, Snowflake and BigQuery, whatever its kind.</li>
<li>Dropping a table closing the tab on a same-named table in another schema or database.</li>
</ul>
<h3>Security</h3>
<ul>
<li>SQL injection through a Snowflake schema or routine name containing a backslash.</li>
<li>Safe Mode on iPhone and iPad letting a write run when it followed a comment or a CTE.</li>
</ul>
]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.69.0/TablePro-0.69.0-x86_64.zip" length="27528180" type="application/octet-stream" sparkle:edSignature="AkIOGUR4r2oi1/6tlPqGVEHc21tzt4W9LMCYUV6J8Jleu/+bZO/pMfiki4zqAohbj/V94tZjmNNHiHqDyvaYAw=="/>
        </item>
        <item>
            <title>0.68.1</title>
            <pubDate>Wed, 26 Aug 2026 02:14:58 +0000</pubDate>
            <sparkle:version>124</sparkle:version>
            <sparkle:shortVersionString>0.68.1</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <sparkle:hardwareRequirements>arm64</sparkle:hardwareRequirements>
            <description><![CDATA[<h3>Changed</h3>
<ul>
<li>Query results stop fetching at the row limit on MySQL, PostgreSQL, SQLite, MSSQL, Oracle, Cassandra, Redis, DynamoDB, BigQuery, Snowflake, Trino and ClickHouse. (#2427)</li>
<li>ClickHouse reads a capped result in one request instead of a separate `LIMIT 0` round trip for the columns.</li>
<li>Structure editor undo granularity set by the operation instead of run loop timing.</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>Row limit ignored for a parenthesised `SELECT`, a `TABLE` statement or a `VALUES` list. (#2427)</li>
<li>Cancelling an export or a schema compare left the database still sending rows on MySQL, PostgreSQL, MSSQL, Oracle, Cassandra and Trino.</li>
<li>An aborted PostgreSQL read charged its leftover rows to the next query on that connection.</li>
<li>Syntax error when sorting a query result whose SQL ends in `LIMIT`.</li>
<li>The user's own `LIMIT` dropped when sorting replaced an existing `ORDER BY`.</li>
<li>Fetch All sending the previous run's parameters after a query that had none.</li>
<li>A SQLite `UPDATE` or `DELETE` alone in a query tab reporting success instead of the rows affected.</li>
<li>Stall on switching between two loaded table tabs, growing with the rows they hold. (#2424)</li>
<li>Scroll position lost when switching away from a table tab and back. (#2424)</li>
<li>A table statistics command on every switch between two table tabs. (#2424)</li>
<li>Another table's size and row count in Table Info when its statistics arrive late. (#2424)</li>
<li>Active editor tab barely distinguishable from the inactive ones, worst in light appearance. (#2428)</li>
<li>No selected editor tab at all in a background window on macOS 14 and 15. (#2428)</li>
<li>Editor tab strip ignoring Increase Contrast and Reduce Transparency. (#2428)</li>
<li>Crash when a file changes in a linked SQL folder. (#2432)</li>
<li>Crash when a file changes in a linked connection folder, and on quit with one configured.</li>
<li>iOS: crash the first time the system reports memory pressure.</li>
</ul>
]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.68.1/TablePro-0.68.1-arm64.zip" length="25501109" type="application/octet-stream" sparkle:edSignature="i58j2sABhXkHHwH7IShuQ44vnVFJITDLQAZORlxeXMt2myD0/6edFuKoHtx0ZonATmiQF8LMr7akOac5Ip2qDQ=="/>
        </item>
        <item>
            <title>0.68.1</title>
            <pubDate>Wed, 26 Aug 2026 02:14:59 +0000</pubDate>
            <sparkle:version>124</sparkle:version>
            <sparkle:shortVersionString>0.68.1</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <description><![CDATA[<h3>Changed</h3>
<ul>
<li>Query results stop fetching at the row limit on MySQL, PostgreSQL, SQLite, MSSQL, Oracle, Cassandra, Redis, DynamoDB, BigQuery, Snowflake, Trino and ClickHouse. (#2427)</li>
<li>ClickHouse reads a capped result in one request instead of a separate `LIMIT 0` round trip for the columns.</li>
<li>Structure editor undo granularity set by the operation instead of run loop timing.</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>Row limit ignored for a parenthesised `SELECT`, a `TABLE` statement or a `VALUES` list. (#2427)</li>
<li>Cancelling an export or a schema compare left the database still sending rows on MySQL, PostgreSQL, MSSQL, Oracle, Cassandra and Trino.</li>
<li>An aborted PostgreSQL read charged its leftover rows to the next query on that connection.</li>
<li>Syntax error when sorting a query result whose SQL ends in `LIMIT`.</li>
<li>The user's own `LIMIT` dropped when sorting replaced an existing `ORDER BY`.</li>
<li>Fetch All sending the previous run's parameters after a query that had none.</li>
<li>A SQLite `UPDATE` or `DELETE` alone in a query tab reporting success instead of the rows affected.</li>
<li>Stall on switching between two loaded table tabs, growing with the rows they hold. (#2424)</li>
<li>Scroll position lost when switching away from a table tab and back. (#2424)</li>
<li>A table statistics command on every switch between two table tabs. (#2424)</li>
<li>Another table's size and row count in Table Info when its statistics arrive late. (#2424)</li>
<li>Active editor tab barely distinguishable from the inactive ones, worst in light appearance. (#2428)</li>
<li>No selected editor tab at all in a background window on macOS 14 and 15. (#2428)</li>
<li>Editor tab strip ignoring Increase Contrast and Reduce Transparency. (#2428)</li>
<li>Crash when a file changes in a linked SQL folder. (#2432)</li>
<li>Crash when a file changes in a linked connection folder, and on quit with one configured.</li>
<li>iOS: crash the first time the system reports memory pressure.</li>
</ul>
]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.68.1/TablePro-0.68.1-x86_64.zip" length="27160361" type="application/octet-stream" sparkle:edSignature="jFIj4cL2+bhRPfUfrHj7+wrtAV+H6O50mvajjVhi6nLSZrIQGyMf0+qdU5uTzgCKFWSFDHNCrQeatapZcRLJBg=="/>
        </item>
        <item>
            <title>0.68.0</title>
            <pubDate>Tue, 25 Aug 2026 10:59:57 +0000</pubDate>
            <sparkle:version>123</sparkle:version>
            <sparkle:shortVersionString>0.68.0</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <sparkle:hardwareRequirements>arm64</sparkle:hardwareRequirements>
            <description><![CDATA[<h3>Added</h3>
<ul>
<li>Compare & Sync between two databases, over schema objects or row data. Starter license. (#721)</li>
<li>Seat release, team roster, per-Mac last use and macOS version, and your role in Settings > License.</li>
<li>Copy Key in Settings > License, kept out of clipboard history.</li>
<li>Support TablePro in the Help menu, the welcome window and the sidebar.</li>
<li>Triggers as a sidebar section, alongside Procedures and Functions. (#2383)</li>
<li>Procedures, functions and triggers on MSSQL, Oracle, SQLite, ClickHouse, DuckDB, Snowflake, BigQuery, Cassandra, LibSQL, Cloudflare D1, Teradata and Dameng. (#2383)</li>
<li>Read-only source viewer for procedures, functions and triggers, with Copy, Export and Open in Editor. (#2383)</li>
<li>Procedures, functions and triggers in the quick switcher, with argument signatures on ambiguous names.</li>
<li>Compare, a fourth EXPLAIN plan mode, and pinning a saved plan. (#2380)</li>
<li>Beancount tables for `directives`, pads, named queries and custom directives. (#2399, #2413, #2415)</li>
<li>Account booking, note tags and links, and balance assertion details on Beancount tables. (#2415)</li>
<li>Local table load history, kept 7 days and never uploaded. (#2395)</li>
<li>`list_triggers` for MCP clients, and `return_type` and `language` on `list_routines`.</li>
</ul>
<h3>Changed</h3>
<ul>
<li>Settings > License in place of Settings > Account, with iCloud Sync in its own pane and Linked Folders under General.</li>
<li>The license pane states an expired, suspended or unverified license inline, with one action and a purchase link.</li>
<li>Sync Paused, a separate state for a license the app could not check.</li>
<li>One Refresh in Settings > License, replacing Refresh and Check Status.</li>
<li>The license key masked and not selectable.</li>
<li>The PRO badge names the gated feature and links to the pricing page.</li>
<li>The data grid draws its own cells and column separators, opening a result with hundreds of columns at once. (#2381)</li>
<li>The inline cell editor scrolls a long line instead of wrapping it. (#2381)</li>
<li>Row inspector fields, cell popovers and the Compare row diff follow the data grid font. (#2393)</li>
<li>The connection color as a badge behind the connection name, a dot on the rail icon, and deeper tag badge fills. (#2398)</li>
<li>A database stays in the connections strip until you close its entry; Close ends the connection on the last one.</li>
<li>Autocomplete and the database picker follow the query tab's own database and schema.</li>
<li>Beancount postings keep their own flag, price, and resolved lot date and label.</li>
<li>Beancount connections report the active `rledger` or Python Beancount version.</li>
<li>Beancount ledger plugins are skipped unless the connection turns on Run Ledger Plugins.</li>
<li>Turkish, Vietnamese, Simplified Chinese and Traditional Chinese cover every remaining English string.</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>A table with 500 columns pinning a core for 20 seconds and taking a gigabyte to open. (#2381)</li>
<li>Flickering columns, blank columns and an unpainted gap while scrolling a wide result sideways. (#2381)</li>
<li>A second of delay opening the inline editor on a result with hundreds of columns. (#2381)</li>
<li>Find, arrow keys, the inline editor and Size All Columns to Fit unable to reach a column scrolled off the side.</li>
<li>Return opening no editor on a row selected with the arrow keys, and Tab or Shift+Tab out of a row's end doing nothing.</li>
<li>An empty grid the first time a table is opened in a window with no tabs. (#2342)</li>
<li>One table click running its query twice, and a closed tab leaving its query counted as running. (#2342)</li>
<li>Crash exporting two same-named tables from different schemas to SQL. (#1968)</li>
<li>SQL export writing rows into another schema's same-named table, and dropping columns and keys after the first. (#1968)</li>
<li>One of two PostgreSQL function overloads missing from the sidebar, with Show DDL opening an arbitrary one. (#2383)</li>
<li>MySQL Show DDL reading the session database instead of the one being browsed. (#2383)</li>
<li>Routine tooltips, VoiceOver labels and Copy with Signature showing a return type in place of the arguments. (#2383)</li>
<li>Duplicate routine rows in the flat sidebar taking the selection back to the first of them. (#2383)</li>
<li>MySQL triggers losing their definer, `WHEN` clause and ordering, and Oracle triggers showing a header with no body.</li>
<li>The welcome window offering Activate License to a Mac that already holds one and has just been offline.</li>
<li>iCloud Sync calling an unverified license missing, and reporting a live sync after the license was removed.</li>
<li>The app freezing after turning on iCloud Sync with many saved column layouts.</li>
<li>Every settings write failing once the sync record cache filled the preferences file past the 4 MB limit.</li>
<li>The sync status returning to Synced when sync was turned off mid-sync.</li>
<li>The license server never contacted again after removing and re-adding a license in one session.</li>
<li>The device list error surviving a later successful refresh, and a renewal warning counting zero days above Expired.</li>
<li>A previous team's shared connections and saved queries surviving deactivation.</li>
<li>The connections strip disappearing when you click the entry you came from.</li>
<li>Cancelling a close leaving the window on the connection the strip revealed.</li>
<li>A strip entry in another window raising that window without showing the connection.</li>
<li>A disconnected connection's entries dropping to the bottom of the strip, and all but one leaving it.</li>
<li>A connection opened from a file or a URL losing its strip entries when its session ends.</li>
<li>The connections strip's saved arrangement growing without bound.</li>
<li>Bulk tab close saving the tab on screen, and asking about another database's unsaved work.</li>
<li>A connection's color and name not reaching the toolbar and workspace rail until the next reconnect. (#2398)</li>
<li>An invisible connection dot in the history drawer and compare strip, and a warning icon in the engine color. (#2398)</li>
<li>The JSON viewer keeping its old font after a font, theme or text-size change, and hex dumps wrapping mid-line. (#2393)</li>
<li>AI inline suggestions written without the schema, and columns missing for a table named with a capital letter.</li>
<li>Case-insensitive filters and completion on Redshift falling back to PostgreSQL's ASCII-only ILIKE.</li>
<li>PGlite connection form offering SSH, SSL, Cloudflare Tunnel and SOCKS panes, and a password field.</li>
<li>Redshift and CockroachDB system databases listed in the sidebar as ordinary user databases.</li>
</ul>
]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.68.0/TablePro-0.68.0-arm64.zip" length="25469554" type="application/octet-stream" sparkle:edSignature="2XzOE7QUOgmOMt3uMiQuH8zjVFrczcTmE3aDo0UOHeAGMTKaD9JBuIPbUp9xS5E+hKDPNUEmkMrI5KAK7zpTCA=="/>
        </item>
        <item>
            <title>0.68.0</title>
            <pubDate>Tue, 25 Aug 2026 10:59:58 +0000</pubDate>
            <sparkle:version>123</sparkle:version>
            <sparkle:shortVersionString>0.68.0</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <description><![CDATA[<h3>Added</h3>
<ul>
<li>Compare & Sync between two databases, over schema objects or row data. Starter license. (#721)</li>
<li>Seat release, team roster, per-Mac last use and macOS version, and your role in Settings > License.</li>
<li>Copy Key in Settings > License, kept out of clipboard history.</li>
<li>Support TablePro in the Help menu, the welcome window and the sidebar.</li>
<li>Triggers as a sidebar section, alongside Procedures and Functions. (#2383)</li>
<li>Procedures, functions and triggers on MSSQL, Oracle, SQLite, ClickHouse, DuckDB, Snowflake, BigQuery, Cassandra, LibSQL, Cloudflare D1, Teradata and Dameng. (#2383)</li>
<li>Read-only source viewer for procedures, functions and triggers, with Copy, Export and Open in Editor. (#2383)</li>
<li>Procedures, functions and triggers in the quick switcher, with argument signatures on ambiguous names.</li>
<li>Compare, a fourth EXPLAIN plan mode, and pinning a saved plan. (#2380)</li>
<li>Beancount tables for `directives`, pads, named queries and custom directives. (#2399, #2413, #2415)</li>
<li>Account booking, note tags and links, and balance assertion details on Beancount tables. (#2415)</li>
<li>Local table load history, kept 7 days and never uploaded. (#2395)</li>
<li>`list_triggers` for MCP clients, and `return_type` and `language` on `list_routines`.</li>
</ul>
<h3>Changed</h3>
<ul>
<li>Settings > License in place of Settings > Account, with iCloud Sync in its own pane and Linked Folders under General.</li>
<li>The license pane states an expired, suspended or unverified license inline, with one action and a purchase link.</li>
<li>Sync Paused, a separate state for a license the app could not check.</li>
<li>One Refresh in Settings > License, replacing Refresh and Check Status.</li>
<li>The license key masked and not selectable.</li>
<li>The PRO badge names the gated feature and links to the pricing page.</li>
<li>The data grid draws its own cells and column separators, opening a result with hundreds of columns at once. (#2381)</li>
<li>The inline cell editor scrolls a long line instead of wrapping it. (#2381)</li>
<li>Row inspector fields, cell popovers and the Compare row diff follow the data grid font. (#2393)</li>
<li>The connection color as a badge behind the connection name, a dot on the rail icon, and deeper tag badge fills. (#2398)</li>
<li>A database stays in the connections strip until you close its entry; Close ends the connection on the last one.</li>
<li>Autocomplete and the database picker follow the query tab's own database and schema.</li>
<li>Beancount postings keep their own flag, price, and resolved lot date and label.</li>
<li>Beancount connections report the active `rledger` or Python Beancount version.</li>
<li>Beancount ledger plugins are skipped unless the connection turns on Run Ledger Plugins.</li>
<li>Turkish, Vietnamese, Simplified Chinese and Traditional Chinese cover every remaining English string.</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>A table with 500 columns pinning a core for 20 seconds and taking a gigabyte to open. (#2381)</li>
<li>Flickering columns, blank columns and an unpainted gap while scrolling a wide result sideways. (#2381)</li>
<li>A second of delay opening the inline editor on a result with hundreds of columns. (#2381)</li>
<li>Find, arrow keys, the inline editor and Size All Columns to Fit unable to reach a column scrolled off the side.</li>
<li>Return opening no editor on a row selected with the arrow keys, and Tab or Shift+Tab out of a row's end doing nothing.</li>
<li>An empty grid the first time a table is opened in a window with no tabs. (#2342)</li>
<li>One table click running its query twice, and a closed tab leaving its query counted as running. (#2342)</li>
<li>Crash exporting two same-named tables from different schemas to SQL. (#1968)</li>
<li>SQL export writing rows into another schema's same-named table, and dropping columns and keys after the first. (#1968)</li>
<li>One of two PostgreSQL function overloads missing from the sidebar, with Show DDL opening an arbitrary one. (#2383)</li>
<li>MySQL Show DDL reading the session database instead of the one being browsed. (#2383)</li>
<li>Routine tooltips, VoiceOver labels and Copy with Signature showing a return type in place of the arguments. (#2383)</li>
<li>Duplicate routine rows in the flat sidebar taking the selection back to the first of them. (#2383)</li>
<li>MySQL triggers losing their definer, `WHEN` clause and ordering, and Oracle triggers showing a header with no body.</li>
<li>The welcome window offering Activate License to a Mac that already holds one and has just been offline.</li>
<li>iCloud Sync calling an unverified license missing, and reporting a live sync after the license was removed.</li>
<li>The app freezing after turning on iCloud Sync with many saved column layouts.</li>
<li>Every settings write failing once the sync record cache filled the preferences file past the 4 MB limit.</li>
<li>The sync status returning to Synced when sync was turned off mid-sync.</li>
<li>The license server never contacted again after removing and re-adding a license in one session.</li>
<li>The device list error surviving a later successful refresh, and a renewal warning counting zero days above Expired.</li>
<li>A previous team's shared connections and saved queries surviving deactivation.</li>
<li>The connections strip disappearing when you click the entry you came from.</li>
<li>Cancelling a close leaving the window on the connection the strip revealed.</li>
<li>A strip entry in another window raising that window without showing the connection.</li>
<li>A disconnected connection's entries dropping to the bottom of the strip, and all but one leaving it.</li>
<li>A connection opened from a file or a URL losing its strip entries when its session ends.</li>
<li>The connections strip's saved arrangement growing without bound.</li>
<li>Bulk tab close saving the tab on screen, and asking about another database's unsaved work.</li>
<li>A connection's color and name not reaching the toolbar and workspace rail until the next reconnect. (#2398)</li>
<li>An invisible connection dot in the history drawer and compare strip, and a warning icon in the engine color. (#2398)</li>
<li>The JSON viewer keeping its old font after a font, theme or text-size change, and hex dumps wrapping mid-line. (#2393)</li>
<li>AI inline suggestions written without the schema, and columns missing for a table named with a capital letter.</li>
<li>Case-insensitive filters and completion on Redshift falling back to PostgreSQL's ASCII-only ILIKE.</li>
<li>PGlite connection form offering SSH, SSL, Cloudflare Tunnel and SOCKS panes, and a password field.</li>
<li>Redshift and CockroachDB system databases listed in the sidebar as ordinary user databases.</li>
</ul>
]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.68.0/TablePro-0.68.0-x86_64.zip" length="27118704" type="application/octet-stream" sparkle:edSignature="9EKVgLdZi3k12O9NDMd4MYcmJnt3ZbNXE3tkMFq43wRtUC0LpBghO1ZUNmNeZVBqZ8WAcEoGAag3oOVJ6ZnLAw=="/>
        </item>
        <item>
            <title>0.67.1</title>
            <pubDate>Sat, 22 Aug 2026 10:44:28 +0000</pubDate>
            <sparkle:version>122</sparkle:version>
            <sparkle:shortVersionString>0.67.1</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <sparkle:hardwareRequirements>arm64</sparkle:hardwareRequirements>
            <description><![CDATA[<h3>Added</h3>
<ul>
<li>Environment tags for database favorites, with sidebar filtering, direct open, and iCloud sync. (#1553)</li>
<li>Open in Window for row inspector text fields.</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>Background tab eviction dropping query, pinned, edited, and in-flight results.</li>
<li>Result display cache exceeding its memory budget when a cached row's values grew.</li>
<li>Data grid reformatting every cell while scrolling after undo, redo, a theme change, or a display-format change.</li>
<li>Autocomplete bulk-loading every column of a schema too large to cache.</li>
<li>Empty grid on a background tab whose column metadata arrived after its rows were freed.</li>
<li>Saved connections failing to load when their SSH settings predated the agent socket field.</li>
<li>Switch Connection and Open Database doing nothing on a narrow window or without their toolbar button.</li>
<li>Large text values clipped in the row inspector, and unselectable when the row is read-only.</li>
<li>Large `VARCHAR(MAX)`, `NCLOB`, and `Nullable(String)` values stuck on one line in the inspector.</li>
<li>Empty context menu when right-clicking a read-only inspector field.</li>
<li>Editor crashes reading text a newer edit had removed. (#2338, #2339, #2340)</li>
<li>Search highlights and diagnostic underlines moving when their text was deleted. (#2341)</li>
<li>Toolbar stuck on "Executing…" after a query ended, and session context buttons emptying mid-query. (#2342)</li>
<li>SQLite queries showing an empty table instead of reporting a lock or volume error. (#2355)</li>
<li>Stop not ending a SQLite query waiting on a database locked by another program. (#2363)</li>
<li>Broken documentation links in the XLSX, MQL, and SQL Import plugins.</li>
</ul>
]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.67.1/TablePro-0.67.1-arm64.zip" length="24418655" type="application/octet-stream" sparkle:edSignature="Blg+oRy7sM8whtLzBG1H2I7b4gYqU8asx6diNtujlxp10/eYbKOwmydFOKYT9ljXy2Btc1RZ06s1KUvn6aWCAg=="/>
        </item>
        <item>
            <title>0.67.1</title>
            <pubDate>Sat, 22 Aug 2026 10:44:29 +0000</pubDate>
            <sparkle:version>122</sparkle:version>
            <sparkle:shortVersionString>0.67.1</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <description><![CDATA[<h3>Added</h3>
<ul>
<li>Environment tags for database favorites, with sidebar filtering, direct open, and iCloud sync. (#1553)</li>
<li>Open in Window for row inspector text fields.</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>Background tab eviction dropping query, pinned, edited, and in-flight results.</li>
<li>Result display cache exceeding its memory budget when a cached row's values grew.</li>
<li>Data grid reformatting every cell while scrolling after undo, redo, a theme change, or a display-format change.</li>
<li>Autocomplete bulk-loading every column of a schema too large to cache.</li>
<li>Empty grid on a background tab whose column metadata arrived after its rows were freed.</li>
<li>Saved connections failing to load when their SSH settings predated the agent socket field.</li>
<li>Switch Connection and Open Database doing nothing on a narrow window or without their toolbar button.</li>
<li>Large text values clipped in the row inspector, and unselectable when the row is read-only.</li>
<li>Large `VARCHAR(MAX)`, `NCLOB`, and `Nullable(String)` values stuck on one line in the inspector.</li>
<li>Empty context menu when right-clicking a read-only inspector field.</li>
<li>Editor crashes reading text a newer edit had removed. (#2338, #2339, #2340)</li>
<li>Search highlights and diagnostic underlines moving when their text was deleted. (#2341)</li>
<li>Toolbar stuck on "Executing…" after a query ended, and session context buttons emptying mid-query. (#2342)</li>
<li>SQLite queries showing an empty table instead of reporting a lock or volume error. (#2355)</li>
<li>Stop not ending a SQLite query waiting on a database locked by another program. (#2363)</li>
<li>Broken documentation links in the XLSX, MQL, and SQL Import plugins.</li>
</ul>
]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.67.1/TablePro-0.67.1-x86_64.zip" length="25966268" type="application/octet-stream" sparkle:edSignature="fcD0SjG5lZTaIj2Y3yhRW4QPcgAFkQP4TkdS4Ro7y/Q9bV+Ca54IsOjfZSfXszbrnJhAvo+4Baj237SedZJLBg=="/>
        </item>
        <item>
            <title>0.67.0</title>
            <pubDate>Fri, 21 Aug 2026 13:40:28 +0000</pubDate>
            <sparkle:version>120</sparkle:version>
            <sparkle:shortVersionString>0.67.0</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <sparkle:hardwareRequirements>arm64</sparkle:hardwareRequirements>
            <description><![CDATA[<h3>Added</h3>
<ul>
<li>MongoDB filters on nested and array-element fields, with any-element and same-element matching and type-aware values. (#2315)</li>
<li>Back and Forward through a tab's browse history, on `Ctrl+Cmd+[` and `Ctrl+Cmd+]`. (#2316)</li>
<li>Drag to reorder editor tabs, with Move Tab Left and Move Tab Right on the tab's menu.</li>
<li>Result tabs named after the statement that produced them; picking one moves the editor cursor to it. (#2280)</li>
<li>Statement navigation on `Ctrl+Cmd+Left`, `Ctrl+Cmd+Right`, `Ctrl+Cmd+Enter`, `Option+Shift+Up` and `Option+Shift+Down`. (#2279)</li>
<li>Swipe left on a saved connection in the welcome window for Edit and Delete. (#2310)</li>
<li>Current-statement highlight and a per-statement run button in the editor gutter. (#2278)</li>
<li>Notifications when a long-running query, import, export, backup or save finishes in the background. (#2265)</li>
<li>Code folding in the SQL editor, the DDL and trigger views, the import preview, the AI review sheet, chat code blocks and the JSON viewer.</li>
<li>`Cmd+F` find bar over table results, with Search All Rows for a server-side match.</li>
<li>Native bar, line, area and scatter charts over query results. Starter feature.</li>
<li>Redis Sentinel and Redis Cluster connection modes. (#1021)</li>
<li>DuckDB connections can open a Parquet, CSV, TSV, JSON or NDJSON file read-only.</li>
<li>Korean localization for macOS, iPhone and iPad. (#2219, #2264)</li>
<li>Help > Acknowledgements, listing the 38 bundled open source libraries with their licenses.</li>
<li>Plugins signed by a third-party Apple Developer ID install after you trust the developer by name.</li>
<li>Local hash-chained execution log of every authorized database operation, with statements stored as digests.</li>
<li>A minimum Safe Mode level enforceable through a macOS configuration profile.</li>
<li>Execute All Statements in the Execute button's menu. (#2230)</li>
<li>Double-click or `Return` on a table keeps its tab instead of reusing the preview tab. (#2235)</li>
<li>View > Result View switches the results pane between Data, Structure, JSON and Chart.</li>
<li>MCP: the 2026-07-28 protocol revision, with the two previous revisions still supported.</li>
<li>MCP: server discovery in one call, naming protocol versions and capabilities.</li>
<li>MCP: cache lifetimes on tool, prompt and resource listings.</li>
<li>MCP: 27 new tools, covering schema metadata, query plans, row counts and statistics, users and grants, sessions, paginated browsing, inserts, transactions and DDL.</li>
<li>MCP: prompt templates built from the live schema, for explaining a schema, writing SQL, reviewing a query, proposing indexes, writing a migration, auditing data quality and summarizing a period.</li>
<li>MCP: argument completion from a connection's real tables, databases, schemas and columns.</li>
<li>MCP: change notifications over a single long-lived stream.</li>
<li>MCP: destructive statements can be confirmed in the client.</li>
<li>MCP: progress updates from a long-running tool call reach the client.</li>
</ul>
<h3>Changed</h3>
<ul>
<li>The results status bar was rebuilt: one fixed height, the row count on the left, controls that keep their place while a table loads, and Add Row moved to the toolbar.</li>
<li>Row counts and number grouping read correctly in every language.</li>
<li>The row count reflects what the grid is showing, and stays on screen for an empty result.</li>
<li>The rows-per-page menu accepts a locale-formatted number and states the range it takes.</li>
<li>An estimated row total is marked as an estimate, and Last and All rows wait for Count Exactly.</li>
<li>`Cmd+F` on a table tab opens the find bar; the filter panel keeps `Cmd+Option+F`.</li>
<li>Find Next and Find Previous work on the data grid.</li>
<li>The JSON and PHP tree filters ignore accents, so `cafe` finds `café`. (#2204)</li>
<li>Picking a database in the connections strip returns you to the tab you last used in it. (#2217)</li>
<li>Tabs showing same-named objects from different databases carry the database in their name. (#2217)</li>
<li>Save in the unsaved-changes prompt closes what you asked to close once the save lands.</li>
<li>A column value filter survives switching result mode and switching tabs. (#2251)</li>
<li>The connection form's Browse button offers only the file kinds the driver opens.</li>
<li>The iOS Shortcuts documentation states which app build each action needs.</li>
<li>TablePro is built in the Swift 6 language mode, across the app, both mobile apps, all 31 plugins and the shared packages.</li>
</ul>
<h3>Removed</h3>
<ul>
<li>MCP remote access. The server binds to this Mac only.</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>Forward slashes inside a nested object or array value are no longer escaped as `\/` in the JSON view and the right sidebar. (#2322)</li>
<li>A MongoDB filter TablePro cannot translate matches nothing instead of the whole collection. (#2315)</li>
<li>The raw filter row works on MongoDB, and reads Raw Filter on databases that do not speak SQL. (#2315)</li>
<li>Match Case applies to text fields only, so turning it off no longer breaks a MongoDB number, date or ObjectId filter. (#2315)</li>
<li>Preview Referenced Row from the Query menu reads the cell you focused instead of counting columns from the left.</li>
<li>Preview Referenced Row works on SQL Server, and on iPhone and iPad on SQL Server and Oracle.</li>
<li>Open Quickly no longer draws a rectangular outline around its Liquid Glass surface on macOS 26 and later. (#2321)</li>
<li>Skip and Continue keeps going after an unreadable line in a JSON import.</li>
<li>An import no longer counts rows it dropped because they matched no mapped column.</li>
<li>Retrying an import of a compressed `.sql` file no longer leaves the expanded copy behind.</li>
<li>Importing into a new table can be retried, reusing and clearing the table the failed attempt created.</li>
<li>The SQL import dialog no longer offers CSV and JSON in its format picker.</li>
<li>Import and export result alerts stay on screen instead of closing with the progress sheet. (#2314)</li>
<li>A failed import reports its line number, statement and server message instead of only the rollback error. (#2314)</li>
<li>The failed statement list is copyable again, with Copy Details on both alerts.</li>
<li>TablePro stays out of the Dock and the app switcher when an MCP client starts it.</li>
<li>A confirmation TablePro cannot attach to a window comes to the front instead of waiting behind other apps.</li>
<li>Settings > Integrations reports a running MCP server even when Enable MCP Server is off.</li>
<li>Deleting a connection with saved queries always warns that they go with it. (#2310)</li>
<li>Deleting a connection you published to the Team Library no longer deletes your local copy instead.</li>
<li>Connections in a linked folder or the Team Library open on double-click and on `Return`.</li>
<li>A connection whose deletion could not be saved no longer vanishes until the next launch.</li>
<li>Select All, `Ctrl+J` and `Ctrl+K` no longer skip favorited connections outside a group.</li>
<li>A column whose cells carry an action button no longer cuts its value short, and late-arriving enum, set and foreign key metadata widens it. (#2303)</li>
<li>Size to Fit and double-clicking a column edge measure the loaded page instead of about 30 rows of it. (#2303)</li>
<li>235 Turkish strings that were marked as translated but still held English now read in Turkish.</li>
<li>A transaction that failed on commit says so instead of blaming the last statement that worked. (#2280)</li>
<li>Opening an Oracle connection for the first time no longer leaves the object list spinning forever. (#2294)</li>
<li>An object list that cannot load says so and offers Retry instead of retrying silently every 15 seconds. (#2294)</li>
<li>A metadata connection whose startup commands stall gives up after a minute and reports it.</li>
<li>Oracle reports a connection that dropped during setup as failed, and closes a connection it gave up on. Needs the updated Oracle plugin.</li>
<li>iOS: a DuckDB contains filter matches case-insensitively, and offers the case sensitivity control.</li>
<li>Deleting a connection removes its saved queries and their folders from iCloud too.</li>
<li>A database type is drawn in the colour its own plugin declares everywhere, instead of two tables that disagreed on 12 of 23 types.</li>
<li>Save on the unsaved-changes prompt applies staged structure edits from any view, and leaves the tab open when the save is refused, cancelled or rejected.</li>
<li>Opening one table in two tabs gives each tab its own structure editor, sub-tab, filter and sort.</li>
<li>Clicking another table in the object browser no longer silently discards staged structure edits.</li>
<li>Switching between two structure or Create Table tabs no longer leaves the incoming tab's buttons dead.</li>
<li>Twelve commands spell their trailing ellipsis the way macOS does, in the menu bar and the context menu alike.</li>
<li>A `BEGIN ... END` body runs whole from the editor instead of being split at every semicolon inside it. (#2278)</li>
<li>A semicolon inside a MySQL `#` comment is no longer read as a statement boundary. (#2278)</li>
<li>A failed refresh no longer empties the stored procedure and function lists.</li>
<li>Switching to a PostgreSQL database you cannot open reports the failure and leaves the connection where it was.</li>
<li>Plugins from the registry are notarized, so Gatekeeper stops blocking them. Every plugin needs to be published again to pick this up.</li>
<li>Installing a plugin from a ZIP clears the quarantine flag from the whole bundle, not only its top level.</li>
<li>DuckDB lists its databases, schemas, tables and columns on a Mac that cannot reach the internet. (#2285)</li>
<li>A DuckDB UUID, ENUM, list, struct, map or time zone aware timestamp column no longer crashes TablePro.</li>
<li>A DuckDB view opens as `CREATE OR REPLACE VIEW` instead of two statements spliced together.</li>
<li>Switching DuckDB database by a name differing only in capitalisation lists that database's objects.</li>
<li>A DuckDB connection that fails after opening the file releases it instead of locking it until quit.</li>
<li>A DuckDB connection that opens a file but reports no catalog fails with that reason.</li>
<li>A mistyped Parquet or CSV path says the file is missing instead of creating an empty database.</li>
<li>A new DuckDB connection can be saved; the form no longer requires a database name it offered no field for.</li>
<li>Opening a table on Dameng, Oracle, BigQuery, Elasticsearch and etcd no longer puts up a Switch Failed alert. (#2262)</li>
<li>Switching schema on Dameng, Oracle, BigQuery, Snowflake and Trino re-reads only the routines instead of the whole object list. (#2262)</li>
<li>Open Quickly lists each schema once on Dameng, Oracle and BigQuery.</li>
<li>The Settings window is titled after the pane you are on, and refuses to be resized smaller than a pane can draw.</li>
<li>Redis Sentinel and Cluster connections show their nodes in the connection list instead of the word Redis alone.</li>
<li>Redis Cluster routes `OBJECT`, `MEMORY`, `SORT`, `GEORADIUS`, `ZUNIONSTORE` and `ZINTERSTORE` to the right shard on Redis 6, where the fallback routing table disagreed with the server on 32 counts.</li>
<li>Redis reports what the server actually answered instead of clearing a refused write from the grid.</li>
<li>`SET key value KEEPTTL` keeps the expiry, and SET options TablePro does not model are sent to the server untouched.</li>
<li>A Redis command whose reply timed out is re-sent only when it provably never reached the server.</li>
<li>A Redis `SCAN` that returns an error reports it instead of showing an empty keyspace.</li>
<li>Floating point values show the shortest digits that identify the stored number exactly, on MongoDB, Elasticsearch, ClickHouse, Snowflake, DynamoDB, Beancount and JSON import.</li>
<li>A 32-bit float bound as a query parameter keeps its own precision, and a value that is not a real number binds as null.</li>
<li>Editing a MongoDB row keeps each field's stored type, decimals and 64-bit integers included, at any nesting depth.</li>
<li>MQL export writes very large and very small numbers as numbers instead of quoted strings.</li>
<li>A nested value too long to show in full is no longer written back or exported as a fragment.</li>
<li>Dameng results are complete instead of stopping at the server's first 32KB batch. (#2262)</li>
<li>Dameng tables with a `CLOB` or `BLOB` column read the right columns, rows and values. (#2262)</li>
<li>Creating a Dameng table works when it has an index or a column comment, and so does changing a column's name and type at once. (#2262)</li>
<li>Column comments show up on Dameng tables in a schema not named after its owner. (#2262)</li>
<li>Dameng connections recover after a query is stopped instead of failing every statement for up to 30 seconds. (#2262)</li>
<li>Query Insights keeps each aggregate's query text and error inside the selected connection, source and date range. (#2268)</li>
<li>A plugin's own connection fields reach the connection form, so Redis gets its Key Separator field and its ElastiCache IAM section. (#1021)</li>
<li>A connection that lists its servers in a host list works over an SSH tunnel, MongoDB replica sets included.</li>
<li>A rows-per-page value large enough to overflow no longer crashes TablePro.</li>
<li>Changing rows-per-page on any page but the first no longer strands you on a mislabelled page.</li>
<li>Rows past a driver's row-count estimate are reachable.</li>
<li>All rows loads all of them instead of the estimated number.</li>
<li>Turning a page while Count Exactly is running no longer leaves a spinner that never stops.</li>
<li>A page number typed for one tab is no longer applied to another.</li>
<li>A table shows the range it actually loaded rather than its estimate.</li>
<li>A fresh query tab no longer shows a sliver of a status bar that jumps to full height on the first run.</li>
<li>VoiceOver reads the applied filter count on the Filters button and skips the status bar's separator dots.</li>
<li>JSON result mode shows the rows the grid is showing, in the same order, and resolves a selection to that same row. (#2251)</li>
<li>Rows marked for deletion no longer appear in JSON result mode, and the count line says how many are held back. (#2251)</li>
<li>Editing a cell in a result you clicked back to writes to that result's own table.</li>
<li>A result whose table TablePro cannot identify no longer looks editable.</li>
<li>Running several statements no longer carries the previous run's primary key over to the new table.</li>
<li>Column details that arrive after a query land on the result that asked for them.</li>
<li>Editing a field in the row inspector obeys the same rules as the grid.</li>
<li>Rows loaded by Fetch All stay with the result they were loaded for.</li>
<li>Switching between results asks before discarding unsaved edits.</li>
<li>Every restored tab hides the columns you hid for its table, not just the tab that was in front.</li>
<li>A query tab reopens with the caret where you left it, on every tab rather than the front one.</li>
<li>Restored tabs keep their sort and page number, not just the tab that was in front.</li>
<li>A restored tab reopens on the rows it was showing, because the page number is saved with its page size.</li>
<li>Scrolling a result with hundreds of columns is no longer slow; only the columns near the viewport are built. (#1219)</li>
<li>A column you hid keeps its place in the column order.</li>
<li>A row you are inserting shows empty values in JSON mode instead of TablePro's internal marker.</li>
<li>`Cmd+F` no longer opens a find bar in JSON result mode, where it could never match. (#2244)</li>
<li>The Filters button and `Cmd+Option+F` open the filter panel in JSON result mode. (#2244)</li>
<li>Add Row, Duplicate Row, Paste and Delete no longer collapse the JSON view to the row they touched. (#2244)</li>
<li>The date picker on an empty date or timestamp cell starts at your own clock and writes your own time. (#2241)</li>
<li>A time value carrying a UTC offset follows your chosen date format instead of showing as raw text. (#2241)</li>
<li>Open in New Tab opens a second tab for a table that is already open. (#2235)</li>
<li>Following a foreign key into a new tab no longer overwrites the filters on a tab already showing that table.</li>
<li>Opening a table from the sidebar while the object list is still loading opens it.</li>
<li>Opening a table from Favorites gives it a tab of its own.</li>
<li>A Cloud SQL Auth Proxy or Cloudflare tunnel that fails to start reports the last thing it said.</li>
<li>The Cloud SQL Auth Proxy and the Copilot language server are checked against their pinned version and digest on every start, and reinstalled when either no longer matches.</li>
<li>Editing a connection keeps its favorite mark, its place in the list, and its always-allowed AI tools.</li>
<li>MongoDB, Cassandra and Trino connections can name a database, keyspace or catalog in the connection form. (#2234)</li>
<li>Sorting a column keeps working after you hide or show columns. (#2234)</li>
<li>Hide All hides every column of a MongoDB collection, fields that appear only in later documents included. (#2234)</li>
<li>Opening a `.sql` file that is already open shows its tab instead of replacing what you typed in it.</li>
<li>A `.sql` tab reopened from Recently Closed knows it has unsaved edits.</li>
<li>Table maintenance from the object browser runs against the database the table you picked lives in.</li>
<li>A table that exists in two databases can be opened in both. (#2217)</li>
<li>Server Dashboard, Users & Roles, Query Insights, ER Diagram and a linked SQL file select the tab they already opened. (#2217)</li>
<li>The empty Favorites sidebar fits its width on macOS 15 instead of running past both edges.</li>
<li>Table favorites and saved queries sync through iCloud; their record types had never been created in the schema.</li>
<li>An SSH profile that uses a jump host syncs.</li>
<li>A connection carries its favorite mark, every tag, and its AI rules and always-allowed tools to your other Macs.</li>
<li>iOS: a connection's query timeout syncs instead of being dropped on upload.</li>
<li>The inspector button stays at the right end of the toolbar when the inspector is open.</li>
<li>The Tables and Favorites switch keeps following the sidebar after you open Customize Toolbar.</li>
<li>Refresh, New Tab, Open Quickly, Export, Database, Results and Dashboard are dimmed in the toolbar's overflow menu when they cannot run.</li>
<li>iOS: a value containing a backslash, a newline or a carriage return is stored as you typed it on PostgreSQL, Redshift, SQL Server, SQLite, DuckDB and Oracle.</li>
<li>iOS: PostgreSQL sessions set `standard_conforming_strings` on.</li>
<li>iOS: Add Row to Table and Add Rows to Table write to the database or schema you picked in Shortcuts.</li>
<li>iOS: all three Shortcuts actions are found by searching TablePro, and are grouped under Database.</li>
<li>`Cmd+W` closes the Settings and Integrations windows, the JSON and PHP viewers, the connection form and the column inspector.</li>
<li>The File menu names the close command after what it will close.</li>
<li>A JSON or PHP tree filter reveals nested matches, searches the whole of a long value, keeps rows expanded, and says when it finds nothing. (#2204)</li>
<li>Copy Value on a JSON object or array copies that part of the document instead of a summary. (#2204)</li>
<li>Closing a tab holding unsaved cell edits, `.sql` changes, staged structure changes or staged user and role changes asks before discarding them.</li>
<li>A tab with unsaved cell edits shows the unsaved dot.</li>
<li>Closing a tab no longer leaves its unsaved cell edits loaded behind it.</li>
<li>A `.sql` file opened from a linked favorite can be opened again after its tab is closed.</li>
<li>Staged structure changes and a table definition in progress survive switching tabs, and switching a table tab between Data and Structure.</li>
<li>Timestamps written with a space before the offset, or with fractional seconds, follow your chosen date format.</li>
<li>Switching connection no longer empties the SQL editor or kills its undo, highlighting, shortcuts and Vim mode. (#2236)</li>
</ul>
<h3>Security</h3>
<ul>
<li>A read-only MCP token can no longer run a statement that writes; an unrecognized statement is treated as a write, and statements that reach the filesystem or run a program are refused.</li>
<li>Safe Mode confirms MCP writes instead of pre-clearing them.</li>
<li>A token limited to some connections no longer sees the names, hosts, ports and usernames of the others.</li>
<li>With authentication off, a local caller is read-only and cannot administer anything.</li>
<li>The activity log records the calling token, stores statements as digests, is hash chained, and is no longer readable by other accounts on the Mac.</li>
<li>Repeated bad tokens are rate limited, requests with no credentials included.</li>
<li>A pairing link is verified and its destination shown before you approve it, a failed exchange burns its code, and approving no longer revokes an unrelated token of the same name.</li>
<li>A malformed cancellation message no longer crashes the app.</li>
<li>An MCP session identifier is no longer usable as a credential.</li>
<li>Secrets belonging to separately distributed drivers are stored in the Keychain and stripped from exported connections. Existing values move to the Keychain on the next save.</li>
</ul>
]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.67.0/TablePro-0.67.0-arm64.zip" length="24340358" type="application/octet-stream" sparkle:edSignature="cjQy/YkXH0cXOd4OivSDN8w74+yjSgf279A4h2+fLyJiZkIu3sAXOM5NSuz0UpjiMLQi5a/S2eYsYix4tqe6Aw=="/>
        </item>
        <item>
            <title>0.67.0</title>
            <pubDate>Fri, 21 Aug 2026 13:40:29 +0000</pubDate>
            <sparkle:version>120</sparkle:version>
            <sparkle:shortVersionString>0.67.0</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <description><![CDATA[<h3>Added</h3>
<ul>
<li>MongoDB filters on nested and array-element fields, with any-element and same-element matching and type-aware values. (#2315)</li>
<li>Back and Forward through a tab's browse history, on `Ctrl+Cmd+[` and `Ctrl+Cmd+]`. (#2316)</li>
<li>Drag to reorder editor tabs, with Move Tab Left and Move Tab Right on the tab's menu.</li>
<li>Result tabs named after the statement that produced them; picking one moves the editor cursor to it. (#2280)</li>
<li>Statement navigation on `Ctrl+Cmd+Left`, `Ctrl+Cmd+Right`, `Ctrl+Cmd+Enter`, `Option+Shift+Up` and `Option+Shift+Down`. (#2279)</li>
<li>Swipe left on a saved connection in the welcome window for Edit and Delete. (#2310)</li>
<li>Current-statement highlight and a per-statement run button in the editor gutter. (#2278)</li>
<li>Notifications when a long-running query, import, export, backup or save finishes in the background. (#2265)</li>
<li>Code folding in the SQL editor, the DDL and trigger views, the import preview, the AI review sheet, chat code blocks and the JSON viewer.</li>
<li>`Cmd+F` find bar over table results, with Search All Rows for a server-side match.</li>
<li>Native bar, line, area and scatter charts over query results. Starter feature.</li>
<li>Redis Sentinel and Redis Cluster connection modes. (#1021)</li>
<li>DuckDB connections can open a Parquet, CSV, TSV, JSON or NDJSON file read-only.</li>
<li>Korean localization for macOS, iPhone and iPad. (#2219, #2264)</li>
<li>Help > Acknowledgements, listing the 38 bundled open source libraries with their licenses.</li>
<li>Plugins signed by a third-party Apple Developer ID install after you trust the developer by name.</li>
<li>Local hash-chained execution log of every authorized database operation, with statements stored as digests.</li>
<li>A minimum Safe Mode level enforceable through a macOS configuration profile.</li>
<li>Execute All Statements in the Execute button's menu. (#2230)</li>
<li>Double-click or `Return` on a table keeps its tab instead of reusing the preview tab. (#2235)</li>
<li>View > Result View switches the results pane between Data, Structure, JSON and Chart.</li>
<li>MCP: the 2026-07-28 protocol revision, with the two previous revisions still supported.</li>
<li>MCP: server discovery in one call, naming protocol versions and capabilities.</li>
<li>MCP: cache lifetimes on tool, prompt and resource listings.</li>
<li>MCP: 27 new tools, covering schema metadata, query plans, row counts and statistics, users and grants, sessions, paginated browsing, inserts, transactions and DDL.</li>
<li>MCP: prompt templates built from the live schema, for explaining a schema, writing SQL, reviewing a query, proposing indexes, writing a migration, auditing data quality and summarizing a period.</li>
<li>MCP: argument completion from a connection's real tables, databases, schemas and columns.</li>
<li>MCP: change notifications over a single long-lived stream.</li>
<li>MCP: destructive statements can be confirmed in the client.</li>
<li>MCP: progress updates from a long-running tool call reach the client.</li>
</ul>
<h3>Changed</h3>
<ul>
<li>The results status bar was rebuilt: one fixed height, the row count on the left, controls that keep their place while a table loads, and Add Row moved to the toolbar.</li>
<li>Row counts and number grouping read correctly in every language.</li>
<li>The row count reflects what the grid is showing, and stays on screen for an empty result.</li>
<li>The rows-per-page menu accepts a locale-formatted number and states the range it takes.</li>
<li>An estimated row total is marked as an estimate, and Last and All rows wait for Count Exactly.</li>
<li>`Cmd+F` on a table tab opens the find bar; the filter panel keeps `Cmd+Option+F`.</li>
<li>Find Next and Find Previous work on the data grid.</li>
<li>The JSON and PHP tree filters ignore accents, so `cafe` finds `café`. (#2204)</li>
<li>Picking a database in the connections strip returns you to the tab you last used in it. (#2217)</li>
<li>Tabs showing same-named objects from different databases carry the database in their name. (#2217)</li>
<li>Save in the unsaved-changes prompt closes what you asked to close once the save lands.</li>
<li>A column value filter survives switching result mode and switching tabs. (#2251)</li>
<li>The connection form's Browse button offers only the file kinds the driver opens.</li>
<li>The iOS Shortcuts documentation states which app build each action needs.</li>
<li>TablePro is built in the Swift 6 language mode, across the app, both mobile apps, all 31 plugins and the shared packages.</li>
</ul>
<h3>Removed</h3>
<ul>
<li>MCP remote access. The server binds to this Mac only.</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>Forward slashes inside a nested object or array value are no longer escaped as `\/` in the JSON view and the right sidebar. (#2322)</li>
<li>A MongoDB filter TablePro cannot translate matches nothing instead of the whole collection. (#2315)</li>
<li>The raw filter row works on MongoDB, and reads Raw Filter on databases that do not speak SQL. (#2315)</li>
<li>Match Case applies to text fields only, so turning it off no longer breaks a MongoDB number, date or ObjectId filter. (#2315)</li>
<li>Preview Referenced Row from the Query menu reads the cell you focused instead of counting columns from the left.</li>
<li>Preview Referenced Row works on SQL Server, and on iPhone and iPad on SQL Server and Oracle.</li>
<li>Open Quickly no longer draws a rectangular outline around its Liquid Glass surface on macOS 26 and later. (#2321)</li>
<li>Skip and Continue keeps going after an unreadable line in a JSON import.</li>
<li>An import no longer counts rows it dropped because they matched no mapped column.</li>
<li>Retrying an import of a compressed `.sql` file no longer leaves the expanded copy behind.</li>
<li>Importing into a new table can be retried, reusing and clearing the table the failed attempt created.</li>
<li>The SQL import dialog no longer offers CSV and JSON in its format picker.</li>
<li>Import and export result alerts stay on screen instead of closing with the progress sheet. (#2314)</li>
<li>A failed import reports its line number, statement and server message instead of only the rollback error. (#2314)</li>
<li>The failed statement list is copyable again, with Copy Details on both alerts.</li>
<li>TablePro stays out of the Dock and the app switcher when an MCP client starts it.</li>
<li>A confirmation TablePro cannot attach to a window comes to the front instead of waiting behind other apps.</li>
<li>Settings > Integrations reports a running MCP server even when Enable MCP Server is off.</li>
<li>Deleting a connection with saved queries always warns that they go with it. (#2310)</li>
<li>Deleting a connection you published to the Team Library no longer deletes your local copy instead.</li>
<li>Connections in a linked folder or the Team Library open on double-click and on `Return`.</li>
<li>A connection whose deletion could not be saved no longer vanishes until the next launch.</li>
<li>Select All, `Ctrl+J` and `Ctrl+K` no longer skip favorited connections outside a group.</li>
<li>A column whose cells carry an action button no longer cuts its value short, and late-arriving enum, set and foreign key metadata widens it. (#2303)</li>
<li>Size to Fit and double-clicking a column edge measure the loaded page instead of about 30 rows of it. (#2303)</li>
<li>235 Turkish strings that were marked as translated but still held English now read in Turkish.</li>
<li>A transaction that failed on commit says so instead of blaming the last statement that worked. (#2280)</li>
<li>Opening an Oracle connection for the first time no longer leaves the object list spinning forever. (#2294)</li>
<li>An object list that cannot load says so and offers Retry instead of retrying silently every 15 seconds. (#2294)</li>
<li>A metadata connection whose startup commands stall gives up after a minute and reports it.</li>
<li>Oracle reports a connection that dropped during setup as failed, and closes a connection it gave up on. Needs the updated Oracle plugin.</li>
<li>iOS: a DuckDB contains filter matches case-insensitively, and offers the case sensitivity control.</li>
<li>Deleting a connection removes its saved queries and their folders from iCloud too.</li>
<li>A database type is drawn in the colour its own plugin declares everywhere, instead of two tables that disagreed on 12 of 23 types.</li>
<li>Save on the unsaved-changes prompt applies staged structure edits from any view, and leaves the tab open when the save is refused, cancelled or rejected.</li>
<li>Opening one table in two tabs gives each tab its own structure editor, sub-tab, filter and sort.</li>
<li>Clicking another table in the object browser no longer silently discards staged structure edits.</li>
<li>Switching between two structure or Create Table tabs no longer leaves the incoming tab's buttons dead.</li>
<li>Twelve commands spell their trailing ellipsis the way macOS does, in the menu bar and the context menu alike.</li>
<li>A `BEGIN ... END` body runs whole from the editor instead of being split at every semicolon inside it. (#2278)</li>
<li>A semicolon inside a MySQL `#` comment is no longer read as a statement boundary. (#2278)</li>
<li>A failed refresh no longer empties the stored procedure and function lists.</li>
<li>Switching to a PostgreSQL database you cannot open reports the failure and leaves the connection where it was.</li>
<li>Plugins from the registry are notarized, so Gatekeeper stops blocking them. Every plugin needs to be published again to pick this up.</li>
<li>Installing a plugin from a ZIP clears the quarantine flag from the whole bundle, not only its top level.</li>
<li>DuckDB lists its databases, schemas, tables and columns on a Mac that cannot reach the internet. (#2285)</li>
<li>A DuckDB UUID, ENUM, list, struct, map or time zone aware timestamp column no longer crashes TablePro.</li>
<li>A DuckDB view opens as `CREATE OR REPLACE VIEW` instead of two statements spliced together.</li>
<li>Switching DuckDB database by a name differing only in capitalisation lists that database's objects.</li>
<li>A DuckDB connection that fails after opening the file releases it instead of locking it until quit.</li>
<li>A DuckDB connection that opens a file but reports no catalog fails with that reason.</li>
<li>A mistyped Parquet or CSV path says the file is missing instead of creating an empty database.</li>
<li>A new DuckDB connection can be saved; the form no longer requires a database name it offered no field for.</li>
<li>Opening a table on Dameng, Oracle, BigQuery, Elasticsearch and etcd no longer puts up a Switch Failed alert. (#2262)</li>
<li>Switching schema on Dameng, Oracle, BigQuery, Snowflake and Trino re-reads only the routines instead of the whole object list. (#2262)</li>
<li>Open Quickly lists each schema once on Dameng, Oracle and BigQuery.</li>
<li>The Settings window is titled after the pane you are on, and refuses to be resized smaller than a pane can draw.</li>
<li>Redis Sentinel and Cluster connections show their nodes in the connection list instead of the word Redis alone.</li>
<li>Redis Cluster routes `OBJECT`, `MEMORY`, `SORT`, `GEORADIUS`, `ZUNIONSTORE` and `ZINTERSTORE` to the right shard on Redis 6, where the fallback routing table disagreed with the server on 32 counts.</li>
<li>Redis reports what the server actually answered instead of clearing a refused write from the grid.</li>
<li>`SET key value KEEPTTL` keeps the expiry, and SET options TablePro does not model are sent to the server untouched.</li>
<li>A Redis command whose reply timed out is re-sent only when it provably never reached the server.</li>
<li>A Redis `SCAN` that returns an error reports it instead of showing an empty keyspace.</li>
<li>Floating point values show the shortest digits that identify the stored number exactly, on MongoDB, Elasticsearch, ClickHouse, Snowflake, DynamoDB, Beancount and JSON import.</li>
<li>A 32-bit float bound as a query parameter keeps its own precision, and a value that is not a real number binds as null.</li>
<li>Editing a MongoDB row keeps each field's stored type, decimals and 64-bit integers included, at any nesting depth.</li>
<li>MQL export writes very large and very small numbers as numbers instead of quoted strings.</li>
<li>A nested value too long to show in full is no longer written back or exported as a fragment.</li>
<li>Dameng results are complete instead of stopping at the server's first 32KB batch. (#2262)</li>
<li>Dameng tables with a `CLOB` or `BLOB` column read the right columns, rows and values. (#2262)</li>
<li>Creating a Dameng table works when it has an index or a column comment, and so does changing a column's name and type at once. (#2262)</li>
<li>Column comments show up on Dameng tables in a schema not named after its owner. (#2262)</li>
<li>Dameng connections recover after a query is stopped instead of failing every statement for up to 30 seconds. (#2262)</li>
<li>Query Insights keeps each aggregate's query text and error inside the selected connection, source and date range. (#2268)</li>
<li>A plugin's own connection fields reach the connection form, so Redis gets its Key Separator field and its ElastiCache IAM section. (#1021)</li>
<li>A connection that lists its servers in a host list works over an SSH tunnel, MongoDB replica sets included.</li>
<li>A rows-per-page value large enough to overflow no longer crashes TablePro.</li>
<li>Changing rows-per-page on any page but the first no longer strands you on a mislabelled page.</li>
<li>Rows past a driver's row-count estimate are reachable.</li>
<li>All rows loads all of them instead of the estimated number.</li>
<li>Turning a page while Count Exactly is running no longer leaves a spinner that never stops.</li>
<li>A page number typed for one tab is no longer applied to another.</li>
<li>A table shows the range it actually loaded rather than its estimate.</li>
<li>A fresh query tab no longer shows a sliver of a status bar that jumps to full height on the first run.</li>
<li>VoiceOver reads the applied filter count on the Filters button and skips the status bar's separator dots.</li>
<li>JSON result mode shows the rows the grid is showing, in the same order, and resolves a selection to that same row. (#2251)</li>
<li>Rows marked for deletion no longer appear in JSON result mode, and the count line says how many are held back. (#2251)</li>
<li>Editing a cell in a result you clicked back to writes to that result's own table.</li>
<li>A result whose table TablePro cannot identify no longer looks editable.</li>
<li>Running several statements no longer carries the previous run's primary key over to the new table.</li>
<li>Column details that arrive after a query land on the result that asked for them.</li>
<li>Editing a field in the row inspector obeys the same rules as the grid.</li>
<li>Rows loaded by Fetch All stay with the result they were loaded for.</li>
<li>Switching between results asks before discarding unsaved edits.</li>
<li>Every restored tab hides the columns you hid for its table, not just the tab that was in front.</li>
<li>A query tab reopens with the caret where you left it, on every tab rather than the front one.</li>
<li>Restored tabs keep their sort and page number, not just the tab that was in front.</li>
<li>A restored tab reopens on the rows it was showing, because the page number is saved with its page size.</li>
<li>Scrolling a result with hundreds of columns is no longer slow; only the columns near the viewport are built. (#1219)</li>
<li>A column you hid keeps its place in the column order.</li>
<li>A row you are inserting shows empty values in JSON mode instead of TablePro's internal marker.</li>
<li>`Cmd+F` no longer opens a find bar in JSON result mode, where it could never match. (#2244)</li>
<li>The Filters button and `Cmd+Option+F` open the filter panel in JSON result mode. (#2244)</li>
<li>Add Row, Duplicate Row, Paste and Delete no longer collapse the JSON view to the row they touched. (#2244)</li>
<li>The date picker on an empty date or timestamp cell starts at your own clock and writes your own time. (#2241)</li>
<li>A time value carrying a UTC offset follows your chosen date format instead of showing as raw text. (#2241)</li>
<li>Open in New Tab opens a second tab for a table that is already open. (#2235)</li>
<li>Following a foreign key into a new tab no longer overwrites the filters on a tab already showing that table.</li>
<li>Opening a table from the sidebar while the object list is still loading opens it.</li>
<li>Opening a table from Favorites gives it a tab of its own.</li>
<li>A Cloud SQL Auth Proxy or Cloudflare tunnel that fails to start reports the last thing it said.</li>
<li>The Cloud SQL Auth Proxy and the Copilot language server are checked against their pinned version and digest on every start, and reinstalled when either no longer matches.</li>
<li>Editing a connection keeps its favorite mark, its place in the list, and its always-allowed AI tools.</li>
<li>MongoDB, Cassandra and Trino connections can name a database, keyspace or catalog in the connection form. (#2234)</li>
<li>Sorting a column keeps working after you hide or show columns. (#2234)</li>
<li>Hide All hides every column of a MongoDB collection, fields that appear only in later documents included. (#2234)</li>
<li>Opening a `.sql` file that is already open shows its tab instead of replacing what you typed in it.</li>
<li>A `.sql` tab reopened from Recently Closed knows it has unsaved edits.</li>
<li>Table maintenance from the object browser runs against the database the table you picked lives in.</li>
<li>A table that exists in two databases can be opened in both. (#2217)</li>
<li>Server Dashboard, Users & Roles, Query Insights, ER Diagram and a linked SQL file select the tab they already opened. (#2217)</li>
<li>The empty Favorites sidebar fits its width on macOS 15 instead of running past both edges.</li>
<li>Table favorites and saved queries sync through iCloud; their record types had never been created in the schema.</li>
<li>An SSH profile that uses a jump host syncs.</li>
<li>A connection carries its favorite mark, every tag, and its AI rules and always-allowed tools to your other Macs.</li>
<li>iOS: a connection's query timeout syncs instead of being dropped on upload.</li>
<li>The inspector button stays at the right end of the toolbar when the inspector is open.</li>
<li>The Tables and Favorites switch keeps following the sidebar after you open Customize Toolbar.</li>
<li>Refresh, New Tab, Open Quickly, Export, Database, Results and Dashboard are dimmed in the toolbar's overflow menu when they cannot run.</li>
<li>iOS: a value containing a backslash, a newline or a carriage return is stored as you typed it on PostgreSQL, Redshift, SQL Server, SQLite, DuckDB and Oracle.</li>
<li>iOS: PostgreSQL sessions set `standard_conforming_strings` on.</li>
<li>iOS: Add Row to Table and Add Rows to Table write to the database or schema you picked in Shortcuts.</li>
<li>iOS: all three Shortcuts actions are found by searching TablePro, and are grouped under Database.</li>
<li>`Cmd+W` closes the Settings and Integrations windows, the JSON and PHP viewers, the connection form and the column inspector.</li>
<li>The File menu names the close command after what it will close.</li>
<li>A JSON or PHP tree filter reveals nested matches, searches the whole of a long value, keeps rows expanded, and says when it finds nothing. (#2204)</li>
<li>Copy Value on a JSON object or array copies that part of the document instead of a summary. (#2204)</li>
<li>Closing a tab holding unsaved cell edits, `.sql` changes, staged structure changes or staged user and role changes asks before discarding them.</li>
<li>A tab with unsaved cell edits shows the unsaved dot.</li>
<li>Closing a tab no longer leaves its unsaved cell edits loaded behind it.</li>
<li>A `.sql` file opened from a linked favorite can be opened again after its tab is closed.</li>
<li>Staged structure changes and a table definition in progress survive switching tabs, and switching a table tab between Data and Structure.</li>
<li>Timestamps written with a space before the offset, or with fractional seconds, follow your chosen date format.</li>
<li>Switching connection no longer empties the SQL editor or kills its undo, highlighting, shortcuts and Vim mode. (#2236)</li>
</ul>
<h3>Security</h3>
<ul>
<li>A read-only MCP token can no longer run a statement that writes; an unrecognized statement is treated as a write, and statements that reach the filesystem or run a program are refused.</li>
<li>Safe Mode confirms MCP writes instead of pre-clearing them.</li>
<li>A token limited to some connections no longer sees the names, hosts, ports and usernames of the others.</li>
<li>With authentication off, a local caller is read-only and cannot administer anything.</li>
<li>The activity log records the calling token, stores statements as digests, is hash chained, and is no longer readable by other accounts on the Mac.</li>
<li>Repeated bad tokens are rate limited, requests with no credentials included.</li>
<li>A pairing link is verified and its destination shown before you approve it, a failed exchange burns its code, and approving no longer revokes an unrelated token of the same name.</li>
<li>A malformed cancellation message no longer crashes the app.</li>
<li>An MCP session identifier is no longer usable as a credential.</li>
<li>Secrets belonging to separately distributed drivers are stored in the Keychain and stripped from exported connections. Existing values move to the Keychain on the next save.</li>
</ul>
]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.67.0/TablePro-0.67.0-x86_64.zip" length="25877204" type="application/octet-stream" sparkle:edSignature="GVJfDHqq9y/elXSgIM+qpeiO1ykGuP/v1mKGqf7kP+7EUPY+nD3S6Ssssuzg6U1nqrNOlap9kOEKujlY2T7sCA=="/>
        </item>
        <item>
            <title>0.66.0</title>
            <pubDate>Tue, 18 Aug 2026 18:46:16 +0000</pubDate>
            <sparkle:version>119</sparkle:version>
            <sparkle:shortVersionString>0.66.0</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <sparkle:hardwareRequirements>arm64</sparkle:hardwareRequirements>
            <description><![CDATA[<h3>Added</h3>
<ul>
<li>Tree sidebar layouts group each database or schema's tables, views, materialized views, foreign tables, procedures and functions into collapsible folders. (#1590)</li>
<li>Dameng DM8 connections through a downloadable plugin, with schema browsing, table editing, metadata, DDL, transactions, EXPLAIN, query cancellation and a query timeout. Stopping a query closes its connection, and TablePro reconnects on the next one. (#1671, #2003, #2010)</li>
<li>SQL Server connections can sign in with Microsoft Entra ID on Azure SQL Database, Azure SQL Managed Instance and SQL Server 2022. Sign-in runs in your browser and honours multifactor authentication and Conditional Access. Set it up on the Mac; iPhone and iPad prompt to sign in once the connection syncs over.</li>
<li>Query Insights, a Starter feature in **Database > Query Insights**, summarizes the query history on your Mac: which queries you run most, which are slowest, which got slower and which fail. Queries that differ only by their values count as one, and nothing leaves the Mac. (#2107)</li>
</ul>
<h3>Changed</h3>
<ul>
<li>Five on-screen strings dropped their em dash, including the JSON and PHP viewer titles and the expired-license banner in **Settings > Account**. The JSON viewer title can be translated now.</li>
<li>The Quick Switcher is now **Open Quickly**, in the **File** menu under Open File. `Cmd+Shift+O` is unchanged and still rebindable in **Settings > Keyboard**. (#2185)</li>
<li>`Cmd+Return` opens the selected item in a new tab in Open Quickly, alongside `Option+Return`. (#2185)</li>
<li>Open Quickly is drawn on the system Liquid Glass material on macOS 26 and later, and keeps the blurred popover material on macOS 14 and 15.</li>
<li>Open Quickly's scope filters are a segmented control inside the panel now, instead of buttons that vanished as soon as anything was listed, which left four of the five scopes unpickable with the mouse.</li>
<li>Open Quickly has a footer for `Return`, `Cmd+Return`, `Cmd+1` to `Cmd+5` and `Escape`. The `Escape` hint reads **Clear** while the field has text and **Close** when it is empty.</li>
<li>A connection that fails because a Microsoft Entra ID or AWS SSO sign-in expired now offers to sign in again and reconnects for you, both when you open it and from the connection form's Test button.</li>
<li>The editor tab bar moved into the window chrome below the toolbar and lines up with the content pane, with its tabs in a filled track and the selected tab raised out of it. It still appears only once a connection has a second tab.</li>
<li>Tree layout opens a connection with its current database and schema expanded, unless the connection already remembers what you left open.</li>
<li>DuckDB connections now report a database name, so their saved column widths, per-table filters, favorites and recent tables reset once.</li>
<li>Turkish, Vietnamese, Simplified Chinese and Traditional Chinese cover 298 strings that were still showing in English, including the Query Insights tab, the Window and Help menus, the query plan viewer, the connection progress steps and the Microsoft Entra ID sign-in prompts.</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>Right-clicking a database and choosing Export now exports from the database you picked, with everything in it selected, instead of listing another database's tables. On DuckDB and PGlite, where a second connection cannot reach another database, Export is offered only for the active one.</li>
<li>On Oracle, Dameng and BigQuery, "Close Tabs for Other Databases" closed every query tab; it now closes only tabs in a different schema. The menu item reads "Close Tabs for Other Schemas" there, and Open Database reads "Open Schema".</li>
<li>A `tablepro://` link applies the database first and the schema second, instead of applying a database name as a schema on connections that have schemas, and ignores a segment the connection has no place for.</li>
<li>On connections with schemas, the toolbar shows the database and the schema side by side, each opening its own list, and the schema picker is back at the foot of the object list. Clicking the schema name used to open the database list. (#2196)</li>
<li>Data-grid columns reserve space for their trailing editor and foreign-key actions, so the foreign-key arrow and the enum dropdown chevron no longer widen a column after the rows are drawn, and a value that would otherwise fit is no longer truncated. A column you resized keeps the width you gave it.</li>
<li>Open Quickly reuses the object list it already built instead of re-querying the database and schema lists and 200 rows of query history each time it opens, which over an SSH tunnel is the whole time the panel sits empty. It rebuilds when the database, schema, database filter, saved queries or query history change, and on disconnect.</li>
<li>The column value filter, date and time picker, array editor, type picker, binary viewer and sidebar database filter now open against the control they belong to, instead of above and to the left of it.</li>
<li>The array, JSON, hex, date and type editors and the column value filter close when the rows change, so an editor left open while sorting, refreshing or filtering can no longer save onto a different row.</li>
<li>Open Quickly's Recent section now includes materialized views, foreign tables, system tables, partitioned tables and external tables, ranks them higher the more you open them, and tells apart two objects with the same name in different schemas.</li>
<li>Opening a table from Open Quickly reuses the tab that is already open, instead of showing the Open badge and Switch to Tab for a same-named table in another schema and then opening a second tab.</li>
<li>Open Quickly says it is loading instead of "No results", both while the object list is being fetched and while a search is being ranked.</li>
<li>Open Quickly's `Cmd+Return`, `Cmd+1` to `Cmd+5`, `Ctrl+J`, `Ctrl+K`, `Ctrl+N` and `Ctrl+P` work with Caps Lock on, and the numeric keypad's Enter works with `Cmd`.</li>
<li>Toolbar buttons show the shortcut you bound in **Settings > Keyboard** in their tooltip and overflow menu, and the Database and Preview buttons keep their shortcut hint after you switch connection. (#2185)</li>
<li>JSON results, Copy as JSON and JSON export no longer crash on unsigned or wider-than-64-bit integers, and keep every digit of large integers and high-precision decimals. A value a numeric column cannot represent, such as `Infinity` or digits outside 0-9, is quoted as a string instead of being written as invalid JSON.</li>
<li>Pasting a large block of text into the query editor no longer crashes the app, and the pasted text keeps its syntax highlighting instead of staying plain for the rest of the session. (#2158, #2172)</li>
<li>The flat sidebar keeps its Materialized Views, Foreign Tables, Procedures and Functions sections while a connection reconnects after a dropped SSH or proxy tunnel, and the flat and tree layouts show the same object kinds. (#2173)</li>
<li>`Cmd+C` in the query editor with nothing selected copies the current line instead of emptying the clipboard, and paste accepts RTF and file-path clipboards, fills a grid cell with a single copied value instead of adding a row, and works with focus in the sidebar. (#2172)</li>
<li>Sixteen-byte SQL binary columns respect their Display As setting in the grid, the row inspector, VoiceOver and single-cell copy, and keep it across a refresh. MongoDB binary UUIDs stay under the driver's Legacy UUID Encoding setting. (#2157)</li>
<li>A very long single line no longer stalls the app or eats memory, in the JSON value viewer, chat code blocks and the SQL review sheet, and in the SQL editor with Word Wrap on.</li>
<li>The data grid picks up a theme or appearance change made while it is on screen: an open cell editor repaints, deleted and newly added rows take the new tint in the results and structure grids, and the foreign-key arrow and dropdown chevron follow the window instead of keeping the colour that drew them first. The arrow has no circle now, and the chevron is drawn at its own size.</li>
<li>Format Query no longer crashes on an unclosed string literal ending in a backslash, as in `select * from t where c like 'C:\`, and leaves an unclosed `/*` comment alone.</li>
<li>Clicking the right edge of the SQL editor, a strip as wide as 140 points, puts the caret on that line, and text selection works there in the trigger editor, the JSON view, the structure DDL, the SQL review sheet, the import preview and chat code blocks. (#2156)</li>
<li>Query results are editable again when the `SELECT` aliases its table, as in `select * from users u where u.id = 1`, and when the query spans several lines, starts with a comment, or ends in `FOR UPDATE`. (#2150)</li>
<li>`UNION`, `EXCEPT` and `INTERSECT` results are read-only now instead of writing an edit into one branch, as are results from a join, a subquery, a CTE, a `FOR SYSTEM_TIME` read, and `FROM ONLY`.</li>
<li>Results stay editable when you write the schema in front of the table, as in `select * from public.users u`, as long as it is the schema the connection is browsing; naming a different schema stays read-only.</li>
<li>The connection window no longer draws a rule under its toolbar, which divided two areas of the same colour.</li>
<li>Empty Structure tabs and structured-value errors keep their toolbar at the top and center the message in the content area below it.</li>
<li>The sidebar star updates as soon as you add or remove a favorite table, instead of waiting for the sidebar to reopen.</li>
<li>Save Changes and Preview update as soon as data-grid changes are added, undone, saved or discarded.</li>
<li>Content stays readable on the blue selection fill in the connection switcher, the database switcher and the query history drawer: the row highlighted when they first open, the failure marker and connection dot, and the connection colour and status marks. (#2140)</li>
<li>Refreshing the database switcher keeps the list on screen instead of showing a spinner, and a failed refresh no longer hides the databases you were already looking at.</li>
<li>The database switcher keeps your highlighted database when its details finish loading, and no longer highlights a database the search filter hides, which left Return doing nothing.</li>
<li>Right-clicking a day heading in the query history drawer no longer clears the selected entry, and right-clicking a row in the connection switcher shows its menu instead of only moving the highlight.</li>
<li>The query history drawer stops re-reading history once you close it, and a drawer that starts closed does not load until you open it.</li>
<li>Query History's "Load in Editor" and "Run in New Tab" work again, from the buttons, the right-click menu, and `Return` or a double-click on a row.</li>
<li>"Run in New Tab" runs the query it opens, and asks first when that query writes, even with safe mode set to Silent.</li>
<li>A history entry opens in the window of the connection it was recorded against, in a tab bound to its own database, instead of loading into whatever is in front of you.</li>
<li>DuckDB `TIMESTAMP_S`, `TIMESTAMP_MS` and `TIMESTAMP_NS` columns show their stored value with sub-second digits, not `1970-01-01 00:00:00` on every row. (#2130)</li>
<li>DuckDB `UUID`, `ENUM`, `BIT`, `LIST`, `STRUCT`, `MAP`, `ARRAY` and `UNION` columns show their value instead of an empty cell. (#2130)</li>
<li>Querying two DuckDB tables that share a column name no longer repeats one table's value in both columns. (#2130)</li>
<li>DuckDB tables outside the `main` schema are visible again, with every schema listed in the sidebar and `ATTACH`ed databases shown alongside the one you opened. (#2131)</li>
<li>An `ATTACH`ed DuckDB file's tables stay out of the database you opened when the two share a schema name.</li>
<li>A DuckDB table in the default schema is titled `orders` again rather than `main.orders`, and exports preselect the schema you are browsing.</li>
<li>**File > Save As...** on a table, structure or diagram tab, and **File > Export > Export Results...** with no rows to export, are dimmed now instead of doing nothing when chosen.</li>
<li>Clearing query history from the drawer says what it will actually delete, instead of promising the connection's whole history when the source filter spares table browsing, row edits, imports and AI queries.</li>
<li>When TablePro cannot open the query history store, the drawer says so and that your history is still on disk, instead of showing "No Query History".</li>
<li>A license the server reports as suspended, expired or no longer activated on this Mac pauses Pro features at that check, instead of running on for up to a month because the reply was treated as if the server could not be reached. **Check Status** in **Settings > Account** says what the server replied.</li>
<li>A license bought with an email address containing a slash can be activated now, instead of telling you to update the app.</li>
</ul>
<h3>Removed</h3>
<ul>
<li>**Settings > Data** no longer lists saved table layouts and filters. TablePro still remembers both; reset columns from the table's Columns popover and filters from the filter panel.</li>
</ul>
<h3>Security</h3>
<ul>
<li>Paid features are read from the signed license, so an edited or copied local copy cannot enable them. TablePro honours a machine binding when the server sends one. (#2181)</li>
</ul>
]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.66.0/TablePro-0.66.0-arm64.zip" length="22436272" type="application/octet-stream" sparkle:edSignature="irQY2h+An1LXKuCyhTyM+t565mHfuziWCkZJpgEHBnLY1u8fzYWi6s465TNINWEod8OlYqaouxqAgto29/nNAA=="/>
        </item>
        <item>
            <title>0.66.0</title>
            <pubDate>Tue, 18 Aug 2026 18:46:17 +0000</pubDate>
            <sparkle:version>119</sparkle:version>
            <sparkle:shortVersionString>0.66.0</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <description><![CDATA[<h3>Added</h3>
<ul>
<li>Tree sidebar layouts group each database or schema's tables, views, materialized views, foreign tables, procedures and functions into collapsible folders. (#1590)</li>
<li>Dameng DM8 connections through a downloadable plugin, with schema browsing, table editing, metadata, DDL, transactions, EXPLAIN, query cancellation and a query timeout. Stopping a query closes its connection, and TablePro reconnects on the next one. (#1671, #2003, #2010)</li>
<li>SQL Server connections can sign in with Microsoft Entra ID on Azure SQL Database, Azure SQL Managed Instance and SQL Server 2022. Sign-in runs in your browser and honours multifactor authentication and Conditional Access. Set it up on the Mac; iPhone and iPad prompt to sign in once the connection syncs over.</li>
<li>Query Insights, a Starter feature in **Database > Query Insights**, summarizes the query history on your Mac: which queries you run most, which are slowest, which got slower and which fail. Queries that differ only by their values count as one, and nothing leaves the Mac. (#2107)</li>
</ul>
<h3>Changed</h3>
<ul>
<li>Five on-screen strings dropped their em dash, including the JSON and PHP viewer titles and the expired-license banner in **Settings > Account**. The JSON viewer title can be translated now.</li>
<li>The Quick Switcher is now **Open Quickly**, in the **File** menu under Open File. `Cmd+Shift+O` is unchanged and still rebindable in **Settings > Keyboard**. (#2185)</li>
<li>`Cmd+Return` opens the selected item in a new tab in Open Quickly, alongside `Option+Return`. (#2185)</li>
<li>Open Quickly is drawn on the system Liquid Glass material on macOS 26 and later, and keeps the blurred popover material on macOS 14 and 15.</li>
<li>Open Quickly's scope filters are a segmented control inside the panel now, instead of buttons that vanished as soon as anything was listed, which left four of the five scopes unpickable with the mouse.</li>
<li>Open Quickly has a footer for `Return`, `Cmd+Return`, `Cmd+1` to `Cmd+5` and `Escape`. The `Escape` hint reads **Clear** while the field has text and **Close** when it is empty.</li>
<li>A connection that fails because a Microsoft Entra ID or AWS SSO sign-in expired now offers to sign in again and reconnects for you, both when you open it and from the connection form's Test button.</li>
<li>The editor tab bar moved into the window chrome below the toolbar and lines up with the content pane, with its tabs in a filled track and the selected tab raised out of it. It still appears only once a connection has a second tab.</li>
<li>Tree layout opens a connection with its current database and schema expanded, unless the connection already remembers what you left open.</li>
<li>DuckDB connections now report a database name, so their saved column widths, per-table filters, favorites and recent tables reset once.</li>
<li>Turkish, Vietnamese, Simplified Chinese and Traditional Chinese cover 298 strings that were still showing in English, including the Query Insights tab, the Window and Help menus, the query plan viewer, the connection progress steps and the Microsoft Entra ID sign-in prompts.</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>Right-clicking a database and choosing Export now exports from the database you picked, with everything in it selected, instead of listing another database's tables. On DuckDB and PGlite, where a second connection cannot reach another database, Export is offered only for the active one.</li>
<li>On Oracle, Dameng and BigQuery, "Close Tabs for Other Databases" closed every query tab; it now closes only tabs in a different schema. The menu item reads "Close Tabs for Other Schemas" there, and Open Database reads "Open Schema".</li>
<li>A `tablepro://` link applies the database first and the schema second, instead of applying a database name as a schema on connections that have schemas, and ignores a segment the connection has no place for.</li>
<li>On connections with schemas, the toolbar shows the database and the schema side by side, each opening its own list, and the schema picker is back at the foot of the object list. Clicking the schema name used to open the database list. (#2196)</li>
<li>Data-grid columns reserve space for their trailing editor and foreign-key actions, so the foreign-key arrow and the enum dropdown chevron no longer widen a column after the rows are drawn, and a value that would otherwise fit is no longer truncated. A column you resized keeps the width you gave it.</li>
<li>Open Quickly reuses the object list it already built instead of re-querying the database and schema lists and 200 rows of query history each time it opens, which over an SSH tunnel is the whole time the panel sits empty. It rebuilds when the database, schema, database filter, saved queries or query history change, and on disconnect.</li>
<li>The column value filter, date and time picker, array editor, type picker, binary viewer and sidebar database filter now open against the control they belong to, instead of above and to the left of it.</li>
<li>The array, JSON, hex, date and type editors and the column value filter close when the rows change, so an editor left open while sorting, refreshing or filtering can no longer save onto a different row.</li>
<li>Open Quickly's Recent section now includes materialized views, foreign tables, system tables, partitioned tables and external tables, ranks them higher the more you open them, and tells apart two objects with the same name in different schemas.</li>
<li>Opening a table from Open Quickly reuses the tab that is already open, instead of showing the Open badge and Switch to Tab for a same-named table in another schema and then opening a second tab.</li>
<li>Open Quickly says it is loading instead of "No results", both while the object list is being fetched and while a search is being ranked.</li>
<li>Open Quickly's `Cmd+Return`, `Cmd+1` to `Cmd+5`, `Ctrl+J`, `Ctrl+K`, `Ctrl+N` and `Ctrl+P` work with Caps Lock on, and the numeric keypad's Enter works with `Cmd`.</li>
<li>Toolbar buttons show the shortcut you bound in **Settings > Keyboard** in their tooltip and overflow menu, and the Database and Preview buttons keep their shortcut hint after you switch connection. (#2185)</li>
<li>JSON results, Copy as JSON and JSON export no longer crash on unsigned or wider-than-64-bit integers, and keep every digit of large integers and high-precision decimals. A value a numeric column cannot represent, such as `Infinity` or digits outside 0-9, is quoted as a string instead of being written as invalid JSON.</li>
<li>Pasting a large block of text into the query editor no longer crashes the app, and the pasted text keeps its syntax highlighting instead of staying plain for the rest of the session. (#2158, #2172)</li>
<li>The flat sidebar keeps its Materialized Views, Foreign Tables, Procedures and Functions sections while a connection reconnects after a dropped SSH or proxy tunnel, and the flat and tree layouts show the same object kinds. (#2173)</li>
<li>`Cmd+C` in the query editor with nothing selected copies the current line instead of emptying the clipboard, and paste accepts RTF and file-path clipboards, fills a grid cell with a single copied value instead of adding a row, and works with focus in the sidebar. (#2172)</li>
<li>Sixteen-byte SQL binary columns respect their Display As setting in the grid, the row inspector, VoiceOver and single-cell copy, and keep it across a refresh. MongoDB binary UUIDs stay under the driver's Legacy UUID Encoding setting. (#2157)</li>
<li>A very long single line no longer stalls the app or eats memory, in the JSON value viewer, chat code blocks and the SQL review sheet, and in the SQL editor with Word Wrap on.</li>
<li>The data grid picks up a theme or appearance change made while it is on screen: an open cell editor repaints, deleted and newly added rows take the new tint in the results and structure grids, and the foreign-key arrow and dropdown chevron follow the window instead of keeping the colour that drew them first. The arrow has no circle now, and the chevron is drawn at its own size.</li>
<li>Format Query no longer crashes on an unclosed string literal ending in a backslash, as in `select * from t where c like 'C:\`, and leaves an unclosed `/*` comment alone.</li>
<li>Clicking the right edge of the SQL editor, a strip as wide as 140 points, puts the caret on that line, and text selection works there in the trigger editor, the JSON view, the structure DDL, the SQL review sheet, the import preview and chat code blocks. (#2156)</li>
<li>Query results are editable again when the `SELECT` aliases its table, as in `select * from users u where u.id = 1`, and when the query spans several lines, starts with a comment, or ends in `FOR UPDATE`. (#2150)</li>
<li>`UNION`, `EXCEPT` and `INTERSECT` results are read-only now instead of writing an edit into one branch, as are results from a join, a subquery, a CTE, a `FOR SYSTEM_TIME` read, and `FROM ONLY`.</li>
<li>Results stay editable when you write the schema in front of the table, as in `select * from public.users u`, as long as it is the schema the connection is browsing; naming a different schema stays read-only.</li>
<li>The connection window no longer draws a rule under its toolbar, which divided two areas of the same colour.</li>
<li>Empty Structure tabs and structured-value errors keep their toolbar at the top and center the message in the content area below it.</li>
<li>The sidebar star updates as soon as you add or remove a favorite table, instead of waiting for the sidebar to reopen.</li>
<li>Save Changes and Preview update as soon as data-grid changes are added, undone, saved or discarded.</li>
<li>Content stays readable on the blue selection fill in the connection switcher, the database switcher and the query history drawer: the row highlighted when they first open, the failure marker and connection dot, and the connection colour and status marks. (#2140)</li>
<li>Refreshing the database switcher keeps the list on screen instead of showing a spinner, and a failed refresh no longer hides the databases you were already looking at.</li>
<li>The database switcher keeps your highlighted database when its details finish loading, and no longer highlights a database the search filter hides, which left Return doing nothing.</li>
<li>Right-clicking a day heading in the query history drawer no longer clears the selected entry, and right-clicking a row in the connection switcher shows its menu instead of only moving the highlight.</li>
<li>The query history drawer stops re-reading history once you close it, and a drawer that starts closed does not load until you open it.</li>
<li>Query History's "Load in Editor" and "Run in New Tab" work again, from the buttons, the right-click menu, and `Return` or a double-click on a row.</li>
<li>"Run in New Tab" runs the query it opens, and asks first when that query writes, even with safe mode set to Silent.</li>
<li>A history entry opens in the window of the connection it was recorded against, in a tab bound to its own database, instead of loading into whatever is in front of you.</li>
<li>DuckDB `TIMESTAMP_S`, `TIMESTAMP_MS` and `TIMESTAMP_NS` columns show their stored value with sub-second digits, not `1970-01-01 00:00:00` on every row. (#2130)</li>
<li>DuckDB `UUID`, `ENUM`, `BIT`, `LIST`, `STRUCT`, `MAP`, `ARRAY` and `UNION` columns show their value instead of an empty cell. (#2130)</li>
<li>Querying two DuckDB tables that share a column name no longer repeats one table's value in both columns. (#2130)</li>
<li>DuckDB tables outside the `main` schema are visible again, with every schema listed in the sidebar and `ATTACH`ed databases shown alongside the one you opened. (#2131)</li>
<li>An `ATTACH`ed DuckDB file's tables stay out of the database you opened when the two share a schema name.</li>
<li>A DuckDB table in the default schema is titled `orders` again rather than `main.orders`, and exports preselect the schema you are browsing.</li>
<li>**File > Save As...** on a table, structure or diagram tab, and **File > Export > Export Results...** with no rows to export, are dimmed now instead of doing nothing when chosen.</li>
<li>Clearing query history from the drawer says what it will actually delete, instead of promising the connection's whole history when the source filter spares table browsing, row edits, imports and AI queries.</li>
<li>When TablePro cannot open the query history store, the drawer says so and that your history is still on disk, instead of showing "No Query History".</li>
<li>A license the server reports as suspended, expired or no longer activated on this Mac pauses Pro features at that check, instead of running on for up to a month because the reply was treated as if the server could not be reached. **Check Status** in **Settings > Account** says what the server replied.</li>
<li>A license bought with an email address containing a slash can be activated now, instead of telling you to update the app.</li>
</ul>
<h3>Removed</h3>
<ul>
<li>**Settings > Data** no longer lists saved table layouts and filters. TablePro still remembers both; reset columns from the table's Columns popover and filters from the filter panel.</li>
</ul>
<h3>Security</h3>
<ul>
<li>Paid features are read from the signed license, so an edited or copied local copy cannot enable them. TablePro honours a machine binding when the server sends one. (#2181)</li>
</ul>
]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.66.0/TablePro-0.66.0-x86_64.zip" length="23796203" type="application/octet-stream" sparkle:edSignature="iF0YUPp678V9gPTNu7PVYrfACiW1BIxuVrfh5vrjhTnC5LVyI2b4DqQRaWg0h0qzTqDmYMR8rDABwG859pe0CA=="/>
        </item>
        <item>
            <title>0.65.0</title>
            <pubDate>Sat, 15 Aug 2026 20:09:38 +0000</pubDate>
            <sparkle:version>118</sparkle:version>
            <sparkle:shortVersionString>0.65.0</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <sparkle:hardwareRequirements>arm64</sparkle:hardwareRequirements>
            <description><![CDATA[<h3>Added</h3>
<ul>
<li>Query History is a resizable drawer that opens per connection and remembers its height, its filters and whether it was open. Entries group by day and load in pages, so history older than the most recent few hundred queries is reachable for the first time.</li>
<li>Query History filters by where a query came from: the editor, EXPLAIN runs, the SELECTs the app generates while you browse a table, grid row edits, structure changes, imports, and AI or MCP clients. It shows your own queries by default.</li>
<li>Query History filters to failed queries only, and by last hour, today, last 7 days or last 4 weeks. It can be scoped to one connection or searched across all of them, and names the connection each query belongs to when you widen the scope.</li>
<li>Query History works from the keyboard. Arrow keys move through entries, and `Return` or a double-click loads one into the editor.</li>
<li>Query History has a pause button. While it is paused nothing is recorded from any source, including row edits, structure changes, imports and AI clients. Pausing stays on the Mac you press it on.</li>
<li>Clearing query history from the drawer clears only what the drawer is showing, so clearing one connection leaves the others alone. Settings still clears everything.</li>
<li>A query plan opens as a result tab next to your query results, so you can switch back to the data without re-running the query, and a plan can be pinned.</li>
<li>MySQL `EXPLAIN FORMAT=TREE` and `EXPLAIN ANALYZE`, plus PGlite, Cloudflare D1, libSQL and Turso plans, render as a diagram and tree instead of raw text.</li>
<li>Plan diagrams zoom with a trackpad pinch, a two-finger double tap, or Cmd and scroll, fit to the window, and copy or export as a PNG.</li>
<li>The plan tree has resizable, sortable Operation, Cost, Rows and Actual Time columns, arrow-key navigation, a right-click menu to copy a step, and a detail panel you can resize.</li>
<li>Plan steps show a cost badge whose shape and colour escalate together, so cost reads without relying on colour.</li>
<li>The ER diagram uses the same zoom and scrolling as the plan diagram, gaining two-finger double tap, Cmd and scroll, and standard scrollers.</li>
<li>PostgreSQL array columns of a simple type, including arrays of an enum, get a list editor in the data grid: one row per element, with reordering, add, remove and NULL per element. An empty array and a NULL column stay separate values. Arrays of `jsonb`, `bytea` or composite types, and multi-dimensional values, keep the plain text editor.</li>
<li>The editor underlines a structural mistake as you type, such as a closing bracket with no opener or an unterminated comment. On MongoDB it also reports what the query parser rejects. A half-written statement is never flagged. (#2095)</li>
<li>PostgreSQL enum values are suggested when you compare against an enum column, so `WHERE status = ` offers the labels the type declares. (#2095)</li>
<li>PostgreSQL autocomplete knows the operators, including `::`, the JSON ones, array and range containment, regex matching and full-text search, about 400 built-in functions, and multi-word syntax such as `ON CONFLICT DO UPDATE SET` and window frame clauses. (#2095)</li>
<li>Autocomplete for MongoDB queries. `db.` lists collections, `db.users.` lists the driver methods, and inside a query you get field names, nested paths such as `address.city`, and the operators valid in that spot. (#2095)</li>
<li>MongoDB updates accept an options argument, so `db.users.updateOne({...}, {...}, {upsert: true})` upserts. `arrayFilters` and `hint` are passed through too. (#2095)</li>
<li>The MongoDB editor accepts mongosh value constructors in filters and pipelines, so a value copied from the grid pastes straight into a query. Covers `ObjectId`, `ISODate`, `Date`, the `Number*` family, `Timestamp`, `BinData`, `HexData`, `MinKey`, `MaxKey` and the UUID names. (#2086)</li>
<li>Format Query follows the editor language. On a MongoDB tab it lays out filters and pipelines by nesting depth instead of running the SQL formatter over them. (#2095)</li>
<li>Legacy UUID Encoding on a MongoDB connection, so binary UUIDs written by the Java, C# or Python drivers read as UUIDs instead of hex. Filters, edits and MQL exports write the same bytes back. (#2086)</li>
<li>Select several databases or schemas in the sidebar tree, or in the database switcher, and drop, refresh, copy names or export them at once. Shift-click and Cmd-click extend the selection.</li>
<li>The sidebar follows Sidebar icon size in System Settings > Appearance, so it matches Finder and Mail. A Row Size control in General settings and in View Options overrides it when you want to fit more objects on screen.</li>
<li>Right-clicking a row in the object list or the Favorites list highlights the row the menu will act on, the way Finder does. Right-clicking the empty space below either list gives a menu too, which is where View Options, New Query, New Favorite, New Folder and Add Linked SQL Folder now live.</li>
<li>Add to Favorites is in the sidebar's right-click menu, so it no longer needs a hover to reach.</li>
<li>Typing a name in the database tree jumps to the matching object.</li>
<li>Quick Switcher searches tables, views, saved queries and recent queries across every open connection.</li>
<li>Drop Schema for PostgreSQL, SQL Server and SurrealDB.</li>
<li>Editor tabs have a right-click menu for closing tabs.</li>
<li>Redis key rows have a right-click menu for copying a key or namespace prefix.</li>
<li>Dropping a SQL file on a connection window opens it. (#2104)</li>
<li>The Settings window can be resized.</li>
<li>SSL settings on mobile for MySQL, PostgreSQL and Redis: a mode picker plus CA, client certificate and client key. Import a PEM file, paste one, or use a PKCS#12 file. (#2083)</li>
</ul>
<h3>Changed</h3>
<ul>
<li>Every open connection now lives in one window. Picking a connection in the connections strip switches that window to it instead of raising a second window. Each connection keeps its own view while it waits its turn, so switching away and back leaves the grid scrolled where it was, the editor's cursor and selection where you left them, and a half-filled sheet still filled in.</li>
<li>Opening a table or query on a connection you already have open adds a tab to that window instead of opening another window. A tab strip appears once a connection holds more than one tab.</li>
<li>Closing the last tab leaves the connection open on its empty state. Close Tab again closes the connection, and the window once that was the last one open.</li>
<li>Window tabs follow your "Prefer tabs when opening documents" setting instead of always forcing tabs, and the Window menu carries Move Tab to New Window and Merge All Windows.</li>
<li>Clicking a table in the sidebar opens it right away. It used to wait out the double-click interval, about half a second, to find out whether a second click was coming. **Open in New Tab** on the table's contextual menu opens a second copy.</li>
<li>Opening a table from the sidebar leaves the keyboard in the sidebar, so you can keep clicking or arrowing through tables and watch each one load. Click into the grid when you want to work in it.</li>
<li>The bar at the bottom of the sidebar is gone, and everything it held moved somewhere a window can never hide. New Table and New View are in the Database menu and the sidebar's right-click menu. Switching schema is Database > Schema. The database filter is View > Filter Databases. The refresh spinner joined the other progress in the toolbar.</li>
<li>A filtered database tree says so, with a banner naming how many of the databases it is showing and a button to show them all.</li>
<li>Sidebar section titles are real source list headers now: a short grey title with no icon, and the objects under it sitting at the same depth as a database instead of one step in.</li>
<li>MongoDB shows a standard binary UUID as `UUID("...")` everywhere, including in exports.</li>
<li>An imported connection link shows the startup SQL and driver options it carries, before you add it.</li>
<li>Mobile keeps remote connections open when you switch apps.</li>
<li>Mobile no longer copies database passwords to iCloud Keychain unless you turn on Sync Passwords. Mac already worked this way.</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>The Quick Switcher opens what you searched for. Typing a new search kept the row you had highlighted selected whenever it still matched anything, and searching across connections matches the connection path in every row, so almost any row stayed highlighted while the list reordered under it. `Return` then opened something you had stopped searching for.</li>
<li>A query that fails shows the database's error again and is recorded in query history. A syntax error, or selecting from a table that does not exist, left the results area blank with no message at all, and opening a table that failed to load was silent the same way. The error also opens the results pane if you had it collapsed. (#2120)</li>
<li>Running a query with parameters, or several statements at once, no longer stops the tab from accepting any later run.</li>
<li>Query History no longer mixes every connection's queries together with no way to tell them apart. Loading one could put another connection's SQL into the editor in front of you.</li>
<li>Searching query history matches as you type, and several words match entries that contain them all, wherever they appear. It only matched whole words sitting next to each other, so `user` found nothing until you finished `users`, and `select customers` found nothing at all.</li>
<li>Query History keeps the older entries you loaded when a query finishes while the drawer is open, and stops refetching once per statement while a large save or import runs.</li>
<li>Deleting a connection deletes its query history, its drawer filters and its saved queries with it.</li>
<li>Trimming history to the retention limits refreshes the open drawer instead of leaving deleted entries on screen, including when the limits arrive from another Mac over iCloud.</li>
<li>Turning off automatic query history cleanup now actually stops it. Entries were still pruned to the limits every hundred queries regardless of the setting.</li>
<li>Query history highlights each query for the database it actually ran on. Everything except MongoDB was coloured as MySQL.</li>
<li>The query history duration column is honest. Ten of the places that record a query reported no duration as `0 ms`; they either measure it now or show `–`, and a query faster than a millisecond shows `<1 ms`.</li>
<li>Query history rows show a SQLite database by file name instead of its full path, which used to fill the row.</li>
<li>A failed grid save records one history entry per statement, the same as a successful one, instead of joining them all into a single unusable entry.</li>
<li>Cancelling an import is no longer recorded as a failed import, and an import no longer reports its statement count as a row count.</li>
<li>The Quick Switcher no longer fills up with the same statement repeated. It shows each distinct query once, most recent first, and a long list of saved queries can no longer push recent queries out of the panel entirely.</li>
<li>On iPhone and iPad, a query that failed is kept in history and marked, instead of vanishing.</li>
<li>Corrected two claims about iCloud sync: it does not sync query history, and there is no history sync limit to reduce when storage is full.</li>
<li>Running EXPLAIN from the toolbar asks for confirmation when Safe Mode requires it. It skipped that check on every database that offers an EXPLAIN variant, even though `EXPLAIN ANALYZE` runs the query.</li>
<li>The Stop button can cancel a running `EXPLAIN ANALYZE`, and EXPLAIN runs started from the toolbar are recorded in Query History.</li>
<li>An EXPLAIN that fails reports the error in the usual place instead of showing it where the plan should be, and Clear Query clears the plan instead of leaving a stale one on screen.</li>
<li>A query plan that cannot be read as a tree says so and shows the raw output instead of leaving an empty pane, and a second EXPLAIN in the same tab redraws the diagram.</li>
<li>Plan diagrams line every node of the same depth up on one row, so a tall box no longer pushes its children up into itself.</li>
<li>Plans that report a cost per node but no startup cost, such as MySQL's, show that cost in the diagram and the tree, and nodes in a plan whose root reports no cost are no longer all painted red.</li>
<li>Closing a connection removes it from the connections strip, ends its session, and closes its tunnel. It stayed listed as connected, could not be clicked, and its database connection stayed open.</li>
<li>The strip's close command ends the connection: every tab across every database it has open, its session, and every row it holds in the strip. It used to close only the tabs of one database and leave the row you clicked exactly where it was. Disconnect still ends only the session and keeps the row. **File > Close Connection** does the same from the menu bar.</li>
<li>Closing a window disconnects every connection it was showing, not just one of them.</li>
<li>**Open in New Window** on a connection in the strip moves it out of the shared window with its tabs, its session and its unsaved work.</li>
<li>Cmd+W closes the welcome window. The shortcut was bound to Close Tab, which the welcome window has no answer for, so it did nothing there.</li>
<li>Typing in a new query tab goes into the editor. Cmd+T left the keyboard on the sidebar, so the first characters you typed went to the object list instead.</li>
<li>An alert opens on the window you were working in. It could attach itself to a floating panel such as the Quick Switcher, which takes the alert with it when it closes.</li>
<li>Switching connection no longer rebuilds the toolbar. Its items resize to the connection you switched to instead of holding the previous one's width, the Results button tracks the pane you are looking at, and Snowflake's role and warehouse menus reload.</li>
<li>Customize Toolbar no longer blanks the connection and status controls, and a connection that loses its session no longer puts your toolbar arrangement at risk. The items you had customized could be dropped for good.</li>
<li>Main window toolbar buttons are real toolbar items, so icon only mode, display mode customization and the overflow menu all work.</li>
<li>Toolbar tooltips name what the button does and show the current keyboard shortcut again. Import works from the toolbar, and the Results button shows whether the pane is open. (#2104)</li>
<li>A tag with no colour is readable in the toolbar instead of drawing white on nothing.</li>
<li>Refreshing a database or schema from the sidebar no longer empties it. The tables and routines came back only when the refresh finished, and a refresh that failed left an error where they had been.</li>
<li>Hiding a database in the sidebar's database filter takes effect straight away. The tree kept listing every database until some other change happened to rebuild it.</li>
<li>The sidebar drops a database from the tree as soon as you drop it on the server, instead of listing it until you reconnect.</li>
<li>Holding an arrow key in the sidebar no longer opens a tab and runs a query for every object it passes. Arrowing through 20 tables fired 20 queries.</li>
<li>Arrowing to a table in the sidebar opens it right away. The open waited out the key repeat rate from System Settings, which is up to two seconds, even for a single press.</li>
<li>Cmd-clicking or Shift-arrowing to select several tables no longer opens the table it added. Building a selection for Truncate, Delete or Export ran a query, replaced the tab, and could switch the active database.</li>
<li>Right-clicking a table that is not part of the current selection acts on that table. It used to act on the selected tables instead, including for Delete.</li>
<li>The sidebar object list is a native outline in every layout, so the selected row is drawn as the active selection, arrow keys move between objects, and typing jumps to a name. The flat and schema layouts were SwiftUI lists that could not do any of that.</li>
<li>Routines, procedures, functions, Redis keys and Recent entries can be selected in the sidebar, so arrow keys reach them and type-to-find lands on them. Clicking a Recent entry highlights the row it opened.</li>
<li>The Favorites tab is a native outline too, so saved queries, folders and linked SQL files take the keyboard and show a real selection. Team Library entries can be selected for the first time; opening one is now a double-click or Return, and its publisher shows beside the name instead of under it.</li>
<li>Truncate, Copy Name and Delete act on the sidebar selection in tree layout. They read a selection the tree never published, so they did nothing at all.</li>
<li>Clicking a row in the sidebar puts the keyboard on the list. The click moved the selection but sometimes left the keyboard in the filter field above it, so the row drew grey and the arrow keys went to the field. Switching to the Favorites tab left the keyboard nowhere at all.</li>
<li>Opening a table from somewhere that asks for the grid, such as Favorites or Show Structure, puts the keyboard in the grid. Only the first such table of a session did.</li>
<li>Down arrow in the sidebar filter field moves into the object list instead of being swallowed.</li>
<li>Typing in the sidebar filter no longer restarts its own delay when anything else redraws the sidebar, so the list settles when you stop typing.</li>
<li>Collapsing a database or a section while the sidebar filter has text no longer throws away the layout you had before searching. The row sprang back open on the next keystroke, and stayed collapsed once the filter was cleared. A collapse is recorded once AppKit has applied it, so the saved state always describes what is on screen.</li>
<li>The favourite star in the sidebar takes a click anywhere in its button, not only on the star itself. A near miss selected the table and opened it instead.</li>
<li>Favorites rows follow the sidebar row size like the object list does, and both lists draw their rows at the same height. One inset its rows a point more than the other, so the two tabs did not line up.</li>
<li>Renaming a favourites folder edits the row itself. The field used to float above the list, so expanding or collapsing anything left it sitting over a different folder, and a new folder could open its rename before its row existed.</li>
<li>Disabling a linked SQL folder no longer makes it disappear with no way back. It stays in the sidebar, marked disabled. Choosing the same folder from Add Linked SQL Folder re-enables it instead of reporting that it is already linked.</li>
<li>Sidebar and inspector widths are remembered per window again. They were saved under whichever connection happened to be selected.</li>
<li>A connection dropping and reconnecting no longer reopens a sidebar you hid, or closes an inspector you opened.</li>
<li>The sidebar scrollers follow Show scroll bars in General settings instead of always overlaying.</li>
<li>The Quick Switcher row that Return will open is highlighted from the moment the panel opens, not only after an arrow key, and Option+Return opens the selected table in a new window tab. The shortcut was documented but never fired.</li>
<li>The highlighted row in the connection switcher and the database switcher is drawn as the active selection while you type and arrow through it, the way Spotlight does.</li>
<li>Database icons and the current-database checkmark stay visible on a selected row in the database switcher. They were drawn in the accent colour, which vanished against the accent-coloured selection.</li>
<li>The drop, truncate, delete and external-link confirmations are standard system alerts that default to Cancel, so Return no longer drops a table, and Escape closes them again. Deleting a column or row from the inspector no longer runs on Return or without asking. (#2104)</li>
<li>Theme, schema and diagram exports report write failures instead of failing silently, and picking a folder that is already linked says so.</li>
<li>The export dialog no longer asks for a file name twice, and the save panel validates it.</li>
<li>Import and export results use standard alerts that size to their content, and the import result lists the statement that failed, not just the line number. (#2104)</li>
<li>Opening Settings, Appearance no longer replaces the theme saved for the light or dark slot, and a dark theme can no longer be assigned to the light slot. A theme that does not match the slot stays listed while it is the one in use.</li>
<li>Selected sidebar rows, quick switcher results and the database switcher use the system foreground colour, so labels stay readable under any accent colour and with Increase Contrast on. A selected row in the Favorites list reads correctly too; the linked file and folder icons were drawn in blue on the blue selection.</li>
<li>Tag badges, the Pro badge, the Vim mode indicator and the date cell picker pick a readable label colour instead of always using white. The selected tag filter is readable on light tag colours, and the picked colour swatch keeps its ring whatever accent colour is set. (#2104)</li>
<li>Cell range selection and the selected column header dim when the window loses focus, matching the rest of the system.</li>
<li>Tab moves focus out of the data grid when no cell is active, and cell to cell tabbing skips hidden columns.</li>
<li>The shortcut recorder captures combinations the menu bar already uses, such as Command W, instead of running the menu command.</li>
<li>Reduce Motion suppresses the remaining animations, and Reduce Transparency makes the Pro feature overlay fully opaque.</li>
<li>VoiceOver follows the data grid cell cursor and reports the selected cell range. Editor tabs, quick switcher results, mention suggestions, the Settings pickers, connection tags and the chat composer placeholder report themselves properly, the filter suggestion list announces when suggestions appear, and split view diffs mark added, removed and changed lines for VoiceOver and for Differentiate Without Colour.</li>
<li>Moving the pointer no longer changes the highlighted mention suggestion.</li>
<li>Dialog buttons are grouped at the trailing edge in six sheets instead of splitting Cancel to the left, and the split column alert grows to fit longer labels in other languages.</li>
<li>The titlebar breaks at the inspector divider, so the inspector toggle sits over the inspector pane.</li>
<li>Two Window menu items that could never run are gone.</li>
<li>Find in the Welcome window runs from the Edit menu and can be rebound in Settings.</li>
<li>The integration pairing prompt is modal, can be closed from its title bar, and sizes to its content.</li>
<li>Installing a missing database plugin shows download progress instead of nothing.</li>
<li>Nested connection groups show their nesting in the group menus again, and the connection group selector is a standard pop up button. (#2104)</li>
<li>Connection tags can be removed from the chip itself, and the host list add and remove buttons match the rest of the app.</li>
<li>Colour swatches show press, hover and keyboard focus, and the selection ring follows the accent colour.</li>
<li>A chained method on a MongoDB aggregation no longer goes missing. `db.orders.aggregate([...]).limit(10)` dropped the limit and returned everything the pipeline matched. Chaining a method that a query does not support now reports an error instead of ignoring it. (#2095)</li>
<li>MQL export writes a MongoDB `_id` as `ObjectId("...")`, a date as `ISODate("...")`, and a binary value as `BinData(...)` with its real BSON subtype, so running the script inserts the same types and bytes back. It wrote Extended JSON that mongosh reads as a plain object, and stamped every value as subtype 0. A value nested inside a subdocument is still exported as a string. (#2086)</li>
<li>MongoDB no longer prints a binary field nested inside a document as a UUID when it is not one, or labels a UUID with the wrong byte order. (#2086)</li>
<li>Deleting a MongoDB document with a binary `_id` deletes that document. It used to match on the other fields and could remove the wrong one.</li>
<li>MongoDB again authenticates against the database the connection names when browsing another database, so credentials that only exist in one database keep working.</li>
<li>PostgreSQL enum columns whose type lives in another schema show their values instead of a plain text box.</li>
<li>Tables you create in an in-memory DuckDB database show up in the sidebar. The object list was reading a second, empty in-memory database, so a refresh always came back with nothing. (#2108)</li>
<li>Mobile sends the client certificate and key on MySQL and PostgreSQL connections that use mutual TLS, and a connection whose certificate is missing says so instead of connecting without it while still demanding server verification. (#2083)</li>
<li>Editing a connection on mobile no longer wipes its SSL settings and per-database options, which then synced the loss back to the Mac. (#2083)</li>
<li>Redis and Valkey ACL users can sign in on mobile. The username was dropped, so every login was rejected. A rejected login now says what to change, and mobile opens the Redis database index saved on a connection instead of always database 0.</li>
<li>Mobile no longer gets killed by iOS when you leave the app with a DuckDB file open.</li>
</ul>
<h3>Security</h3>
<ul>
<li>SQL Server Verify CA and Verify Identity check the certificate for real on Mac. Both used to encrypt without checking anything, while the picker said otherwise.</li>
<li>ClickHouse Verify CA reads a PEM certificate authority file, and refuses to connect when the file cannot be read. It used to fall back to the public root store without saying so.</li>
<li>Mobile checks the SSH server's host key before sending any credential, and asks you the first time it sees a server. It never checked at all, so anyone intercepting the connection received the SSH password.</li>
<li>A changed SSH host key is reported as changed even when the server offers a different key type. Presenting a new key type used to get the milder first-use prompt.</li>
<li>Trusting an unknown SSH host key takes a click. Return picks Cancel.</li>
<li>libssh2 is patched against CVE-2026-55199, where a malicious SSH server could pin a CPU core before authentication.</li>
<li>A connection's password source no longer runs if the connections file was edited outside TablePro. Save the connection again from the app to confirm the change.</li>
<li>An import link can no longer make TablePro fetch an AWS credential and send it to the link author's server, or preset the fields that decide where a connection looks for its password.</li>
<li>Connections pulled from a team library no longer carry startup SQL or credential-resolution options.</li>
<li>MySQL and MariaDB connections refuse a server's request to read a local file.</li>
<li>A plugin's signature is rechecked immediately before it is loaded, and again before a staged update replaces the installed copy.</li>
<li>An MCP client could read the query history and schema of a connection whose external access was turned off, or that its token was not allowed to reach, by naming the connection directly. Both now go through the same permission check as every other request.</li>
<li>The MCP server refuses a request whose browser Origin is not on its allow list, which closes a DNS-rebinding route to the local port, and the connection listing no longer names connections you set to AI Never.</li>
<li>Query parameter values are no longer written to the history database. They were stored in the clear, nothing read them back, and existing ones are deleted on upgrade. The history database is protected at rest, matching the AI chat store.</li>
<li>Copy Connection String marks the clipboard item so clipboard-history apps leave it out of their history.</li>
<li>Release and test workflows pin their third-party actions to an exact commit.</li>
</ul>
]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.65.0/TablePro-0.65.0-arm64.zip" length="22152507" type="application/octet-stream" sparkle:edSignature="BT9JOLqxxtcNEIJpvf4ZP06HkK6Fca12AIH0X0jPK9IonpYpvPEUAK70Hsy2An+veGIHCmWPN9hTtQitsr7OAA=="/>
        </item>
        <item>
            <title>0.65.0</title>
            <pubDate>Sat, 15 Aug 2026 20:09:39 +0000</pubDate>
            <sparkle:version>118</sparkle:version>
            <sparkle:shortVersionString>0.65.0</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <description><![CDATA[<h3>Added</h3>
<ul>
<li>Query History is a resizable drawer that opens per connection and remembers its height, its filters and whether it was open. Entries group by day and load in pages, so history older than the most recent few hundred queries is reachable for the first time.</li>
<li>Query History filters by where a query came from: the editor, EXPLAIN runs, the SELECTs the app generates while you browse a table, grid row edits, structure changes, imports, and AI or MCP clients. It shows your own queries by default.</li>
<li>Query History filters to failed queries only, and by last hour, today, last 7 days or last 4 weeks. It can be scoped to one connection or searched across all of them, and names the connection each query belongs to when you widen the scope.</li>
<li>Query History works from the keyboard. Arrow keys move through entries, and `Return` or a double-click loads one into the editor.</li>
<li>Query History has a pause button. While it is paused nothing is recorded from any source, including row edits, structure changes, imports and AI clients. Pausing stays on the Mac you press it on.</li>
<li>Clearing query history from the drawer clears only what the drawer is showing, so clearing one connection leaves the others alone. Settings still clears everything.</li>
<li>A query plan opens as a result tab next to your query results, so you can switch back to the data without re-running the query, and a plan can be pinned.</li>
<li>MySQL `EXPLAIN FORMAT=TREE` and `EXPLAIN ANALYZE`, plus PGlite, Cloudflare D1, libSQL and Turso plans, render as a diagram and tree instead of raw text.</li>
<li>Plan diagrams zoom with a trackpad pinch, a two-finger double tap, or Cmd and scroll, fit to the window, and copy or export as a PNG.</li>
<li>The plan tree has resizable, sortable Operation, Cost, Rows and Actual Time columns, arrow-key navigation, a right-click menu to copy a step, and a detail panel you can resize.</li>
<li>Plan steps show a cost badge whose shape and colour escalate together, so cost reads without relying on colour.</li>
<li>The ER diagram uses the same zoom and scrolling as the plan diagram, gaining two-finger double tap, Cmd and scroll, and standard scrollers.</li>
<li>PostgreSQL array columns of a simple type, including arrays of an enum, get a list editor in the data grid: one row per element, with reordering, add, remove and NULL per element. An empty array and a NULL column stay separate values. Arrays of `jsonb`, `bytea` or composite types, and multi-dimensional values, keep the plain text editor.</li>
<li>The editor underlines a structural mistake as you type, such as a closing bracket with no opener or an unterminated comment. On MongoDB it also reports what the query parser rejects. A half-written statement is never flagged. (#2095)</li>
<li>PostgreSQL enum values are suggested when you compare against an enum column, so `WHERE status = ` offers the labels the type declares. (#2095)</li>
<li>PostgreSQL autocomplete knows the operators, including `::`, the JSON ones, array and range containment, regex matching and full-text search, about 400 built-in functions, and multi-word syntax such as `ON CONFLICT DO UPDATE SET` and window frame clauses. (#2095)</li>
<li>Autocomplete for MongoDB queries. `db.` lists collections, `db.users.` lists the driver methods, and inside a query you get field names, nested paths such as `address.city`, and the operators valid in that spot. (#2095)</li>
<li>MongoDB updates accept an options argument, so `db.users.updateOne({...}, {...}, {upsert: true})` upserts. `arrayFilters` and `hint` are passed through too. (#2095)</li>
<li>The MongoDB editor accepts mongosh value constructors in filters and pipelines, so a value copied from the grid pastes straight into a query. Covers `ObjectId`, `ISODate`, `Date`, the `Number*` family, `Timestamp`, `BinData`, `HexData`, `MinKey`, `MaxKey` and the UUID names. (#2086)</li>
<li>Format Query follows the editor language. On a MongoDB tab it lays out filters and pipelines by nesting depth instead of running the SQL formatter over them. (#2095)</li>
<li>Legacy UUID Encoding on a MongoDB connection, so binary UUIDs written by the Java, C# or Python drivers read as UUIDs instead of hex. Filters, edits and MQL exports write the same bytes back. (#2086)</li>
<li>Select several databases or schemas in the sidebar tree, or in the database switcher, and drop, refresh, copy names or export them at once. Shift-click and Cmd-click extend the selection.</li>
<li>The sidebar follows Sidebar icon size in System Settings > Appearance, so it matches Finder and Mail. A Row Size control in General settings and in View Options overrides it when you want to fit more objects on screen.</li>
<li>Right-clicking a row in the object list or the Favorites list highlights the row the menu will act on, the way Finder does. Right-clicking the empty space below either list gives a menu too, which is where View Options, New Query, New Favorite, New Folder and Add Linked SQL Folder now live.</li>
<li>Add to Favorites is in the sidebar's right-click menu, so it no longer needs a hover to reach.</li>
<li>Typing a name in the database tree jumps to the matching object.</li>
<li>Quick Switcher searches tables, views, saved queries and recent queries across every open connection.</li>
<li>Drop Schema for PostgreSQL, SQL Server and SurrealDB.</li>
<li>Editor tabs have a right-click menu for closing tabs.</li>
<li>Redis key rows have a right-click menu for copying a key or namespace prefix.</li>
<li>Dropping a SQL file on a connection window opens it. (#2104)</li>
<li>The Settings window can be resized.</li>
<li>SSL settings on mobile for MySQL, PostgreSQL and Redis: a mode picker plus CA, client certificate and client key. Import a PEM file, paste one, or use a PKCS#12 file. (#2083)</li>
</ul>
<h3>Changed</h3>
<ul>
<li>Every open connection now lives in one window. Picking a connection in the connections strip switches that window to it instead of raising a second window. Each connection keeps its own view while it waits its turn, so switching away and back leaves the grid scrolled where it was, the editor's cursor and selection where you left them, and a half-filled sheet still filled in.</li>
<li>Opening a table or query on a connection you already have open adds a tab to that window instead of opening another window. A tab strip appears once a connection holds more than one tab.</li>
<li>Closing the last tab leaves the connection open on its empty state. Close Tab again closes the connection, and the window once that was the last one open.</li>
<li>Window tabs follow your "Prefer tabs when opening documents" setting instead of always forcing tabs, and the Window menu carries Move Tab to New Window and Merge All Windows.</li>
<li>Clicking a table in the sidebar opens it right away. It used to wait out the double-click interval, about half a second, to find out whether a second click was coming. **Open in New Tab** on the table's contextual menu opens a second copy.</li>
<li>Opening a table from the sidebar leaves the keyboard in the sidebar, so you can keep clicking or arrowing through tables and watch each one load. Click into the grid when you want to work in it.</li>
<li>The bar at the bottom of the sidebar is gone, and everything it held moved somewhere a window can never hide. New Table and New View are in the Database menu and the sidebar's right-click menu. Switching schema is Database > Schema. The database filter is View > Filter Databases. The refresh spinner joined the other progress in the toolbar.</li>
<li>A filtered database tree says so, with a banner naming how many of the databases it is showing and a button to show them all.</li>
<li>Sidebar section titles are real source list headers now: a short grey title with no icon, and the objects under it sitting at the same depth as a database instead of one step in.</li>
<li>MongoDB shows a standard binary UUID as `UUID("...")` everywhere, including in exports.</li>
<li>An imported connection link shows the startup SQL and driver options it carries, before you add it.</li>
<li>Mobile keeps remote connections open when you switch apps.</li>
<li>Mobile no longer copies database passwords to iCloud Keychain unless you turn on Sync Passwords. Mac already worked this way.</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>The Quick Switcher opens what you searched for. Typing a new search kept the row you had highlighted selected whenever it still matched anything, and searching across connections matches the connection path in every row, so almost any row stayed highlighted while the list reordered under it. `Return` then opened something you had stopped searching for.</li>
<li>A query that fails shows the database's error again and is recorded in query history. A syntax error, or selecting from a table that does not exist, left the results area blank with no message at all, and opening a table that failed to load was silent the same way. The error also opens the results pane if you had it collapsed. (#2120)</li>
<li>Running a query with parameters, or several statements at once, no longer stops the tab from accepting any later run.</li>
<li>Query History no longer mixes every connection's queries together with no way to tell them apart. Loading one could put another connection's SQL into the editor in front of you.</li>
<li>Searching query history matches as you type, and several words match entries that contain them all, wherever they appear. It only matched whole words sitting next to each other, so `user` found nothing until you finished `users`, and `select customers` found nothing at all.</li>
<li>Query History keeps the older entries you loaded when a query finishes while the drawer is open, and stops refetching once per statement while a large save or import runs.</li>
<li>Deleting a connection deletes its query history, its drawer filters and its saved queries with it.</li>
<li>Trimming history to the retention limits refreshes the open drawer instead of leaving deleted entries on screen, including when the limits arrive from another Mac over iCloud.</li>
<li>Turning off automatic query history cleanup now actually stops it. Entries were still pruned to the limits every hundred queries regardless of the setting.</li>
<li>Query history highlights each query for the database it actually ran on. Everything except MongoDB was coloured as MySQL.</li>
<li>The query history duration column is honest. Ten of the places that record a query reported no duration as `0 ms`; they either measure it now or show `–`, and a query faster than a millisecond shows `<1 ms`.</li>
<li>Query history rows show a SQLite database by file name instead of its full path, which used to fill the row.</li>
<li>A failed grid save records one history entry per statement, the same as a successful one, instead of joining them all into a single unusable entry.</li>
<li>Cancelling an import is no longer recorded as a failed import, and an import no longer reports its statement count as a row count.</li>
<li>The Quick Switcher no longer fills up with the same statement repeated. It shows each distinct query once, most recent first, and a long list of saved queries can no longer push recent queries out of the panel entirely.</li>
<li>On iPhone and iPad, a query that failed is kept in history and marked, instead of vanishing.</li>
<li>Corrected two claims about iCloud sync: it does not sync query history, and there is no history sync limit to reduce when storage is full.</li>
<li>Running EXPLAIN from the toolbar asks for confirmation when Safe Mode requires it. It skipped that check on every database that offers an EXPLAIN variant, even though `EXPLAIN ANALYZE` runs the query.</li>
<li>The Stop button can cancel a running `EXPLAIN ANALYZE`, and EXPLAIN runs started from the toolbar are recorded in Query History.</li>
<li>An EXPLAIN that fails reports the error in the usual place instead of showing it where the plan should be, and Clear Query clears the plan instead of leaving a stale one on screen.</li>
<li>A query plan that cannot be read as a tree says so and shows the raw output instead of leaving an empty pane, and a second EXPLAIN in the same tab redraws the diagram.</li>
<li>Plan diagrams line every node of the same depth up on one row, so a tall box no longer pushes its children up into itself.</li>
<li>Plans that report a cost per node but no startup cost, such as MySQL's, show that cost in the diagram and the tree, and nodes in a plan whose root reports no cost are no longer all painted red.</li>
<li>Closing a connection removes it from the connections strip, ends its session, and closes its tunnel. It stayed listed as connected, could not be clicked, and its database connection stayed open.</li>
<li>The strip's close command ends the connection: every tab across every database it has open, its session, and every row it holds in the strip. It used to close only the tabs of one database and leave the row you clicked exactly where it was. Disconnect still ends only the session and keeps the row. **File > Close Connection** does the same from the menu bar.</li>
<li>Closing a window disconnects every connection it was showing, not just one of them.</li>
<li>**Open in New Window** on a connection in the strip moves it out of the shared window with its tabs, its session and its unsaved work.</li>
<li>Cmd+W closes the welcome window. The shortcut was bound to Close Tab, which the welcome window has no answer for, so it did nothing there.</li>
<li>Typing in a new query tab goes into the editor. Cmd+T left the keyboard on the sidebar, so the first characters you typed went to the object list instead.</li>
<li>An alert opens on the window you were working in. It could attach itself to a floating panel such as the Quick Switcher, which takes the alert with it when it closes.</li>
<li>Switching connection no longer rebuilds the toolbar. Its items resize to the connection you switched to instead of holding the previous one's width, the Results button tracks the pane you are looking at, and Snowflake's role and warehouse menus reload.</li>
<li>Customize Toolbar no longer blanks the connection and status controls, and a connection that loses its session no longer puts your toolbar arrangement at risk. The items you had customized could be dropped for good.</li>
<li>Main window toolbar buttons are real toolbar items, so icon only mode, display mode customization and the overflow menu all work.</li>
<li>Toolbar tooltips name what the button does and show the current keyboard shortcut again. Import works from the toolbar, and the Results button shows whether the pane is open. (#2104)</li>
<li>A tag with no colour is readable in the toolbar instead of drawing white on nothing.</li>
<li>Refreshing a database or schema from the sidebar no longer empties it. The tables and routines came back only when the refresh finished, and a refresh that failed left an error where they had been.</li>
<li>Hiding a database in the sidebar's database filter takes effect straight away. The tree kept listing every database until some other change happened to rebuild it.</li>
<li>The sidebar drops a database from the tree as soon as you drop it on the server, instead of listing it until you reconnect.</li>
<li>Holding an arrow key in the sidebar no longer opens a tab and runs a query for every object it passes. Arrowing through 20 tables fired 20 queries.</li>
<li>Arrowing to a table in the sidebar opens it right away. The open waited out the key repeat rate from System Settings, which is up to two seconds, even for a single press.</li>
<li>Cmd-clicking or Shift-arrowing to select several tables no longer opens the table it added. Building a selection for Truncate, Delete or Export ran a query, replaced the tab, and could switch the active database.</li>
<li>Right-clicking a table that is not part of the current selection acts on that table. It used to act on the selected tables instead, including for Delete.</li>
<li>The sidebar object list is a native outline in every layout, so the selected row is drawn as the active selection, arrow keys move between objects, and typing jumps to a name. The flat and schema layouts were SwiftUI lists that could not do any of that.</li>
<li>Routines, procedures, functions, Redis keys and Recent entries can be selected in the sidebar, so arrow keys reach them and type-to-find lands on them. Clicking a Recent entry highlights the row it opened.</li>
<li>The Favorites tab is a native outline too, so saved queries, folders and linked SQL files take the keyboard and show a real selection. Team Library entries can be selected for the first time; opening one is now a double-click or Return, and its publisher shows beside the name instead of under it.</li>
<li>Truncate, Copy Name and Delete act on the sidebar selection in tree layout. They read a selection the tree never published, so they did nothing at all.</li>
<li>Clicking a row in the sidebar puts the keyboard on the list. The click moved the selection but sometimes left the keyboard in the filter field above it, so the row drew grey and the arrow keys went to the field. Switching to the Favorites tab left the keyboard nowhere at all.</li>
<li>Opening a table from somewhere that asks for the grid, such as Favorites or Show Structure, puts the keyboard in the grid. Only the first such table of a session did.</li>
<li>Down arrow in the sidebar filter field moves into the object list instead of being swallowed.</li>
<li>Typing in the sidebar filter no longer restarts its own delay when anything else redraws the sidebar, so the list settles when you stop typing.</li>
<li>Collapsing a database or a section while the sidebar filter has text no longer throws away the layout you had before searching. The row sprang back open on the next keystroke, and stayed collapsed once the filter was cleared. A collapse is recorded once AppKit has applied it, so the saved state always describes what is on screen.</li>
<li>The favourite star in the sidebar takes a click anywhere in its button, not only on the star itself. A near miss selected the table and opened it instead.</li>
<li>Favorites rows follow the sidebar row size like the object list does, and both lists draw their rows at the same height. One inset its rows a point more than the other, so the two tabs did not line up.</li>
<li>Renaming a favourites folder edits the row itself. The field used to float above the list, so expanding or collapsing anything left it sitting over a different folder, and a new folder could open its rename before its row existed.</li>
<li>Disabling a linked SQL folder no longer makes it disappear with no way back. It stays in the sidebar, marked disabled. Choosing the same folder from Add Linked SQL Folder re-enables it instead of reporting that it is already linked.</li>
<li>Sidebar and inspector widths are remembered per window again. They were saved under whichever connection happened to be selected.</li>
<li>A connection dropping and reconnecting no longer reopens a sidebar you hid, or closes an inspector you opened.</li>
<li>The sidebar scrollers follow Show scroll bars in General settings instead of always overlaying.</li>
<li>The Quick Switcher row that Return will open is highlighted from the moment the panel opens, not only after an arrow key, and Option+Return opens the selected table in a new window tab. The shortcut was documented but never fired.</li>
<li>The highlighted row in the connection switcher and the database switcher is drawn as the active selection while you type and arrow through it, the way Spotlight does.</li>
<li>Database icons and the current-database checkmark stay visible on a selected row in the database switcher. They were drawn in the accent colour, which vanished against the accent-coloured selection.</li>
<li>The drop, truncate, delete and external-link confirmations are standard system alerts that default to Cancel, so Return no longer drops a table, and Escape closes them again. Deleting a column or row from the inspector no longer runs on Return or without asking. (#2104)</li>
<li>Theme, schema and diagram exports report write failures instead of failing silently, and picking a folder that is already linked says so.</li>
<li>The export dialog no longer asks for a file name twice, and the save panel validates it.</li>
<li>Import and export results use standard alerts that size to their content, and the import result lists the statement that failed, not just the line number. (#2104)</li>
<li>Opening Settings, Appearance no longer replaces the theme saved for the light or dark slot, and a dark theme can no longer be assigned to the light slot. A theme that does not match the slot stays listed while it is the one in use.</li>
<li>Selected sidebar rows, quick switcher results and the database switcher use the system foreground colour, so labels stay readable under any accent colour and with Increase Contrast on. A selected row in the Favorites list reads correctly too; the linked file and folder icons were drawn in blue on the blue selection.</li>
<li>Tag badges, the Pro badge, the Vim mode indicator and the date cell picker pick a readable label colour instead of always using white. The selected tag filter is readable on light tag colours, and the picked colour swatch keeps its ring whatever accent colour is set. (#2104)</li>
<li>Cell range selection and the selected column header dim when the window loses focus, matching the rest of the system.</li>
<li>Tab moves focus out of the data grid when no cell is active, and cell to cell tabbing skips hidden columns.</li>
<li>The shortcut recorder captures combinations the menu bar already uses, such as Command W, instead of running the menu command.</li>
<li>Reduce Motion suppresses the remaining animations, and Reduce Transparency makes the Pro feature overlay fully opaque.</li>
<li>VoiceOver follows the data grid cell cursor and reports the selected cell range. Editor tabs, quick switcher results, mention suggestions, the Settings pickers, connection tags and the chat composer placeholder report themselves properly, the filter suggestion list announces when suggestions appear, and split view diffs mark added, removed and changed lines for VoiceOver and for Differentiate Without Colour.</li>
<li>Moving the pointer no longer changes the highlighted mention suggestion.</li>
<li>Dialog buttons are grouped at the trailing edge in six sheets instead of splitting Cancel to the left, and the split column alert grows to fit longer labels in other languages.</li>
<li>The titlebar breaks at the inspector divider, so the inspector toggle sits over the inspector pane.</li>
<li>Two Window menu items that could never run are gone.</li>
<li>Find in the Welcome window runs from the Edit menu and can be rebound in Settings.</li>
<li>The integration pairing prompt is modal, can be closed from its title bar, and sizes to its content.</li>
<li>Installing a missing database plugin shows download progress instead of nothing.</li>
<li>Nested connection groups show their nesting in the group menus again, and the connection group selector is a standard pop up button. (#2104)</li>
<li>Connection tags can be removed from the chip itself, and the host list add and remove buttons match the rest of the app.</li>
<li>Colour swatches show press, hover and keyboard focus, and the selection ring follows the accent colour.</li>
<li>A chained method on a MongoDB aggregation no longer goes missing. `db.orders.aggregate([...]).limit(10)` dropped the limit and returned everything the pipeline matched. Chaining a method that a query does not support now reports an error instead of ignoring it. (#2095)</li>
<li>MQL export writes a MongoDB `_id` as `ObjectId("...")`, a date as `ISODate("...")`, and a binary value as `BinData(...)` with its real BSON subtype, so running the script inserts the same types and bytes back. It wrote Extended JSON that mongosh reads as a plain object, and stamped every value as subtype 0. A value nested inside a subdocument is still exported as a string. (#2086)</li>
<li>MongoDB no longer prints a binary field nested inside a document as a UUID when it is not one, or labels a UUID with the wrong byte order. (#2086)</li>
<li>Deleting a MongoDB document with a binary `_id` deletes that document. It used to match on the other fields and could remove the wrong one.</li>
<li>MongoDB again authenticates against the database the connection names when browsing another database, so credentials that only exist in one database keep working.</li>
<li>PostgreSQL enum columns whose type lives in another schema show their values instead of a plain text box.</li>
<li>Tables you create in an in-memory DuckDB database show up in the sidebar. The object list was reading a second, empty in-memory database, so a refresh always came back with nothing. (#2108)</li>
<li>Mobile sends the client certificate and key on MySQL and PostgreSQL connections that use mutual TLS, and a connection whose certificate is missing says so instead of connecting without it while still demanding server verification. (#2083)</li>
<li>Editing a connection on mobile no longer wipes its SSL settings and per-database options, which then synced the loss back to the Mac. (#2083)</li>
<li>Redis and Valkey ACL users can sign in on mobile. The username was dropped, so every login was rejected. A rejected login now says what to change, and mobile opens the Redis database index saved on a connection instead of always database 0.</li>
<li>Mobile no longer gets killed by iOS when you leave the app with a DuckDB file open.</li>
</ul>
<h3>Security</h3>
<ul>
<li>SQL Server Verify CA and Verify Identity check the certificate for real on Mac. Both used to encrypt without checking anything, while the picker said otherwise.</li>
<li>ClickHouse Verify CA reads a PEM certificate authority file, and refuses to connect when the file cannot be read. It used to fall back to the public root store without saying so.</li>
<li>Mobile checks the SSH server's host key before sending any credential, and asks you the first time it sees a server. It never checked at all, so anyone intercepting the connection received the SSH password.</li>
<li>A changed SSH host key is reported as changed even when the server offers a different key type. Presenting a new key type used to get the milder first-use prompt.</li>
<li>Trusting an unknown SSH host key takes a click. Return picks Cancel.</li>
<li>libssh2 is patched against CVE-2026-55199, where a malicious SSH server could pin a CPU core before authentication.</li>
<li>A connection's password source no longer runs if the connections file was edited outside TablePro. Save the connection again from the app to confirm the change.</li>
<li>An import link can no longer make TablePro fetch an AWS credential and send it to the link author's server, or preset the fields that decide where a connection looks for its password.</li>
<li>Connections pulled from a team library no longer carry startup SQL or credential-resolution options.</li>
<li>MySQL and MariaDB connections refuse a server's request to read a local file.</li>
<li>A plugin's signature is rechecked immediately before it is loaded, and again before a staged update replaces the installed copy.</li>
<li>An MCP client could read the query history and schema of a connection whose external access was turned off, or that its token was not allowed to reach, by naming the connection directly. Both now go through the same permission check as every other request.</li>
<li>The MCP server refuses a request whose browser Origin is not on its allow list, which closes a DNS-rebinding route to the local port, and the connection listing no longer names connections you set to AI Never.</li>
<li>Query parameter values are no longer written to the history database. They were stored in the clear, nothing read them back, and existing ones are deleted on upgrade. The history database is protected at rest, matching the AI chat store.</li>
<li>Copy Connection String marks the clipboard item so clipboard-history apps leave it out of their history.</li>
<li>Release and test workflows pin their third-party actions to an exact commit.</li>
</ul>
]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.65.0/TablePro-0.65.0-x86_64.zip" length="23489789" type="application/octet-stream" sparkle:edSignature="eRInx1TdWDYlFaZvat8RRR38SSJ3J46UyuxMcJ9YsfCv1ln6rUnG0r+aoL6r7zvJsqJgeJnOqUu4xpBKmV6zDA=="/>
        </item>
        <item>
            <title>0.64.0</title>
            <pubDate>Mon, 10 Aug 2026 16:19:26 +0000</pubDate>
            <sparkle:version>117</sparkle:version>
            <sparkle:shortVersionString>0.64.0</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <sparkle:hardwareRequirements>arm64</sparkle:hardwareRequirements>
            <description><![CDATA[<h3>Added</h3>
<ul>
<li>Match Case in the filter operator menu, on every database that can express it. (#2048)</li>
<li>Oracle SYSDBA and SYSOPER logons, on Mac and mobile. (#2039)</li>
<li>Oracle in TablePro Mobile, with a Service Name or SID picker. (#2033)</li>
<li>A workspace rail listing every open connection and database, with its own shortcuts. (#1282)</li>
<li>Disconnect and Reconnect, from the Database menu, the rail, or the connection list.</li>
<li>A Database menu, plus Minimize, Zoom, Move Tab to New Window, Show Toolbar, and Customize Toolbar.</li>
<li>Show Object Icons, to turn sidebar icons off.</li>
<li>Redshift external schemas now list their tables, marked in the sidebar and opened read-only.</li>
</ul>
<h3>Changed</h3>
<ul>
<li>The menu bar is rebuilt on native macOS menus, so every command follows the window you are using.</li>
<li>`Cmd+F` finds in whatever is in front of you: rows in a table, text in the editor, the CSV inspector.</li>
<li>Connecting fills the window and names the step it is on, and a failure shows the database's own error.</li>
<li>Running a query or opening a table replaces whatever that tab was already running.</li>
<li>Reopening the last session connects the window you land on first.</li>
<li>A saved pre-connect script no longer runs on an automatic reopen.</li>
<li>Settings opens in a standard preferences window.</li>
<li>A tab's subtitle names the database it is bound to, and its toolbar repoints only that tab. (#2026)</li>
<li>MCP and AI table tools take a `database` argument, so another database can be inspected. (#2026)</li>
<li>Building from source now needs XcodeGen.</li>
</ul>
<h3>Removed</h3>
<ul>
<li>Show Tables Sidebar and Show Favorites Sidebar from the menu bar.</li>
<li>The "Group all connections in one window" setting. Each connection has its own window. (#1282)</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>A tab keeps running against the database it was opened on, so changing database no longer breaks it or writes to the wrong place. (#2026)</li>
<li>Menu commands act on the selected row, column, and editor, and stay disabled when they would do nothing.</li>
<li>A reconnect brings every window's tabs back, each in the window it was in.</li>
<li>Window and tab names follow what the window is showing, and a name you chose is kept.</li>
<li>Tabs are saved before a session ends, and a window that never loaded tabs no longer deletes them.</li>
<li>A cancelled or failed connect leaves the window usable.</li>
<li>A connection unreachable at launch is reopened next time, and reconnects once the server is up.</li>
<li>Opening a connection that already has a window reuses it, and MCP-opened windows connect.</li>
<li>Row counts refine to an exact count, and stop when you close the window or leave the table. (#2059)</li>
<li>Clicking through the sidebar no longer loads tables you have left, or shows the previous table's rows. (#2058)</li>
<li>Stop cancels reads only, so it can no longer cut off a save or a schema change.</li>
<li>A brief SSH tunnel reconnect no longer closes tabs, and a dead tunnel offers to retry.</li>
<li>The JSON view follows the grid's sort, filters, and hidden columns, and updates with every query.</li>
<li>The editor restores your cursor and selection, and keeps autocomplete after a reconnect.</li>
<li>AI replies stream smoothly, render markdown as they arrive, and no longer slow the panel down.</li>
<li>Claude, Gemini, and local models no longer fail on thinking and image settings. (#2031)</li>
<li>Filters compare text columns as text, match NULL and TRUE literally, and no longer break IS EMPTY. (#2029)</li>
<li>Alerts, focus, calendars, number formats, and Reduce Motion follow system conventions.</li>
<li>Oracle connections turn on TCP keepalive, so idle sessions survive. (#2038)</li>
<li>iPhone and iPad ask for Local Network access only when it is needed. (#2040)</li>
</ul>
<h3>Security</h3>
<ul>
<li>Requests to TablePro's own servers require TLS again.</li>
<li>Updated Sparkle to 2.9.5, patching CVE-2026-47121 and CVE-2026-47122 in the updater.</li>
</ul>
]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.64.0/TablePro-0.64.0-arm64.zip" length="21756902" type="application/octet-stream" sparkle:edSignature="JeNIX/rJxgU5GCItB9BdgFY8kjFg0hlrl8dYOhfhX+1oSDbzTT+lCcD58n02MrxSigF7VzjZip8lVoKkAQu6Dg=="/>
        </item>
        <item>
            <title>0.64.0</title>
            <pubDate>Mon, 10 Aug 2026 16:19:27 +0000</pubDate>
            <sparkle:version>117</sparkle:version>
            <sparkle:shortVersionString>0.64.0</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <description><![CDATA[<h3>Added</h3>
<ul>
<li>Match Case in the filter operator menu, on every database that can express it. (#2048)</li>
<li>Oracle SYSDBA and SYSOPER logons, on Mac and mobile. (#2039)</li>
<li>Oracle in TablePro Mobile, with a Service Name or SID picker. (#2033)</li>
<li>A workspace rail listing every open connection and database, with its own shortcuts. (#1282)</li>
<li>Disconnect and Reconnect, from the Database menu, the rail, or the connection list.</li>
<li>A Database menu, plus Minimize, Zoom, Move Tab to New Window, Show Toolbar, and Customize Toolbar.</li>
<li>Show Object Icons, to turn sidebar icons off.</li>
<li>Redshift external schemas now list their tables, marked in the sidebar and opened read-only.</li>
</ul>
<h3>Changed</h3>
<ul>
<li>The menu bar is rebuilt on native macOS menus, so every command follows the window you are using.</li>
<li>`Cmd+F` finds in whatever is in front of you: rows in a table, text in the editor, the CSV inspector.</li>
<li>Connecting fills the window and names the step it is on, and a failure shows the database's own error.</li>
<li>Running a query or opening a table replaces whatever that tab was already running.</li>
<li>Reopening the last session connects the window you land on first.</li>
<li>A saved pre-connect script no longer runs on an automatic reopen.</li>
<li>Settings opens in a standard preferences window.</li>
<li>A tab's subtitle names the database it is bound to, and its toolbar repoints only that tab. (#2026)</li>
<li>MCP and AI table tools take a `database` argument, so another database can be inspected. (#2026)</li>
<li>Building from source now needs XcodeGen.</li>
</ul>
<h3>Removed</h3>
<ul>
<li>Show Tables Sidebar and Show Favorites Sidebar from the menu bar.</li>
<li>The "Group all connections in one window" setting. Each connection has its own window. (#1282)</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>A tab keeps running against the database it was opened on, so changing database no longer breaks it or writes to the wrong place. (#2026)</li>
<li>Menu commands act on the selected row, column, and editor, and stay disabled when they would do nothing.</li>
<li>A reconnect brings every window's tabs back, each in the window it was in.</li>
<li>Window and tab names follow what the window is showing, and a name you chose is kept.</li>
<li>Tabs are saved before a session ends, and a window that never loaded tabs no longer deletes them.</li>
<li>A cancelled or failed connect leaves the window usable.</li>
<li>A connection unreachable at launch is reopened next time, and reconnects once the server is up.</li>
<li>Opening a connection that already has a window reuses it, and MCP-opened windows connect.</li>
<li>Row counts refine to an exact count, and stop when you close the window or leave the table. (#2059)</li>
<li>Clicking through the sidebar no longer loads tables you have left, or shows the previous table's rows. (#2058)</li>
<li>Stop cancels reads only, so it can no longer cut off a save or a schema change.</li>
<li>A brief SSH tunnel reconnect no longer closes tabs, and a dead tunnel offers to retry.</li>
<li>The JSON view follows the grid's sort, filters, and hidden columns, and updates with every query.</li>
<li>The editor restores your cursor and selection, and keeps autocomplete after a reconnect.</li>
<li>AI replies stream smoothly, render markdown as they arrive, and no longer slow the panel down.</li>
<li>Claude, Gemini, and local models no longer fail on thinking and image settings. (#2031)</li>
<li>Filters compare text columns as text, match NULL and TRUE literally, and no longer break IS EMPTY. (#2029)</li>
<li>Alerts, focus, calendars, number formats, and Reduce Motion follow system conventions.</li>
<li>Oracle connections turn on TCP keepalive, so idle sessions survive. (#2038)</li>
<li>iPhone and iPad ask for Local Network access only when it is needed. (#2040)</li>
</ul>
<h3>Security</h3>
<ul>
<li>Requests to TablePro's own servers require TLS again.</li>
<li>Updated Sparkle to 2.9.5, patching CVE-2026-47121 and CVE-2026-47122 in the updater.</li>
</ul>
]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.64.0/TablePro-0.64.0-x86_64.zip" length="23036941" type="application/octet-stream" sparkle:edSignature="7Z+13+cX7rRX7M2jAsRSRiJr74eyzb2Uska76rb6395SuCt3ttk+ybf+lMvd7hMSWmQGgf4ewOX6Rh85tbNgDg=="/>
        </item>
        <item>
            <title>0.63.0</title>
            <pubDate>Wed, 05 Aug 2026 13:34:43 +0000</pubDate>
            <sparkle:version>116</sparkle:version>
            <sparkle:shortVersionString>0.63.0</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <sparkle:hardwareRequirements>arm64</sparkle:hardwareRequirements>
            <description><![CDATA[<h3>Added</h3>
<ul>
<li>Turkish, Vietnamese, Simplified Chinese, and Traditional Chinese translations for the strings that still showed in English.</li>
</ul>
<h3>Changed</h3>
<ul>
<li>The Claude Agent provider now lists its tradeoffs: Anthropic's terms decide whether this use is allowed, replies share the limits of your other Claude work, and any API key in your environment is ignored.</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>`Cmd+Delete` in the SQL editor deletes to the start of the line again. (#2022)</li>
<li>`Option+Delete` in the SQL editor deletes the previous word again. (#2022)</li>
<li>Refreshing a table no longer fails with "Query cancelled" on every second click. (#2021)</li>
<li>Stopping a query no longer shows a red error or records the query as failed in history.</li>
<li>Stopping a MySQL, MariaDB, or Redis query no longer cancels the next one you run.</li>
<li>SQL export no longer writes generated columns into the INSERT statements, so the dump imports cleanly. Covers MySQL, MariaDB, and ClickHouse. (#2023)</li>
<li>Adding or duplicating a row on a table with a generated column now saves. (#2023)</li>
<li>Generated columns are now read-only in the data grid. (#2023)</li>
<li>The data grid header's bottom line no longer cuts through a column comment. It sits on the header's bottom edge. (#2017)</li>
<li>A sorted column's header no longer draws an extra line and a divider that no other column has. (#2017)</li>
<li>Selecting a column highlights the full height of its header instead of a band across the middle. (#2017)</li>
<li>The New Connection window opens in front of the Welcome window on the first try, from every command that opens it.</li>
<li>Importing a connection URL while a New Connection window is open no longer discards the pasted URL. Each import opens its own window.</li>
<li>Clicking a foreign key arrow in a query tab's results no longer replaces the tab and loses the query. The referenced table opens in its own tab, and clicking the same reference again returns to it. A tab with unsaved edits is kept the same way.</li>
<li>A foreign key jump keeps the filters saved for the table you left and applies the hidden columns saved for the table you open.</li>
<li>Saving a table structure change with more than one connection open now runs against the connection, database, and schema the edited table belongs to, instead of a different one. It stops with an error rather than writing to the wrong place. (#2015)</li>
<li>Saving on a server that starts sessions read-only no longer fails with "Cannot execute statement in a READ ONLY transaction". TablePro marks the transaction read-write before it writes. Covers MySQL, MariaDB, PostgreSQL, CockroachDB, and Redshift. (#2009)</li>
<li>Changing Safe Mode in the connection form now applies to an open connection instead of waiting for a reconnect. (#2009)</li>
<li>A read-only error now says whether the database server or Safe Mode refused the write. (#2009)</li>
<li>The MCP server's **Default row limit** and **Maximum row limit** now apply to the `export_data` tool. Export ignored both and used a fixed 50,000, so an export with no `max_rows` now returns 500 rows until you raise the default. Export also honours the query timeout, reports when the limit clipped the result, and appears in query history and the activity log. (#2012)</li>
<li>Exporting a table over MCP no longer fails on SQL Server, Oracle, and Teradata. The row limit now uses each database's own syntax instead of `LIMIT`. (#2012)</li>
<li>Setting the MCP server's row limits or query timeout to zero or less no longer crashes the app on the next tool call. Out-of-range values are corrected as you type. (#2012)</li>
<li>AI chat no longer crashes when a tool is asked for a row limit or timeout larger than TablePro can count to. It falls back to the configured limit. (#2012)</li>
</ul>
]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.63.0/TablePro-0.63.0-arm64.zip" length="21306731" type="application/octet-stream" sparkle:edSignature="IGwTpd15RGUlfxytrW4hq3BEqFr2leuYitenyqmXzSfBn+gKMUCj2GF498j/IuzsxpTCtTbixNTvyuxjYMjuBg=="/>
        </item>
        <item>
            <title>0.63.0</title>
            <pubDate>Wed, 05 Aug 2026 13:34:44 +0000</pubDate>
            <sparkle:version>116</sparkle:version>
            <sparkle:shortVersionString>0.63.0</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <description><![CDATA[<h3>Added</h3>
<ul>
<li>Turkish, Vietnamese, Simplified Chinese, and Traditional Chinese translations for the strings that still showed in English.</li>
</ul>
<h3>Changed</h3>
<ul>
<li>The Claude Agent provider now lists its tradeoffs: Anthropic's terms decide whether this use is allowed, replies share the limits of your other Claude work, and any API key in your environment is ignored.</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>`Cmd+Delete` in the SQL editor deletes to the start of the line again. (#2022)</li>
<li>`Option+Delete` in the SQL editor deletes the previous word again. (#2022)</li>
<li>Refreshing a table no longer fails with "Query cancelled" on every second click. (#2021)</li>
<li>Stopping a query no longer shows a red error or records the query as failed in history.</li>
<li>Stopping a MySQL, MariaDB, or Redis query no longer cancels the next one you run.</li>
<li>SQL export no longer writes generated columns into the INSERT statements, so the dump imports cleanly. Covers MySQL, MariaDB, and ClickHouse. (#2023)</li>
<li>Adding or duplicating a row on a table with a generated column now saves. (#2023)</li>
<li>Generated columns are now read-only in the data grid. (#2023)</li>
<li>The data grid header's bottom line no longer cuts through a column comment. It sits on the header's bottom edge. (#2017)</li>
<li>A sorted column's header no longer draws an extra line and a divider that no other column has. (#2017)</li>
<li>Selecting a column highlights the full height of its header instead of a band across the middle. (#2017)</li>
<li>The New Connection window opens in front of the Welcome window on the first try, from every command that opens it.</li>
<li>Importing a connection URL while a New Connection window is open no longer discards the pasted URL. Each import opens its own window.</li>
<li>Clicking a foreign key arrow in a query tab's results no longer replaces the tab and loses the query. The referenced table opens in its own tab, and clicking the same reference again returns to it. A tab with unsaved edits is kept the same way.</li>
<li>A foreign key jump keeps the filters saved for the table you left and applies the hidden columns saved for the table you open.</li>
<li>Saving a table structure change with more than one connection open now runs against the connection, database, and schema the edited table belongs to, instead of a different one. It stops with an error rather than writing to the wrong place. (#2015)</li>
<li>Saving on a server that starts sessions read-only no longer fails with "Cannot execute statement in a READ ONLY transaction". TablePro marks the transaction read-write before it writes. Covers MySQL, MariaDB, PostgreSQL, CockroachDB, and Redshift. (#2009)</li>
<li>Changing Safe Mode in the connection form now applies to an open connection instead of waiting for a reconnect. (#2009)</li>
<li>A read-only error now says whether the database server or Safe Mode refused the write. (#2009)</li>
<li>The MCP server's **Default row limit** and **Maximum row limit** now apply to the `export_data` tool. Export ignored both and used a fixed 50,000, so an export with no `max_rows` now returns 500 rows until you raise the default. Export also honours the query timeout, reports when the limit clipped the result, and appears in query history and the activity log. (#2012)</li>
<li>Exporting a table over MCP no longer fails on SQL Server, Oracle, and Teradata. The row limit now uses each database's own syntax instead of `LIMIT`. (#2012)</li>
<li>Setting the MCP server's row limits or query timeout to zero or less no longer crashes the app on the next tool call. Out-of-range values are corrected as you type. (#2012)</li>
<li>AI chat no longer crashes when a tool is asked for a row limit or timeout larger than TablePro can count to. It falls back to the configured limit. (#2012)</li>
</ul>
]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.63.0/TablePro-0.63.0-x86_64.zip" length="22576165" type="application/octet-stream" sparkle:edSignature="HiI5OekulZtw0ENhWGI/DoCR2Cyx8mtDFDwNlLcmaOPAjY/sy4r3uX/3TEZG0pfRzJtjqEoiOiW0+XeAa+1FCQ=="/>
        </item>
        <item>
            <title>0.62.0</title>
            <pubDate>Sat, 01 Aug 2026 19:58:19 +0000</pubDate>
            <sparkle:version>115</sparkle:version>
            <sparkle:shortVersionString>0.62.0</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <sparkle:hardwareRequirements>arm64</sparkle:hardwareRequirements>
            <description><![CDATA[<h3>Added</h3>
<ul>
<li>**Claude Agent** provider, which runs AI chat through the Claude Code command line tool so you can use a Claude subscription instead of a metered API key. It answers from your schema when the MCP server is on.</li>
<li>**Count exactly** next to an estimated row total, to replace the estimate with a real count when you want it.</li>
<li>Reorder filter rows by dragging the grip on the left of each row, or with **Move Up** and **Move Down** in the row's right-click menu.</li>
<li>An **On Update** column in the Structure tab for MySQL and MariaDB, so a timestamp column can be set to update itself without writing the SQL by hand. (#2005)</li>
<li>A **Length** column in the Redis key grid, showing the size Redis reports: bytes for a string, element count for a hash, list, set, sorted set, or stream. It tells you how much a preview leaves out.</li>
</ul>
<h3>Changed</h3>
<ul>
<li>The Redis key tree in the sidebar lists keys with `SCAN` instead of `KEYS`, and no longer reads every key's value to draw the tree. `KEYS` blocks the server for the whole scan.</li>
<li>Redis command arguments now follow the same quoting rules as `redis-cli`, so `\xHH` writes a raw byte and a command you paste from `redis-cli` behaves the same way in TablePro. A command with unbalanced quotes is rejected instead of being guessed at.</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>A Redis string key now shows its whole value in the grid. Values were cut at 1,000 characters with `...` on the end, and because that was the only copy the app held, the same cut value reached the JSON tab, Copy JSON, the row inspector, and exports. Editing one of those cells and saving wrote the cut value back to Redis and lost the rest.</li>
<li>Hash, list, set, sorted set, and stream previews are now valid JSON. They were cut mid-token, so a preview could end in the middle of a key or an escape sequence.</li>
<li>A Redis key that expires between listing and reading now shows an empty cell instead of the text `(nil)`.</li>
<li>A Redis value that is not text, such as a gzip or MessagePack payload, is no longer shown as base64 and rewritten as base64 when you save. It now displays as binary, opens in the hex editor, and is written back byte for byte. This applies to the query editor too, so `SET`, `HSET`, `LPUSH`, `SADD`, and `ZADD` keep binary arguments intact.</li>
<li>Opening a SQL Server table or view that lives outside the default schema no longer fails with "Invalid object name". A table now keeps the schema it was listed under, and switching database no longer leaves the sidebar and the query on different schemas. (#2004)</li>
<li>Editing a MySQL or MariaDB column no longer drops its `ON UPDATE CURRENT_TIMESTAMP`. Saving a change to any other part of the column, even just its comment, silently removed the clause. (#2005)</li>
<li>A MySQL or MariaDB timestamp column that keeps fractional seconds now saves. Its default was written as text instead of an expression, so the change was rejected. (#2005)</li>
<li>Quitting no longer loses unsaved SQL editor tabs. A window with no tabs loaded yet, such as one still waiting on its connection, could erase the saved tabs for that connection, so nothing came back on relaunch. Editors are also saved about a second after you stop typing instead of waiting up to 30 seconds, and a query over 500KB is kept in full rather than restored empty. (#1997)</li>
<li>Opening a large MongoDB collection no longer hangs. Row counts that back the pagination display now give up after 5 seconds and leave the estimate in place instead of holding the tab.</li>
<li>Stop now cancels a running MongoDB query on the server, rather than leaving it running until it finishes on its own.</li>
<li>A MongoDB query that runs out of time now says so, and points at the missing index that usually causes it.</li>
<li>Running a MongoDB query with no limit no longer pulls the whole collection into memory before applying the row limit.</li>
<li>Exporting a MongoDB query that has a skip or limit now exports that range, not the whole collection.</li>
<li>A row count estimate of zero from a table that was never analyzed no longer displays as an empty table.</li>
</ul>
]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.62.0/TablePro-0.62.0-arm64.zip" length="21208350" type="application/octet-stream" sparkle:edSignature="xertxUyx0Qn12iMJu+b1E2LwstEJxnPjnWWHQfweQIlHXRc86Dd1pM6+0LyD0tfhjnsYVwEZq0BNozElg1ALAg=="/>
        </item>
        <item>
            <title>0.62.0</title>
            <pubDate>Sat, 01 Aug 2026 19:58:20 +0000</pubDate>
            <sparkle:version>115</sparkle:version>
            <sparkle:shortVersionString>0.62.0</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <description><![CDATA[<h3>Added</h3>
<ul>
<li>**Claude Agent** provider, which runs AI chat through the Claude Code command line tool so you can use a Claude subscription instead of a metered API key. It answers from your schema when the MCP server is on.</li>
<li>**Count exactly** next to an estimated row total, to replace the estimate with a real count when you want it.</li>
<li>Reorder filter rows by dragging the grip on the left of each row, or with **Move Up** and **Move Down** in the row's right-click menu.</li>
<li>An **On Update** column in the Structure tab for MySQL and MariaDB, so a timestamp column can be set to update itself without writing the SQL by hand. (#2005)</li>
<li>A **Length** column in the Redis key grid, showing the size Redis reports: bytes for a string, element count for a hash, list, set, sorted set, or stream. It tells you how much a preview leaves out.</li>
</ul>
<h3>Changed</h3>
<ul>
<li>The Redis key tree in the sidebar lists keys with `SCAN` instead of `KEYS`, and no longer reads every key's value to draw the tree. `KEYS` blocks the server for the whole scan.</li>
<li>Redis command arguments now follow the same quoting rules as `redis-cli`, so `\xHH` writes a raw byte and a command you paste from `redis-cli` behaves the same way in TablePro. A command with unbalanced quotes is rejected instead of being guessed at.</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>A Redis string key now shows its whole value in the grid. Values were cut at 1,000 characters with `...` on the end, and because that was the only copy the app held, the same cut value reached the JSON tab, Copy JSON, the row inspector, and exports. Editing one of those cells and saving wrote the cut value back to Redis and lost the rest.</li>
<li>Hash, list, set, sorted set, and stream previews are now valid JSON. They were cut mid-token, so a preview could end in the middle of a key or an escape sequence.</li>
<li>A Redis key that expires between listing and reading now shows an empty cell instead of the text `(nil)`.</li>
<li>A Redis value that is not text, such as a gzip or MessagePack payload, is no longer shown as base64 and rewritten as base64 when you save. It now displays as binary, opens in the hex editor, and is written back byte for byte. This applies to the query editor too, so `SET`, `HSET`, `LPUSH`, `SADD`, and `ZADD` keep binary arguments intact.</li>
<li>Opening a SQL Server table or view that lives outside the default schema no longer fails with "Invalid object name". A table now keeps the schema it was listed under, and switching database no longer leaves the sidebar and the query on different schemas. (#2004)</li>
<li>Editing a MySQL or MariaDB column no longer drops its `ON UPDATE CURRENT_TIMESTAMP`. Saving a change to any other part of the column, even just its comment, silently removed the clause. (#2005)</li>
<li>A MySQL or MariaDB timestamp column that keeps fractional seconds now saves. Its default was written as text instead of an expression, so the change was rejected. (#2005)</li>
<li>Quitting no longer loses unsaved SQL editor tabs. A window with no tabs loaded yet, such as one still waiting on its connection, could erase the saved tabs for that connection, so nothing came back on relaunch. Editors are also saved about a second after you stop typing instead of waiting up to 30 seconds, and a query over 500KB is kept in full rather than restored empty. (#1997)</li>
<li>Opening a large MongoDB collection no longer hangs. Row counts that back the pagination display now give up after 5 seconds and leave the estimate in place instead of holding the tab.</li>
<li>Stop now cancels a running MongoDB query on the server, rather than leaving it running until it finishes on its own.</li>
<li>A MongoDB query that runs out of time now says so, and points at the missing index that usually causes it.</li>
<li>Running a MongoDB query with no limit no longer pulls the whole collection into memory before applying the row limit.</li>
<li>Exporting a MongoDB query that has a skip or limit now exports that range, not the whole collection.</li>
<li>A row count estimate of zero from a table that was never analyzed no longer displays as an empty table.</li>
</ul>
]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.62.0/TablePro-0.62.0-x86_64.zip" length="22473703" type="application/octet-stream" sparkle:edSignature="vxG/D34vBpZetYeogdrxGBJ/d+3k0pl7q+kteio0lDaiIAc6fyoBcDp647dj0YWuTaEqOLJ6W4v1moI119uWCA=="/>
        </item>
        <item>
            <title>0.61.0</title>
            <pubDate>Thu, 30 Jul 2026 15:00:56 +0000</pubDate>
            <sparkle:version>114</sparkle:version>
            <sparkle:shortVersionString>0.61.0</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <sparkle:hardwareRequirements>arm64</sparkle:hardwareRequirements>
            <description><![CDATA[<h3>Added</h3>
<ul>
<li>**Close All Tabs**, **Close Other Tabs**, and **Close Tabs for Other Databases** in the File menu, bindable in **Settings > Keyboard**. (#1972)</li>
<li>AI Explain, Optimize, and Fix Error answer with numbered steps and a before/after SQL diff you can apply to the editor. (#1945)</li>
<li>Create a connection from a project folder, filled in from `.env`, `wp-config.php`, `prisma/schema.prisma`, `config/database.yml`, `docker-compose.yml`, `application.properties`, `application.yml`, or `appsettings.json`. (#1959)</li>
<li>An AI tool call limit in **Settings > AI**, now 25 per reply instead of a fixed 10, with **Continue** to resume a paused reply. (#1987)</li>
<li>A pin button on result tabs. (#1982)</li>
<li>The inspector edits the column, index, or foreign key you select in the Structure tab.</li>
</ul>
<h3>Changed</h3>
<ul>
<li>The welcome screen groups Import from Other App, Open Project Folder, Import Connections, and Import from URL into one **Add from Existing** menu, and offers **Try Sample Database** in the connection list.</li>
<li>A failed iCloud sync on iPhone and iPad names the cause: out of storage, no network, or a rejected change. (#1990)</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>The inspector shows the Structure row you selected, not the data row in the same position. Duplicate Row and Copy with Headers no longer act on a data row there either.</li>
<li>Editing a connection on two devices no longer overwrites the field the other one changed. (#643)</li>
<li>A per-connection query timeout set on iPhone or iPad no longer stops that connection from syncing. (#643)</li>
<li>iCloud Sync downloads every page of changes when catching up, instead of only the first. (#643)</li>
<li>iCloud Sync reports a failed sync instead of always reporting success. (#643)</li>
<li>A change iCloud rejects no longer stops the rest of its batch from syncing. (#643)</li>
<li>Connection changes made on the Mac reach the iPhone and iPad app again. (#643)</li>
<li>A connection synced from iPhone or iPad keeps its safe mode setting on the Mac. (#643)</li>
<li>A connection's colour tag set on iPhone or iPad no longer overwrites its colour label on the Mac. (#643)</li>
<li>**Primary Key**, **Nullable**, **Auto Inc**, and the index **Unique** flag now change when you pick a value, and no longer offer Set NULL. (#1991)</li>
<li>A message sent in the AI panel right after launch is no longer replaced by the restored conversation.</li>
<li>A PostgreSQL partitioned table is listed once, with its partitions nested under it. (#1984)</li>
<li>An SSH tunnel that cannot reach the database reports whether the connection was refused or timed out. (#1981)</li>
<li>The macOS local network prompt says why TablePro needs access.</li>
<li>An SSH tunnel no longer drops under heavy traffic.</li>
<li>A `ProxyCommand` line in `~/.ssh/config` is logged as skipped instead of ignored without a word.</li>
<li>Right-clicking below the SQL editor opens the menu for the result tabs, data grid, or status bar instead of the editor's. (#1982)</li>
<li>**View > Pin Result** is enabled only when there is a result tab to pin, and result tabs now show in JSON view. (#1982)</li>
<li>Closing a window asks about unsaved changes in every tab, not just the one on screen. (#1972)</li>
<li>MongoDB authenticates against the database set on the connection, not the one you are browsing. (#1970)</li>
<li>Creating a MongoDB database asks for its first collection, so the database is kept. (#1970)</li>
<li>A MongoDB authentication error names the user and the database it tried. (#1970)</li>
<li>Exporting a MongoDB collection works again, and `db.getCollection("name")` is accepted in the query editor. (#1971)</li>
<li>A MongoDB export includes fields that first appear after the opening document. (#1971)</li>
<li>`postgres`, CockroachDB's `defaultdb`, and Redshift's `dev` are no longer hidden as system databases. (#1967)</li>
<li>The database a connection is using always shows in the sidebar and the database switchers. (#1967)</li>
<li>The license activation sheet opens when you click **Activate License**.</li>
<li>File > New Connection..., ⌘N, and the other File menu connection commands work once a connection is open. (#1975)</li>
<li>The tooltip on the welcome screen's **+** button shows the shortcut bound to New Connection.</li>
<li>The AI chat panel stays inside the right panel at narrow widths. (#1956)</li>
<li>BigQuery `REPEATED` columns (including repeated `STRUCT`) no longer show every element as `null`. (#1963)</li>
<li>BigQuery `STRUCT` cells show nested structs and arrays as JSON instead of escaped text. (#1963)</li>
</ul>
]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.61.0/TablePro-0.61.0-arm64.zip" length="21128604" type="application/octet-stream" sparkle:edSignature="WKcSdVnjYrutOxeh2qE6rV+0yH8O2ztRp5/uGWgdw68iv/kwPudJ9ap4OdgxgRyw1U8M8t2BM2FJaE/hkCzlDQ=="/>
        </item>
        <item>
            <title>0.61.0</title>
            <pubDate>Thu, 30 Jul 2026 15:00:57 +0000</pubDate>
            <sparkle:version>114</sparkle:version>
            <sparkle:shortVersionString>0.61.0</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <description><![CDATA[<h3>Added</h3>
<ul>
<li>**Close All Tabs**, **Close Other Tabs**, and **Close Tabs for Other Databases** in the File menu, bindable in **Settings > Keyboard**. (#1972)</li>
<li>AI Explain, Optimize, and Fix Error answer with numbered steps and a before/after SQL diff you can apply to the editor. (#1945)</li>
<li>Create a connection from a project folder, filled in from `.env`, `wp-config.php`, `prisma/schema.prisma`, `config/database.yml`, `docker-compose.yml`, `application.properties`, `application.yml`, or `appsettings.json`. (#1959)</li>
<li>An AI tool call limit in **Settings > AI**, now 25 per reply instead of a fixed 10, with **Continue** to resume a paused reply. (#1987)</li>
<li>A pin button on result tabs. (#1982)</li>
<li>The inspector edits the column, index, or foreign key you select in the Structure tab.</li>
</ul>
<h3>Changed</h3>
<ul>
<li>The welcome screen groups Import from Other App, Open Project Folder, Import Connections, and Import from URL into one **Add from Existing** menu, and offers **Try Sample Database** in the connection list.</li>
<li>A failed iCloud sync on iPhone and iPad names the cause: out of storage, no network, or a rejected change. (#1990)</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>The inspector shows the Structure row you selected, not the data row in the same position. Duplicate Row and Copy with Headers no longer act on a data row there either.</li>
<li>Editing a connection on two devices no longer overwrites the field the other one changed. (#643)</li>
<li>A per-connection query timeout set on iPhone or iPad no longer stops that connection from syncing. (#643)</li>
<li>iCloud Sync downloads every page of changes when catching up, instead of only the first. (#643)</li>
<li>iCloud Sync reports a failed sync instead of always reporting success. (#643)</li>
<li>A change iCloud rejects no longer stops the rest of its batch from syncing. (#643)</li>
<li>Connection changes made on the Mac reach the iPhone and iPad app again. (#643)</li>
<li>A connection synced from iPhone or iPad keeps its safe mode setting on the Mac. (#643)</li>
<li>A connection's colour tag set on iPhone or iPad no longer overwrites its colour label on the Mac. (#643)</li>
<li>**Primary Key**, **Nullable**, **Auto Inc**, and the index **Unique** flag now change when you pick a value, and no longer offer Set NULL. (#1991)</li>
<li>A message sent in the AI panel right after launch is no longer replaced by the restored conversation.</li>
<li>A PostgreSQL partitioned table is listed once, with its partitions nested under it. (#1984)</li>
<li>An SSH tunnel that cannot reach the database reports whether the connection was refused or timed out. (#1981)</li>
<li>The macOS local network prompt says why TablePro needs access.</li>
<li>An SSH tunnel no longer drops under heavy traffic.</li>
<li>A `ProxyCommand` line in `~/.ssh/config` is logged as skipped instead of ignored without a word.</li>
<li>Right-clicking below the SQL editor opens the menu for the result tabs, data grid, or status bar instead of the editor's. (#1982)</li>
<li>**View > Pin Result** is enabled only when there is a result tab to pin, and result tabs now show in JSON view. (#1982)</li>
<li>Closing a window asks about unsaved changes in every tab, not just the one on screen. (#1972)</li>
<li>MongoDB authenticates against the database set on the connection, not the one you are browsing. (#1970)</li>
<li>Creating a MongoDB database asks for its first collection, so the database is kept. (#1970)</li>
<li>A MongoDB authentication error names the user and the database it tried. (#1970)</li>
<li>Exporting a MongoDB collection works again, and `db.getCollection("name")` is accepted in the query editor. (#1971)</li>
<li>A MongoDB export includes fields that first appear after the opening document. (#1971)</li>
<li>`postgres`, CockroachDB's `defaultdb`, and Redshift's `dev` are no longer hidden as system databases. (#1967)</li>
<li>The database a connection is using always shows in the sidebar and the database switchers. (#1967)</li>
<li>The license activation sheet opens when you click **Activate License**.</li>
<li>File > New Connection..., ⌘N, and the other File menu connection commands work once a connection is open. (#1975)</li>
<li>The tooltip on the welcome screen's **+** button shows the shortcut bound to New Connection.</li>
<li>The AI chat panel stays inside the right panel at narrow widths. (#1956)</li>
<li>BigQuery `REPEATED` columns (including repeated `STRUCT`) no longer show every element as `null`. (#1963)</li>
<li>BigQuery `STRUCT` cells show nested structs and arrays as JSON instead of escaped text. (#1963)</li>
</ul>
]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.61.0/TablePro-0.61.0-x86_64.zip" length="22387221" type="application/octet-stream" sparkle:edSignature="mkg1TzLfigDtTX+xb+StWzdONjNT8uFHSBtSv1asqjsKbG1glO204GZIjUriT+qM8+KmvRY+JPIehWM+lBlpBQ=="/>
        </item>
        <item>
            <title>0.60.1</title>
            <pubDate>Sat, 25 Jul 2026 08:09:47 +0000</pubDate>
            <sparkle:version>113</sparkle:version>
            <sparkle:shortVersionString>0.60.1</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <sparkle:hardwareRequirements>arm64</sparkle:hardwareRequirements>
            <description><![CDATA[<h3>Added</h3>
<ul>
<li>AWS IAM connections have an **RDS Endpoint** field, for when the connection points at a port forward you run yourself and TablePro cannot tell which database is behind it. The MySQL and PostgreSQL profile fields now list the profiles found on disk, like MariaDB already did. (#1432)</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>AWS IAM authentication now works through a tunnel. The token was signed for the local forward instead of the database endpoint, so RDS rejected every connection made over SSH, Cloudflare, Cloud SQL Auth Proxy, or SOCKS. A tunneled connection also no longer needs the region filled in by hand. (#1432)</li>
</ul>
]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.60.1/TablePro-0.60.1-arm64.zip" length="20657327" type="application/octet-stream" sparkle:edSignature="2atAiuol4yJ/tKXO5MXAIBKSP4dMvEHI8ABQLsdlfthNya7orKoe7ftunpODNR82D3Y5Q0eyJrFZ1LF872kkAw=="/>
        </item>
        <item>
            <title>0.60.1</title>
            <pubDate>Sat, 25 Jul 2026 08:09:48 +0000</pubDate>
            <sparkle:version>113</sparkle:version>
            <sparkle:shortVersionString>0.60.1</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <description><![CDATA[<h3>Added</h3>
<ul>
<li>AWS IAM connections have an **RDS Endpoint** field, for when the connection points at a port forward you run yourself and TablePro cannot tell which database is behind it. The MySQL and PostgreSQL profile fields now list the profiles found on disk, like MariaDB already did. (#1432)</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>AWS IAM authentication now works through a tunnel. The token was signed for the local forward instead of the database endpoint, so RDS rejected every connection made over SSH, Cloudflare, Cloud SQL Auth Proxy, or SOCKS. A tunneled connection also no longer needs the region filled in by hand. (#1432)</li>
</ul>
]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.60.1/TablePro-0.60.1-x86_64.zip" length="21871172" type="application/octet-stream" sparkle:edSignature="rM20letf1DDToK5X1qyKdqlb6Q1o7VJeFvObHXN4l3fVq7rlf49qcX2wgJzzHeAd6n7qseVlDQDKF2yZN1HOAg=="/>
        </item>
        <item>
            <title>0.60.0</title>
            <pubDate>Fri, 24 Jul 2026 05:12:21 +0000</pubDate>
            <sparkle:version>112</sparkle:version>
            <sparkle:shortVersionString>0.60.0</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <sparkle:hardwareRequirements>arm64</sparkle:hardwareRequirements>
            <description><![CDATA[<h3>Added</h3>
<ul>
<li>Connect to PGlite through its socket server (`pglite-server`), with schema browsing, SQL editing, and row editing like PostgreSQL. (#1911)</li>
<li>In the CSV editor, edit structure from the column-header and row right-click menus and the Edit menu: rename, insert, delete, or change the type of columns; insert or delete rows; split a column by a delimiter or regex and merge adjacent columns, each a single undo step. Deleting columns or rows that hold data asks you to confirm first. (#1469, #1913)</li>
<li>In the CSV editor, set the delimiter, quote character, escape character, encoding, and line ending from Edit > Set CSV Properties…, then Reload to re-read the file. Switch the first row between header and data with `Cmd+Shift+H`, and files without a header no longer save with a generated header row. (#1469)</li>
<li>Add llama.cpp and MLX as local AI providers, each pointing at the server's default local endpoint with no API key. (#1777)</li>
<li>SQL Server Windows Authentication (Kerberos) now connects to servers whose Kerberos realm differs from your Mac's default realm, read from the `[domain_realm]` mapping in your Kerberos configuration. (#1947)</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>The object list no longer blanks out and shows a spinner while a schema change refreshes it. A spinner now only appears for a first load or a refresh that runs long. (#1916)</li>
<li>Saving a change in the Structure tab no longer strips and re-adds the Columns, Indexes, and Foreign Keys counts, so the picker stops shifting on every save. (#1916)</li>
<li>SSH tunnels using the None auth method now work on iPhone and iPad, including connections synced from the Mac. (#1912)</li>
<li>Browsing a Trino table no longer fails with a syntax error. (#1936)</li>
<li>The Authentication method selector no longer shifts position in the connection form when you switch between methods that show or hide the username and password. (#1947)</li>
<li>Switching schemas no longer flashes the sidebar loading spinner once per open tab. The table list now reloads once per connection instead of once per window. (#1946)</li>
</ul>
]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.60.0/TablePro-0.60.0-arm64.zip" length="20658423" type="application/octet-stream" sparkle:edSignature="bQnIUjzLb2Qq85iJREoCgW4jwvmwo0IDfJtRyNTu1vhDu4YFwfJk2q60wZNMgxCKUoBYd3wmowC3Yai8qGfBDg=="/>
        </item>
        <item>
            <title>0.60.0</title>
            <pubDate>Fri, 24 Jul 2026 05:12:21 +0000</pubDate>
            <sparkle:version>112</sparkle:version>
            <sparkle:shortVersionString>0.60.0</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <description><![CDATA[<h3>Added</h3>
<ul>
<li>Connect to PGlite through its socket server (`pglite-server`), with schema browsing, SQL editing, and row editing like PostgreSQL. (#1911)</li>
<li>In the CSV editor, edit structure from the column-header and row right-click menus and the Edit menu: rename, insert, delete, or change the type of columns; insert or delete rows; split a column by a delimiter or regex and merge adjacent columns, each a single undo step. Deleting columns or rows that hold data asks you to confirm first. (#1469, #1913)</li>
<li>In the CSV editor, set the delimiter, quote character, escape character, encoding, and line ending from Edit > Set CSV Properties…, then Reload to re-read the file. Switch the first row between header and data with `Cmd+Shift+H`, and files without a header no longer save with a generated header row. (#1469)</li>
<li>Add llama.cpp and MLX as local AI providers, each pointing at the server's default local endpoint with no API key. (#1777)</li>
<li>SQL Server Windows Authentication (Kerberos) now connects to servers whose Kerberos realm differs from your Mac's default realm, read from the `[domain_realm]` mapping in your Kerberos configuration. (#1947)</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>The object list no longer blanks out and shows a spinner while a schema change refreshes it. A spinner now only appears for a first load or a refresh that runs long. (#1916)</li>
<li>Saving a change in the Structure tab no longer strips and re-adds the Columns, Indexes, and Foreign Keys counts, so the picker stops shifting on every save. (#1916)</li>
<li>SSH tunnels using the None auth method now work on iPhone and iPad, including connections synced from the Mac. (#1912)</li>
<li>Browsing a Trino table no longer fails with a syntax error. (#1936)</li>
<li>The Authentication method selector no longer shifts position in the connection form when you switch between methods that show or hide the username and password. (#1947)</li>
<li>Switching schemas no longer flashes the sidebar loading spinner once per open tab. The table list now reloads once per connection instead of once per window. (#1946)</li>
</ul>
]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.60.0/TablePro-0.60.0-x86_64.zip" length="21870533" type="application/octet-stream" sparkle:edSignature="re4KTQlR2QdUYzPSCf5N7peg6552pOZHKy+6TaAXKDvF2EGQT99B9JhBxAF795Zml8QcvIC6VMKf/yscB25WAw=="/>
        </item>
        <item>
            <title>0.59.0</title>
            <pubDate>Tue, 21 Jul 2026 12:11:30 +0000</pubDate>
            <sparkle:version>111</sparkle:version>
            <sparkle:shortVersionString>0.59.0</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <sparkle:hardwareRequirements>arm64</sparkle:hardwareRequirements>
            <description><![CDATA[<h3>Added</h3>
<ul>
<li>Trino support through a downloadable native Swift driver. Connect with a username and password or a JWT token, optionally over TLS with a custom CA or client certificate, browse catalogs, schemas, tables, and columns, run SQL, edit rows, and create or alter tables. (#1906)</li>
<li>SSH tunnels support **None** as an auth method, for hosts that handle SSH auth themselves like Tailscale SSH. (#1907)</li>
<li>SSH tunnels support keyboard-interactive 2FA, so a server that needs a private key plus a one-time code now connects. TablePro shows the prompt and you type the code. (#1920)</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>Copy as INSERT, UPDATE, JSON, CSV, or Markdown now copies only the cells you selected, not the whole row. Copy as UPDATE still keys its WHERE clause on the primary key. (#1929)</li>
<li>Windows Authentication (Kerberos) to SQL Server now works on macOS. The bundled driver was built without Kerberos support, so every Windows-auth connect failed. (#1918)</li>
<li>MySQL and MariaDB queries no longer fail after about a minute when a longer query timeout is set. The client read timeout now follows the query timeout. (#1921)</li>
<li>A dropped MySQL or MariaDB connection no longer re-runs a statement that changes data, so a lost connection can't run an insert or update twice. (#1921)</li>
<li>The MongoDB, Oracle, Cassandra, and Elasticsearch plugins failed to install on 0.58 with "Bundle failed to load executable". (#1917)</li>
<li>Plugin install and load failures now name the real cause (wrong architecture, missing dependency, or version incompatibility) instead of a generic error. A plugin that fails to load on demand is reported instead of silently disappearing. (#1915)</li>
</ul>
<h3>Changed</h3>
<ul>
<li>AI Chat renders Markdown while the assistant reply streams, including open code blocks, instead of waiting for the reply to finish.</li>
</ul>
]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.59.0/TablePro-0.59.0-arm64.zip" length="20606975" type="application/octet-stream" sparkle:edSignature="tu8lCVoC4T/mwTGbNORt+qZkdyeErOGHw2kG0CfKGBfWMJZImNXKkclwX6xygSGTGGnRfYymW/6yfKqQmORVDQ=="/>
        </item>
        <item>
            <title>0.59.0</title>
            <pubDate>Tue, 21 Jul 2026 12:11:30 +0000</pubDate>
            <sparkle:version>111</sparkle:version>
            <sparkle:shortVersionString>0.59.0</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <description><![CDATA[<h3>Added</h3>
<ul>
<li>Trino support through a downloadable native Swift driver. Connect with a username and password or a JWT token, optionally over TLS with a custom CA or client certificate, browse catalogs, schemas, tables, and columns, run SQL, edit rows, and create or alter tables. (#1906)</li>
<li>SSH tunnels support **None** as an auth method, for hosts that handle SSH auth themselves like Tailscale SSH. (#1907)</li>
<li>SSH tunnels support keyboard-interactive 2FA, so a server that needs a private key plus a one-time code now connects. TablePro shows the prompt and you type the code. (#1920)</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>Copy as INSERT, UPDATE, JSON, CSV, or Markdown now copies only the cells you selected, not the whole row. Copy as UPDATE still keys its WHERE clause on the primary key. (#1929)</li>
<li>Windows Authentication (Kerberos) to SQL Server now works on macOS. The bundled driver was built without Kerberos support, so every Windows-auth connect failed. (#1918)</li>
<li>MySQL and MariaDB queries no longer fail after about a minute when a longer query timeout is set. The client read timeout now follows the query timeout. (#1921)</li>
<li>A dropped MySQL or MariaDB connection no longer re-runs a statement that changes data, so a lost connection can't run an insert or update twice. (#1921)</li>
<li>The MongoDB, Oracle, Cassandra, and Elasticsearch plugins failed to install on 0.58 with "Bundle failed to load executable". (#1917)</li>
<li>Plugin install and load failures now name the real cause (wrong architecture, missing dependency, or version incompatibility) instead of a generic error. A plugin that fails to load on demand is reported instead of silently disappearing. (#1915)</li>
</ul>
<h3>Changed</h3>
<ul>
<li>AI Chat renders Markdown while the assistant reply streams, including open code blocks, instead of waiting for the reply to finish.</li>
</ul>
]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.59.0/TablePro-0.59.0-x86_64.zip" length="21809729" type="application/octet-stream" sparkle:edSignature="+tewO2Pz3Zht/O3a5IPj61z3GumFZacUxcgwPz+z9nJS5Cayp+9qRchj6HgUs8bZXiE0flca9pr4RCPbjLJ/Cg=="/>
        </item>
        <item>
            <title>0.58.0</title>
            <pubDate>Sat, 18 Jul 2026 10:59:46 +0000</pubDate>
            <sparkle:version>110</sparkle:version>
            <sparkle:shortVersionString>0.58.0</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <sparkle:hardwareRequirements>arm64</sparkle:hardwareRequirements>
            <description><![CDATA[<h3>Added</h3>
<ul>
<li>Connect through a SOCKS5 proxy, set on the connection form's new SOCKS Proxy pane. The proxy resolves the database hostname, so names that only resolve behind it work. (#1882)</li>
<li>SQL Server connections can use Windows Authentication (Kerberos) on macOS. Sign in with your existing `kinit` ticket, or enter a Kerberos principal and password. Connect by hostname, not IP address. (#1879)</li>
<li>Teradata support through a downloadable native Swift driver. Connect over TD2 or TDNEGO, optionally with TLS, browse databases, tables, and columns, run SQL, edit rows, and create or alter tables. (#1867)</li>
<li>The sidebar database tree remembers which databases and schemas you expanded, per connection, so reopening a window keeps them open.</li>
<li>A Saved Customizations section in Settings lists the tables where you set column layouts or filters, and lets you reset any or all of them.</li>
<li>Per-table column layouts (widths, order, and hidden columns) sync across your Macs with iCloud when Settings sync is on.</li>
</ul>
<h3>Changed</h3>
<ul>
<li>The query result row cap no longer adds a LIMIT to your SQL. The query runs as written and TablePro stops reading at the cap, so a query with ORDER BY no longer returns different rows than you typed. (#1884)</li>
<li>Settings groups the data grid, pagination, result formatting, JSON viewer, query history, and saved customizations under a new Data tab, and moves recent tables, object comments, and default sidebar layout under General.</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>Fixed the split dividers in the Users and Roles, Structure, Server Dashboard, and SQL editor panels not showing the resize cursor on hover, even though they could be dragged. (#1905)</li>
<li>Fixed a query with a comment after the closing semicolon, such as `SELECT 1; -- note`, running as two statements and failing on the trailing comment. A comment-only query now does nothing instead of erroring. (#1895)</li>
<li>Fixed duplicating a connection dropping its Cloudflare Tunnel and Cloud SQL Auth Proxy settings and stored secrets.</li>
<li>Fixed the tunnel panes warning about only some conflicting connection methods. Each pane now lists every one with a button to turn it off.</li>
<li>Fixed Copy as, Delete, and the Edit menu's copy and delete acting on only the active row instead of the whole cell-range or column selection in the data grid. (#1898)</li>
<li>Fixed the Edit menu's Copy as JSON copying the wrong rows when the grid was sorted or filtered. (#1898)</li>
<li>Fixed tables picked in the sidebar showing up unchecked and being skipped when exporting to SQL or MQL. The export sheet now checks them with the format's default options. (#1897)</li>
<li>Fixed ClickHouse queries showing only a success message with no result table. TablePro now reads what the server returned instead of guessing from the first word, so any query that produces rows shows the grid, including empty results and queries with their own FORMAT clause. (#1886)</li>
<li>Fixed ClickHouse INSERT statements always reporting 0 rows affected. (#1886)</li>
<li>Fixed ClickHouse query exports writing an empty file when the query started with a comment. (#1886)</li>
<li>Fixed a restored window tab sometimes showing a blank name until you switched to it. (#1894)</li>
<li>Fixed the window title not updating right away after saving a query to a file or opening a favorite into the current tab. (#1894)</li>
<li>Fixed the SQL editor in a new tab losing keyboard focus after the first couple of characters, which stopped typing until you clicked back in. (#1885)</li>
<li>Cancelling a SQL Server connection now stops right away instead of waiting for the attempt to finish. A connection also gives up after the login timeout with a clear message, and for Windows Authentication points at the usual Kerberos causes (KDC unreachable, missing SPN, or clock skew). (#1889)</li>
<li>Fixed MySQL and MariaDB queries ending in ORDER BY showing an empty result instead of a server error. A failure while reading rows was treated as the end of results, so TablePro now shows the error. (#1884)</li>
<li>Fixed the query result row cap returning a single row when it was set to unlimited. (#1884)</li>
<li>Fixed SSH tunnels that could accept a connection then go quiet, which showed up as MySQL and MariaDB timing out while reading the server greeting. A tunnel that cannot open its forwarding channel now gives up after 10 seconds and logs the reason. (#1883)</li>
<li>Fixed connections being reset when a single session opened several at once through one SSH tunnel, which could happen while browsing schemas. (#1883)</li>
<li>Fixed the connection form's SSH tunnel Socket Path hint always showing the PostgreSQL path. It now shows the right default per database type, such as `/var/run/mysqld/mysqld.sock` for MySQL and MariaDB. (#1902)</li>
<li>Per-column display formats (Display As) are now kept per table, so two tables with the same name in different databases or schemas no longer share formatting.</li>
<li>Reopening a table now restores a saved filter's AND/OR match mode instead of resetting it to AND.</li>
</ul>
]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.58.0/TablePro-0.58.0-arm64.zip" length="20592959" type="application/octet-stream" sparkle:edSignature="g0a3cfxvfQMFknBE4b+/wl4dDllD48sNW2ETriBy7SlovtoWnmHflqxLAv8ilDLIjj1jsf+/PofJ39fUwxhbBg=="/>
        </item>
        <item>
            <title>0.58.0</title>
            <pubDate>Sat, 18 Jul 2026 10:59:47 +0000</pubDate>
            <sparkle:version>110</sparkle:version>
            <sparkle:shortVersionString>0.58.0</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <description><![CDATA[<h3>Added</h3>
<ul>
<li>Connect through a SOCKS5 proxy, set on the connection form's new SOCKS Proxy pane. The proxy resolves the database hostname, so names that only resolve behind it work. (#1882)</li>
<li>SQL Server connections can use Windows Authentication (Kerberos) on macOS. Sign in with your existing `kinit` ticket, or enter a Kerberos principal and password. Connect by hostname, not IP address. (#1879)</li>
<li>Teradata support through a downloadable native Swift driver. Connect over TD2 or TDNEGO, optionally with TLS, browse databases, tables, and columns, run SQL, edit rows, and create or alter tables. (#1867)</li>
<li>The sidebar database tree remembers which databases and schemas you expanded, per connection, so reopening a window keeps them open.</li>
<li>A Saved Customizations section in Settings lists the tables where you set column layouts or filters, and lets you reset any or all of them.</li>
<li>Per-table column layouts (widths, order, and hidden columns) sync across your Macs with iCloud when Settings sync is on.</li>
</ul>
<h3>Changed</h3>
<ul>
<li>The query result row cap no longer adds a LIMIT to your SQL. The query runs as written and TablePro stops reading at the cap, so a query with ORDER BY no longer returns different rows than you typed. (#1884)</li>
<li>Settings groups the data grid, pagination, result formatting, JSON viewer, query history, and saved customizations under a new Data tab, and moves recent tables, object comments, and default sidebar layout under General.</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>Fixed the split dividers in the Users and Roles, Structure, Server Dashboard, and SQL editor panels not showing the resize cursor on hover, even though they could be dragged. (#1905)</li>
<li>Fixed a query with a comment after the closing semicolon, such as `SELECT 1; -- note`, running as two statements and failing on the trailing comment. A comment-only query now does nothing instead of erroring. (#1895)</li>
<li>Fixed duplicating a connection dropping its Cloudflare Tunnel and Cloud SQL Auth Proxy settings and stored secrets.</li>
<li>Fixed the tunnel panes warning about only some conflicting connection methods. Each pane now lists every one with a button to turn it off.</li>
<li>Fixed Copy as, Delete, and the Edit menu's copy and delete acting on only the active row instead of the whole cell-range or column selection in the data grid. (#1898)</li>
<li>Fixed the Edit menu's Copy as JSON copying the wrong rows when the grid was sorted or filtered. (#1898)</li>
<li>Fixed tables picked in the sidebar showing up unchecked and being skipped when exporting to SQL or MQL. The export sheet now checks them with the format's default options. (#1897)</li>
<li>Fixed ClickHouse queries showing only a success message with no result table. TablePro now reads what the server returned instead of guessing from the first word, so any query that produces rows shows the grid, including empty results and queries with their own FORMAT clause. (#1886)</li>
<li>Fixed ClickHouse INSERT statements always reporting 0 rows affected. (#1886)</li>
<li>Fixed ClickHouse query exports writing an empty file when the query started with a comment. (#1886)</li>
<li>Fixed a restored window tab sometimes showing a blank name until you switched to it. (#1894)</li>
<li>Fixed the window title not updating right away after saving a query to a file or opening a favorite into the current tab. (#1894)</li>
<li>Fixed the SQL editor in a new tab losing keyboard focus after the first couple of characters, which stopped typing until you clicked back in. (#1885)</li>
<li>Cancelling a SQL Server connection now stops right away instead of waiting for the attempt to finish. A connection also gives up after the login timeout with a clear message, and for Windows Authentication points at the usual Kerberos causes (KDC unreachable, missing SPN, or clock skew). (#1889)</li>
<li>Fixed MySQL and MariaDB queries ending in ORDER BY showing an empty result instead of a server error. A failure while reading rows was treated as the end of results, so TablePro now shows the error. (#1884)</li>
<li>Fixed the query result row cap returning a single row when it was set to unlimited. (#1884)</li>
<li>Fixed SSH tunnels that could accept a connection then go quiet, which showed up as MySQL and MariaDB timing out while reading the server greeting. A tunnel that cannot open its forwarding channel now gives up after 10 seconds and logs the reason. (#1883)</li>
<li>Fixed connections being reset when a single session opened several at once through one SSH tunnel, which could happen while browsing schemas. (#1883)</li>
<li>Fixed the connection form's SSH tunnel Socket Path hint always showing the PostgreSQL path. It now shows the right default per database type, such as `/var/run/mysqld/mysqld.sock` for MySQL and MariaDB. (#1902)</li>
<li>Per-column display formats (Display As) are now kept per table, so two tables with the same name in different databases or schemas no longer share formatting.</li>
<li>Reopening a table now restores a saved filter's AND/OR match mode instead of resetting it to AND.</li>
</ul>
]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.58.0/TablePro-0.58.0-x86_64.zip" length="21796313" type="application/octet-stream" sparkle:edSignature="ZA0X8JwbJoi7qclPVFAOdBOWgC9lsj+1UVADaTdlxIbIqnvkaZeIyQKpWzHY6S1CErdlhtOGPUQnB1LqXKb1Dg=="/>
        </item>
        <item>
            <title>0.57.1</title>
            <pubDate>Wed, 15 Jul 2026 08:19:05 +0000</pubDate>
            <sparkle:version>109</sparkle:version>
            <sparkle:shortVersionString>0.57.1</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <sparkle:hardwareRequirements>arm64</sparkle:hardwareRequirements>
            <description><![CDATA[<h3>Added</h3>
<ul>
<li>SSH tunnels can now forward to a Unix socket on the remote server, for databases that only listen on a socket. Set the socket path on the General pane, and TablePro forwards to it instead of a host and port, through jump hosts if you use them. (#1871)</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>Fixed the right sidebar refusing to resize while a Users & Roles tab was open. The user list now collapses on its own when the tab gets too narrow to hold it. (#1872)</li>
<li>Fixed dividers that could not be dragged at all: the user list and privilege panes in Users & Roles, the trigger list in Structure, and the metrics and slow query panes in Server Dashboard. (#1872)</li>
<li>Cancel now stops a connection attempt right away instead of letting it run on in the background. A cancelled connection is also dropped from the last session, so restarting no longer reconnects to a host you gave up on, and it can no longer interrupt a later successful connection. (#1358)</li>
<li>Fixed a crash that could quit the app when opening or sorting a table whose columns have comments. (#1869, #1880)</li>
<li>MCP tools no longer stop working after the server sits idle for 15 minutes, or after the MCP server is restarted from Settings. TablePro's bridge now starts a new session by itself instead of reusing a dead one, so agents like Claude Code and Cursor keep working without being turned off and on again. (#1881)</li>
</ul>
<h3>Security</h3>
<ul>
<li>Patched CVE-2026-55200 in libssh2, a critical out-of-bounds write that let a malicious SSH server corrupt memory and run code before authentication finished. It affected every SSH tunnel and jump host.</li>
</ul>
]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.57.1/TablePro-0.57.1-arm64.zip" length="20508373" type="application/octet-stream" sparkle:edSignature="lN8ctEUkJO4uc1Xj1IK3g/ZnSc2EazB8UxxSP89lFs36hNIOmZuMh9tO49cZYrIpElZi6eh9lg/dmhRA18MmAg=="/>
        </item>
        <item>
            <title>0.57.1</title>
            <pubDate>Wed, 15 Jul 2026 08:19:05 +0000</pubDate>
            <sparkle:version>109</sparkle:version>
            <sparkle:shortVersionString>0.57.1</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <description><![CDATA[<h3>Added</h3>
<ul>
<li>SSH tunnels can now forward to a Unix socket on the remote server, for databases that only listen on a socket. Set the socket path on the General pane, and TablePro forwards to it instead of a host and port, through jump hosts if you use them. (#1871)</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>Fixed the right sidebar refusing to resize while a Users & Roles tab was open. The user list now collapses on its own when the tab gets too narrow to hold it. (#1872)</li>
<li>Fixed dividers that could not be dragged at all: the user list and privilege panes in Users & Roles, the trigger list in Structure, and the metrics and slow query panes in Server Dashboard. (#1872)</li>
<li>Cancel now stops a connection attempt right away instead of letting it run on in the background. A cancelled connection is also dropped from the last session, so restarting no longer reconnects to a host you gave up on, and it can no longer interrupt a later successful connection. (#1358)</li>
<li>Fixed a crash that could quit the app when opening or sorting a table whose columns have comments. (#1869, #1880)</li>
<li>MCP tools no longer stop working after the server sits idle for 15 minutes, or after the MCP server is restarted from Settings. TablePro's bridge now starts a new session by itself instead of reusing a dead one, so agents like Claude Code and Cursor keep working without being turned off and on again. (#1881)</li>
</ul>
<h3>Security</h3>
<ul>
<li>Patched CVE-2026-55200 in libssh2, a critical out-of-bounds write that let a malicious SSH server corrupt memory and run code before authentication finished. It affected every SSH tunnel and jump host.</li>
</ul>
]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.57.1/TablePro-0.57.1-x86_64.zip" length="21702343" type="application/octet-stream" sparkle:edSignature="bxgNPE4nzUUQ4gyQFsQ4TSIdLIdZQrZyJ5emyhK5I/YPAFhlrZnUWgFUC6X7SFCs6vS2NGewnGgWjscVel5mAQ=="/>
        </item>
        <item>
            <title>0.57.0</title>
            <pubDate>Tue, 14 Jul 2026 07:23:31 +0000</pubDate>
            <sparkle:version>108</sparkle:version>
            <sparkle:shortVersionString>0.57.0</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <sparkle:hardwareRequirements>arm64</sparkle:hardwareRequirements>
            <description><![CDATA[<h3>Added</h3>
<ul>
<li>SurrealDB 2.x and 3.x support as a downloadable driver. Browse namespaces and databases, run SurrealQL, and edit records in the grid. (#1862)</li>
<li>xAI (Grok) as an AI provider. Paste a key from the xAI Console, or sign in with a SuperGrok or X Premium+ subscription and use no key at all.</li>
<li>Google Cloud SQL connections through the Cloud SQL Auth Proxy. Turn it on for a MySQL, PostgreSQL, or SQL Server connection, set the instance connection name, and TablePro starts and stops the proxy for you. (#1728)</li>
<li>Beancount ledger support as a downloadable, read-only driver. Transactions, postings, accounts, prices, and balances read as SQL tables, and BQL runs with a `BQL:` prefix. (#1474)</li>
<li>**New Query** in the Favorites sidebar **+** menu, which opens an empty SQL query tab.</li>
<li>Database users, roles, and privileges on MySQL and PostgreSQL, under **View > Users & Roles**. Pick any object from the server down to a single column, see where each privilege comes from, and grant or revoke it. Changes are staged, undoable, and shown as SQL before they run. (#1413)</li>
<li>**File > Reopen Closed Tab** (`Cmd+Shift+T`) brings back the tab you just closed, and **File > Recently Closed** lists the last 20, kept for 30 days. (#1854)</li>
<li>Open a database from the terminal. Install the `tablepro` command from **Settings > General**, then run `ddev tablepro` in a DDEV project. Links to a database on your own Mac can be trusted with **Always Allow** and reviewed in **Settings > General**. (#1486)</li>
</ul>
<h3>Changed</h3>
<ul>
<li>Query results always open in a result tab, so a single result can be pinned before the next query replaces it. Pin from the tab's context menu or with `Cmd+Option+P`. (#1855)</li>
<li>Closing a query tab keeps its SQL in **Recently Closed** instead of throwing it away, and the close button shows the unsaved dot for a query you typed into. (#1854)</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>Fixed a crash when several tables on the same connection ran queries at the same time.</li>
<li>Column comments now show in the data grid header as soon as the table opens, and the header grows to fit them. (#1861)</li>
<li>**Size to Fit** no longer stretches a column of long text past the window. Fitted columns stop at half the visible grid, and every column has a maximum width.</li>
<li>The username is now optional. Leaving it empty no longer fills in `root`, and lets the database pick its own default the way `psql` and `mysql` do.</li>
<li>A failed or cancelled connection that uses a Cloudflare tunnel no longer leaves the `cloudflared` process running in the background.</li>
<li>Pinned results are no longer discarded by **Clear Results**, and a tab holding one is no longer reused to browse a different table. (#1855)</li>
<li>Quitting now warns about unsaved changes in any tab, not just the visible one. (#1854)</li>
</ul>
]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.57.0/TablePro-0.57.0-arm64.zip" length="20439741" type="application/octet-stream" sparkle:edSignature="PZOszTUlsJ7SE/kJH8s96kPydXoVo807m4XMmZCjYNM80l7tA6Cy42uvQXmZ+IWIBkTG9VHCx4VE+Ns6Ly+iCg=="/>
        </item>
        <item>
            <title>0.57.0</title>
            <pubDate>Tue, 14 Jul 2026 07:23:32 +0000</pubDate>
            <sparkle:version>108</sparkle:version>
            <sparkle:shortVersionString>0.57.0</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <description><![CDATA[<h3>Added</h3>
<ul>
<li>SurrealDB 2.x and 3.x support as a downloadable driver. Browse namespaces and databases, run SurrealQL, and edit records in the grid. (#1862)</li>
<li>xAI (Grok) as an AI provider. Paste a key from the xAI Console, or sign in with a SuperGrok or X Premium+ subscription and use no key at all.</li>
<li>Google Cloud SQL connections through the Cloud SQL Auth Proxy. Turn it on for a MySQL, PostgreSQL, or SQL Server connection, set the instance connection name, and TablePro starts and stops the proxy for you. (#1728)</li>
<li>Beancount ledger support as a downloadable, read-only driver. Transactions, postings, accounts, prices, and balances read as SQL tables, and BQL runs with a `BQL:` prefix. (#1474)</li>
<li>**New Query** in the Favorites sidebar **+** menu, which opens an empty SQL query tab.</li>
<li>Database users, roles, and privileges on MySQL and PostgreSQL, under **View > Users & Roles**. Pick any object from the server down to a single column, see where each privilege comes from, and grant or revoke it. Changes are staged, undoable, and shown as SQL before they run. (#1413)</li>
<li>**File > Reopen Closed Tab** (`Cmd+Shift+T`) brings back the tab you just closed, and **File > Recently Closed** lists the last 20, kept for 30 days. (#1854)</li>
<li>Open a database from the terminal. Install the `tablepro` command from **Settings > General**, then run `ddev tablepro` in a DDEV project. Links to a database on your own Mac can be trusted with **Always Allow** and reviewed in **Settings > General**. (#1486)</li>
</ul>
<h3>Changed</h3>
<ul>
<li>Query results always open in a result tab, so a single result can be pinned before the next query replaces it. Pin from the tab's context menu or with `Cmd+Option+P`. (#1855)</li>
<li>Closing a query tab keeps its SQL in **Recently Closed** instead of throwing it away, and the close button shows the unsaved dot for a query you typed into. (#1854)</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>Fixed a crash when several tables on the same connection ran queries at the same time.</li>
<li>Column comments now show in the data grid header as soon as the table opens, and the header grows to fit them. (#1861)</li>
<li>**Size to Fit** no longer stretches a column of long text past the window. Fitted columns stop at half the visible grid, and every column has a maximum width.</li>
<li>The username is now optional. Leaving it empty no longer fills in `root`, and lets the database pick its own default the way `psql` and `mysql` do.</li>
<li>A failed or cancelled connection that uses a Cloudflare tunnel no longer leaves the `cloudflared` process running in the background.</li>
<li>Pinned results are no longer discarded by **Clear Results**, and a tab holding one is no longer reused to browse a different table. (#1855)</li>
<li>Quitting now warns about unsaved changes in any tab, not just the visible one. (#1854)</li>
</ul>
]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.57.0/TablePro-0.57.0-x86_64.zip" length="21640649" type="application/octet-stream" sparkle:edSignature="QB96ZUGgVinyMcOou+pPtJWnd3+a5VDt2ssD8n4cVvWW0qSMSZx/z0aEMN4PqJeaHq/Bi2QE3e0uK+9rsgxFCA=="/>
        </item>
        <item>
            <title>0.56.2</title>
            <pubDate>Fri, 10 Jul 2026 09:39:54 +0000</pubDate>
            <sparkle:version>107</sparkle:version>
            <sparkle:shortVersionString>0.56.2</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <sparkle:hardwareRequirements>arm64</sparkle:hardwareRequirements>
            <description><![CDATA[<h3>Added</h3>
<ul>
<li>The JSON field in the row details inspector can now be resized by dragging the handle below it. The height is remembered across rows and app restarts. (#1849)</li>
</ul>
<h3>Changed</h3>
<ul>
<li>Data grid column comments now appear directly in column headers when object comments are enabled, instead of only being available from the header tooltip. (#1789)</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>MySQL and MariaDB connections that prompt for a password at connect time now ask for a fresh password after an auto-reconnect hits an authentication failure such as `1045` / `SQLSTATE 28000`, instead of looping forever in Connecting with the expired session password.</li>
<li>Pressing Escape to dismiss the SQL autocomplete popup no longer moves focus out of the editor, so you can keep typing. (#1845)</li>
</ul>
]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.56.2/TablePro-0.56.2-arm64.zip" length="19895746" type="application/octet-stream" sparkle:edSignature="IMA0fPXSWCXPQF6JfcPnugW8dgL5bRd4czp3uMX37nSUIT0miqCPJ70tFFLQ79auB9ihoGnaBGuFw1Znwsp3BA=="/>
        </item>
        <item>
            <title>0.56.2</title>
            <pubDate>Fri, 10 Jul 2026 09:39:55 +0000</pubDate>
            <sparkle:version>107</sparkle:version>
            <sparkle:shortVersionString>0.56.2</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <description><![CDATA[<h3>Added</h3>
<ul>
<li>The JSON field in the row details inspector can now be resized by dragging the handle below it. The height is remembered across rows and app restarts. (#1849)</li>
</ul>
<h3>Changed</h3>
<ul>
<li>Data grid column comments now appear directly in column headers when object comments are enabled, instead of only being available from the header tooltip. (#1789)</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>MySQL and MariaDB connections that prompt for a password at connect time now ask for a fresh password after an auto-reconnect hits an authentication failure such as `1045` / `SQLSTATE 28000`, instead of looping forever in Connecting with the expired session password.</li>
<li>Pressing Escape to dismiss the SQL autocomplete popup no longer moves focus out of the editor, so you can keep typing. (#1845)</li>
</ul>
]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.56.2/TablePro-0.56.2-x86_64.zip" length="21040920" type="application/octet-stream" sparkle:edSignature="QvA2KVr1gbSdONO2T4uOgAoi+Y16MpebZ1mOKT2JqDpgs1FuJBN+E+4BqTTs2LigZ9RsFXrO5RZCuFR9SljwDg=="/>
        </item>
        <item>
            <title>0.56.1</title>
            <pubDate>Thu, 09 Jul 2026 08:15:28 +0000</pubDate>
            <sparkle:version>106</sparkle:version>
            <sparkle:shortVersionString>0.56.1</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <sparkle:hardwareRequirements>arm64</sparkle:hardwareRequirements>
            <description><![CDATA[<h3>Removed</h3>
<ul>
<li>The Oracle Native network encryption connection option. Encryption is now offered to every server and used only when the server requires it, so the option is no longer needed. (#483)</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>Oracle now connects to servers that require native network encryption (`SQLNET.ENCRYPTION_SERVER = REQUIRED`), negotiating AES with a SHA checksum automatically like SQL Developer and DBeaver. (#483)</li>
<li>MongoDB filters and generated edit statements now quote numeric-looking values that would not round-trip, such as `.5`, `1.`, `+7`, `01`, integers larger than 64 bits, and exponents that overflow a double like `1e400`, instead of emitting invalid or lossy JSON. A row whose `_id` is a decimal or exponent string is matched as that string, so delete and update target the right document. Update the MongoDB plugin to get the fix. (#1813)</li>
<li>Reading a connection password from a command, 1Password, Vault, or AWS Secrets Manager no longer occasionally returns corrupted output from a race while reading the command's output. (#1841)</li>
<li>The Structure tab filter and column sort now update the grid instead of leaving stale rows on screen.</li>
<li>The row details inspector now shows the selected row's values, including JSON, when a column value filter is active, and a JSON or serialized value you open now follows the selected row as you move between rows. (#1837)</li>
<li>Copying, duplicating, and deleting rows now act on the rows you selected when a column value filter is active, instead of the rows sitting at those positions in the unfiltered result. (#1837)</li>
</ul>
]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.56.1/TablePro-0.56.1-arm64.zip" length="19896760" type="application/octet-stream" sparkle:edSignature="c5vjEbCVXq7fuMnV+pcZkJhj1AQlgjpc5Z8fkY5XyFBBYOv51kDyRxrVs0IeCWgJc9SKQZU5+iYHJ613RdBgDw=="/>
        </item>
        <item>
            <title>0.56.1</title>
            <pubDate>Thu, 09 Jul 2026 08:15:29 +0000</pubDate>
            <sparkle:version>106</sparkle:version>
            <sparkle:shortVersionString>0.56.1</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <description><![CDATA[<h3>Removed</h3>
<ul>
<li>The Oracle Native network encryption connection option. Encryption is now offered to every server and used only when the server requires it, so the option is no longer needed. (#483)</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>Oracle now connects to servers that require native network encryption (`SQLNET.ENCRYPTION_SERVER = REQUIRED`), negotiating AES with a SHA checksum automatically like SQL Developer and DBeaver. (#483)</li>
<li>MongoDB filters and generated edit statements now quote numeric-looking values that would not round-trip, such as `.5`, `1.`, `+7`, `01`, integers larger than 64 bits, and exponents that overflow a double like `1e400`, instead of emitting invalid or lossy JSON. A row whose `_id` is a decimal or exponent string is matched as that string, so delete and update target the right document. Update the MongoDB plugin to get the fix. (#1813)</li>
<li>Reading a connection password from a command, 1Password, Vault, or AWS Secrets Manager no longer occasionally returns corrupted output from a race while reading the command's output. (#1841)</li>
<li>The Structure tab filter and column sort now update the grid instead of leaving stale rows on screen.</li>
<li>The row details inspector now shows the selected row's values, including JSON, when a column value filter is active, and a JSON or serialized value you open now follows the selected row as you move between rows. (#1837)</li>
<li>Copying, duplicating, and deleting rows now act on the rows you selected when a column value filter is active, instead of the rows sitting at those positions in the unfiltered result. (#1837)</li>
</ul>
]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.56.1/TablePro-0.56.1-x86_64.zip" length="21033608" type="application/octet-stream" sparkle:edSignature="85Nt521LPdCO7QoMIqoKapX50kBc/ZEeYpoUZvfjTfoOrOdcg3EyIsWlZPkKx0peKHL651w4+O+QnghykCysBw=="/>
        </item>
        <item>
            <title>0.56.0</title>
            <pubDate>Wed, 08 Jul 2026 19:24:37 +0000</pubDate>
            <sparkle:version>105</sparkle:version>
            <sparkle:shortVersionString>0.56.0</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <sparkle:hardwareRequirements>arm64</sparkle:hardwareRequirements>
            <description><![CDATA[<h3>Added</h3>
<ul>
<li>Custom AI slash commands now sync across your Macs with iCloud Sync.</li>
<li>Join a team by pasting your invite code where a license key goes, in Settings > Account. Owners manage members and seats on tablepro.app.</li>
<li>Connections can pull their password from 1Password, HashiCorp Vault, or AWS Secrets Manager at connect time, so the secret is never stored in TablePro.</li>
<li>Team plan: publish connections to a shared folder for your team, without passwords. Right-click a connection, then Share > Publish to Team Catalog; teammates add the folder under Settings > Linked Folders.</li>
<li>Team Library: share connections and saved queries with your team through your account. Right-click a connection or use the Favorites sidebar to Publish to Team Library. Teammates see them in their list and sidebar; you manage the library on tablepro.app. Passwords are never included.</li>
<li>Saved SQL queries and their folders now sync across your Macs. Turn on Saved Queries under Settings > Sync.</li>
<li>A Recent section at the top of the sidebar lists the last 10 tables you opened per connection and database, and remembers them between launches. Click one to reopen it, or right-click to remove or clear. Off by default; turn it on in Settings > General > Sidebar. (#1352)</li>
<li>Saving now asks you to confirm before it permanently deletes rows. (#1823)</li>
</ul>
<h3>Changed</h3>
<ul>
<li>The sidebar filter now matches table and view names by substring instead of fuzzy matching, and sorts names that start with what you typed to the top. (#1822)</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>Typing in the SQL editor no longer crashes the app, and the autocomplete popup appears again as you type. (#1835)</li>
<li>Deleting many rows at once no longer freezes the app. Multi-row deletes now run as one batched statement instead of one query per row. (#1823)</li>
<li>Deleting rows from a table without a primary key now matches on every column, so it won't remove other rows that share the first column's value. (#1823)</li>
<li>Sorting a table column no longer discards your rows-per-page setting. It keeps the page size and returns to page 1 instead of loading the whole table. (#1826)</li>
<li>Sorting a column no longer fails with a SQL error on tables whose name contains a word like limit or offset. (#1825)</li>
<li>Elasticsearch connections using Username & Password now show a Password field, so you can set up basic auth on a secured cluster. Update the Elasticsearch plugin to get the fix. (#1816)</li>
<li>Switching schemas on an Oracle connection no longer hangs on an infinite spinner. Oracle now switches by schema, loads each schema's tables on expand, respects the query timeout with automatic reconnect, and shows an error with a Retry button when a schema fails to load. Update the plugin to get query timeout enforcement. (#1807)</li>
<li>Resizing a data grid column no longer triggers a sort. Dragging a column edge only resizes it; clicking the header still sorts. (#1815)</li>
<li>Hidden columns stay hidden, and resizing a column no longer brings them back. Column widths, order, and visibility are remembered per table, kept separately for each connection, database, and schema so tables with the same name don't overwrite each other. A new Reset Columns button in the Columns popover restores defaults. Existing saved column layouts reset once with this change. (#1815)</li>
<li>Query and filter errors now appear in a banner you can select and copy, with a Fix with AI button, instead of a dialog that cut the message off. The banner sizes to the message. (#1815)</li>
<li>The filter autocomplete no longer pops up on empty input, and pressing Escape to close it no longer also closes the filter bar. (#1815)</li>
<li>The editor autocomplete popup drops below the line you're typing instead of covering it, no longer overlaps the Columns and Add buttons, and dismisses when you click anywhere on it that isn't a suggestion. (#1815, #1831)</li>
</ul>
]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.56.0/TablePro-0.56.0-arm64.zip" length="19886482" type="application/octet-stream" sparkle:edSignature="Kc5z94swmmLZruvymGKOxC4/nxxhB1WdivJElbgPRgWMOJpOCGGiMPAPz6CGZq/M3okJFLOJObQhhIYOj7syCw=="/>
        </item>
        <item>
            <title>0.56.0</title>
            <pubDate>Wed, 08 Jul 2026 19:24:38 +0000</pubDate>
            <sparkle:version>105</sparkle:version>
            <sparkle:shortVersionString>0.56.0</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <description><![CDATA[<h3>Added</h3>
<ul>
<li>Custom AI slash commands now sync across your Macs with iCloud Sync.</li>
<li>Join a team by pasting your invite code where a license key goes, in Settings > Account. Owners manage members and seats on tablepro.app.</li>
<li>Connections can pull their password from 1Password, HashiCorp Vault, or AWS Secrets Manager at connect time, so the secret is never stored in TablePro.</li>
<li>Team plan: publish connections to a shared folder for your team, without passwords. Right-click a connection, then Share > Publish to Team Catalog; teammates add the folder under Settings > Linked Folders.</li>
<li>Team Library: share connections and saved queries with your team through your account. Right-click a connection or use the Favorites sidebar to Publish to Team Library. Teammates see them in their list and sidebar; you manage the library on tablepro.app. Passwords are never included.</li>
<li>Saved SQL queries and their folders now sync across your Macs. Turn on Saved Queries under Settings > Sync.</li>
<li>A Recent section at the top of the sidebar lists the last 10 tables you opened per connection and database, and remembers them between launches. Click one to reopen it, or right-click to remove or clear. Off by default; turn it on in Settings > General > Sidebar. (#1352)</li>
<li>Saving now asks you to confirm before it permanently deletes rows. (#1823)</li>
</ul>
<h3>Changed</h3>
<ul>
<li>The sidebar filter now matches table and view names by substring instead of fuzzy matching, and sorts names that start with what you typed to the top. (#1822)</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>Typing in the SQL editor no longer crashes the app, and the autocomplete popup appears again as you type. (#1835)</li>
<li>Deleting many rows at once no longer freezes the app. Multi-row deletes now run as one batched statement instead of one query per row. (#1823)</li>
<li>Deleting rows from a table without a primary key now matches on every column, so it won't remove other rows that share the first column's value. (#1823)</li>
<li>Sorting a table column no longer discards your rows-per-page setting. It keeps the page size and returns to page 1 instead of loading the whole table. (#1826)</li>
<li>Sorting a column no longer fails with a SQL error on tables whose name contains a word like limit or offset. (#1825)</li>
<li>Elasticsearch connections using Username & Password now show a Password field, so you can set up basic auth on a secured cluster. Update the Elasticsearch plugin to get the fix. (#1816)</li>
<li>Switching schemas on an Oracle connection no longer hangs on an infinite spinner. Oracle now switches by schema, loads each schema's tables on expand, respects the query timeout with automatic reconnect, and shows an error with a Retry button when a schema fails to load. Update the plugin to get query timeout enforcement. (#1807)</li>
<li>Resizing a data grid column no longer triggers a sort. Dragging a column edge only resizes it; clicking the header still sorts. (#1815)</li>
<li>Hidden columns stay hidden, and resizing a column no longer brings them back. Column widths, order, and visibility are remembered per table, kept separately for each connection, database, and schema so tables with the same name don't overwrite each other. A new Reset Columns button in the Columns popover restores defaults. Existing saved column layouts reset once with this change. (#1815)</li>
<li>Query and filter errors now appear in a banner you can select and copy, with a Fix with AI button, instead of a dialog that cut the message off. The banner sizes to the message. (#1815)</li>
<li>The filter autocomplete no longer pops up on empty input, and pressing Escape to close it no longer also closes the filter bar. (#1815)</li>
<li>The editor autocomplete popup drops below the line you're typing instead of covering it, no longer overlaps the Columns and Add buttons, and dismisses when you click anywhere on it that isn't a suggestion. (#1815, #1831)</li>
</ul>
]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.56.0/TablePro-0.56.0-x86_64.zip" length="21020775" type="application/octet-stream" sparkle:edSignature="Txlf1UA3+5CAt4CY9NJq4oJOttzRNFQmeawfgYeHORmNPz6xcRLxgo9L5DI4Uz/7HK4Ko/JtiTmm0cWnLPshCA=="/>
        </item>
        <item>
            <title>0.55.0</title>
            <pubDate>Fri, 03 Jul 2026 18:36:36 +0000</pubDate>
            <sparkle:version>104</sparkle:version>
            <sparkle:shortVersionString>0.55.0</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <sparkle:hardwareRequirements>arm64</sparkle:hardwareRequirements>
            <description><![CDATA[<h3>Added</h3>
<ul>
<li>iOS: add data to a table from the Shortcuts app. Two new shortcuts, Add Row to Table and Add Rows to Table, pick a saved connection, database or schema, and table, then insert from JSON or CSV. They run without opening the app. (#1788)</li>
<li>Refresh button in Settings > Plugins > Browse to reload the plugin list on demand. (#1799)</li>
<li>SQL Favorites: put ;; in a saved query to set where the cursor lands after keyword expansion in the editor. (#1795)</li>
<li>SELECT queries without their own LIMIT now run with the row cap applied as a real LIMIT on the statement sent to the server, so forgetting a LIMIT no longer pulls millions of rows. The query text in the editor never changes, a query with its own LIMIT is sent as written and not capped, and Execute Query Without Limit (Option+Cmd+Enter, also in the Query menu and the Execute button menu) skips the cap for one run. (#1794)</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>The New/Edit Favorite and linked file metadata dialogs no longer open with a large empty area and fields crushed against the right edge. They now use the standard macOS grouped form layout: the query editor spans the full dialog width, the cursor marker hint sits under the editor, keyword validation shows under the keyword field, and the Global option is a checkbox that explains its scope. (#1809)</li>
<li>Tables and views outside the connection's default schema now open and save correctly when reached through the Quick Switcher, a restored tab, or the MCP table-open tool. These paths used to send unqualified queries, which failed with "Invalid object name" on SQL Server or silently targeted the wrong table. (#1774)</li>
<li>Quick Switcher opens again on macOS Sequoia. Since 0.51.0 the panel came up invisible on macOS 15, and the toolbar button and keyboard shortcut appeared to do nothing. (#1806)</li>
<li>Quick Switcher keyboard navigation (Ctrl-J/K and arrow shortcuts) no longer goes dead after the switcher has been opened and closed repeatedly. (#1806)</li>
<li>Restored table tabs no longer reload all at once or flood failure dialogs on launch. Only the frontmost tab loads immediately; other restored tabs load when you switch to them, and a load failure now shows inline in the tab instead of a dialog. (#1796)</li>
<li>Execute All Statements now applies the query result row cap to each SELECT in the batch instead of returning every row, and each result tab tracks its own truncation state so Fetch All loads the rest of the result you are viewing. (#1794)</li>
<li>Queries starting with a comment or with a newline right after the first keyword are now classified correctly: the row cap applies to them, and Safe Mode recognizes such writes and destructive statements instead of letting them through unprompted.</li>
<li>Enum and set value pickers now populate when the driver reports the values only inside the column type instead of as a separate list.</li>
<li>The plugin list no longer goes stale. The app now checks the plugin registry for changes at launch, when the plugin browser opens, and before reporting a plugin as missing, so newly published plugins show up and install right away. A registry that cannot be reached now reports a connection problem instead of "Plugin not found". (#1799)</li>
<li>Dropping a materialized view or foreign table from the sidebar now generates the correct DROP statement instead of DROP TABLE, and drop and truncate statements are schema-qualified. ClickHouse now lists materialized views as their own sidebar section and drops them with the DROP VIEW syntax it requires. (#1800)</li>
<li>Scrolling no longer goes dead across the whole app after opening an ER diagram and switching tabs while the pointer rests over the canvas. Diagram pan and zoom now handle scroll events on the canvas itself instead of intercepting them app-wide. (#1805)</li>
</ul>
]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.55.0/TablePro-0.55.0-arm64.zip" length="19773292" type="application/octet-stream" sparkle:edSignature="OvsNnoGtxmhT9Xy1itRQAk1Ms+owESG8Qiqs64fpZNTNguzKC1TbPIQAFuVBx4FtuwNgPDFTPB/8W5hukI55Dg=="/>
        </item>
        <item>
            <title>0.55.0</title>
            <pubDate>Fri, 03 Jul 2026 18:36:37 +0000</pubDate>
            <sparkle:version>104</sparkle:version>
            <sparkle:shortVersionString>0.55.0</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <description><![CDATA[<h3>Added</h3>
<ul>
<li>iOS: add data to a table from the Shortcuts app. Two new shortcuts, Add Row to Table and Add Rows to Table, pick a saved connection, database or schema, and table, then insert from JSON or CSV. They run without opening the app. (#1788)</li>
<li>Refresh button in Settings > Plugins > Browse to reload the plugin list on demand. (#1799)</li>
<li>SQL Favorites: put ;; in a saved query to set where the cursor lands after keyword expansion in the editor. (#1795)</li>
<li>SELECT queries without their own LIMIT now run with the row cap applied as a real LIMIT on the statement sent to the server, so forgetting a LIMIT no longer pulls millions of rows. The query text in the editor never changes, a query with its own LIMIT is sent as written and not capped, and Execute Query Without Limit (Option+Cmd+Enter, also in the Query menu and the Execute button menu) skips the cap for one run. (#1794)</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>The New/Edit Favorite and linked file metadata dialogs no longer open with a large empty area and fields crushed against the right edge. They now use the standard macOS grouped form layout: the query editor spans the full dialog width, the cursor marker hint sits under the editor, keyword validation shows under the keyword field, and the Global option is a checkbox that explains its scope. (#1809)</li>
<li>Tables and views outside the connection's default schema now open and save correctly when reached through the Quick Switcher, a restored tab, or the MCP table-open tool. These paths used to send unqualified queries, which failed with "Invalid object name" on SQL Server or silently targeted the wrong table. (#1774)</li>
<li>Quick Switcher opens again on macOS Sequoia. Since 0.51.0 the panel came up invisible on macOS 15, and the toolbar button and keyboard shortcut appeared to do nothing. (#1806)</li>
<li>Quick Switcher keyboard navigation (Ctrl-J/K and arrow shortcuts) no longer goes dead after the switcher has been opened and closed repeatedly. (#1806)</li>
<li>Restored table tabs no longer reload all at once or flood failure dialogs on launch. Only the frontmost tab loads immediately; other restored tabs load when you switch to them, and a load failure now shows inline in the tab instead of a dialog. (#1796)</li>
<li>Execute All Statements now applies the query result row cap to each SELECT in the batch instead of returning every row, and each result tab tracks its own truncation state so Fetch All loads the rest of the result you are viewing. (#1794)</li>
<li>Queries starting with a comment or with a newline right after the first keyword are now classified correctly: the row cap applies to them, and Safe Mode recognizes such writes and destructive statements instead of letting them through unprompted.</li>
<li>Enum and set value pickers now populate when the driver reports the values only inside the column type instead of as a separate list.</li>
<li>The plugin list no longer goes stale. The app now checks the plugin registry for changes at launch, when the plugin browser opens, and before reporting a plugin as missing, so newly published plugins show up and install right away. A registry that cannot be reached now reports a connection problem instead of "Plugin not found". (#1799)</li>
<li>Dropping a materialized view or foreign table from the sidebar now generates the correct DROP statement instead of DROP TABLE, and drop and truncate statements are schema-qualified. ClickHouse now lists materialized views as their own sidebar section and drops them with the DROP VIEW syntax it requires. (#1800)</li>
<li>Scrolling no longer goes dead across the whole app after opening an ER diagram and switching tabs while the pointer rests over the canvas. Diagram pan and zoom now handle scroll events on the canvas itself instead of intercepting them app-wide. (#1805)</li>
</ul>
]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.55.0/TablePro-0.55.0-x86_64.zip" length="20902976" type="application/octet-stream" sparkle:edSignature="fo1R3Eb0FnHEsyYZ/rLlsPvNB7m3AQveuj0sxW4jT9AozZrwP/3Dl7CgPYZpoxnVB8PR9ism7TddwRdrMcXFAQ=="/>
        </item>
        <item>
            <title>0.54.0</title>
            <pubDate>Tue, 30 Jun 2026 02:10:25 +0000</pubDate>
            <sparkle:version>103</sparkle:version>
            <sparkle:shortVersionString>0.54.0</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <sparkle:hardwareRequirements>arm64</sparkle:hardwareRequirements>
            <description><![CDATA[<h3>Added</h3>
<ul>
<li>Per-tab database picker in the query editor toolbar. Each SQL tab can target its own database without clearing other tabs.</li>
<li>Single-clicking a table in the sidebar tree opens it in the current tab; double-clicking opens it in a new tab.</li>
<li>Table and column comments from the database now show in the UI. The sidebar shows a table's comment in dimmed text after its name, the data grid column header tooltip includes the column comment, and the table inspector shows the table comment. Toggle from View > Show Object Comments. Available for MySQL and PostgreSQL. (#1771)</li>
</ul>
<h3>Changed</h3>
<ul>
<li>Switching the active database keeps existing tabs open instead of closing them.</li>
<li>The toolbar, quick switcher, and query editor database pickers follow the sidebar database filter.</li>
<li>Switching table tabs is faster: filter settings persist off the main thread, and only the active database's table list refreshes.</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>Safe mode no longer jumps to the first tab after you confirm a query. The confirmation now stays on the tab you ran the query from. (#1781)</li>
<li>Switching between sidebar tables no longer leaves extra blank space above the list. (#1675)</li>
<li>SSH tunnels no longer pin a CPU core after the connection drops. A dropped tunnel is now detected and torn down instead of spinning in its relay loop. (#1769)</li>
<li>Restored table tabs now load with the current page size instead of the page size from the previous session.</li>
<li>MSSQL: large `nvarchar(max)` and `text` values no longer truncate to 2048 bytes when copied or viewed. TEXTSIZE is raised at connect time. (#1783)</li>
<li>Oracle connections with Native network encryption turned on no longer hang for about a minute against servers that do not complete it, such as Oracle 11g. The login now stops after 30 seconds and explains how to turn the option off. (#1746)</li>
</ul>
]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.54.0/TablePro-0.54.0-arm64.zip" length="19779134" type="application/octet-stream" sparkle:edSignature="D5K7XH3RwIjHi5rxNt5RDb8nSe8zXsAJDSeGyUMoDvyKTQdcHdmTYPP7VDpNTmauPvHNGodcZot+CAe1Eb/gAw=="/>
        </item>
        <item>
            <title>0.54.0</title>
            <pubDate>Tue, 30 Jun 2026 02:10:26 +0000</pubDate>
            <sparkle:version>103</sparkle:version>
            <sparkle:shortVersionString>0.54.0</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <description><![CDATA[<h3>Added</h3>
<ul>
<li>Per-tab database picker in the query editor toolbar. Each SQL tab can target its own database without clearing other tabs.</li>
<li>Single-clicking a table in the sidebar tree opens it in the current tab; double-clicking opens it in a new tab.</li>
<li>Table and column comments from the database now show in the UI. The sidebar shows a table's comment in dimmed text after its name, the data grid column header tooltip includes the column comment, and the table inspector shows the table comment. Toggle from View > Show Object Comments. Available for MySQL and PostgreSQL. (#1771)</li>
</ul>
<h3>Changed</h3>
<ul>
<li>Switching the active database keeps existing tabs open instead of closing them.</li>
<li>The toolbar, quick switcher, and query editor database pickers follow the sidebar database filter.</li>
<li>Switching table tabs is faster: filter settings persist off the main thread, and only the active database's table list refreshes.</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>Safe mode no longer jumps to the first tab after you confirm a query. The confirmation now stays on the tab you ran the query from. (#1781)</li>
<li>Switching between sidebar tables no longer leaves extra blank space above the list. (#1675)</li>
<li>SSH tunnels no longer pin a CPU core after the connection drops. A dropped tunnel is now detected and torn down instead of spinning in its relay loop. (#1769)</li>
<li>Restored table tabs now load with the current page size instead of the page size from the previous session.</li>
<li>MSSQL: large `nvarchar(max)` and `text` values no longer truncate to 2048 bytes when copied or viewed. TEXTSIZE is raised at connect time. (#1783)</li>
<li>Oracle connections with Native network encryption turned on no longer hang for about a minute against servers that do not complete it, such as Oracle 11g. The login now stops after 30 seconds and explains how to turn the option off. (#1746)</li>
</ul>
]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.54.0/TablePro-0.54.0-x86_64.zip" length="20882416" type="application/octet-stream" sparkle:edSignature="7joUU4zAisxx6ZXRUKKz1mqy4iDrtFzlRW7HP/veFeiFKx5bnnb8E/To+l4DFKNA7nkgjtvo2dWmbtGx5X7SAw=="/>
        </item>
        <item>
            <title>0.53.0</title>
            <pubDate>Thu, 25 Jun 2026 12:13:55 +0000</pubDate>
            <sparkle:version>102</sparkle:version>
            <sparkle:shortVersionString>0.53.0</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <sparkle:hardwareRequirements>arm64</sparkle:hardwareRequirements>
            <description><![CDATA[<h3>Added</h3>
<ul>
<li>Connections can have multiple tags. Assign them in the connection form and filter the welcome list by tag with Match Any or Match All. (#744)</li>
<li>Per-column value filter in the data grid. Click the funnel icon on a column header to choose which loaded values to show, across several columns at once. Filters loaded rows without re-querying. (#1454)</li>
<li>Elasticsearch support: connect to 7.x and 8.x, browse indices, run Query DSL in a console, and edit documents in the data grid. Install from Settings > Plugins. (#1529)</li>
<li>The connection switcher and welcome list now show each connection's tags and group. (#1323)</li>
<li>The ER diagram marks relationship cardinality (one-to-one, one-to-many, and optional variants) with crow's foot notation, read from primary keys and unique indexes. Junction tables collapse into a single many-to-many link, with a toolbar toggle to expand them. (#1335)</li>
<li>Export the ER diagram to SQL. A toolbar button opens a query tab with CREATE TABLE and foreign key statements for the current schema. (#1335)</li>
<li>Oracle connections have a Native network encryption option, off by default, for servers that require encrypted traffic. (#1746)</li>
</ul>
<h3>Changed</h3>
<ul>
<li>The ER diagram uses a more compact layout, keeps foreign-key-linked tables together, and tints each connected group with its own header color. (#1755)</li>
<li>When an Oracle server drops the connection during login, the error dialog now shows which handshake phase it stopped at (helps diagnose Oracle 11g). (#1746)</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>Opening a new query tab now puts keyboard focus in the SQL editor instead of the sidebar filter, so you can type right away. (#1765)</li>
<li>Raw filters in the data grid now work on document and key-value databases; the typed text was dropped before reaching the driver. (#1529)</li>
<li>Connecting to Oracle no longer crashes on certain server values during the handshake; a bad packet now fails the connection with an error. (#1746)</li>
<li>Connecting to Oracle no longer hangs when the server permits but does not require native network encryption; TablePro now connects in clear text by default, like Oracle's own clients. (#1746)</li>
<li>Following a foreign key into another schema now opens the correct table on SQL Server and Oracle, instead of falling back to the default schema. (#1754)</li>
<li>Browsing or editing a SQL Server or Oracle table outside the default schema no longer fails with "Invalid object name" or writes to the wrong table; queries now qualify the table with its schema. (#1754)</li>
</ul>
]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.53.0/TablePro-0.53.0-arm64.zip" length="19736897" type="application/octet-stream" sparkle:edSignature="7FlcHfdwVyikZXhpatdXZVAuuwgeT6FhLpnecVwQrIrvSE0vqij7hVlhibMGUOJ6tupDeWCkmNbNTwvHA5laBw=="/>
        </item>
        <item>
            <title>0.53.0</title>
            <pubDate>Thu, 25 Jun 2026 12:13:56 +0000</pubDate>
            <sparkle:version>102</sparkle:version>
            <sparkle:shortVersionString>0.53.0</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <description><![CDATA[<h3>Added</h3>
<ul>
<li>Connections can have multiple tags. Assign them in the connection form and filter the welcome list by tag with Match Any or Match All. (#744)</li>
<li>Per-column value filter in the data grid. Click the funnel icon on a column header to choose which loaded values to show, across several columns at once. Filters loaded rows without re-querying. (#1454)</li>
<li>Elasticsearch support: connect to 7.x and 8.x, browse indices, run Query DSL in a console, and edit documents in the data grid. Install from Settings > Plugins. (#1529)</li>
<li>The connection switcher and welcome list now show each connection's tags and group. (#1323)</li>
<li>The ER diagram marks relationship cardinality (one-to-one, one-to-many, and optional variants) with crow's foot notation, read from primary keys and unique indexes. Junction tables collapse into a single many-to-many link, with a toolbar toggle to expand them. (#1335)</li>
<li>Export the ER diagram to SQL. A toolbar button opens a query tab with CREATE TABLE and foreign key statements for the current schema. (#1335)</li>
<li>Oracle connections have a Native network encryption option, off by default, for servers that require encrypted traffic. (#1746)</li>
</ul>
<h3>Changed</h3>
<ul>
<li>The ER diagram uses a more compact layout, keeps foreign-key-linked tables together, and tints each connected group with its own header color. (#1755)</li>
<li>When an Oracle server drops the connection during login, the error dialog now shows which handshake phase it stopped at (helps diagnose Oracle 11g). (#1746)</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>Opening a new query tab now puts keyboard focus in the SQL editor instead of the sidebar filter, so you can type right away. (#1765)</li>
<li>Raw filters in the data grid now work on document and key-value databases; the typed text was dropped before reaching the driver. (#1529)</li>
<li>Connecting to Oracle no longer crashes on certain server values during the handshake; a bad packet now fails the connection with an error. (#1746)</li>
<li>Connecting to Oracle no longer hangs when the server permits but does not require native network encryption; TablePro now connects in clear text by default, like Oracle's own clients. (#1746)</li>
<li>Following a foreign key into another schema now opens the correct table on SQL Server and Oracle, instead of falling back to the default schema. (#1754)</li>
<li>Browsing or editing a SQL Server or Oracle table outside the default schema no longer fails with "Invalid object name" or writes to the wrong table; queries now qualify the table with its schema. (#1754)</li>
</ul>
]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.53.0/TablePro-0.53.0-x86_64.zip" length="20855719" type="application/octet-stream" sparkle:edSignature="R1qsNF3AzwtrWqa3Dm8VjASpvw3D1EE7OUj7ZHBQmV2uUu0Et/ZO7cQDcoXa0Hef1fmSSFsV9XPDPHw7aUpHDw=="/>
        </item>
        <item>
            <title>0.52.1</title>
            <pubDate>Sun, 21 Jun 2026 17:47:33 +0000</pubDate>
            <sparkle:version>101</sparkle:version>
            <sparkle:shortVersionString>0.52.1</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <sparkle:hardwareRequirements>arm64</sparkle:hardwareRequirements>
            <description><![CDATA[<h3>Added</h3>
<ul>
<li>Import connections on iPhone. Open a .tablepro file from Files or AirDrop, or use Import Connections in the list menu. Encrypted files prompt for the passphrase, and you choose how to handle duplicates.</li>
<li>Export connections on iPhone from the list menu. Passwords are left out by default; include them by setting a passphrase that encrypts the file.</li>
</ul>
<h3>Changed</h3>
<ul>
<li>Drag-selecting many columns in a wide result set scrolls smoothly instead of lagging.</li>
<li>The connection Export Options dialog keeps a steady size when you turn on Include Credentials, and saves through the standard macOS save dialog.</li>
<li>Scrolling large result sets uses less CPU.</li>
<li>Typing in the sidebar table search stays responsive on databases with thousands of tables.</li>
<li>The welcome sidebar stays responsive with many connections and nested groups.</li>
<li>Autocomplete stays snappy on wide SELECT clauses with hundreds of columns.</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>SQL autocomplete no longer stops appearing until the app is relaunched; it stays available after switching query tabs and windows. (#1731)</li>
<li>Oracle connections no longer crash the app when the server sends a message the driver cannot decode; the query fails with a clear error and reconnects. (#483)</li>
<li>MongoDB TLS handshake failures now report the actual cause instead of always blaming a cipher or protocol mismatch. (#1418)</li>
<li>The External Clients access level no longer reverts to Read Only after saving and reopening a connection, so MCP clients keep the write access you granted. (#1730)</li>
<li>Typing fast with the autocomplete window open no longer stalls each keystroke.</li>
</ul>
]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.52.1/TablePro-0.52.1-arm64.zip" length="28611372" type="application/octet-stream" sparkle:edSignature="4OmudyMEPPv/4KAOn+EUnbUNrZEH1XfKEeRI1eJfVs6wJXcA8QBmob5z5Qd6zZ2HdSIZCGxThSvSQHFBO6ciBg=="/>
        </item>
        <item>
            <title>0.52.1</title>
            <pubDate>Sun, 21 Jun 2026 17:47:34 +0000</pubDate>
            <sparkle:version>101</sparkle:version>
            <sparkle:shortVersionString>0.52.1</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <description><![CDATA[<h3>Added</h3>
<ul>
<li>Import connections on iPhone. Open a .tablepro file from Files or AirDrop, or use Import Connections in the list menu. Encrypted files prompt for the passphrase, and you choose how to handle duplicates.</li>
<li>Export connections on iPhone from the list menu. Passwords are left out by default; include them by setting a passphrase that encrypts the file.</li>
</ul>
<h3>Changed</h3>
<ul>
<li>Drag-selecting many columns in a wide result set scrolls smoothly instead of lagging.</li>
<li>The connection Export Options dialog keeps a steady size when you turn on Include Credentials, and saves through the standard macOS save dialog.</li>
<li>Scrolling large result sets uses less CPU.</li>
<li>Typing in the sidebar table search stays responsive on databases with thousands of tables.</li>
<li>The welcome sidebar stays responsive with many connections and nested groups.</li>
<li>Autocomplete stays snappy on wide SELECT clauses with hundreds of columns.</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>SQL autocomplete no longer stops appearing until the app is relaunched; it stays available after switching query tabs and windows. (#1731)</li>
<li>Oracle connections no longer crash the app when the server sends a message the driver cannot decode; the query fails with a clear error and reconnects. (#483)</li>
<li>MongoDB TLS handshake failures now report the actual cause instead of always blaming a cipher or protocol mismatch. (#1418)</li>
<li>The External Clients access level no longer reverts to Read Only after saving and reopening a connection, so MCP clients keep the write access you granted. (#1730)</li>
<li>Typing fast with the autocomplete window open no longer stalls each keystroke.</li>
</ul>
]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.52.1/TablePro-0.52.1-x86_64.zip" length="31439964" type="application/octet-stream" sparkle:edSignature="Rq9S0AIBT0i/1P1ZGaxngf7zLXqCI5M/fSGDgocKZMvDLHGlWCv5VSnDR0dc2kNr7XGueJW/pqnaAFOE/CTKBA=="/>
        </item>
        <item>
            <title>0.52.0</title>
            <pubDate>Thu, 18 Jun 2026 17:53:31 +0000</pubDate>
            <sparkle:version>100</sparkle:version>
            <sparkle:shortVersionString>0.52.0</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <sparkle:hardwareRequirements>arm64</sparkle:hardwareRequirements>
            <description><![CDATA[<h3>Added</h3>
<ul>
<li>Triggers tab in the table structure view for MySQL, MariaDB, PostgreSQL, SQLite, SQL Server, Oracle, libSQL, and Cloudflare D1, with a filter field, sortable columns, and a read-only view of each trigger's definition. (#1695)</li>
<li>Create, edit, and drop triggers from the Triggers tab. (#1695)</li>
<li>Traditional Chinese (繁體中文) language in Settings > General.</li>
<li>An Add button in the table status bar inserts a new row at the end of the grid and starts editing it.</li>
<li>DuckDB connections can now reach a remote server over the Quack protocol (experimental). Pick Remote (Quack) in the connection form, enter the host, port, and token. Listing remote tables in the sidebar is not available yet. Needs DuckDB 1.5.3 or later. (#1716)</li>
</ul>
<h3>Changed</h3>
<ul>
<li>Selecting a Redis namespace in the sidebar now filters the open view to that prefix with paging, instead of opening a separate tab. (#1701)</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>SQL autocomplete now suggests columns for a derived-table or CTE alias. (#1697)</li>
<li>Redis entries no longer disappear after the connection sits idle. (#1701)</li>
<li>Redis key browsing now lists and pages through every key in a database or namespace, instead of only the first SCAN batch. (#1701)</li>
<li>A dropped Redis connection now reconnects on the next command and replays auth and the selected database. (#1701)</li>
<li>DuckDB VARIANT columns now show their value as text instead of an empty cell.</li>
<li>A new database group now appears in the connection list right away instead of only after restarting the app. (#1704)</li>
<li>The SQL formatter keeps nested indentation for UNION, INTERSECT, and EXCEPT inside a subquery, and puts the closing parenthesis on its own line. (#1698)</li>
<li>Toolbar button tooltips now show each action's real keyboard shortcut and follow your custom bindings. (#1694)</li>
<li>Deleting a table from the sidebar now removes it from the tree right away on multi-database servers like MySQL and PostgreSQL. (#1714)</li>
<li>The row detail panel no longer stays blank when a table is opened in a second tab.</li>
<li>The sidebar filter now stays put when you open another tab.</li>
<li>Fixed a crash when browsing the database tree on servers with many schemas, such as PostgreSQL.</li>
<li>Reopening the app no longer shows a "Not connected to database" error when it restores a table tab on a connection that is still reconnecting, such as one over SSH.</li>
</ul>
]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.52.0/TablePro-0.52.0-arm64.zip" length="28596216" type="application/octet-stream" sparkle:edSignature="ewOJfOiG9Hw8TDpWtirtGi7gpZJcptGj8FYILMs+Bix/EI8yWXNs1QbApGQpoQ6eyTtnQwFW9KiI+qvj8Vp5Ag=="/>
        </item>
        <item>
            <title>0.52.0</title>
            <pubDate>Thu, 18 Jun 2026 17:53:32 +0000</pubDate>
            <sparkle:version>100</sparkle:version>
            <sparkle:shortVersionString>0.52.0</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <description><![CDATA[<h3>Added</h3>
<ul>
<li>Triggers tab in the table structure view for MySQL, MariaDB, PostgreSQL, SQLite, SQL Server, Oracle, libSQL, and Cloudflare D1, with a filter field, sortable columns, and a read-only view of each trigger's definition. (#1695)</li>
<li>Create, edit, and drop triggers from the Triggers tab. (#1695)</li>
<li>Traditional Chinese (繁體中文) language in Settings > General.</li>
<li>An Add button in the table status bar inserts a new row at the end of the grid and starts editing it.</li>
<li>DuckDB connections can now reach a remote server over the Quack protocol (experimental). Pick Remote (Quack) in the connection form, enter the host, port, and token. Listing remote tables in the sidebar is not available yet. Needs DuckDB 1.5.3 or later. (#1716)</li>
</ul>
<h3>Changed</h3>
<ul>
<li>Selecting a Redis namespace in the sidebar now filters the open view to that prefix with paging, instead of opening a separate tab. (#1701)</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>SQL autocomplete now suggests columns for a derived-table or CTE alias. (#1697)</li>
<li>Redis entries no longer disappear after the connection sits idle. (#1701)</li>
<li>Redis key browsing now lists and pages through every key in a database or namespace, instead of only the first SCAN batch. (#1701)</li>
<li>A dropped Redis connection now reconnects on the next command and replays auth and the selected database. (#1701)</li>
<li>DuckDB VARIANT columns now show their value as text instead of an empty cell.</li>
<li>A new database group now appears in the connection list right away instead of only after restarting the app. (#1704)</li>
<li>The SQL formatter keeps nested indentation for UNION, INTERSECT, and EXCEPT inside a subquery, and puts the closing parenthesis on its own line. (#1698)</li>
<li>Toolbar button tooltips now show each action's real keyboard shortcut and follow your custom bindings. (#1694)</li>
<li>Deleting a table from the sidebar now removes it from the tree right away on multi-database servers like MySQL and PostgreSQL. (#1714)</li>
<li>The row detail panel no longer stays blank when a table is opened in a second tab.</li>
<li>The sidebar filter now stays put when you open another tab.</li>
<li>Fixed a crash when browsing the database tree on servers with many schemas, such as PostgreSQL.</li>
<li>Reopening the app no longer shows a "Not connected to database" error when it restores a table tab on a connection that is still reconnecting, such as one over SSH.</li>
</ul>
]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.52.0/TablePro-0.52.0-x86_64.zip" length="31428627" type="application/octet-stream" sparkle:edSignature="ioCxzt6hmrfq6aLX6iqlwzWfVrQvNMVPgj9GTvHvS5aiXdCabdTAPedneIX3NPq0qGXbtS+jyXFJ4mGqsB0ZAQ=="/>
        </item>
        <item>
            <title>0.51.1</title>
            <pubDate>Mon, 15 Jun 2026 17:49:43 +0000</pubDate>
            <sparkle:version>99</sparkle:version>
            <sparkle:shortVersionString>0.51.1</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <sparkle:hardwareRequirements>arm64</sparkle:hardwareRequirements>
            <description><![CDATA[<h3>Added</h3>
<ul>
<li>The tree sidebar can filter to only the databases you pick, saved per connection. (#1667)</li>
<li>Closing a query tab no longer loses unsaved SQL. The next blank query tab for the same connection restores the last closed draft. (#1686)</li>
<li>A checkbox in the filter panel header turns every filter row on or off at once, with a dash when only some are on.</li>
</ul>
<h3>Changed</h3>
<ul>
<li>The filter panel's "Unset" button is now "Clear". It keeps your filter rows and only drops the applied state. To remove the rows, use "Remove All Filters" in the filter options menu.</li>
<li>A row's right-click menu now has "Apply Only This Filter". The inline per-row Apply button is gone.</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>Shift+Arrow in the data grid now starts and extends a cell selection from the focused cell. Cmd+Shift+Arrow extends to the row or column edge.</li>
<li>Delete key now removes all rows covered by a cell-range selection instead of ignoring it.</li>
<li>Right-clicking inside a multi-row or cell-range selection no longer collapses the selection first.</li>
<li>Oracle connections no longer crash during connect when the server sends a short or unexpected handshake packet. (#1683)</li>
<li>MongoDB filters on `_id` and other ObjectId fields now match. A 24-character hex value is matched as an ObjectId as well as a string. (#1682)</li>
<li>The sidebar and inspector keep their width per connection, the sidebar its collapsed state, and the inspector its selected tab, across quit and reopen.</li>
</ul>
]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.51.1/TablePro-0.51.1-arm64.zip" length="19441611" type="application/octet-stream" sparkle:edSignature="Wi4wcHS1/oemMC1WxgMNOVKzzzFpo4tJoTi+rlpeysp3/IOghsehlriRwi0s3bVec+7YQ2WYqgbj9Bg5X1knAw=="/>
        </item>
        <item>
            <title>0.51.1</title>
            <pubDate>Mon, 15 Jun 2026 17:49:44 +0000</pubDate>
            <sparkle:version>99</sparkle:version>
            <sparkle:shortVersionString>0.51.1</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <description><![CDATA[<h3>Added</h3>
<ul>
<li>The tree sidebar can filter to only the databases you pick, saved per connection. (#1667)</li>
<li>Closing a query tab no longer loses unsaved SQL. The next blank query tab for the same connection restores the last closed draft. (#1686)</li>
<li>A checkbox in the filter panel header turns every filter row on or off at once, with a dash when only some are on.</li>
</ul>
<h3>Changed</h3>
<ul>
<li>The filter panel's "Unset" button is now "Clear". It keeps your filter rows and only drops the applied state. To remove the rows, use "Remove All Filters" in the filter options menu.</li>
<li>A row's right-click menu now has "Apply Only This Filter". The inline per-row Apply button is gone.</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>Shift+Arrow in the data grid now starts and extends a cell selection from the focused cell. Cmd+Shift+Arrow extends to the row or column edge.</li>
<li>Delete key now removes all rows covered by a cell-range selection instead of ignoring it.</li>
<li>Right-clicking inside a multi-row or cell-range selection no longer collapses the selection first.</li>
<li>Oracle connections no longer crash during connect when the server sends a short or unexpected handshake packet. (#1683)</li>
<li>MongoDB filters on `_id` and other ObjectId fields now match. A 24-character hex value is matched as an ObjectId as well as a string. (#1682)</li>
<li>The sidebar and inspector keep their width per connection, the sidebar its collapsed state, and the inspector its selected tab, across quit and reopen.</li>
</ul>
]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.51.1/TablePro-0.51.1-x86_64.zip" length="20542670" type="application/octet-stream" sparkle:edSignature="makYitcy1p/GLeMCpcjbxzNUB4tP1A2chFT7wR4AlPRpbCklxJ7OaPsdLnT+xTEJH8x7ToLpUkyN8WYV8IglDg=="/>
        </item>
        <item>
            <title>0.51.0</title>
            <pubDate>Sat, 13 Jun 2026 17:58:13 +0000</pubDate>
            <sparkle:version>98</sparkle:version>
            <sparkle:shortVersionString>0.51.0</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <sparkle:hardwareRequirements>arm64</sparkle:hardwareRequirements>
            <description><![CDATA[<h3>Added</h3>
<ul>
<li>BigQuery datasets can be switched, created, and dropped from the toolbar, Cmd+K switcher, and File menu. (#509)</li>
<li>Quick Switcher now searches saved queries too, alongside tables, views, databases, and history.</li>
<li>Quick Switcher scopes: an empty search shows recent items, and Cmd+1 to Cmd+4 browse all tables, databases, or queries.</li>
<li>Quick Switcher: Option+Return opens a table in a new tab, and right-click opens its structure or copies the name or query.</li>
<li>Tables already open show an Open badge in the Quick Switcher, rank higher, and Return switches to the existing tab.</li>
<li>`.psql` and `.pgsql` files now open in the SQL editor like `.sql`. (#1641)</li>
<li>Session restore brings back each tab's sort, page, cursor position, and column widths, plus the connection's active database and schema. Tabs autosave every 30 seconds, so a crash recovers your last session and reopens its connections. (#1673)</li>
</ul>
<h3>Changed</h3>
<ul>
<li>Redis connections now filter with a key-pattern search field and a key-type scope. Glob patterns like `user:*` match server-side across the whole keyspace, replacing the SQL-style filter row that only matched one batch of keys.</li>
<li>Switcher, menus, and alerts now use each database's own container name: Dataset for BigQuery, Keyspace for Cassandra and ScyllaDB. (#509)</li>
<li>Quick Switcher highlights matched characters, aligns camelCase and snake_case names better, and ranks items you open often and recently higher.</li>
<li>Quick Switcher now opens as a Spotlight-style floating panel instead of a modal sheet, with Liquid Glass on macOS 26.</li>
<li>The sidebar filter, database switcher, and connection switcher now use the same fuzzy matching as the Quick Switcher, so `upv` finds `user_profile_view`.</li>
<li>Refresh (Cmd+R) now acts only on the focused window's connection instead of reloading every open connection.</li>
<li>Holding Cmd+R no longer queues a backlog of refreshes; rapid presses collapse into a single reload.</li>
<li>Switching PostgreSQL schemas now sets the search path to just the selected schema. Unqualified references to "public" objects, such as extension functions, need a "public." prefix while another schema is selected. (#1662)</li>
<li>The inspector panel can now be resized freely by dragging its divider.</li>
<li>TablePro now reopens your last session on launch by default instead of the welcome screen. Existing installs move over once; change it under Settings > General > Startup Behavior. (#1673)</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>PostgreSQL and Redshift autocomplete now completes tables and columns from schemas other than the selected one, so `SELECT * FROM s2.orders` suggests `s2`'s columns. (#1668)</li>
<li>Favorite keywords work again. Deleting a connection now also deletes its saved queries, folders, and per-table filters, the confirmation says so, and favorites orphaned by an earlier delete are cleaned up at launch.</li>
<li>Keyword and SQL keyword autocomplete now work in editors without a connection, and favorites appear in the completion popup immediately.</li>
<li>Typing a favorite's keyword in the Quick Switcher now finds the saved query instead of ranking it below name matches.</li>
<li>PostgreSQL databases without a "public" schema now load tables from the first available schema, show the schema selector even with one schema, and count tables across every user schema. (#1662)</li>
<li>Switching schemas no longer closes open tabs or discards unsaved SQL; the sidebar, schema chip, and autocomplete update to the new schema. (#1669)</li>
<li>Creating a table now turns the Create Table tab into the new table's tab and shows it in the sidebar without a manual refresh. (#1664)</li>
<li>Cmd+S in the Create Table tab now creates the table, matching the Save shortcut everywhere else. (#1664)</li>
<li>Format Query can now be undone with Cmd+Z. (#1645)</li>
<li>Format Query now formats only the selection when one is active, and the full query otherwise. (#1656)</li>
<li>Foreign key jump arrows no longer disappear after sorting, filtering, or paginating, and a failed lookup is retried on the next load.</li>
<li>PostgreSQL foreign keys are now read from the system catalogs, so FK jump arrows appear even when the role does not own the referenced tables.</li>
<li>Sorting a query result no longer overwrites the SQL editor or an opened `.sql` file; the sort runs as a separate query. (#1645)</li>
<li>iCloud Sync between the iPhone and Mac apps now uses the Production CloudKit environment, so development builds no longer sync into a separate database.</li>
<li>Exports no longer fail mid-table on servers with a statement time limit; the export session disables the limit and restores it afterwards. (#1633)</li>
<li>Quick Switcher no longer shows an empty table list when opened before the schema finishes loading.</li>
<li>Loading a saved query or history entry from the no-tabs screen now opens it in the current window instead of a second tab.</li>
<li>Opening a query from history in the Quick Switcher loads the full query instead of a 100-character preview.</li>
<li>Refreshing a table now reloads its data even when the previous load is still running. (#1637)</li>
<li>Cmd+R on a table now reloads its rows instead of failing with a query error.</li>
<li>SQL autocomplete now suggests tables after JOIN across multi-join and multi-clause queries, with tables leading the list. (#1646)</li>
<li>Large SQL scripts no longer freeze the editor or pin the CPU. Above 2 MB the editor suspends syntax highlighting and inline AI so typing and scrolling stay responsive. (#1652)</li>
</ul>
<h3>Security</h3>
<ul>
<li>Imported connections from a deep link or shared file can no longer carry a pre-connect script that runs a shell command on connect.</li>
<li>External database links now ask for confirmation before connecting, and a password in the link is never saved to the Keychain.</li>
<li>MCP tools now enforce each connection's external access level, AI policy, and token scope on every request.</li>
<li>The MCP server now requires a paired token by default, even over loopback.</li>
<li>An installed plugin's code signature is re-checked right before it loads, so the binary cannot be swapped after the first check.</li>
<li>MongoDB filter values in the Contains, Not Contains, Starts With, Ends With, and Regex operators can no longer inject query operators.</li>
<li>iOS validates TLS certificates for MySQL, PostgreSQL, and Redis connections set to a verify SSL mode.</li>
<li>Database values copied on iOS stay on the device and clear from the clipboard after a minute.</li>
<li>The iOS home screen widget no longer stores database host and port on disk.</li>
</ul>
]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.51.0/TablePro-0.51.0-arm64.zip" length="19418182" type="application/octet-stream" sparkle:edSignature="8dnPYxwG80E6TFkWAYVj5vTQo454grXBkTowiHYVwL3fFGdagPyTQz8qT6Dr6PGXMDaObuzDAmBS2bViz2qqBA=="/>
        </item>
        <item>
            <title>0.51.0</title>
            <pubDate>Sat, 13 Jun 2026 17:58:14 +0000</pubDate>
            <sparkle:version>98</sparkle:version>
            <sparkle:shortVersionString>0.51.0</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <description><![CDATA[<h3>Added</h3>
<ul>
<li>BigQuery datasets can be switched, created, and dropped from the toolbar, Cmd+K switcher, and File menu. (#509)</li>
<li>Quick Switcher now searches saved queries too, alongside tables, views, databases, and history.</li>
<li>Quick Switcher scopes: an empty search shows recent items, and Cmd+1 to Cmd+4 browse all tables, databases, or queries.</li>
<li>Quick Switcher: Option+Return opens a table in a new tab, and right-click opens its structure or copies the name or query.</li>
<li>Tables already open show an Open badge in the Quick Switcher, rank higher, and Return switches to the existing tab.</li>
<li>`.psql` and `.pgsql` files now open in the SQL editor like `.sql`. (#1641)</li>
<li>Session restore brings back each tab's sort, page, cursor position, and column widths, plus the connection's active database and schema. Tabs autosave every 30 seconds, so a crash recovers your last session and reopens its connections. (#1673)</li>
</ul>
<h3>Changed</h3>
<ul>
<li>Redis connections now filter with a key-pattern search field and a key-type scope. Glob patterns like `user:*` match server-side across the whole keyspace, replacing the SQL-style filter row that only matched one batch of keys.</li>
<li>Switcher, menus, and alerts now use each database's own container name: Dataset for BigQuery, Keyspace for Cassandra and ScyllaDB. (#509)</li>
<li>Quick Switcher highlights matched characters, aligns camelCase and snake_case names better, and ranks items you open often and recently higher.</li>
<li>Quick Switcher now opens as a Spotlight-style floating panel instead of a modal sheet, with Liquid Glass on macOS 26.</li>
<li>The sidebar filter, database switcher, and connection switcher now use the same fuzzy matching as the Quick Switcher, so `upv` finds `user_profile_view`.</li>
<li>Refresh (Cmd+R) now acts only on the focused window's connection instead of reloading every open connection.</li>
<li>Holding Cmd+R no longer queues a backlog of refreshes; rapid presses collapse into a single reload.</li>
<li>Switching PostgreSQL schemas now sets the search path to just the selected schema. Unqualified references to "public" objects, such as extension functions, need a "public." prefix while another schema is selected. (#1662)</li>
<li>The inspector panel can now be resized freely by dragging its divider.</li>
<li>TablePro now reopens your last session on launch by default instead of the welcome screen. Existing installs move over once; change it under Settings > General > Startup Behavior. (#1673)</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>PostgreSQL and Redshift autocomplete now completes tables and columns from schemas other than the selected one, so `SELECT * FROM s2.orders` suggests `s2`'s columns. (#1668)</li>
<li>Favorite keywords work again. Deleting a connection now also deletes its saved queries, folders, and per-table filters, the confirmation says so, and favorites orphaned by an earlier delete are cleaned up at launch.</li>
<li>Keyword and SQL keyword autocomplete now work in editors without a connection, and favorites appear in the completion popup immediately.</li>
<li>Typing a favorite's keyword in the Quick Switcher now finds the saved query instead of ranking it below name matches.</li>
<li>PostgreSQL databases without a "public" schema now load tables from the first available schema, show the schema selector even with one schema, and count tables across every user schema. (#1662)</li>
<li>Switching schemas no longer closes open tabs or discards unsaved SQL; the sidebar, schema chip, and autocomplete update to the new schema. (#1669)</li>
<li>Creating a table now turns the Create Table tab into the new table's tab and shows it in the sidebar without a manual refresh. (#1664)</li>
<li>Cmd+S in the Create Table tab now creates the table, matching the Save shortcut everywhere else. (#1664)</li>
<li>Format Query can now be undone with Cmd+Z. (#1645)</li>
<li>Format Query now formats only the selection when one is active, and the full query otherwise. (#1656)</li>
<li>Foreign key jump arrows no longer disappear after sorting, filtering, or paginating, and a failed lookup is retried on the next load.</li>
<li>PostgreSQL foreign keys are now read from the system catalogs, so FK jump arrows appear even when the role does not own the referenced tables.</li>
<li>Sorting a query result no longer overwrites the SQL editor or an opened `.sql` file; the sort runs as a separate query. (#1645)</li>
<li>iCloud Sync between the iPhone and Mac apps now uses the Production CloudKit environment, so development builds no longer sync into a separate database.</li>
<li>Exports no longer fail mid-table on servers with a statement time limit; the export session disables the limit and restores it afterwards. (#1633)</li>
<li>Quick Switcher no longer shows an empty table list when opened before the schema finishes loading.</li>
<li>Loading a saved query or history entry from the no-tabs screen now opens it in the current window instead of a second tab.</li>
<li>Opening a query from history in the Quick Switcher loads the full query instead of a 100-character preview.</li>
<li>Refreshing a table now reloads its data even when the previous load is still running. (#1637)</li>
<li>Cmd+R on a table now reloads its rows instead of failing with a query error.</li>
<li>SQL autocomplete now suggests tables after JOIN across multi-join and multi-clause queries, with tables leading the list. (#1646)</li>
<li>Large SQL scripts no longer freeze the editor or pin the CPU. Above 2 MB the editor suspends syntax highlighting and inline AI so typing and scrolling stay responsive. (#1652)</li>
</ul>
<h3>Security</h3>
<ul>
<li>Imported connections from a deep link or shared file can no longer carry a pre-connect script that runs a shell command on connect.</li>
<li>External database links now ask for confirmation before connecting, and a password in the link is never saved to the Keychain.</li>
<li>MCP tools now enforce each connection's external access level, AI policy, and token scope on every request.</li>
<li>The MCP server now requires a paired token by default, even over loopback.</li>
<li>An installed plugin's code signature is re-checked right before it loads, so the binary cannot be swapped after the first check.</li>
<li>MongoDB filter values in the Contains, Not Contains, Starts With, Ends With, and Regex operators can no longer inject query operators.</li>
<li>iOS validates TLS certificates for MySQL, PostgreSQL, and Redis connections set to a verify SSL mode.</li>
<li>Database values copied on iOS stay on the device and clear from the clipboard after a minute.</li>
<li>The iOS home screen widget no longer stores database host and port on disk.</li>
</ul>
]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.51.0/TablePro-0.51.0-x86_64.zip" length="20518024" type="application/octet-stream" sparkle:edSignature="pxb0F5pGVQ8Hm93t78p4wBTYyJGPCgoq3rRt/Ng9r4Q6vGNOu7xszrVazhHDgTLMqGHucHgA5xhhkRpL4cSVAQ=="/>
        </item>
        <item>
            <title>0.50.0</title>
            <pubDate>Mon, 08 Jun 2026 18:51:51 +0000</pubDate>
            <sparkle:version>97</sparkle:version>
            <sparkle:shortVersionString>0.50.0</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <sparkle:hardwareRequirements>arm64</sparkle:hardwareRequirements>
            <description><![CDATA[<h3>Added</h3>
<ul>
<li>Cursor as an AI provider: use a Cursor API key or sign in with the Cursor CLI. (#1624)</li>
<li>Sign in with ChatGPT to run AI chat and inline suggestions without an API key. Existing Codex CLI logins can be imported. (#1617)</li>
<li>libSQL / Turso connections can open a local database file offline, transactions included. (#1607)</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>Default row sort now applies to the first table opened after launch. (#1603)</li>
<li>Cancelling a SQLite query no longer races a disconnect. (#1610)</li>
<li>Typing in the query editor no longer erases characters or drops focus, most visible on macOS 15. (#1608)</li>
<li>The autocomplete popup now filters in place instead of closing and reopening on each keystroke. (#1608)</li>
<li>Syntax highlighting no longer disappears after formatting a query. (#1612)</li>
<li>The GitHub Copilot provider no longer shows a Max output tokens field it ignores or leaves a stray model ID field behind.</li>
<li>Oracle connections with native network encryption no longer crash on a server error; the real ORA error is shown and the connection keeps working. (#483)</li>
<li>Clicking an already-open table switches to its tab instead of opening a duplicate. (#1613)</li>
<li>MongoDB now connects over an SSH or Cloudflare tunnel instead of failing with connection refused. (#1621)</li>
<li>A plugin updated in Settings stays marked Installed instead of showing the Update button again.</li>
<li>DBeaver connections import from any edition, based on your DBeaver data rather than which app is installed. (#1628)</li>
</ul>
]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.50.0/TablePro-0.50.0-arm64.zip" length="19281503" type="application/octet-stream" sparkle:edSignature="HGNzi8h2mrfDQLbglVdRdp4/si0W64xXwMtXAkDEQgpA0DLB8AasMOW3+6SkU0CN+4FdlEVf27yH0uDUr8aQAA=="/>
        </item>
        <item>
            <title>0.50.0</title>
            <pubDate>Mon, 08 Jun 2026 18:51:52 +0000</pubDate>
            <sparkle:version>97</sparkle:version>
            <sparkle:shortVersionString>0.50.0</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <description><![CDATA[<h3>Added</h3>
<ul>
<li>Cursor as an AI provider: use a Cursor API key or sign in with the Cursor CLI. (#1624)</li>
<li>Sign in with ChatGPT to run AI chat and inline suggestions without an API key. Existing Codex CLI logins can be imported. (#1617)</li>
<li>libSQL / Turso connections can open a local database file offline, transactions included. (#1607)</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>Default row sort now applies to the first table opened after launch. (#1603)</li>
<li>Cancelling a SQLite query no longer races a disconnect. (#1610)</li>
<li>Typing in the query editor no longer erases characters or drops focus, most visible on macOS 15. (#1608)</li>
<li>The autocomplete popup now filters in place instead of closing and reopening on each keystroke. (#1608)</li>
<li>Syntax highlighting no longer disappears after formatting a query. (#1612)</li>
<li>The GitHub Copilot provider no longer shows a Max output tokens field it ignores or leaves a stray model ID field behind.</li>
<li>Oracle connections with native network encryption no longer crash on a server error; the real ORA error is shown and the connection keeps working. (#483)</li>
<li>Clicking an already-open table switches to its tab instead of opening a duplicate. (#1613)</li>
<li>MongoDB now connects over an SSH or Cloudflare tunnel instead of failing with connection refused. (#1621)</li>
<li>A plugin updated in Settings stays marked Installed instead of showing the Update button again.</li>
<li>DBeaver connections import from any edition, based on your DBeaver data rather than which app is installed. (#1628)</li>
</ul>
]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.50.0/TablePro-0.50.0-x86_64.zip" length="20366453" type="application/octet-stream" sparkle:edSignature="Y5B6VLkx1/ZoOj4v92ywK5P/H1hdz42tt7YRZ9LeWvZdhvqTYJH5RjH42Qnt4qJ3lBMaNJqcF0E/fsD0UloZBg=="/>
        </item>
        <item>
            <title>0.49.1</title>
            <pubDate>Sat, 06 Jun 2026 12:45:55 +0000</pubDate>
            <sparkle:version>96</sparkle:version>
            <sparkle:shortVersionString>0.49.1</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <sparkle:hardwareRequirements>arm64</sparkle:hardwareRequirements>
            <description><![CDATA[<h3>Fixed</h3>
<ul>
<li>Default row sort by primary key works again for PostgreSQL and other databases, and the rows arrive already sorted on the first load instead of re-sorting after they appear. (#1603)</li>
<li>Registry plugins built before 0.49.0 install and load again instead of failing with an invalid plugin bundle error.</li>
</ul>
]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.49.1/TablePro-0.49.1-arm64.zip" length="19184638" type="application/octet-stream" sparkle:edSignature="w29qs8hAIYcCGafV4bTR1wOh+yBsEnCHcWKDlmfDTAQadBM6m6vlMyWGwg35CtsMObfm3OXp9ZR4/g98bz8SAA=="/>
        </item>
        <item>
            <title>0.49.1</title>
            <pubDate>Sat, 06 Jun 2026 12:45:56 +0000</pubDate>
            <sparkle:version>96</sparkle:version>
            <sparkle:shortVersionString>0.49.1</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <description><![CDATA[<h3>Fixed</h3>
<ul>
<li>Default row sort by primary key works again for PostgreSQL and other databases, and the rows arrive already sorted on the first load instead of re-sorting after they appear. (#1603)</li>
<li>Registry plugins built before 0.49.0 install and load again instead of failing with an invalid plugin bundle error.</li>
</ul>
]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.49.1/TablePro-0.49.1-x86_64.zip" length="20263206" type="application/octet-stream" sparkle:edSignature="omQBCGIFfRcn6QypSj1xEzGYBtpdVG6t7frPVVkP3ZYuZ9w+1WE0aDPmNfDqWQjMyrvz2xD+mtP1eHOmYoqIAw=="/>
        </item>
        <item>
            <title>0.49.0</title>
            <pubDate>Sat, 06 Jun 2026 10:45:41 +0000</pubDate>
            <sparkle:version>95</sparkle:version>
            <sparkle:shortVersionString>0.49.0</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <sparkle:hardwareRequirements>arm64</sparkle:hardwareRequirements>
            <description><![CDATA[<h3>Added</h3>
<ul>
<li>Snowflake support: sign in with username & password (MFA included), key-pair, browser SSO, or an access token; browse and edit data, import CSV and JSON, edit table structure, run scripts, and switch warehouse and role from the toolbar. Snowflake CLI connections can be reused by name. (#1420)</li>
<li>Import CSV and TSV files into a table: map columns to an existing table or create a new one, with delimiter, quote, encoding, header, and NULL options. (#1568)</li>
<li>SQL autocomplete completes each segment of qualified names (database, schema, table), loads tables of unopened schemas on demand, resolves alias columns, and suggests the connection's dialect functions.</li>
<li>Each filter row can be switched on or off and applied on its own; disabled rows stay in the panel. (#1561)</li>
<li>Importing connections from other apps detects duplicates and lets you replace, add a copy, or skip each one.</li>
<li>Oracle connections negotiate Native Network Encryption, so servers that require it now connect. (#483)</li>
<li>Oracle connections follow listener redirects, so RAC SCAN, shared server, and load-balanced setups connect. (#483)</li>
<li>AWS connections can assume an IAM role through STS, including chained source profiles, external IDs, and custom durations. (#1567)</li>
<li>Redis connects to Amazon ElastiCache with IAM auth via access key, profile, or SSO. (#1567)</li>
<li>AWS SSO sign-in from TablePro: an expired session prompts a browser sign-in and refreshes the token. (#1567)</li>
<li>Cassandra connects to Amazon Keyspaces with AWS IAM (SigV4) auth via access keys, profile, or SSO. (#1567)</li>
<li>Export and import dialogs remember the last-used format, options, and encoding; cancelling keeps your saved settings, and Reset to Defaults restores the stock options. (#1591)</li>
</ul>
<h3>Changed</h3>
<ul>
<li>The results status bar has a divider, balanced margins, and a spinner sized to its controls. (#1569)</li>
<li>Custom keyboard shortcuts work on non-US layouts, and shifted symbols like Cmd+[ record correctly.</li>
<li>The Keyboard settings list is grouped by where shortcuts act (Editor, Data Grid, Navigation, Connections), with a reset button per changed shortcut.</li>
<li>Shortcut conflict detection checks live macOS system shortcuts and editor commands, and allows the same key in the editor and the data grid since focus decides which runs.</li>
<li>Show Tables and Show Favorites moved to Cmd+Option+1 and Cmd+Option+2; Control+1 and Control+2 belong to macOS Spaces.</li>
<li>Cmd+N opens a new connection; Manage Connections stays in the File menu.</li>
<li>First Page and Last Page default to Cmd+Option+Up and Cmd+Option+Down.</li>
<li>Shortcuts can use function keys (F1 through F12), with or without a modifier.</li>
<li>AWS connections list the profiles from `~/.aws/config` and `~/.aws/credentials`, and still accept a typed name. (#1567)</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>Accepting an autocomplete suggestion replaces the whole typed word; it could leave part of the word behind, turning `mess` plus Tab into `memessage`.</li>
<li>MongoDB: connecting to Atlas no longer fails with TLS internal error (-9838); the plugin ships the OpenSSL TLS stack again. (#1599)</li>
<li>DuckDB: the plugin runs DuckDB 1.5.2 again after a rollback to 1.5.0.</li>
<li>JSON import: a failed import with "Delete existing rows before import" restores the deleted rows.</li>
<li>JSON import: skip-and-continue no longer inserts duplicate rows after a mid-batch error.</li>
<li>JSON import: "Stop and Commit" keeps the rows inserted before the error.</li>
<li>The connection and database switcher focuses its search field even while a filter input is being edited; the filter text is kept. (#1575)</li>
<li>TablePro no longer shows its icon for .sql, .sqlite, and .duckdb files when it is not their default app. (#1594)</li>
<li>The JSON results view shows row data right away, follows the row selection, and shows a spinner while large results format. (#1576)</li>
<li>Double-click or Enter edits a JSON cell inline and opens the hex editor on a blob cell; the chevron still opens the tree or hex editor. (#1588)</li>
<li>Query results appear as soon as rows return; metadata loads in the background, removing a multi-second wait on slow remote databases. (#1574)</li>
<li>MySQL and MariaDB queries are editable right away instead of waiting on a separate metadata query. (#1574)</li>
<li>Status bar buttons no longer get blocked by the bottom-right window resize zone. (#1569)</li>
<li>VoiceOver reads clear labels for the results status bar controls. (#1569)</li>
<li>The custom rows-per-page popover points at the page-size menu. (#1569)</li>
<li>DynamoDB AWS Profile auth reads `~/.aws/config` too and supports `credential_process`, so config-only, SSO, and credential-process profiles work. (#1567)</li>
<li>Query result columns follow the SELECT order; new columns no longer get stuck at the end of the grid. (#1565)</li>
<li>JSON file import works again. It failed to load in 0.48.0.</li>
<li>SQL export quotes empty or malformed numeric values instead of producing invalid INSERT statements.</li>
<li>SQL Server: logins restricted to one database, such as Azure SQL contained users, now connect; the database is sent during login.</li>
<li>Custom Copy and Cut shortcuts take effect in the SQL editor.</li>
<li>The Delete shortcut in the data grid follows a custom binding.</li>
<li>Find Next (Cmd+G) and Find Previous (Cmd+Shift+G) work in the editor.</li>
<li>Pagination buttons no longer fire their page shortcut twice.</li>
<li>PostgreSQL scripts with `DO $$ ... $$` blocks or dollar-quoted bodies no longer fail with an unterminated string error. (#1559)</li>
<li>AWS IAM connections no longer ask for a password; the same holds for any auth mode that replaces the password, such as a Postgres password file.</li>
<li>Oracle connection failures show the listener's actual reason instead of a generic message. (#483)</li>
<li>A connection password read from a command no longer fails when the command finishes quickly.</li>
<li>A cancelled MCP query returns a cancelled error instead of an invalid-parameters error, and emits an initial progress notification.</li>
</ul>
]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.49.0/TablePro-0.49.0-arm64.zip" length="19185696" type="application/octet-stream" sparkle:edSignature="5gBABpZDh+ReriNOq0uocEnJUOr+oyqlUvlcPSu+t1hwCf40I6hPNHoy17iohIcm6C+5iWNfhSmDBkTwIRsnAQ=="/>
        </item>
        <item>
            <title>0.49.0</title>
            <pubDate>Sat, 06 Jun 2026 10:45:41 +0000</pubDate>
            <sparkle:version>95</sparkle:version>
            <sparkle:shortVersionString>0.49.0</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <description><![CDATA[<h3>Added</h3>
<ul>
<li>Snowflake support: sign in with username & password (MFA included), key-pair, browser SSO, or an access token; browse and edit data, import CSV and JSON, edit table structure, run scripts, and switch warehouse and role from the toolbar. Snowflake CLI connections can be reused by name. (#1420)</li>
<li>Import CSV and TSV files into a table: map columns to an existing table or create a new one, with delimiter, quote, encoding, header, and NULL options. (#1568)</li>
<li>SQL autocomplete completes each segment of qualified names (database, schema, table), loads tables of unopened schemas on demand, resolves alias columns, and suggests the connection's dialect functions.</li>
<li>Each filter row can be switched on or off and applied on its own; disabled rows stay in the panel. (#1561)</li>
<li>Importing connections from other apps detects duplicates and lets you replace, add a copy, or skip each one.</li>
<li>Oracle connections negotiate Native Network Encryption, so servers that require it now connect. (#483)</li>
<li>Oracle connections follow listener redirects, so RAC SCAN, shared server, and load-balanced setups connect. (#483)</li>
<li>AWS connections can assume an IAM role through STS, including chained source profiles, external IDs, and custom durations. (#1567)</li>
<li>Redis connects to Amazon ElastiCache with IAM auth via access key, profile, or SSO. (#1567)</li>
<li>AWS SSO sign-in from TablePro: an expired session prompts a browser sign-in and refreshes the token. (#1567)</li>
<li>Cassandra connects to Amazon Keyspaces with AWS IAM (SigV4) auth via access keys, profile, or SSO. (#1567)</li>
<li>Export and import dialogs remember the last-used format, options, and encoding; cancelling keeps your saved settings, and Reset to Defaults restores the stock options. (#1591)</li>
</ul>
<h3>Changed</h3>
<ul>
<li>The results status bar has a divider, balanced margins, and a spinner sized to its controls. (#1569)</li>
<li>Custom keyboard shortcuts work on non-US layouts, and shifted symbols like Cmd+[ record correctly.</li>
<li>The Keyboard settings list is grouped by where shortcuts act (Editor, Data Grid, Navigation, Connections), with a reset button per changed shortcut.</li>
<li>Shortcut conflict detection checks live macOS system shortcuts and editor commands, and allows the same key in the editor and the data grid since focus decides which runs.</li>
<li>Show Tables and Show Favorites moved to Cmd+Option+1 and Cmd+Option+2; Control+1 and Control+2 belong to macOS Spaces.</li>
<li>Cmd+N opens a new connection; Manage Connections stays in the File menu.</li>
<li>First Page and Last Page default to Cmd+Option+Up and Cmd+Option+Down.</li>
<li>Shortcuts can use function keys (F1 through F12), with or without a modifier.</li>
<li>AWS connections list the profiles from `~/.aws/config` and `~/.aws/credentials`, and still accept a typed name. (#1567)</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>Accepting an autocomplete suggestion replaces the whole typed word; it could leave part of the word behind, turning `mess` plus Tab into `memessage`.</li>
<li>MongoDB: connecting to Atlas no longer fails with TLS internal error (-9838); the plugin ships the OpenSSL TLS stack again. (#1599)</li>
<li>DuckDB: the plugin runs DuckDB 1.5.2 again after a rollback to 1.5.0.</li>
<li>JSON import: a failed import with "Delete existing rows before import" restores the deleted rows.</li>
<li>JSON import: skip-and-continue no longer inserts duplicate rows after a mid-batch error.</li>
<li>JSON import: "Stop and Commit" keeps the rows inserted before the error.</li>
<li>The connection and database switcher focuses its search field even while a filter input is being edited; the filter text is kept. (#1575)</li>
<li>TablePro no longer shows its icon for .sql, .sqlite, and .duckdb files when it is not their default app. (#1594)</li>
<li>The JSON results view shows row data right away, follows the row selection, and shows a spinner while large results format. (#1576)</li>
<li>Double-click or Enter edits a JSON cell inline and opens the hex editor on a blob cell; the chevron still opens the tree or hex editor. (#1588)</li>
<li>Query results appear as soon as rows return; metadata loads in the background, removing a multi-second wait on slow remote databases. (#1574)</li>
<li>MySQL and MariaDB queries are editable right away instead of waiting on a separate metadata query. (#1574)</li>
<li>Status bar buttons no longer get blocked by the bottom-right window resize zone. (#1569)</li>
<li>VoiceOver reads clear labels for the results status bar controls. (#1569)</li>
<li>The custom rows-per-page popover points at the page-size menu. (#1569)</li>
<li>DynamoDB AWS Profile auth reads `~/.aws/config` too and supports `credential_process`, so config-only, SSO, and credential-process profiles work. (#1567)</li>
<li>Query result columns follow the SELECT order; new columns no longer get stuck at the end of the grid. (#1565)</li>
<li>JSON file import works again. It failed to load in 0.48.0.</li>
<li>SQL export quotes empty or malformed numeric values instead of producing invalid INSERT statements.</li>
<li>SQL Server: logins restricted to one database, such as Azure SQL contained users, now connect; the database is sent during login.</li>
<li>Custom Copy and Cut shortcuts take effect in the SQL editor.</li>
<li>The Delete shortcut in the data grid follows a custom binding.</li>
<li>Find Next (Cmd+G) and Find Previous (Cmd+Shift+G) work in the editor.</li>
<li>Pagination buttons no longer fire their page shortcut twice.</li>
<li>PostgreSQL scripts with `DO $$ ... $$` blocks or dollar-quoted bodies no longer fail with an unterminated string error. (#1559)</li>
<li>AWS IAM connections no longer ask for a password; the same holds for any auth mode that replaces the password, such as a Postgres password file.</li>
<li>Oracle connection failures show the listener's actual reason instead of a generic message. (#483)</li>
<li>A connection password read from a command no longer fails when the command finishes quickly.</li>
<li>A cancelled MCP query returns a cancelled error instead of an invalid-parameters error, and emits an initial progress notification.</li>
</ul>
]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.49.0/TablePro-0.49.0-x86_64.zip" length="20261777" type="application/octet-stream" sparkle:edSignature="ThwSpJbt+CETx+G4yrNbr4X+1MIJMu2okBxw91FDbBNI8+6MOCWPUL582jwl+vsWLbN1RFth91YDeieFoQ0ZAg=="/>
        </item>
        <item>
            <title>0.48.0</title>
            <pubDate>Tue, 02 Jun 2026 01:48:05 +0000</pubDate>
            <sparkle:version>94</sparkle:version>
            <sparkle:shortVersionString>0.48.0</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <sparkle:hardwareRequirements>arm64</sparkle:hardwareRequirements>
            <description><![CDATA[<h3>Added</h3>
<ul>
<li>Import a JSON file into a table: an array of objects, newline-delimited JSON, or TablePro's JSON export, mapped to a new or existing table. Pick SQL or JSON from the Import menu.</li>
<li>The title bar shows the open table's name, with its database and schema below. (#1475)</li>
<li>iOS: open DuckDB database files and in-memory DuckDB databases. (#1526)</li>
<li>Save the current query as a favorite from the SQL editor toolbar.</li>
<li>Select and copy field names and types in the row Details panel.</li>
</ul>
<h3>Changed</h3>
<ul>
<li>The plugin interface is now binary-stable, so app updates that add plugin capabilities no longer force installed plugins to be rebuilt.</li>
<li>Connection list rows show the database name after the host, so look-alike connections are easier to tell apart. (#1535)</li>
<li>Save as Favorite uses Cmd+D again. The 0.47.0 Cmd+Control+D was reserved by macOS for Look Up.</li>
<li>Editor toolbar buttons show their keyboard shortcut in the tooltip, updated when you rebind it.</li>
<li>Window toolbar: connection and database selectors move left as navigation items, Refresh and Save move right. Customized toolbars reset once.</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>PostgreSQL: the selected schema stays applied after an automatic reconnect, so unqualified table names keep resolving against it. (#1540)</li>
<li>Import now finds the Setapp edition of TablePlus and reads its connections. (#1528)</li>
<li>Favorite keyword suggestions now appear in editor autocomplete. They were dropped before reaching the popup.</li>
<li>Editor autocomplete refreshes when you switch schema, suggesting the new schema's tables and columns.</li>
</ul>
]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.48.0/TablePro-0.48.0-arm64.zip" length="18888782" type="application/octet-stream" sparkle:edSignature="C7V7OS/oMXPP22+PCXE3Q5wHjqi27Of5zME6xnqrGWoLmjchH2ySi0lyqIog4KwK/Vo5kJB6qjyJRbc6CMXBCA=="/>
        </item>
        <item>
            <title>0.48.0</title>
            <pubDate>Tue, 02 Jun 2026 01:48:05 +0000</pubDate>
            <sparkle:version>94</sparkle:version>
            <sparkle:shortVersionString>0.48.0</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <description><![CDATA[<h3>Added</h3>
<ul>
<li>Import a JSON file into a table: an array of objects, newline-delimited JSON, or TablePro's JSON export, mapped to a new or existing table. Pick SQL or JSON from the Import menu.</li>
<li>The title bar shows the open table's name, with its database and schema below. (#1475)</li>
<li>iOS: open DuckDB database files and in-memory DuckDB databases. (#1526)</li>
<li>Save the current query as a favorite from the SQL editor toolbar.</li>
<li>Select and copy field names and types in the row Details panel.</li>
</ul>
<h3>Changed</h3>
<ul>
<li>The plugin interface is now binary-stable, so app updates that add plugin capabilities no longer force installed plugins to be rebuilt.</li>
<li>Connection list rows show the database name after the host, so look-alike connections are easier to tell apart. (#1535)</li>
<li>Save as Favorite uses Cmd+D again. The 0.47.0 Cmd+Control+D was reserved by macOS for Look Up.</li>
<li>Editor toolbar buttons show their keyboard shortcut in the tooltip, updated when you rebind it.</li>
<li>Window toolbar: connection and database selectors move left as navigation items, Refresh and Save move right. Customized toolbars reset once.</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>PostgreSQL: the selected schema stays applied after an automatic reconnect, so unqualified table names keep resolving against it. (#1540)</li>
<li>Import now finds the Setapp edition of TablePlus and reads its connections. (#1528)</li>
<li>Favorite keyword suggestions now appear in editor autocomplete. They were dropped before reaching the popup.</li>
<li>Editor autocomplete refreshes when you switch schema, suggesting the new schema's tables and columns.</li>
</ul>
]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.48.0/TablePro-0.48.0-x86_64.zip" length="19939827" type="application/octet-stream" sparkle:edSignature="EANStMlhNvzbz/JuC35ngk3KH/GXcNq+4q21VL9fl88LlTawa5+VRHUGznXIkrlYQgCxRwv6BF9yrmd5EKR0Dg=="/>
        </item>
        <item>
            <title>0.47.0</title>
            <pubDate>Mon, 01 Jun 2026 01:06:48 +0000</pubDate>
            <sparkle:version>93</sparkle:version>
            <sparkle:shortVersionString>0.47.0</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <sparkle:hardwareRequirements>arm64</sparkle:hardwareRequirements>
            <description><![CDATA[<h3>Added</h3>
<ul>
<li>Previous Page, Next Page, First Page, and Last Page are now in the Query menu. Previous and Next keep their Cmd+[ and Cmd+] shortcuts. (#1490)</li>
<li>Keyboard control of the sidebar: focus the filter field (Cmd+Option+F) and switch between the Tables and Favorites sidebars (Ctrl+1 and Ctrl+2). Tab or Down moves from the filter field into the list. All rebindable in Settings, Keyboard. (#1490)</li>
<li>Star a table in its sidebar row to favorite it. Favorites are scoped to the connection, database, and schema, pinned to the top of their section, listed in the Favorites tab, and synced through iCloud when Table Favorites is on.</li>
<li>A plus button in the Tables sidebar footer creates a new table or view without right-clicking. Disabled while safe mode blocks writes.</li>
<li>The sidebar can show every database on the server as an expandable tree. Switch between the flat list and the tree in the View menu (Sidebar Layout), and right-click a database or schema to set it active. Set the default for new connections in Settings, General. Applies to MySQL, MariaDB, PostgreSQL, MSSQL, ClickHouse, Redshift; SQLite, Redis, MongoDB, BigQuery keep their existing sidebar. (#139)</li>
<li>A connection can read its password from a file, environment variable, or command at connect time instead of the Keychain, so scripts can provision it without typing the password. (#1254)</li>
<li>PostgreSQL: PostGIS `geometry` and `geography` columns render as WKT with SRID instead of raw hex. (#1458)</li>
<li>Import connections from Navicat: export from Navicat (File, Export Connections), then pick the file under Import from Other App. SSH tunnel and SSL settings carry over, and saved passwords are decrypted during import. (#1485)</li>
</ul>
<h3>Changed</h3>
<ul>
<li>Save as Favorite moved from Cmd+D to Cmd+Control+D, leaving Cmd+D free for the system "Don't Save" action. Rebindable in Settings, Keyboard. (#1490)</li>
<li>The Tables sidebar footer uses native macOS styling. The schema switcher is now a borderless pull-down matching the Favorites footer, and switching schemas goes through the same path as the toolbar so filters and the active tab stay in sync.</li>
<li>The Maintenance submenu in the sidebar context menu is hidden when no operations apply or the target is read-only, instead of showing an empty disabled menu.</li>
<li>The window minimum width adjusts to the visible panes, so opening the inspector on a small window no longer pushes content off-screen.</li>
<li>Destructive queries (DROP, TRUNCATE, DELETE without WHERE) always ask for confirmation, even with Safe Mode off. (#1481)</li>
<li>Table structure changes, table creation, maintenance, column reorder, and saved data-grid edits follow the connection's Safe Mode and read-only setting. (#1481)</li>
<li>AI assistant and MCP queries follow the same Safe Mode, read-only, and authentication rules as the editor. (#1481)</li>
<li>iOS: sheet close, cancel, and confirm buttons use the native iOS 26 button roles, matching system apps like Mail. iOS 18 keeps titled buttons. (#1524)</li>
</ul>
<h3>Removed</h3>
<ul>
<li>"Create New Table…" from the sidebar right-click menu. Use the plus button in the Tables sidebar footer instead.</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>The Details pane updates when you select a row by clicking, not only with the arrow keys. (#1496)</li>
<li>Tab and Shift-Tab move focus out of the AI rules field in the connection form instead of inserting a tab. (#1490)</li>
<li>The cell inspector's Set NULL, Set DEFAULT, copy, and SQL-function actions are now in a right-click menu on each field, reachable by keyboard, Full Keyboard Access, and VoiceOver, not only on hover. (#1490)</li>
<li>VoiceOver: grid selection changes are announced even when VoiceOver was not already on, the drop and truncate dialog toggles describe their effect, and the favorite query editor is labeled. (#1490)</li>
<li>Tab moves keyboard focus between the window panes (sidebar, results, inspector). (#1490)</li>
<li>The license activation sheet focuses the key field on open, the SQL review sheet closes with Escape even while the editor has focus, and the integration token sheet focuses its Done button. (#1490)</li>
<li>Copy with Headers now has a default shortcut (Cmd+Option+C) instead of appearing in the Edit menu with no key. (#1490)</li>
<li>VoiceOver reads the column name and current value for each field editor in the cell inspector. (#1490)</li>
<li>VoiceOver announces the active tab title when you switch between window tabs. (#1490)</li>
<li>Opening a query tab no longer pulls keyboard focus from the sidebar or another control; the editor takes focus only when nothing else holds it. VoiceOver now labels the SQL editor and the Clear and Format buttons. (#1490)</li>
<li>The connection form opens with the Name field focused, Return or Down in the welcome search moves to the connection list, and focus returns to the list after a sheet closes, so you can set up a connection without the mouse. (#1490)</li>
<li>Escape dismisses search-based sheets and popovers (database switcher, quick switcher, column and connection pickers). The first Escape clears the search text; a second closes the sheet. (#1490)</li>
<li>Running `EXPLAIN` or `EXPLAIN ANALYZE` in the editor opens the plan viewer instead of squashing the plan into one truncated grid cell. (#1480)</li>
<li>Filtering the data grid keeps you on the keyboard: applying or clearing a filter returns focus to the grid, Return applies the filter, and Escape closes the filter panel. (#1490)</li>
<li>Opening a table (Return or double-click in the sidebar) moves keyboard focus into the data grid for arrow-key navigation. Arrowing the sidebar still previews tables without taking focus. (#1490)</li>
<li>Moving a connection into or out of a group syncs across devices instead of leaving it ungrouped on your other Macs.</li>
<li>Opening a table on a connection with many tables no longer stalls for several seconds while autocomplete and metadata load. Schema introspection runs on separate connections instead of blocking the query that fills the grid. (#1483)</li>
<li>Cassandra SSL connections with a client certificate now have a Key Passphrase field for an encrypted private key, and report "key is encrypted" or "passphrase is incorrect" instead of a generic handshake failure. The passphrase is stored in the Keychain. (#1487)</li>
</ul>
]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.47.0/TablePro-0.47.0-arm64.zip" length="18590030" type="application/octet-stream" sparkle:edSignature="mhpyWSQVMTDJDUdc5N4ngAWLJi3buADabCYFuUXKND64Leq+QiGJQpdguzfz90P/EWrwwbUhNubU5dcmghdqBQ=="/>
        </item>
        <item>
            <title>0.47.0</title>
            <pubDate>Mon, 01 Jun 2026 01:06:48 +0000</pubDate>
            <sparkle:version>93</sparkle:version>
            <sparkle:shortVersionString>0.47.0</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <description><![CDATA[<h3>Added</h3>
<ul>
<li>Previous Page, Next Page, First Page, and Last Page are now in the Query menu. Previous and Next keep their Cmd+[ and Cmd+] shortcuts. (#1490)</li>
<li>Keyboard control of the sidebar: focus the filter field (Cmd+Option+F) and switch between the Tables and Favorites sidebars (Ctrl+1 and Ctrl+2). Tab or Down moves from the filter field into the list. All rebindable in Settings, Keyboard. (#1490)</li>
<li>Star a table in its sidebar row to favorite it. Favorites are scoped to the connection, database, and schema, pinned to the top of their section, listed in the Favorites tab, and synced through iCloud when Table Favorites is on.</li>
<li>A plus button in the Tables sidebar footer creates a new table or view without right-clicking. Disabled while safe mode blocks writes.</li>
<li>The sidebar can show every database on the server as an expandable tree. Switch between the flat list and the tree in the View menu (Sidebar Layout), and right-click a database or schema to set it active. Set the default for new connections in Settings, General. Applies to MySQL, MariaDB, PostgreSQL, MSSQL, ClickHouse, Redshift; SQLite, Redis, MongoDB, BigQuery keep their existing sidebar. (#139)</li>
<li>A connection can read its password from a file, environment variable, or command at connect time instead of the Keychain, so scripts can provision it without typing the password. (#1254)</li>
<li>PostgreSQL: PostGIS `geometry` and `geography` columns render as WKT with SRID instead of raw hex. (#1458)</li>
<li>Import connections from Navicat: export from Navicat (File, Export Connections), then pick the file under Import from Other App. SSH tunnel and SSL settings carry over, and saved passwords are decrypted during import. (#1485)</li>
</ul>
<h3>Changed</h3>
<ul>
<li>Save as Favorite moved from Cmd+D to Cmd+Control+D, leaving Cmd+D free for the system "Don't Save" action. Rebindable in Settings, Keyboard. (#1490)</li>
<li>The Tables sidebar footer uses native macOS styling. The schema switcher is now a borderless pull-down matching the Favorites footer, and switching schemas goes through the same path as the toolbar so filters and the active tab stay in sync.</li>
<li>The Maintenance submenu in the sidebar context menu is hidden when no operations apply or the target is read-only, instead of showing an empty disabled menu.</li>
<li>The window minimum width adjusts to the visible panes, so opening the inspector on a small window no longer pushes content off-screen.</li>
<li>Destructive queries (DROP, TRUNCATE, DELETE without WHERE) always ask for confirmation, even with Safe Mode off. (#1481)</li>
<li>Table structure changes, table creation, maintenance, column reorder, and saved data-grid edits follow the connection's Safe Mode and read-only setting. (#1481)</li>
<li>AI assistant and MCP queries follow the same Safe Mode, read-only, and authentication rules as the editor. (#1481)</li>
<li>iOS: sheet close, cancel, and confirm buttons use the native iOS 26 button roles, matching system apps like Mail. iOS 18 keeps titled buttons. (#1524)</li>
</ul>
<h3>Removed</h3>
<ul>
<li>"Create New Table…" from the sidebar right-click menu. Use the plus button in the Tables sidebar footer instead.</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>The Details pane updates when you select a row by clicking, not only with the arrow keys. (#1496)</li>
<li>Tab and Shift-Tab move focus out of the AI rules field in the connection form instead of inserting a tab. (#1490)</li>
<li>The cell inspector's Set NULL, Set DEFAULT, copy, and SQL-function actions are now in a right-click menu on each field, reachable by keyboard, Full Keyboard Access, and VoiceOver, not only on hover. (#1490)</li>
<li>VoiceOver: grid selection changes are announced even when VoiceOver was not already on, the drop and truncate dialog toggles describe their effect, and the favorite query editor is labeled. (#1490)</li>
<li>Tab moves keyboard focus between the window panes (sidebar, results, inspector). (#1490)</li>
<li>The license activation sheet focuses the key field on open, the SQL review sheet closes with Escape even while the editor has focus, and the integration token sheet focuses its Done button. (#1490)</li>
<li>Copy with Headers now has a default shortcut (Cmd+Option+C) instead of appearing in the Edit menu with no key. (#1490)</li>
<li>VoiceOver reads the column name and current value for each field editor in the cell inspector. (#1490)</li>
<li>VoiceOver announces the active tab title when you switch between window tabs. (#1490)</li>
<li>Opening a query tab no longer pulls keyboard focus from the sidebar or another control; the editor takes focus only when nothing else holds it. VoiceOver now labels the SQL editor and the Clear and Format buttons. (#1490)</li>
<li>The connection form opens with the Name field focused, Return or Down in the welcome search moves to the connection list, and focus returns to the list after a sheet closes, so you can set up a connection without the mouse. (#1490)</li>
<li>Escape dismisses search-based sheets and popovers (database switcher, quick switcher, column and connection pickers). The first Escape clears the search text; a second closes the sheet. (#1490)</li>
<li>Running `EXPLAIN` or `EXPLAIN ANALYZE` in the editor opens the plan viewer instead of squashing the plan into one truncated grid cell. (#1480)</li>
<li>Filtering the data grid keeps you on the keyboard: applying or clearing a filter returns focus to the grid, Return applies the filter, and Escape closes the filter panel. (#1490)</li>
<li>Opening a table (Return or double-click in the sidebar) moves keyboard focus into the data grid for arrow-key navigation. Arrowing the sidebar still previews tables without taking focus. (#1490)</li>
<li>Moving a connection into or out of a group syncs across devices instead of leaving it ungrouped on your other Macs.</li>
<li>Opening a table on a connection with many tables no longer stalls for several seconds while autocomplete and metadata load. Schema introspection runs on separate connections instead of blocking the query that fills the grid. (#1483)</li>
<li>Cassandra SSL connections with a client certificate now have a Key Passphrase field for an encrypted private key, and report "key is encrypted" or "passphrase is incorrect" instead of a generic handshake failure. The passphrase is stored in the Keychain. (#1487)</li>
</ul>
]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.47.0/TablePro-0.47.0-x86_64.zip" length="19637958" type="application/octet-stream" sparkle:edSignature="Ok6UQocGu+VLnFxAZyOCdUILy+zXUjkCo83Z9effbAdeP3Chh794JBP0V/yRnbAc0dewhjNWXfitXiDWfe5fDQ=="/>
        </item>
        <item>
            <title>0.46.0</title>
            <pubDate>Thu, 28 May 2026 15:22:43 +0000</pubDate>
            <sparkle:version>92</sparkle:version>
            <sparkle:shortVersionString>0.46.0</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <sparkle:hardwareRequirements>arm64</sparkle:hardwareRequirements>
            <description><![CDATA[<h3>Added</h3>
<ul>
<li>Rectangular cell selection in the data grid, with Shift and Cmd modifiers to extend or add cells, and Cmd+C to copy as TSV. (#1446)</li>
<li>BigQuery datasets show as expandable nodes in the sidebar instead of one at a time behind a picker.</li>
<li>OpenCode Zen as an AI provider, with free models when no key is set. (#1400)</li>
<li>Oracle Database 11g (11.1 and 11.2) now connects. (#1425)</li>
<li>Oracle connections can use a SID instead of a service name. (#1425)</li>
<li>Cmd-click a foreign key arrow (or pick Open in New Tab from the right-click menu) to open the referenced table in a new tab. (#1421)</li>
<li>Favorite a connection from the welcome screen; starred connections appear in a Favorites section at the top of the list. (#1302)</li>
<li>Text-column cells holding JSON or PHP serialized values open in the structured viewer automatically.</li>
<li>Add and remove buttons in the table structure editor (Cmd+Shift+N to add, Cmd+Delete to remove), and a labelled add button on empty Indexes or Foreign Keys tabs. (#1319)</li>
</ul>
<h3>Changed</h3>
<ul>
<li>The query trash button clears results too, and a Clear Results item on the right-click menu clears results alone. (#1256)</li>
<li>Inserting SQL from AI Chat opens a new query tab, or fills an empty editor in place. (#1257)</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>Toolbar customizations persist after closing and reopening a session window. (#1455)</li>
<li>Pasting rows with commas in a cell keeps each value in its own column and preserves NULL vs the literal text "NULL".</li>
<li>BigQuery: switching to another table loads its data immediately instead of leaving the grid empty.</li>
<li>Custom and OpenAI-compatible AI providers work when the base URL ends in `/v1`. (#1400)</li>
<li>MongoDB: opening a collection no longer crashes on documents containing NaN or infinite numbers. (#1418)</li>
<li>Connecting after an app update waits for in-progress plugin updates; when no compatible plugin build exists yet, the message asks you to update TablePro. (#1380)</li>
<li>Failed saved connections show the Test Connection troubleshooting dialog instead of a generic alert. (#1425, #483)</li>
<li>Oracle connection errors explain the cause in plain language instead of the driver's raw message. (#483)</li>
<li>AWS IAM authentication with a named profile reads `~/.aws/config` and supports `credential_process`, so SSO, IAM Identity Center, and assume-role profiles work. (#1291)</li>
<li>Opening a table no longer runs the initial query multiple times before data arrives.</li>
<li>iOS: Safe Mode setting survives relaunch instead of reverting to Off after iCloud sync.</li>
<li>iOS: large query results no longer crash the app; the editor keeps the rows it loaded and suggests adding LIMIT.</li>
<li>iOS: Safe Mode "Confirm Writes" prompts before grid edits and inserts, matching the query editor.</li>
<li>Redshift: schema switching, table search, and contains/starts with/ends with filters now work. (#1439)</li>
<li>MCP server: turning on Require Authentication no longer hangs the first request, generates a default token if needed, and shows it once. (#1093)</li>
<li>The Generate Token sheet focuses the Token Name field on first open. (#1093)</li>
<li>Double-clicking a CSV or TSV file when TablePro is closed opens the file directly. (#1443)</li>
<li>Opening a `.sql` file names the tab after the file instead of "SQL Query". (#1220)</li>
<li>Server Dashboard shows the Slow Queries panel, with a draggable vertical split and remembered divider positions. (#1464)</li>
</ul>
]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.46.0/TablePro-0.46.0-arm64.zip" length="18357432" type="application/octet-stream" sparkle:edSignature="AwHlQCAaJvX/KJThzAKugmJnFSCdb42E9tG19QPujozubxxRV+FIuTHD5wRGO2JYHHmcO3+gCXywSHga2StBBg=="/>
        </item>
        <item>
            <title>0.46.0</title>
            <pubDate>Thu, 28 May 2026 15:22:44 +0000</pubDate>
            <sparkle:version>92</sparkle:version>
            <sparkle:shortVersionString>0.46.0</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <description><![CDATA[<h3>Added</h3>
<ul>
<li>Rectangular cell selection in the data grid, with Shift and Cmd modifiers to extend or add cells, and Cmd+C to copy as TSV. (#1446)</li>
<li>BigQuery datasets show as expandable nodes in the sidebar instead of one at a time behind a picker.</li>
<li>OpenCode Zen as an AI provider, with free models when no key is set. (#1400)</li>
<li>Oracle Database 11g (11.1 and 11.2) now connects. (#1425)</li>
<li>Oracle connections can use a SID instead of a service name. (#1425)</li>
<li>Cmd-click a foreign key arrow (or pick Open in New Tab from the right-click menu) to open the referenced table in a new tab. (#1421)</li>
<li>Favorite a connection from the welcome screen; starred connections appear in a Favorites section at the top of the list. (#1302)</li>
<li>Text-column cells holding JSON or PHP serialized values open in the structured viewer automatically.</li>
<li>Add and remove buttons in the table structure editor (Cmd+Shift+N to add, Cmd+Delete to remove), and a labelled add button on empty Indexes or Foreign Keys tabs. (#1319)</li>
</ul>
<h3>Changed</h3>
<ul>
<li>The query trash button clears results too, and a Clear Results item on the right-click menu clears results alone. (#1256)</li>
<li>Inserting SQL from AI Chat opens a new query tab, or fills an empty editor in place. (#1257)</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>Toolbar customizations persist after closing and reopening a session window. (#1455)</li>
<li>Pasting rows with commas in a cell keeps each value in its own column and preserves NULL vs the literal text "NULL".</li>
<li>BigQuery: switching to another table loads its data immediately instead of leaving the grid empty.</li>
<li>Custom and OpenAI-compatible AI providers work when the base URL ends in `/v1`. (#1400)</li>
<li>MongoDB: opening a collection no longer crashes on documents containing NaN or infinite numbers. (#1418)</li>
<li>Connecting after an app update waits for in-progress plugin updates; when no compatible plugin build exists yet, the message asks you to update TablePro. (#1380)</li>
<li>Failed saved connections show the Test Connection troubleshooting dialog instead of a generic alert. (#1425, #483)</li>
<li>Oracle connection errors explain the cause in plain language instead of the driver's raw message. (#483)</li>
<li>AWS IAM authentication with a named profile reads `~/.aws/config` and supports `credential_process`, so SSO, IAM Identity Center, and assume-role profiles work. (#1291)</li>
<li>Opening a table no longer runs the initial query multiple times before data arrives.</li>
<li>iOS: Safe Mode setting survives relaunch instead of reverting to Off after iCloud sync.</li>
<li>iOS: large query results no longer crash the app; the editor keeps the rows it loaded and suggests adding LIMIT.</li>
<li>iOS: Safe Mode "Confirm Writes" prompts before grid edits and inserts, matching the query editor.</li>
<li>Redshift: schema switching, table search, and contains/starts with/ends with filters now work. (#1439)</li>
<li>MCP server: turning on Require Authentication no longer hangs the first request, generates a default token if needed, and shows it once. (#1093)</li>
<li>The Generate Token sheet focuses the Token Name field on first open. (#1093)</li>
<li>Double-clicking a CSV or TSV file when TablePro is closed opens the file directly. (#1443)</li>
<li>Opening a `.sql` file names the tab after the file instead of "SQL Query". (#1220)</li>
<li>Server Dashboard shows the Slow Queries panel, with a draggable vertical split and remembered divider positions. (#1464)</li>
</ul>
]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.46.0/TablePro-0.46.0-x86_64.zip" length="19371195" type="application/octet-stream" sparkle:edSignature="Cs7UZTXKjJu6OVhKf7/SVx14n3v6/+x19PrZ1038Y0EZz+VMht8usSK0ZIJCL9pPXLNzWNgGZpTT5F+UGZhYCg=="/>
        </item>
        <item>
            <title>0.45.0</title>
            <pubDate>Tue, 26 May 2026 01:02:36 +0000</pubDate>
            <sparkle:version>91</sparkle:version>
            <sparkle:shortVersionString>0.45.0</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <sparkle:hardwareRequirements>arm64</sparkle:hardwareRequirements>
            <description><![CDATA[<h3>Added</h3>
<ul>
<li>Cloudflare Tunnel: connect to a database behind Cloudflare Access. TablePro starts and stops `cloudflared access tcp` per connection, the same way it manages SSH tunnels, with browser sign-in or a service token. Needs cloudflared (`brew install cloudflared`). (#1285)</li>
<li>Fill Column: right-click a column header to set one value across all loaded rows. The fill is staged like a normal edit, and one undo reverts it. Not available on primary key columns. (#1304)</li>
<li>AWS IAM authentication for PostgreSQL and MySQL on RDS and Aurora. Pick AWS IAM in the connection's Authentication field with an access key, named profile, or SSO. TablePro generates a fresh token on every connect and requires SSL. (#1291)</li>
<li>Date picker for date, datetime, timestamp, and time cells, from the chevron button. Double-clicking still edits the cell as text, and the picker keeps the value's format, fractional seconds, and timezone offset. (#1405)</li>
<li>Pagination bar for table tabs with a rows-per-page menu (5, 10, 20, 100, 500, 1,000, All rows, or custom) and First, Previous, Next, and Last buttons. (#1364)</li>
<li>Click the page indicator in the pagination bar to jump to a specific page. (#1364)</li>
<li>Pagination now appears for filtered tables with an unknown total row count, instead of showing only the first page. (#1364)</li>
<li>First Page and Last Page keyboard actions, unbound by default and assignable in Settings > Keyboard. (#1364)</li>
<li>JSON and JSONB cells display pretty-printed by default, keeping your key order and exact numbers. Viewing or reformatting no longer marks the row changed, and saving no longer reorders keys or rounds large integers.</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>Changing the editor or data grid font size in Appearance settings now applies immediately and persists across relaunch, with no orphan custom themes left behind. (#1381)</li>
<li>Installing or updating a plugin right after updating TablePro now refetches the plugin list first, so it no longer fails against a stale cached list. (#1380)</li>
<li>Pressing Esc to close the Raw SQL filter suggestions, or to clear a search field, no longer also exits fullscreen. (#1403)</li>
<li>Connecting an OAuth-capable MCP client like Claude Code with an invalid or expired token now shows a clear error instead of "Invalid OAuth error response". (#1409)</li>
</ul>
]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.45.0/TablePro-0.45.0-arm64.zip" length="18214729" type="application/octet-stream" sparkle:edSignature="5Umo2HWkq0Xl55S0hjlDPUCGBScG8ckgPDBtNOCg109frVmaruWbjq4Y4R0lnT7HrscQYC2BDUQrWm4cPzSOAQ=="/>
        </item>
        <item>
            <title>0.45.0</title>
            <pubDate>Tue, 26 May 2026 01:02:36 +0000</pubDate>
            <sparkle:version>91</sparkle:version>
            <sparkle:shortVersionString>0.45.0</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <description><![CDATA[<h3>Added</h3>
<ul>
<li>Cloudflare Tunnel: connect to a database behind Cloudflare Access. TablePro starts and stops `cloudflared access tcp` per connection, the same way it manages SSH tunnels, with browser sign-in or a service token. Needs cloudflared (`brew install cloudflared`). (#1285)</li>
<li>Fill Column: right-click a column header to set one value across all loaded rows. The fill is staged like a normal edit, and one undo reverts it. Not available on primary key columns. (#1304)</li>
<li>AWS IAM authentication for PostgreSQL and MySQL on RDS and Aurora. Pick AWS IAM in the connection's Authentication field with an access key, named profile, or SSO. TablePro generates a fresh token on every connect and requires SSL. (#1291)</li>
<li>Date picker for date, datetime, timestamp, and time cells, from the chevron button. Double-clicking still edits the cell as text, and the picker keeps the value's format, fractional seconds, and timezone offset. (#1405)</li>
<li>Pagination bar for table tabs with a rows-per-page menu (5, 10, 20, 100, 500, 1,000, All rows, or custom) and First, Previous, Next, and Last buttons. (#1364)</li>
<li>Click the page indicator in the pagination bar to jump to a specific page. (#1364)</li>
<li>Pagination now appears for filtered tables with an unknown total row count, instead of showing only the first page. (#1364)</li>
<li>First Page and Last Page keyboard actions, unbound by default and assignable in Settings > Keyboard. (#1364)</li>
<li>JSON and JSONB cells display pretty-printed by default, keeping your key order and exact numbers. Viewing or reformatting no longer marks the row changed, and saving no longer reorders keys or rounds large integers.</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>Changing the editor or data grid font size in Appearance settings now applies immediately and persists across relaunch, with no orphan custom themes left behind. (#1381)</li>
<li>Installing or updating a plugin right after updating TablePro now refetches the plugin list first, so it no longer fails against a stale cached list. (#1380)</li>
<li>Pressing Esc to close the Raw SQL filter suggestions, or to clear a search field, no longer also exits fullscreen. (#1403)</li>
<li>Connecting an OAuth-capable MCP client like Claude Code with an invalid or expired token now shows a clear error instead of "Invalid OAuth error response". (#1409)</li>
</ul>
]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.45.0/TablePro-0.45.0-x86_64.zip" length="19201117" type="application/octet-stream" sparkle:edSignature="TD7ArKWPvhDL2UC05+rWhh+Rg7R0lBWpuaPRWlFqrdKIcLf/Oj4wzrBVReBXKsxhly2IAXRABoMPH6zf6x5TAg=="/>
        </item>
        <item>
            <title>0.44.0</title>
            <pubDate>Fri, 22 May 2026 17:58:29 +0000</pubDate>
            <sparkle:version>90</sparkle:version>
            <sparkle:shortVersionString>0.44.0</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <sparkle:hardwareRequirements>arm64</sparkle:hardwareRequirements>
            <description><![CDATA[<h3>Added</h3>
<ul>
<li>Import connections and passwords from DataGrip, including SSH tunnels and SSL settings. The source app doesn't need to be running. (#1374)</li>
</ul>
<h3>Changed</h3>
<ul>
<li>Active Connections is now a searchable toolbar popover instead of a modal dialog. (#1350)</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>Connecting to a PostgreSQL-compatible engine without the pg_matviews catalog (such as db9.ai) no longer fails to load tables. (#1383)</li>
<li>Filtering a table now updates the row and page counts to match the filtered result, instead of the whole-table totals.</li>
<li>Reopening a table restores the filter you had applied, per connection. Removing or clearing a filter is remembered too, so an unfiltered table reopens with no filter. (#1347)</li>
<li>Quick switcher panel height now fits its results instead of leaving empty space below short lists. (#1349)</li>
<li>Importing connections from TablePlus brings over saved passwords again, after a recent release looked under the wrong keychain name.</li>
<li>Importing an SSH connection from TablePlus no longer fills in a fake private key path when no key was selected, and skips empty TLS certificate paths.</li>
<li>Importing from DBeaver no longer shows an unnecessary keychain permission warning, since DBeaver stores passwords in its own file.</li>
<li>Raw SQL filter now suggests columns and keywords at every position, including after AND and OR, instead of only the first. (#1346)</li>
<li>Plugins left incompatible after a TablePro update now update quietly in the background instead of showing a premature "could not be loaded" alert. You are notified only when no compatible version exists yet. (#1322)</li>
<li>A plugin you download and install by hand is no longer blocked by macOS Gatekeeper once its signature is verified. (#1322)</li>
<li>Clicking a table now replaces the active tab instead of opening a new one when you have multiple tabs open. A new tab still opens for unsaved edits, an applied filter, or sorting; double-click always opens a new tab. (#1348)</li>
</ul>
]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.44.0/TablePro-0.44.0-arm64.zip" length="18102107" type="application/octet-stream" sparkle:edSignature="eJCYVjYy3MT4186wjyS/Xp3AUAH0pS2WxgS+UHjTkjHm9OeP8LMcDSn6QYlWTm2Nq0L+Fs7A9d0wiNTED20OBw=="/>
        </item>
        <item>
            <title>0.44.0</title>
            <pubDate>Fri, 22 May 2026 17:58:29 +0000</pubDate>
            <sparkle:version>90</sparkle:version>
            <sparkle:shortVersionString>0.44.0</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <description><![CDATA[<h3>Added</h3>
<ul>
<li>Import connections and passwords from DataGrip, including SSH tunnels and SSL settings. The source app doesn't need to be running. (#1374)</li>
</ul>
<h3>Changed</h3>
<ul>
<li>Active Connections is now a searchable toolbar popover instead of a modal dialog. (#1350)</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>Connecting to a PostgreSQL-compatible engine without the pg_matviews catalog (such as db9.ai) no longer fails to load tables. (#1383)</li>
<li>Filtering a table now updates the row and page counts to match the filtered result, instead of the whole-table totals.</li>
<li>Reopening a table restores the filter you had applied, per connection. Removing or clearing a filter is remembered too, so an unfiltered table reopens with no filter. (#1347)</li>
<li>Quick switcher panel height now fits its results instead of leaving empty space below short lists. (#1349)</li>
<li>Importing connections from TablePlus brings over saved passwords again, after a recent release looked under the wrong keychain name.</li>
<li>Importing an SSH connection from TablePlus no longer fills in a fake private key path when no key was selected, and skips empty TLS certificate paths.</li>
<li>Importing from DBeaver no longer shows an unnecessary keychain permission warning, since DBeaver stores passwords in its own file.</li>
<li>Raw SQL filter now suggests columns and keywords at every position, including after AND and OR, instead of only the first. (#1346)</li>
<li>Plugins left incompatible after a TablePro update now update quietly in the background instead of showing a premature "could not be loaded" alert. You are notified only when no compatible version exists yet. (#1322)</li>
<li>A plugin you download and install by hand is no longer blocked by macOS Gatekeeper once its signature is verified. (#1322)</li>
<li>Clicking a table now replaces the active tab instead of opening a new one when you have multiple tabs open. A new tab still opens for unsaved edits, an applied filter, or sorting; double-click always opens a new tab. (#1348)</li>
</ul>
]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.44.0/TablePro-0.44.0-x86_64.zip" length="19090612" type="application/octet-stream" sparkle:edSignature="y1JdtLJQSZujIKETdWJy18d61IgblmKCEjjsvY2G7As637aDAhpEsCnpVJ4E9eYoeWbvR2EMhM0eMrnIJnIDAQ=="/>
        </item>
        <item>
            <title>0.43.3</title>
            <pubDate>Fri, 22 May 2026 08:28:56 +0000</pubDate>
            <sparkle:version>89</sparkle:version>
            <sparkle:shortVersionString>0.43.3</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <sparkle:hardwareRequirements>arm64</sparkle:hardwareRequirements>
            <description><![CDATA[<h3>Fixed</h3>
<ul>
<li>Fixed a crash when adding a row while a cell editor or value viewer was open (#1378)</li>
</ul>
]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.43.3/TablePro-0.43.3-arm64.zip" length="18067071" type="application/octet-stream" sparkle:edSignature="IUr2nMUqpRT2yaifFxRaQQfe1owlHUzbUiE0W88EG5God8FFK9wnJEEVRPHJnentpDUHQZhPZBqmiT7tDJRRBA=="/>
        </item>
        <item>
            <title>0.43.3</title>
            <pubDate>Fri, 22 May 2026 08:28:56 +0000</pubDate>
            <sparkle:version>89</sparkle:version>
            <sparkle:shortVersionString>0.43.3</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <description><![CDATA[<h3>Fixed</h3>
<ul>
<li>Fixed a crash when adding a row while a cell editor or value viewer was open (#1378)</li>
</ul>
]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.43.3/TablePro-0.43.3-x86_64.zip" length="19044179" type="application/octet-stream" sparkle:edSignature="g3Y6MKhJ/nZBj+Gb1tK1OKqOZhpAdiTfEh/adS4m+q6deSdqtDTLLhFxTpHNNt54+B0XjrpKgNKi54ruAQLsDg=="/>
        </item>
        <item>
            <title>0.43.2</title>
            <pubDate>Fri, 22 May 2026 03:54:07 +0000</pubDate>
            <sparkle:version>88</sparkle:version>
            <sparkle:shortVersionString>0.43.2</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <sparkle:hardwareRequirements>arm64</sparkle:hardwareRequirements>
            <description><![CDATA[<h3>Changed</h3>
<ul>
<li>Hiding a column now also drops it from the query, so tables with one heavy column load faster. The primary key is always fetched so editing still works</li>
</ul>
<h3>Removed</h3>
<ul>
<li>Removed the embedded database CLI terminal. Use your system terminal for command-line access to your databases.</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>Opening a connection no longer crashes when its database plugin is older than the app; outdated plugins now update automatically (#1371)</li>
<li>Safe mode no longer resets when you open another table; it stays set for the connection until you change it (#1351)</li>
<li>Reassigning the Execute Query, Execute All Statements, and Cancel Query shortcuts now takes effect and shows in the Query menu (#1357)</li>
<li>Custom shortcuts now require a modifier key, so a plain key like Space is no longer silently ignored (#1357)</li>
<li>Cancelling a pending connection no longer lets the abandoned attempt overwrite a later successful one to the same database (#1358)</li>
<li>Cancelling a pending SSH connection now closes its tunnel instead of leaving the port open (#1369)</li>
<li>Importing connections from DBeaver now brings over the username (#1355)</li>
<li>Copying rows now includes only the visible columns, in their current order (#1354)</li>
<li>The query shown in the editor when you open a table now matches the query that actually runs</li>
<li>Large text columns are no longer truncated to 256 characters when browsing a table, so editing a cell can't save a shortened value</li>
</ul>
]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.43.2/TablePro-0.43.2-arm64.zip" length="18067387" type="application/octet-stream" sparkle:edSignature="fPQsiMESe00NDoRV6IPCL2pNqFKVsuPOiL4Tzm9DHcSzz4rNHaQ/Z/k/i5ZAxKtVeNRU3z4tuCwIV2ibG3t0DQ=="/>
        </item>
        <item>
            <title>0.43.2</title>
            <pubDate>Fri, 22 May 2026 03:54:08 +0000</pubDate>
            <sparkle:version>88</sparkle:version>
            <sparkle:shortVersionString>0.43.2</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <description><![CDATA[<h3>Changed</h3>
<ul>
<li>Hiding a column now also drops it from the query, so tables with one heavy column load faster. The primary key is always fetched so editing still works</li>
</ul>
<h3>Removed</h3>
<ul>
<li>Removed the embedded database CLI terminal. Use your system terminal for command-line access to your databases.</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>Opening a connection no longer crashes when its database plugin is older than the app; outdated plugins now update automatically (#1371)</li>
<li>Safe mode no longer resets when you open another table; it stays set for the connection until you change it (#1351)</li>
<li>Reassigning the Execute Query, Execute All Statements, and Cancel Query shortcuts now takes effect and shows in the Query menu (#1357)</li>
<li>Custom shortcuts now require a modifier key, so a plain key like Space is no longer silently ignored (#1357)</li>
<li>Cancelling a pending connection no longer lets the abandoned attempt overwrite a later successful one to the same database (#1358)</li>
<li>Cancelling a pending SSH connection now closes its tunnel instead of leaving the port open (#1369)</li>
<li>Importing connections from DBeaver now brings over the username (#1355)</li>
<li>Copying rows now includes only the visible columns, in their current order (#1354)</li>
<li>The query shown in the editor when you open a table now matches the query that actually runs</li>
<li>Large text columns are no longer truncated to 256 characters when browsing a table, so editing a cell can't save a shortened value</li>
</ul>
]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.43.2/TablePro-0.43.2-x86_64.zip" length="19044053" type="application/octet-stream" sparkle:edSignature="ddiAMRY5NTNDnXhz+0ZVpZ0fTWOPVY5WxlrkWuLRek63sdQyF5JM8I1yXR6EJvSBMkKHjLiWW08wVGLXVS5uAw=="/>
        </item>
        <item>
            <title>0.43.1</title>
            <pubDate>Wed, 20 May 2026 16:44:05 +0000</pubDate>
            <sparkle:version>87</sparkle:version>
            <sparkle:shortVersionString>0.43.1</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <sparkle:hardwareRequirements>arm64</sparkle:hardwareRequirements>
            <description><![CDATA[<h3>Added</h3>
<ul>
<li>Right-click a column header to copy all its loaded values (#1325)</li>
<li>The row "Copy as" submenu adds CSV, CSV with Headers, Markdown table, and an IN clause for `WHERE id IN (...)` lookups (#1325)</li>
<li>A plugin update that arrives while a connection is open installs when you close the connection or quit, instead of being blocked</li>
<li>Settings > Plugins shows a badge with the count of rejected plugins and available updates</li>
<li>Connections whose driver plugin failed to load show a yellow warning triangle in the welcome list</li>
<li>A rejected driver plugin shows an inline banner with an Update Plugin button in the connection form</li>
</ul>
<h3>Changed</h3>
<ul>
<li>A plugin that fails to load no longer interrupts launch with an alert. The app posts a notification and lists rejected plugins in Settings > Plugins with Update Now and Remove buttons (#1322)</li>
<li>Rejected plugins auto-update from the registry in the background, retrying with backoff until they load (#1322)</li>
<li>The app installs the plugin binary built for its own version, even when the registry holds binaries for other versions (#1322)</li>
<li>Double-click or press Return on a read-only result cell to open a selectable text viewer. JSON columns open a viewer popover and BLOB columns open the hex viewer (#1336)</li>
<li>`Cmd+C` copies the focused cell when one row is selected and a cell has focus; otherwise it copies the selected row(s) as TSV. `Cmd+Shift+C` always copies row(s) as TSV. "Copy with Headers" stays in the Edit and row context menus (#1332)</li>
<li>`Cmd+F` toggles the filter panel on a table and opens Find in the SQL editor. The old `Cmd+Shift+F` filter shortcut is removed</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>Fixes the recurring "Plugin was built with PluginKit version N, but version M is required" error after app updates. Rejected plugins now recover without manual steps (#1322, #1237, #923, #912, #443)</li>
<li>DuckDB spatial `GEOMETRY` columns show as WKT instead of NULL (#1324)</li>
<li>DuckDB `HUGEINT` and `UHUGEINT` keep full precision and no longer crash on negative values</li>
<li>DuckDB streaming results respect the row cap and show `TIMESTAMPTZ`, `TIMETZ`, and `GEOMETRY` instead of NULL</li>
<li>DuckDB schema reads handle apostrophes and concurrent schema switches</li>
<li>DuckDB ENUMs in schemas other than `main` resolve correctly</li>
<li>DuckDB `DATE` and `TIMESTAMP` BC years show a leading minus</li>
<li>`.db`, `.db3`, `.s3db`, `.sl3`, and `.sqlitedb` files open from Finder (#1327)</li>
<li>DynamoDB SSO connections work with `sso-session` profiles right after `aws sso login`, with no extra AWS CLI command (#1333)</li>
</ul>
]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.43.1/TablePro-0.43.1-arm64.zip" length="21509655" type="application/octet-stream" sparkle:edSignature="2X/b17C5ockObQxRC+iomg4s56rTZtroSbFwMBCdpJAcsP4bLukDrLQknPxtxXf8mtIrgFaZEPrCQCPCua2QDA=="/>
        </item>
        <item>
            <title>0.43.1</title>
            <pubDate>Wed, 20 May 2026 16:44:06 +0000</pubDate>
            <sparkle:version>87</sparkle:version>
            <sparkle:shortVersionString>0.43.1</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <description><![CDATA[<h3>Added</h3>
<ul>
<li>Right-click a column header to copy all its loaded values (#1325)</li>
<li>The row "Copy as" submenu adds CSV, CSV with Headers, Markdown table, and an IN clause for `WHERE id IN (...)` lookups (#1325)</li>
<li>A plugin update that arrives while a connection is open installs when you close the connection or quit, instead of being blocked</li>
<li>Settings > Plugins shows a badge with the count of rejected plugins and available updates</li>
<li>Connections whose driver plugin failed to load show a yellow warning triangle in the welcome list</li>
<li>A rejected driver plugin shows an inline banner with an Update Plugin button in the connection form</li>
</ul>
<h3>Changed</h3>
<ul>
<li>A plugin that fails to load no longer interrupts launch with an alert. The app posts a notification and lists rejected plugins in Settings > Plugins with Update Now and Remove buttons (#1322)</li>
<li>Rejected plugins auto-update from the registry in the background, retrying with backoff until they load (#1322)</li>
<li>The app installs the plugin binary built for its own version, even when the registry holds binaries for other versions (#1322)</li>
<li>Double-click or press Return on a read-only result cell to open a selectable text viewer. JSON columns open a viewer popover and BLOB columns open the hex viewer (#1336)</li>
<li>`Cmd+C` copies the focused cell when one row is selected and a cell has focus; otherwise it copies the selected row(s) as TSV. `Cmd+Shift+C` always copies row(s) as TSV. "Copy with Headers" stays in the Edit and row context menus (#1332)</li>
<li>`Cmd+F` toggles the filter panel on a table and opens Find in the SQL editor. The old `Cmd+Shift+F` filter shortcut is removed</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>Fixes the recurring "Plugin was built with PluginKit version N, but version M is required" error after app updates. Rejected plugins now recover without manual steps (#1322, #1237, #923, #912, #443)</li>
<li>DuckDB spatial `GEOMETRY` columns show as WKT instead of NULL (#1324)</li>
<li>DuckDB `HUGEINT` and `UHUGEINT` keep full precision and no longer crash on negative values</li>
<li>DuckDB streaming results respect the row cap and show `TIMESTAMPTZ`, `TIMETZ`, and `GEOMETRY` instead of NULL</li>
<li>DuckDB schema reads handle apostrophes and concurrent schema switches</li>
<li>DuckDB ENUMs in schemas other than `main` resolve correctly</li>
<li>DuckDB `DATE` and `TIMESTAMP` BC years show a leading minus</li>
<li>`.db`, `.db3`, `.s3db`, `.sl3`, and `.sqlitedb` files open from Finder (#1327)</li>
<li>DynamoDB SSO connections work with `sso-session` profiles right after `aws sso login`, with no extra AWS CLI command (#1333)</li>
</ul>
]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.43.1/TablePro-0.43.1-x86_64.zip" length="22790983" type="application/octet-stream" sparkle:edSignature="6l99P9FnTLZK1no/cjLSHTnQNUbaZtwIh5+aUl/cUT3YNigvfpfvveh44htTUICqepTA749XD8cZWEgysRPVDw=="/>
        </item>
        <item>
            <title>0.43.0</title>
            <pubDate>Mon, 18 May 2026 12:08:28 +0000</pubDate>
            <sparkle:version>86</sparkle:version>
            <sparkle:shortVersionString>0.43.0</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <sparkle:hardwareRequirements>arm64</sparkle:hardwareRequirements>
            <description><![CDATA[<h3>Added</h3>
<ul>
<li>Import connections from Beekeeper Studio, including encrypted passwords and SSH bastion hosts</li>
<li>Schema picker at the bottom of the Tables sidebar to switch the active schema (#1296)</li>
<li>Inline dropdown picker when editing ENUM and SET columns across MySQL, MariaDB, PostgreSQL, ClickHouse, DuckDB, and MongoDB JSON-schema enums (#1283)</li>
<li>Filter rows show an enum dropdown for `=` and `!=` operators on enum columns (#1283)</li>
<li>CSV/TSV inspector: edit cells, filter, sort, add/remove/rename columns, undo/redo, auto-reload on external changes; large files stream from disk (#1259)</li>
<li>iOS: SQL Server connections via FreeTDS over TDS 7.4, with schema browsing, data browser, search, filter, pagination, and explicit transactions</li>
<li>iOS: Settings > Sync shows last sync time with Sync Now and Refresh from iCloud actions</li>
</ul>
<h3>Changed</h3>
<ul>
<li>Drivers populate allowed enum values directly in column metadata instead of parsing them downstream</li>
<li>PluginKit ABI bumped to version 13; all registry plugins need to be re-tagged</li>
<li>New PostgreSQL, MySQL, MariaDB, SQL Server, Redshift, and CockroachDB connections default to Preferred SSL mode, matching the native client library defaults</li>
<li>MySQL and MariaDB Preferred mode now does a 2-pass connect: tries TLS first, falls back to plaintext only on SSL handshake errors (auth and network errors are not retried)</li>
<li>SSL pane shows per-engine guidance and warns inline when the driver has no TLS fallback for Preferred mode</li>
<li>Connection failures caused by SSL handshake errors now show a structured message naming the cause and recommending an SSL Mode to switch to, with credentials redacted from the raw driver response</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>Import from other apps now detects TablePlus, Sequel Ace, and DBeaver via LaunchServices, so newly installed apps are picked up before they have been opened (#1305)</li>
<li>ClickHouse and other HTTP-based drivers can now connect to plain-HTTP servers addressed by DNS hostname (#1316)</li>
<li>Import from TablePlus now reads passwords from the keychain correctly instead of returning empty</li>
<li>Port numbers no longer render with a thousand separator under locales that use a dot as a digit separator</li>
<li>New query tab (Cmd+T) no longer jumps focus back to the previous table tab on SQLite and other file-based databases (#1313)</li>
<li>File-based databases (SQLite, DuckDB) no longer flash the sidebar table list on window focus; external changes are picked up via the file watcher instead</li>
<li>PostgreSQL connections to AWS RDS, Cloud SQL, Azure, and other hosted Postgres no longer fail with "no pg_hba.conf entry for host" (#1298)</li>
<li>Oracle SSL/TCPS settings from the SSL pane are now respected; previously every Oracle connection was plain TCP</li>
<li>Cassandra SSL settings from the SSL pane are now respected; previously every Cassandra connection was plain TCP</li>
<li>MySQL and MariaDB Preferred mode no longer fails against Cloud SQL, Azure Database, and other hosted MySQL servers that require TLS</li>
</ul>
]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.43.0/TablePro-0.43.0-arm64.zip" length="21466498" type="application/octet-stream" sparkle:edSignature="lxV17IhynOmNfircQwwVypEOnydqaeLw0uENCxBvCveXUjD+6ZH84oVMbHltGOHtLjmT3yu1YOH0A+O40ArBCQ=="/>
        </item>
        <item>
            <title>0.43.0</title>
            <pubDate>Mon, 18 May 2026 12:08:30 +0000</pubDate>
            <sparkle:version>86</sparkle:version>
            <sparkle:shortVersionString>0.43.0</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <description><![CDATA[<h3>Added</h3>
<ul>
<li>Import connections from Beekeeper Studio, including encrypted passwords and SSH bastion hosts</li>
<li>Schema picker at the bottom of the Tables sidebar to switch the active schema (#1296)</li>
<li>Inline dropdown picker when editing ENUM and SET columns across MySQL, MariaDB, PostgreSQL, ClickHouse, DuckDB, and MongoDB JSON-schema enums (#1283)</li>
<li>Filter rows show an enum dropdown for `=` and `!=` operators on enum columns (#1283)</li>
<li>CSV/TSV inspector: edit cells, filter, sort, add/remove/rename columns, undo/redo, auto-reload on external changes; large files stream from disk (#1259)</li>
<li>iOS: SQL Server connections via FreeTDS over TDS 7.4, with schema browsing, data browser, search, filter, pagination, and explicit transactions</li>
<li>iOS: Settings > Sync shows last sync time with Sync Now and Refresh from iCloud actions</li>
</ul>
<h3>Changed</h3>
<ul>
<li>Drivers populate allowed enum values directly in column metadata instead of parsing them downstream</li>
<li>PluginKit ABI bumped to version 13; all registry plugins need to be re-tagged</li>
<li>New PostgreSQL, MySQL, MariaDB, SQL Server, Redshift, and CockroachDB connections default to Preferred SSL mode, matching the native client library defaults</li>
<li>MySQL and MariaDB Preferred mode now does a 2-pass connect: tries TLS first, falls back to plaintext only on SSL handshake errors (auth and network errors are not retried)</li>
<li>SSL pane shows per-engine guidance and warns inline when the driver has no TLS fallback for Preferred mode</li>
<li>Connection failures caused by SSL handshake errors now show a structured message naming the cause and recommending an SSL Mode to switch to, with credentials redacted from the raw driver response</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>Import from other apps now detects TablePlus, Sequel Ace, and DBeaver via LaunchServices, so newly installed apps are picked up before they have been opened (#1305)</li>
<li>ClickHouse and other HTTP-based drivers can now connect to plain-HTTP servers addressed by DNS hostname (#1316)</li>
<li>Import from TablePlus now reads passwords from the keychain correctly instead of returning empty</li>
<li>Port numbers no longer render with a thousand separator under locales that use a dot as a digit separator</li>
<li>New query tab (Cmd+T) no longer jumps focus back to the previous table tab on SQLite and other file-based databases (#1313)</li>
<li>File-based databases (SQLite, DuckDB) no longer flash the sidebar table list on window focus; external changes are picked up via the file watcher instead</li>
<li>PostgreSQL connections to AWS RDS, Cloud SQL, Azure, and other hosted Postgres no longer fail with "no pg_hba.conf entry for host" (#1298)</li>
<li>Oracle SSL/TCPS settings from the SSL pane are now respected; previously every Oracle connection was plain TCP</li>
<li>Cassandra SSL settings from the SSL pane are now respected; previously every Cassandra connection was plain TCP</li>
<li>MySQL and MariaDB Preferred mode no longer fails against Cloud SQL, Azure Database, and other hosted MySQL servers that require TLS</li>
</ul>
]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.43.0/TablePro-0.43.0-x86_64.zip" length="22743912" type="application/octet-stream" sparkle:edSignature="OR4zeBY7cafy++iEZR7TUSbcc1xtc1tCNhxFTy8uca6rQQBkZyhXwNovFuRa1GUFBG5k+mz/9aDRL5K7JAEHCA=="/>
        </item>
        <item>
            <title>0.42.0</title>
            <pubDate>Sat, 16 May 2026 14:19:38 +0000</pubDate>
            <sparkle:version>85</sparkle:version>
            <sparkle:shortVersionString>0.42.0</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <sparkle:hardwareRequirements>arm64</sparkle:hardwareRequirements>
            <description><![CDATA[<h3>Added</h3>
<ul>
<li>CockroachDB support over the PostgreSQL wire protocol, with a Connection Options field for libpq routing (#1226)</li>
<li>AI Chat: OpenAI Responses API for GPT-5 and Codex with a collapsible Thinking panel, reasoning effort picker, curated model picker, strict tool schemas by default, and image input via paste or drop (HEIC/TIFF/BMP converted, EXIF/GPS stripped) (#1112)</li>
<li>AI Chat: Claude extended thinking on Opus 4.7, Sonnet 4.6, and Haiku 4.5 (#1112)</li>
<li>iOS: SQL Server connections via FreeTDS over TDS 7.4, including data browser, search, filter, and pagination with SQL Server syntax</li>
<li>iOS: Settings > Sync shows last sync time with Sync Now and Refresh from iCloud</li>
<li>Settings > Data Grid > Default row sort opens tables sorted by primary key or first column (#1284)</li>
</ul>
<h3>Changed</h3>
<ul>
<li>Database switcher is now a toolbar popover with active-row checkmark, search, Refresh, and an engine-aware New Database footer (⌘N and ⌘R bound globally)</li>
<li>New Database and Drop Database use native sheet and confirmation dialogs</li>
</ul>
<h3>Security</h3>
<ul>
<li>AI Chat: destructive operations (DROP, TRUNCATE, ALTER...DROP) always prompt for approval; Silent mode and Always Allow no longer bypass the check</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>AI Chat: Gemini tool calls no longer fail with 400, `thoughtSignature` round-trips after tool runs, and DeepSeek V4 thinking is captured across turns</li>
<li>AI Chat: GitHub Copilot tool registration accepts optional fields</li>
<li>MySQL/MariaDB: `BIT(N)` columns display as decimal numbers instead of raw bytes (#1272)</li>
<li>Structure tab: Refresh and ⌘R show external schema changes on Columns, DDL, and ClickHouse Parts (#1281)</li>
<li>Query editor: Enter and ⌘+Enter work after accepting an autocomplete suggestion, and double-click no longer closes the window (#1278)</li>
<li>Query editor autocomplete reflects external column renames after Refresh</li>
<li>ClickHouse, BigQuery, CloudflareD1, LibSQL, Etcd, and DynamoDB: long-running queries honor Settings > Query timeout instead of failing at 30 seconds (#1267)</li>
<li>MongoDB: connection form shows the Username field so auth-enabled servers stop failing with "requires authentication"</li>
<li>MongoDB: deleting a host from the multi-host editor keeps the list interactive (#1293)</li>
<li>SQL import: PostgreSQL dollar-quoted function bodies parse correctly, and statements are no longer dropped when the database is slower than the parser (#1264)</li>
<li>SQL export: views, materialized views, and foreign tables emit the matching `DROP` (#1264)</li>
<li>iOS: connections, groups, and tags survive TestFlight and App Store updates</li>
</ul>
<h3>Removed</h3>
<ul>
<li>Help > Report an Issue; the menu item opens GitHub Issues in a browser</li>
</ul>
]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.42.0/TablePro-0.42.0-arm64.zip" length="21284405" type="application/octet-stream" sparkle:edSignature="3ljJwmQYJcKg4UzVZcF38oGXLr+zxJMerkhGIc6s8dpfhLT23A5ClU9nP3Zs7THVf5PELqId8e0LfuHa9H7wDA=="/>
        </item>
        <item>
            <title>0.42.0</title>
            <pubDate>Sat, 16 May 2026 14:19:39 +0000</pubDate>
            <sparkle:version>85</sparkle:version>
            <sparkle:shortVersionString>0.42.0</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <description><![CDATA[<h3>Added</h3>
<ul>
<li>CockroachDB support over the PostgreSQL wire protocol, with a Connection Options field for libpq routing (#1226)</li>
<li>AI Chat: OpenAI Responses API for GPT-5 and Codex with a collapsible Thinking panel, reasoning effort picker, curated model picker, strict tool schemas by default, and image input via paste or drop (HEIC/TIFF/BMP converted, EXIF/GPS stripped) (#1112)</li>
<li>AI Chat: Claude extended thinking on Opus 4.7, Sonnet 4.6, and Haiku 4.5 (#1112)</li>
<li>iOS: SQL Server connections via FreeTDS over TDS 7.4, including data browser, search, filter, and pagination with SQL Server syntax</li>
<li>iOS: Settings > Sync shows last sync time with Sync Now and Refresh from iCloud</li>
<li>Settings > Data Grid > Default row sort opens tables sorted by primary key or first column (#1284)</li>
</ul>
<h3>Changed</h3>
<ul>
<li>Database switcher is now a toolbar popover with active-row checkmark, search, Refresh, and an engine-aware New Database footer (⌘N and ⌘R bound globally)</li>
<li>New Database and Drop Database use native sheet and confirmation dialogs</li>
</ul>
<h3>Security</h3>
<ul>
<li>AI Chat: destructive operations (DROP, TRUNCATE, ALTER...DROP) always prompt for approval; Silent mode and Always Allow no longer bypass the check</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>AI Chat: Gemini tool calls no longer fail with 400, `thoughtSignature` round-trips after tool runs, and DeepSeek V4 thinking is captured across turns</li>
<li>AI Chat: GitHub Copilot tool registration accepts optional fields</li>
<li>MySQL/MariaDB: `BIT(N)` columns display as decimal numbers instead of raw bytes (#1272)</li>
<li>Structure tab: Refresh and ⌘R show external schema changes on Columns, DDL, and ClickHouse Parts (#1281)</li>
<li>Query editor: Enter and ⌘+Enter work after accepting an autocomplete suggestion, and double-click no longer closes the window (#1278)</li>
<li>Query editor autocomplete reflects external column renames after Refresh</li>
<li>ClickHouse, BigQuery, CloudflareD1, LibSQL, Etcd, and DynamoDB: long-running queries honor Settings > Query timeout instead of failing at 30 seconds (#1267)</li>
<li>MongoDB: connection form shows the Username field so auth-enabled servers stop failing with "requires authentication"</li>
<li>MongoDB: deleting a host from the multi-host editor keeps the list interactive (#1293)</li>
<li>SQL import: PostgreSQL dollar-quoted function bodies parse correctly, and statements are no longer dropped when the database is slower than the parser (#1264)</li>
<li>SQL export: views, materialized views, and foreign tables emit the matching `DROP` (#1264)</li>
<li>iOS: connections, groups, and tags survive TestFlight and App Store updates</li>
</ul>
<h3>Removed</h3>
<ul>
<li>Help > Report an Issue; the menu item opens GitHub Issues in a browser</li>
</ul>
]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.42.0/TablePro-0.42.0-x86_64.zip" length="22547352" type="application/octet-stream" sparkle:edSignature="ClCgvrU+kIjweERtNj8vCNjexU1vV3tNaCiuNVrljOpZ7byfvwe+FjIkxP1a3TZX2y+/bvA8BM+C0EO2p/okAg=="/>
        </item>
        <item>
            <title>0.41.0</title>
            <pubDate>Wed, 13 May 2026 08:52:30 +0000</pubDate>
            <sparkle:version>84</sparkle:version>
            <sparkle:shortVersionString>0.41.0</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <sparkle:hardwareRequirements>arm64</sparkle:hardwareRequirements>
            <description><![CDATA[<h3>Added</h3>
<ul>
<li>File > Backup Dump… and Restore Dump… for PostgreSQL and Redshift, with live progress, cancel, and SSH tunnel reuse (#1211).</li>
</ul>
<h3>Changed</h3>
<ul>
<li>Plugin format updated. Older plugin builds no longer load; reinstall plugins from the registry after updating.</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>Redis: "Required (skip verify)" SSL mode now actually skips certificate verification, so Upstash and other untrusted-CA endpoints connect (#1247).</li>
<li>MSSQL: SSL mode setting now affects the connection. Previously every mode was silently ignored.</li>
<li>MongoDB: "Required" and "Verify CA" SSL modes connect to self-signed and untrusted-CA servers instead of failing.</li>
<li>MongoDB: connecting no longer crashes with `dispatch_sync called on queue already owned by current thread` (#1249).</li>
<li>MongoDB: TLS handshake to Atlas no longer fails with `internal error (-9838)` on macOS 26.</li>
<li>MongoDB: importing a connection URL with no database path now works for Atlas users restricted to one database.</li>
<li>MySQL: CA certificate is no longer loaded when the SSL mode skips verification, matching PostgreSQL.</li>
</ul>
]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.41.0/TablePro-0.41.0-arm64.zip" length="20938745" type="application/octet-stream" sparkle:edSignature="kN0zs3i+oBFaeSpRDbCWWWj8E5lhgBtyBOaRHy2slb7Bffe2R4UeKLtyQp9Im6G+teA0dc3ksz8aFfmulGp2CQ=="/>
        </item>
        <item>
            <title>0.41.0</title>
            <pubDate>Wed, 13 May 2026 08:52:31 +0000</pubDate>
            <sparkle:version>84</sparkle:version>
            <sparkle:shortVersionString>0.41.0</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <description><![CDATA[<h3>Added</h3>
<ul>
<li>File > Backup Dump… and Restore Dump… for PostgreSQL and Redshift, with live progress, cancel, and SSH tunnel reuse (#1211).</li>
</ul>
<h3>Changed</h3>
<ul>
<li>Plugin format updated. Older plugin builds no longer load; reinstall plugins from the registry after updating.</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>Redis: "Required (skip verify)" SSL mode now actually skips certificate verification, so Upstash and other untrusted-CA endpoints connect (#1247).</li>
<li>MSSQL: SSL mode setting now affects the connection. Previously every mode was silently ignored.</li>
<li>MongoDB: "Required" and "Verify CA" SSL modes connect to self-signed and untrusted-CA servers instead of failing.</li>
<li>MongoDB: connecting no longer crashes with `dispatch_sync called on queue already owned by current thread` (#1249).</li>
<li>MongoDB: TLS handshake to Atlas no longer fails with `internal error (-9838)` on macOS 26.</li>
<li>MongoDB: importing a connection URL with no database path now works for Atlas users restricted to one database.</li>
<li>MySQL: CA certificate is no longer loaded when the SSL mode skips verification, matching PostgreSQL.</li>
</ul>
]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.41.0/TablePro-0.41.0-x86_64.zip" length="22189374" type="application/octet-stream" sparkle:edSignature="MHYFSxTaskh8m6U8M8L6V5VME3vPsCICh647g57H5uwB0xpMdx7ZKsr4vijT9uQXHOAW5UvEKNqPx8WrM4AUCg=="/>
        </item>
        <item>
            <title>0.40.3</title>
            <pubDate>Wed, 13 May 2026 03:13:19 +0000</pubDate>
            <sparkle:version>83</sparkle:version>
            <sparkle:shortVersionString>0.40.3</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <sparkle:hardwareRequirements>arm64</sparkle:hardwareRequirements>
            <description><![CDATA[<h3>Fixed</h3>
<ul>
<li>AI Chat: scrolling stays smooth on long conversations and stream completion no longer briefly hides the chat. (#1239)</li>
<li>AI Chat: starting a new conversation no longer carries context from the previous one.</li>
<li>PostgreSQL: connecting to servers older than 9.3 no longer fails on schema load. (#1240)</li>
<li>MySQL: EXPLAIN now offers a plain variant that works on every version.</li>
<li>MSSQL: editing a view on SQL Server 2014 or earlier no longer fails with a syntax error.</li>
<li>Cassandra: connecting to a 2.x server now shows a clear unsupported-version message instead of failing on sidebar load.</li>
<li>MongoDB: connecting to servers older than 3.4 no longer fails on the database listing.</li>
<li>ClickHouse: the index sidebar no longer fails on versions older than 19.17.</li>
</ul>
]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.40.3/TablePro-0.40.3-arm64.zip" length="20864292" type="application/octet-stream" sparkle:edSignature="Kp61TugT3u1yi6niZwPlo3bAzS8bHfhcqhCBThnUAg9RWir8xW3KTwFzO0MgBrj+khYgsXpUTgE+W4MlL4G8Cw=="/>
        </item>
        <item>
            <title>0.40.3</title>
            <pubDate>Wed, 13 May 2026 03:13:19 +0000</pubDate>
            <sparkle:version>83</sparkle:version>
            <sparkle:shortVersionString>0.40.3</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <description><![CDATA[<h3>Fixed</h3>
<ul>
<li>AI Chat: scrolling stays smooth on long conversations and stream completion no longer briefly hides the chat. (#1239)</li>
<li>AI Chat: starting a new conversation no longer carries context from the previous one.</li>
<li>PostgreSQL: connecting to servers older than 9.3 no longer fails on schema load. (#1240)</li>
<li>MySQL: EXPLAIN now offers a plain variant that works on every version.</li>
<li>MSSQL: editing a view on SQL Server 2014 or earlier no longer fails with a syntax error.</li>
<li>Cassandra: connecting to a 2.x server now shows a clear unsupported-version message instead of failing on sidebar load.</li>
<li>MongoDB: connecting to servers older than 3.4 no longer fails on the database listing.</li>
<li>ClickHouse: the index sidebar no longer fails on versions older than 19.17.</li>
</ul>
]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.40.3/TablePro-0.40.3-x86_64.zip" length="22109667" type="application/octet-stream" sparkle:edSignature="mG9gGdUJKbG8QIpVQWO4BWJcyQ6HzoY60Qhmb29ziLcjT0kKg5Eu2wzd3WrAKADe8DRNBjIeKmduAvYjYBjgDw=="/>
        </item>
        <item>
            <title>0.40.2</title>
            <pubDate>Tue, 12 May 2026 13:39:58 +0000</pubDate>
            <sparkle:version>82</sparkle:version>
            <sparkle:shortVersionString>0.40.2</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <sparkle:hardwareRequirements>arm64</sparkle:hardwareRequirements>
            <description><![CDATA[<h3>Added</h3>
<ul>
<li>Right-click Set Value on date, datetime, and timestamp cells now offers `CURRENT_DATE`, `CURRENT_TIME`, `NOW()`, and `CURRENT_TIMESTAMP`, filtered by column type.</li>
<li>Welcome screen left pane gains an Import from Other App button.</li>
</ul>
<h3>Changed</h3>
<ul>
<li>Row numbers in the data grid continue across pages and the `#` column auto-sizes to fit the widest visible number.</li>
<li>Date, datetime, and timestamp cells use the standard inline text editor; the popover date picker is removed.</li>
<li>Foreign key preview popover follows the selected row as you arrow up or down.</li>
<li>The connection window shows the connecting state inline with a Cancel button.</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>Closing the connection window during a slow connect no longer leaves a stuck "Connecting…" window or a stray failure alert (#1185).</li>
<li>Cmd+Z while editing a cell now undoes typing in the editor; pressing it after dismissing the editor no longer crashes.</li>
<li>Cmd+Z right after Add Row no longer leaves a stranded editor floating over the removed row.</li>
<li>Editing a NULL cell and dismissing without typing no longer flips the value to an empty string.</li>
<li>Double-clicking another cell while editing no longer delays the new editor or drops pending changes on the previous one.</li>
<li>Double-clicking an enum, set, or boolean cell now opens the inline text editor; the chevron still opens the picker popover.</li>
<li>Chevron-accessory cells (enum, boolean, JSON, blob) no longer truncate short values that fit the full cell width.</li>
<li>DATE columns no longer render a phantom `00:00:00` time suffix.</li>
<li>Adding a new row no longer renders the row on top of the auto-opened cell editor mid-animation.</li>
</ul>
]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.40.2/TablePro-0.40.2-arm64.zip" length="20859947" type="application/octet-stream" sparkle:edSignature="WdCliaccGeJi4vP6dKQ94f98IOk4nxifqD32eLo3E//uOsBQKPFfOg2xFHmmbfQfwCGYE5jXpIvO/k5rtRlVDQ=="/>
        </item>
        <item>
            <title>0.40.2</title>
            <pubDate>Tue, 12 May 2026 13:39:59 +0000</pubDate>
            <sparkle:version>82</sparkle:version>
            <sparkle:shortVersionString>0.40.2</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <description><![CDATA[<h3>Added</h3>
<ul>
<li>Right-click Set Value on date, datetime, and timestamp cells now offers `CURRENT_DATE`, `CURRENT_TIME`, `NOW()`, and `CURRENT_TIMESTAMP`, filtered by column type.</li>
<li>Welcome screen left pane gains an Import from Other App button.</li>
</ul>
<h3>Changed</h3>
<ul>
<li>Row numbers in the data grid continue across pages and the `#` column auto-sizes to fit the widest visible number.</li>
<li>Date, datetime, and timestamp cells use the standard inline text editor; the popover date picker is removed.</li>
<li>Foreign key preview popover follows the selected row as you arrow up or down.</li>
<li>The connection window shows the connecting state inline with a Cancel button.</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>Closing the connection window during a slow connect no longer leaves a stuck "Connecting…" window or a stray failure alert (#1185).</li>
<li>Cmd+Z while editing a cell now undoes typing in the editor; pressing it after dismissing the editor no longer crashes.</li>
<li>Cmd+Z right after Add Row no longer leaves a stranded editor floating over the removed row.</li>
<li>Editing a NULL cell and dismissing without typing no longer flips the value to an empty string.</li>
<li>Double-clicking another cell while editing no longer delays the new editor or drops pending changes on the previous one.</li>
<li>Double-clicking an enum, set, or boolean cell now opens the inline text editor; the chevron still opens the picker popover.</li>
<li>Chevron-accessory cells (enum, boolean, JSON, blob) no longer truncate short values that fit the full cell width.</li>
<li>DATE columns no longer render a phantom `00:00:00` time suffix.</li>
<li>Adding a new row no longer renders the row on top of the auto-opened cell editor mid-animation.</li>
</ul>
]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.40.2/TablePro-0.40.2-x86_64.zip" length="22104518" type="application/octet-stream" sparkle:edSignature="DsPPoCITf8i5HWoD/6ifeWjDnJXqCKbOg4H+rc6hUSXHJF4ZhX37oP+73VpVZrM6GFI+gagoIrewpbMbbMH+Ag=="/>
        </item>
        <item>
            <title>0.40.1</title>
            <pubDate>Tue, 12 May 2026 06:27:15 +0000</pubDate>
            <sparkle:version>81</sparkle:version>
            <sparkle:shortVersionString>0.40.1</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <sparkle:hardwareRequirements>arm64</sparkle:hardwareRequirements>
            <description><![CDATA[<h3>Changed</h3>
<ul>
<li>Quick Switcher matches the Open Database dialog and shows a Recent section per connection.</li>
<li>Connection Switcher and SQL Preview open as sheets so they work from the toolbar, overflow menu, and keyboard shortcuts.</li>
<li>Filters button moved out of the toolbar; the bottom-bar Filters control remains.</li>
<li>Welcome screen drops the Import Connections button; both import flows remain in the + menu.</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>Toolbar overflow menu entries now fire their action when the window is narrowed.</li>
<li>SQL Preview no longer freezes when previewing a very large batch.</li>
<li>Quick Switcher crash on macOS 26.</li>
<li>Registry updates for bundled drivers (ClickHouse, Redis) now persist after restart.</li>
</ul>
]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.40.1/TablePro-0.40.1-arm64.zip" length="20849463" type="application/octet-stream" sparkle:edSignature="cGyMlou/Kad39luVXnLrIdLUiXXs+8tuKssEeXZGLxp04Bw6X1De2piaD3eE5h/8Pq96mqx6yR3L5XenJzIHBg=="/>
        </item>
        <item>
            <title>0.40.1</title>
            <pubDate>Tue, 12 May 2026 06:27:16 +0000</pubDate>
            <sparkle:version>81</sparkle:version>
            <sparkle:shortVersionString>0.40.1</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <description><![CDATA[<h3>Changed</h3>
<ul>
<li>Quick Switcher matches the Open Database dialog and shows a Recent section per connection.</li>
<li>Connection Switcher and SQL Preview open as sheets so they work from the toolbar, overflow menu, and keyboard shortcuts.</li>
<li>Filters button moved out of the toolbar; the bottom-bar Filters control remains.</li>
<li>Welcome screen drops the Import Connections button; both import flows remain in the + menu.</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>Toolbar overflow menu entries now fire their action when the window is narrowed.</li>
<li>SQL Preview no longer freezes when previewing a very large batch.</li>
<li>Quick Switcher crash on macOS 26.</li>
<li>Registry updates for bundled drivers (ClickHouse, Redis) now persist after restart.</li>
</ul>
]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.40.1/TablePro-0.40.1-x86_64.zip" length="22085709" type="application/octet-stream" sparkle:edSignature="QG1xzuQVMj2JIAA7bmCRx8Zp0HFgGrM2UtKCACTNC07gcDuZAImt2ymTO4DeEQGogRGygYoM8DWLK49Ink+MAg=="/>
        </item>
        <item>
            <title>0.40.0</title>
            <pubDate>Mon, 11 May 2026 17:38:50 +0000</pubDate>
            <sparkle:version>80</sparkle:version>
            <sparkle:shortVersionString>0.40.0</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <sparkle:hardwareRequirements>arm64</sparkle:hardwareRequirements>
            <description><![CDATA[<h3>Added</h3>
<ul>
<li>Vim mode in the SQL editor (motions, operators, text objects, registers, macros, marks, search)</li>
<li>Sidebar groups views, materialized views, foreign tables, procedures, and functions; Show DDL opens in a new tab (#1038)</li>
<li>iOS: Live Activity for running queries</li>
<li>iOS: multi-window on iPad</li>
<li>iOS: Face ID / Touch ID / Optic ID app lock with idle timeout</li>
<li>iOS: background iCloud sync</li>
<li>iOS: Connection Info tab</li>
<li>iOS: Cmd+F focuses search; search text persists across kill</li>
<li>iOS: VoiceOver delete actions; Create button on empty Groups/Tags; No Results empty state; alert when active connection is deleted mid-session</li>
<li>iOS Settings: iCloud Sync, Rows per Page, Default Safe Mode, Hide query in Live Activities</li>
<li>MCP Setup adds Zed</li>
</ul>
<h3>Changed</h3>
<ul>
<li>Wide-table scroll faster (max main-thread stall 3.5s to 1.3s); display cache 50k rows / 64 MB</li>
<li>Sidebar: white tint on selected row; per-section count removed</li>
<li>Edits in one window no longer refresh unrelated windows (favorites, history, linked folders)</li>
<li>iOS: Vietnamese localization complete</li>
<li>iOS: centered nav title dropped on connection tabs</li>
<li>iOS accessibility: combined VoiceOver row labels, icon-button labels, badge size caps</li>
<li>iOS: SQL editor uses the system keyboard input view; Edit Connection moved to the nav bar</li>
<li>PostgreSQL SQL export round-trips foreign keys and sequences cleanly (#1114)</li>
<li>SQL import parser streams large files (#1114)</li>
<li>AI inline-suggestion debounce configurable (default 500 ms)</li>
<li>Copilot LSP: 10s shutdown cap; quarantine attribute stripped</li>
<li>MCP HTTP: 30-second SSE keep-alive</li>
<li>AI providers: 5s timeout, known-model fallback, model list through Claude 4.7</li>
<li>AI Chat: per-tool access modes; duplicate slash command names rejected</li>
<li>AI Chat views: native button styles, semantic colors, 8-pt grid, a11y labels</li>
<li>Translucent surfaces honor Reduce Transparency and Increase Contrast</li>
<li>Result grid: direct-draw cells on a layer-backed view</li>
<li>Plugin contract: typed binary cells across all engines (#1188)</li>
<li>Double-click and Return on a binary cell open the hex editor</li>
<li>Plugin registry cache moved to `~/Library/Caches`</li>
<li>Plugin install off the main actor; signing reads team id at runtime</li>
<li>Restart TablePro banner gains Quit & Reopen; state in-memory only</li>
<li>Default PluginKit escape no longer escapes backslashes</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>Vim: Shift+J joins lines; `w`/`W` no longer overshoot; Esc switches mode after `;` (#1222)</li>
<li>AI Chat: Copilot Agent mode persists across turns</li>
<li>AI Chat: streaming no longer hangs at 100% CPU; pre-tool text shows above the tool card; native Markdown renderer (#1205)</li>
<li>AI Chat: `@` mentions handle emoji at the cursor</li>
<li>AI Chat: Fix Error prompt uses the database display name</li>
<li>iOS: data browser no longer flashes No Data during reload</li>
<li>iOS: row detail pager crash on filter</li>
<li>MySQL/MariaDB: `BINARY(N)`/`VARBINARY(N)` route to blob editor; numeric/date/time render as values; stable JSON detection</li>
<li>Data grid chevrons refresh on editability change and dim on deleted rows</li>
<li>Welcome window opens reliably on launch and Dock click</li>
<li>Plugin upgrades for built-in drivers persist after restart (#1192)</li>
<li>Plugin install: end-to-end install lock; no continuation leak; multi-bundle ZIP rejected; auto-update preserves concurrent rejections</li>
<li>Built-in plugins enforce the PluginKit version check</li>
<li>Query cancelled alert no longer appears on tab supersession, refresh, or teardown</li>
<li>Structure tab: tinting repaints on edit, delete, undo, save, discard; right-click Undo Delete; dropdown columns enter inline edit; Cmd+Shift+N adds a row</li>
<li>Structure view: switching Columns / Indexes / Foreign Keys no longer shows stale data (#1110)</li>
<li>Create Table: foreign-key and index-type columns render as dropdowns</li>
<li>Sidebar tables load after a slow connect</li>
<li>SQL Server: `USE <database>` switches DB; IDENTITY skipped on INSERT; default schema from `SELECT SCHEMA_NAME()`; DATETIME round-trips as ISO 8601</li>
<li>Toolbar database/schema chip correct on connect</li>
<li>Safe Mode silent: Cmd+Return no longer double-fires</li>
<li>LSP `cancelRequest` no longer leaks continuations</li>
<li>Schema provider no longer leaks via throwaway coordinator</li>
<li>Reconnect-then-disconnect no longer writes into a tearing-down coordinator</li>
<li>Sync coordinator: no overlapping tasks; `syncNow` is re-entrant-safe</li>
<li>Failed connections-file save aborts dependent steps; form stays open with error</li>
<li>iCloud sync: decode failures skip the record/category and log; cloud copy preserved</li>
<li>Keychain reads distinguish cancelled prompts, failed biometrics, and not-found</li>
<li>Terminal PTY writes retry on `EINTR`</li>
<li>MCP HTTP: structured internal-error envelope on encode failure</li>
<li>Closing the last window no longer flashes Connection lost</li>
<li>Smart-quote and dash substitution in cell editors and filter inputs</li>
<li>Connecting one window no longer fetches tables in unrelated windows</li>
<li>Foreign-key navigation from a tab with unsaved edits opens in a new tab without wiping the original</li>
<li>SQL import: tables sidebar refreshes; PostgreSQL Disable foreign key checks; identity columns and dollar-quoted blocks round-trip (#1114)</li>
<li>PostgreSQL/Redshift: schema picker no longer hides `pg*` user schemas</li>
<li>Connection-only payloads no longer create an empty `Query 1` tab</li>
<li>Import from Other App: Cancel button stops the keychain prompt loop (#1134)</li>
</ul>
]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.40.0/TablePro-0.40.0-arm64.zip" length="20837564" type="application/octet-stream" sparkle:edSignature="gA17JJkIWrSfDgnYMpCnIeGkKBMJCSCIODIlxGgnqT1NArBCaQWT9E2dwv6209KWKgA9cwQsET+XZwpkdssHDA=="/>
        </item>
        <item>
            <title>0.40.0</title>
            <pubDate>Mon, 11 May 2026 17:38:51 +0000</pubDate>
            <sparkle:version>80</sparkle:version>
            <sparkle:shortVersionString>0.40.0</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <description><![CDATA[<h3>Added</h3>
<ul>
<li>Vim mode in the SQL editor (motions, operators, text objects, registers, macros, marks, search)</li>
<li>Sidebar groups views, materialized views, foreign tables, procedures, and functions; Show DDL opens in a new tab (#1038)</li>
<li>iOS: Live Activity for running queries</li>
<li>iOS: multi-window on iPad</li>
<li>iOS: Face ID / Touch ID / Optic ID app lock with idle timeout</li>
<li>iOS: background iCloud sync</li>
<li>iOS: Connection Info tab</li>
<li>iOS: Cmd+F focuses search; search text persists across kill</li>
<li>iOS: VoiceOver delete actions; Create button on empty Groups/Tags; No Results empty state; alert when active connection is deleted mid-session</li>
<li>iOS Settings: iCloud Sync, Rows per Page, Default Safe Mode, Hide query in Live Activities</li>
<li>MCP Setup adds Zed</li>
</ul>
<h3>Changed</h3>
<ul>
<li>Wide-table scroll faster (max main-thread stall 3.5s to 1.3s); display cache 50k rows / 64 MB</li>
<li>Sidebar: white tint on selected row; per-section count removed</li>
<li>Edits in one window no longer refresh unrelated windows (favorites, history, linked folders)</li>
<li>iOS: Vietnamese localization complete</li>
<li>iOS: centered nav title dropped on connection tabs</li>
<li>iOS accessibility: combined VoiceOver row labels, icon-button labels, badge size caps</li>
<li>iOS: SQL editor uses the system keyboard input view; Edit Connection moved to the nav bar</li>
<li>PostgreSQL SQL export round-trips foreign keys and sequences cleanly (#1114)</li>
<li>SQL import parser streams large files (#1114)</li>
<li>AI inline-suggestion debounce configurable (default 500 ms)</li>
<li>Copilot LSP: 10s shutdown cap; quarantine attribute stripped</li>
<li>MCP HTTP: 30-second SSE keep-alive</li>
<li>AI providers: 5s timeout, known-model fallback, model list through Claude 4.7</li>
<li>AI Chat: per-tool access modes; duplicate slash command names rejected</li>
<li>AI Chat views: native button styles, semantic colors, 8-pt grid, a11y labels</li>
<li>Translucent surfaces honor Reduce Transparency and Increase Contrast</li>
<li>Result grid: direct-draw cells on a layer-backed view</li>
<li>Plugin contract: typed binary cells across all engines (#1188)</li>
<li>Double-click and Return on a binary cell open the hex editor</li>
<li>Plugin registry cache moved to `~/Library/Caches`</li>
<li>Plugin install off the main actor; signing reads team id at runtime</li>
<li>Restart TablePro banner gains Quit & Reopen; state in-memory only</li>
<li>Default PluginKit escape no longer escapes backslashes</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>Vim: Shift+J joins lines; `w`/`W` no longer overshoot; Esc switches mode after `;` (#1222)</li>
<li>AI Chat: Copilot Agent mode persists across turns</li>
<li>AI Chat: streaming no longer hangs at 100% CPU; pre-tool text shows above the tool card; native Markdown renderer (#1205)</li>
<li>AI Chat: `@` mentions handle emoji at the cursor</li>
<li>AI Chat: Fix Error prompt uses the database display name</li>
<li>iOS: data browser no longer flashes No Data during reload</li>
<li>iOS: row detail pager crash on filter</li>
<li>MySQL/MariaDB: `BINARY(N)`/`VARBINARY(N)` route to blob editor; numeric/date/time render as values; stable JSON detection</li>
<li>Data grid chevrons refresh on editability change and dim on deleted rows</li>
<li>Welcome window opens reliably on launch and Dock click</li>
<li>Plugin upgrades for built-in drivers persist after restart (#1192)</li>
<li>Plugin install: end-to-end install lock; no continuation leak; multi-bundle ZIP rejected; auto-update preserves concurrent rejections</li>
<li>Built-in plugins enforce the PluginKit version check</li>
<li>Query cancelled alert no longer appears on tab supersession, refresh, or teardown</li>
<li>Structure tab: tinting repaints on edit, delete, undo, save, discard; right-click Undo Delete; dropdown columns enter inline edit; Cmd+Shift+N adds a row</li>
<li>Structure view: switching Columns / Indexes / Foreign Keys no longer shows stale data (#1110)</li>
<li>Create Table: foreign-key and index-type columns render as dropdowns</li>
<li>Sidebar tables load after a slow connect</li>
<li>SQL Server: `USE <database>` switches DB; IDENTITY skipped on INSERT; default schema from `SELECT SCHEMA_NAME()`; DATETIME round-trips as ISO 8601</li>
<li>Toolbar database/schema chip correct on connect</li>
<li>Safe Mode silent: Cmd+Return no longer double-fires</li>
<li>LSP `cancelRequest` no longer leaks continuations</li>
<li>Schema provider no longer leaks via throwaway coordinator</li>
<li>Reconnect-then-disconnect no longer writes into a tearing-down coordinator</li>
<li>Sync coordinator: no overlapping tasks; `syncNow` is re-entrant-safe</li>
<li>Failed connections-file save aborts dependent steps; form stays open with error</li>
<li>iCloud sync: decode failures skip the record/category and log; cloud copy preserved</li>
<li>Keychain reads distinguish cancelled prompts, failed biometrics, and not-found</li>
<li>Terminal PTY writes retry on `EINTR`</li>
<li>MCP HTTP: structured internal-error envelope on encode failure</li>
<li>Closing the last window no longer flashes Connection lost</li>
<li>Smart-quote and dash substitution in cell editors and filter inputs</li>
<li>Connecting one window no longer fetches tables in unrelated windows</li>
<li>Foreign-key navigation from a tab with unsaved edits opens in a new tab without wiping the original</li>
<li>SQL import: tables sidebar refreshes; PostgreSQL Disable foreign key checks; identity columns and dollar-quoted blocks round-trip (#1114)</li>
<li>PostgreSQL/Redshift: schema picker no longer hides `pg*` user schemas</li>
<li>Connection-only payloads no longer create an empty `Query 1` tab</li>
<li>Import from Other App: Cancel button stops the keychain prompt loop (#1134)</li>
</ul>
]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.40.0/TablePro-0.40.0-x86_64.zip" length="22078267" type="application/octet-stream" sparkle:edSignature="m3fxx804+LGUPvBDFMio4gy1mykMRa50/aiKPHxzOvesKkNktqCOCsEkDSuJ9qu5japSmIL9FXyJNEloVNBTBQ=="/>
        </item>
        <item>
            <title>0.39.1</title>
            <pubDate>Thu, 07 May 2026 19:08:49 +0000</pubDate>
            <sparkle:version>79</sparkle:version>
            <sparkle:shortVersionString>0.39.1</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <sparkle:hardwareRequirements>arm64</sparkle:hardwareRequirements>
            <description><![CDATA[<h3>Added</h3>
<ul>
<li>AI Chat: tool calling with per-card approval, Ask / Edit / Agent modes, and 7 providers (Anthropic, OpenAI, OpenRouter, Gemini, Ollama, GitHub Copilot, custom OpenAI-compatible)</li>
<li>AI Chat: `@` mentions for Schema, Table, Current Query, Query Results, and saved queries</li>
<li>AI Chat: slash commands (`/explain`, `/optimize`, `/fix`, `/help`) plus user-defined commands</li>
<li>AI Chat: inline model picker with per-turn model attribution</li>
<li>AI Chat: per-connection rules for the assistant</li>
<li>Linked SQL Folders: two-way sync between Favorites and a folder of `.sql` files</li>
<li>Database type chooser sheet for new connections</li>
<li>Connection URL import in the database type chooser</li>
</ul>
<h3>Changed</h3>
<ul>
<li>iOS: streaming data layer for large queries</li>
<li>Toolbar shows a tinted engine icon to distinguish windows on the same database (#1044)</li>
<li>XLSX export is free</li>
<li>Safe Mode is free</li>
<li>Favorites sidebar is connection-scoped</li>
<li>Connection Form: sidebar navigation with native toolbar actions</li>
<li>"Read-Only" / "Read-Write" renamed to "Read Only" / "Read & Write"</li>
<li>ER diagram nodes scale with system text size</li>
<li>Welcome, Connection Form, and Integrations Activity use SwiftUI scenes</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>App fails to launch on 0.39.0 with errno 163 "Launchd job spawn failed" (#1104)</li>
<li>"MariaDB plugin not installed" prompt for built-in lazy drivers</li>
<li>Cmd+K Quick Switcher schema selection on SQL Server and Oracle</li>
<li>iOS: crash opening some MySQL tables</li>
<li>iOS: silent timeout on `.local` and local-network addresses</li>
<li>iOS: row list "Index out of range" crash on shrink (#1094)</li>
<li>iOS: out-of-range port crash on MySQL, PostgreSQL, Redis (#1094)</li>
<li>IME editor jump after committing words like "测试" (#1012)</li>
<li>Cmd+T tab focus flash</li>
<li>Cmd+X with no selection now cuts the line (#1075)</li>
<li>Cmd+A on a query with a trailing newline (#1075)</li>
<li>Editor window size, position, and zoom across launches</li>
<li>Personal Apple Developer team builds (#1020)</li>
<li>SSH auth-failure alerts labelled the wrong cause (#1005)</li>
<li>TOTP codes rejected across rotation boundary</li>
<li>SSH Password against keyboard-interactive-only servers (#1005)</li>
<li>SSH Password + Google Authenticator (#1005)</li>
<li>Up/Down arrow at end-of-document caret</li>
<li>Caret line-number color in the gutter</li>
<li>Cmd+Left/Right at end of a line without a trailing newline (#1007)</li>
<li>Multi-window tab persistence dropped all but one tab on relaunch</li>
<li>Filter autocomplete focus on Full Keyboard Access</li>
<li>Toolbar database name on relaunch</li>
<li>Cmd+K database switch reverted in Cmd+T and other paths (#1043)</li>
<li>AI provider Test Connection showed `unsupported URL` on draft endpoint</li>
<li>Connection Form coordinator rebuilt on every parent re-render (#1102)</li>
<li>MongoDB SRV connection strings include the port (#1101)</li>
<li>AI Chat composer: IME, scroll bar, Shift+Return (#1100)</li>
<li>AI Chat tool roundtrip limit raised 5 → 10 (#1096)</li>
<li>AI Chat per-connection rules CloudKit sync (#1098)</li>
<li>AI Chat Retry button on non-recoverable errors</li>
<li>AI Chat code blocks without a language tag</li>
<li>AI Chat Insert button focus</li>
<li>MCP errors surface readable messages (#1095)</li>
<li>Data grid column header inset</li>
<li>Toolbar connection status left inset</li>
</ul>
]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.39.1/TablePro-0.39.1-arm64.zip" length="20929675" type="application/octet-stream" sparkle:edSignature="nBKNicjeowEPD1wmUxHDPL2OlRVJpCxDlvRUj0z8OD2QwO2/YKKxoPxE+r4WCTeVkcMyD9Z3l6Ttf062uH1QAg=="/>
        </item>
        <item>
            <title>0.39.1</title>
            <pubDate>Thu, 07 May 2026 19:08:49 +0000</pubDate>
            <sparkle:version>79</sparkle:version>
            <sparkle:shortVersionString>0.39.1</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <description><![CDATA[<h3>Added</h3>
<ul>
<li>AI Chat: tool calling with per-card approval, Ask / Edit / Agent modes, and 7 providers (Anthropic, OpenAI, OpenRouter, Gemini, Ollama, GitHub Copilot, custom OpenAI-compatible)</li>
<li>AI Chat: `@` mentions for Schema, Table, Current Query, Query Results, and saved queries</li>
<li>AI Chat: slash commands (`/explain`, `/optimize`, `/fix`, `/help`) plus user-defined commands</li>
<li>AI Chat: inline model picker with per-turn model attribution</li>
<li>AI Chat: per-connection rules for the assistant</li>
<li>Linked SQL Folders: two-way sync between Favorites and a folder of `.sql` files</li>
<li>Database type chooser sheet for new connections</li>
<li>Connection URL import in the database type chooser</li>
</ul>
<h3>Changed</h3>
<ul>
<li>iOS: streaming data layer for large queries</li>
<li>Toolbar shows a tinted engine icon to distinguish windows on the same database (#1044)</li>
<li>XLSX export is free</li>
<li>Safe Mode is free</li>
<li>Favorites sidebar is connection-scoped</li>
<li>Connection Form: sidebar navigation with native toolbar actions</li>
<li>"Read-Only" / "Read-Write" renamed to "Read Only" / "Read & Write"</li>
<li>ER diagram nodes scale with system text size</li>
<li>Welcome, Connection Form, and Integrations Activity use SwiftUI scenes</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>App fails to launch on 0.39.0 with errno 163 "Launchd job spawn failed" (#1104)</li>
<li>"MariaDB plugin not installed" prompt for built-in lazy drivers</li>
<li>Cmd+K Quick Switcher schema selection on SQL Server and Oracle</li>
<li>iOS: crash opening some MySQL tables</li>
<li>iOS: silent timeout on `.local` and local-network addresses</li>
<li>iOS: row list "Index out of range" crash on shrink (#1094)</li>
<li>iOS: out-of-range port crash on MySQL, PostgreSQL, Redis (#1094)</li>
<li>IME editor jump after committing words like "测试" (#1012)</li>
<li>Cmd+T tab focus flash</li>
<li>Cmd+X with no selection now cuts the line (#1075)</li>
<li>Cmd+A on a query with a trailing newline (#1075)</li>
<li>Editor window size, position, and zoom across launches</li>
<li>Personal Apple Developer team builds (#1020)</li>
<li>SSH auth-failure alerts labelled the wrong cause (#1005)</li>
<li>TOTP codes rejected across rotation boundary</li>
<li>SSH Password against keyboard-interactive-only servers (#1005)</li>
<li>SSH Password + Google Authenticator (#1005)</li>
<li>Up/Down arrow at end-of-document caret</li>
<li>Caret line-number color in the gutter</li>
<li>Cmd+Left/Right at end of a line without a trailing newline (#1007)</li>
<li>Multi-window tab persistence dropped all but one tab on relaunch</li>
<li>Filter autocomplete focus on Full Keyboard Access</li>
<li>Toolbar database name on relaunch</li>
<li>Cmd+K database switch reverted in Cmd+T and other paths (#1043)</li>
<li>AI provider Test Connection showed `unsupported URL` on draft endpoint</li>
<li>Connection Form coordinator rebuilt on every parent re-render (#1102)</li>
<li>MongoDB SRV connection strings include the port (#1101)</li>
<li>AI Chat composer: IME, scroll bar, Shift+Return (#1100)</li>
<li>AI Chat tool roundtrip limit raised 5 → 10 (#1096)</li>
<li>AI Chat per-connection rules CloudKit sync (#1098)</li>
<li>AI Chat Retry button on non-recoverable errors</li>
<li>AI Chat code blocks without a language tag</li>
<li>AI Chat Insert button focus</li>
<li>MCP errors surface readable messages (#1095)</li>
<li>Data grid column header inset</li>
<li>Toolbar connection status left inset</li>
</ul>
]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.39.1/TablePro-0.39.1-x86_64.zip" length="22174952" type="application/octet-stream" sparkle:edSignature="aGIOUI4MrfwyaLsoVhcHnSd48kzc0cuwso0hCYXKEglHw3eTyltWLhimOMosfNnYoycrzCyHORUeP+HroZe+BQ=="/>
        </item>
        <item>
            <title>0.39.0</title>
            <pubDate>Thu, 07 May 2026 17:52:59 +0000</pubDate>
            <sparkle:version>78</sparkle:version>
            <sparkle:shortVersionString>0.39.0</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <sparkle:hardwareRequirements>arm64</sparkle:hardwareRequirements>
            <description><![CDATA[<h3>Added</h3>
<ul>
<li>AI Chat: tool calling with per-card approval, Ask / Edit / Agent modes, and 7 providers (Anthropic, OpenAI, OpenRouter, Gemini, Ollama, GitHub Copilot, custom OpenAI-compatible)</li>
<li>AI Chat: `@` mentions for Schema, Table, Current Query, Query Results, and saved queries</li>
<li>AI Chat: slash commands (`/explain`, `/optimize`, `/fix`, `/help`) plus user-defined commands</li>
<li>AI Chat: inline model picker with per-turn model attribution</li>
<li>AI Chat: per-connection rules for the assistant</li>
<li>Linked SQL Folders: two-way sync between Favorites and a folder of `.sql` files</li>
<li>Database type chooser sheet for new connections</li>
<li>Connection URL import in the database type chooser</li>
</ul>
<h3>Changed</h3>
<ul>
<li>iOS: streaming data layer for large queries</li>
<li>Toolbar shows a tinted engine icon to distinguish windows on the same database (#1044)</li>
<li>XLSX export is free</li>
<li>Safe Mode is free</li>
<li>Favorites sidebar is connection-scoped</li>
<li>Connection Form: sidebar navigation with native toolbar actions</li>
<li>"Read-Only" / "Read-Write" renamed to "Read Only" / "Read & Write"</li>
<li>ER diagram nodes scale with system text size</li>
<li>Welcome, Connection Form, and Integrations Activity use SwiftUI scenes</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>"MariaDB plugin not installed" prompt for built-in lazy drivers</li>
<li>Cmd+K Quick Switcher schema selection on SQL Server and Oracle</li>
<li>iOS: crash opening some MySQL tables</li>
<li>iOS: silent timeout on `.local` and local-network addresses</li>
<li>iOS: row list "Index out of range" crash on shrink (#1094)</li>
<li>iOS: out-of-range port crash on MySQL, PostgreSQL, Redis (#1094)</li>
<li>IME editor jump after committing words like "测试" (#1012)</li>
<li>Cmd+T tab focus flash</li>
<li>Cmd+X with no selection now cuts the line (#1075)</li>
<li>Cmd+A on a query with a trailing newline (#1075)</li>
<li>Editor window size, position, and zoom across launches</li>
<li>Personal Apple Developer team builds (#1020)</li>
<li>SSH auth-failure alerts labelled the wrong cause (#1005)</li>
<li>TOTP codes rejected across rotation boundary</li>
<li>SSH Password against keyboard-interactive-only servers (#1005)</li>
<li>SSH Password + Google Authenticator (#1005)</li>
<li>Up/Down arrow at end-of-document caret</li>
<li>Caret line-number color in the gutter</li>
<li>Cmd+Left/Right at end of a line without a trailing newline (#1007)</li>
<li>Multi-window tab persistence dropped all but one tab on relaunch</li>
<li>Filter autocomplete focus on Full Keyboard Access</li>
<li>Toolbar database name on relaunch</li>
<li>Cmd+K database switch reverted in Cmd+T and other paths (#1043)</li>
<li>AI provider Test Connection showed `unsupported URL` on draft endpoint</li>
<li>Connection Form coordinator rebuilt on every parent re-render (#1102)</li>
<li>MongoDB SRV connection strings include the port (#1101)</li>
<li>AI Chat composer: IME, scroll bar, Shift+Return (#1100)</li>
<li>AI Chat tool roundtrip limit raised 5 → 10 (#1096)</li>
<li>AI Chat per-connection rules CloudKit sync (#1098)</li>
<li>AI Chat Retry button on non-recoverable errors</li>
<li>AI Chat code blocks without a language tag</li>
<li>AI Chat Insert button focus</li>
<li>MCP errors surface readable messages (#1095)</li>
<li>Data grid column header inset</li>
<li>Toolbar connection status left inset</li>
</ul>
]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.39.0/TablePro-0.39.0-arm64.zip" length="20929966" type="application/octet-stream" sparkle:edSignature="W5+QRBlkcFfNiWAAuTkXgIbh7+T53oyedo5YaIdoQTk9tOoZx2Izs3DDiIjxXOo5OFt7cfCCIt6sR+NTu4jaCg=="/>
        </item>
        <item>
            <title>0.39.0</title>
            <pubDate>Thu, 07 May 2026 17:52:59 +0000</pubDate>
            <sparkle:version>78</sparkle:version>
            <sparkle:shortVersionString>0.39.0</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <description><![CDATA[<h3>Added</h3>
<ul>
<li>AI Chat: tool calling with per-card approval, Ask / Edit / Agent modes, and 7 providers (Anthropic, OpenAI, OpenRouter, Gemini, Ollama, GitHub Copilot, custom OpenAI-compatible)</li>
<li>AI Chat: `@` mentions for Schema, Table, Current Query, Query Results, and saved queries</li>
<li>AI Chat: slash commands (`/explain`, `/optimize`, `/fix`, `/help`) plus user-defined commands</li>
<li>AI Chat: inline model picker with per-turn model attribution</li>
<li>AI Chat: per-connection rules for the assistant</li>
<li>Linked SQL Folders: two-way sync between Favorites and a folder of `.sql` files</li>
<li>Database type chooser sheet for new connections</li>
<li>Connection URL import in the database type chooser</li>
</ul>
<h3>Changed</h3>
<ul>
<li>iOS: streaming data layer for large queries</li>
<li>Toolbar shows a tinted engine icon to distinguish windows on the same database (#1044)</li>
<li>XLSX export is free</li>
<li>Safe Mode is free</li>
<li>Favorites sidebar is connection-scoped</li>
<li>Connection Form: sidebar navigation with native toolbar actions</li>
<li>"Read-Only" / "Read-Write" renamed to "Read Only" / "Read & Write"</li>
<li>ER diagram nodes scale with system text size</li>
<li>Welcome, Connection Form, and Integrations Activity use SwiftUI scenes</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>"MariaDB plugin not installed" prompt for built-in lazy drivers</li>
<li>Cmd+K Quick Switcher schema selection on SQL Server and Oracle</li>
<li>iOS: crash opening some MySQL tables</li>
<li>iOS: silent timeout on `.local` and local-network addresses</li>
<li>iOS: row list "Index out of range" crash on shrink (#1094)</li>
<li>iOS: out-of-range port crash on MySQL, PostgreSQL, Redis (#1094)</li>
<li>IME editor jump after committing words like "测试" (#1012)</li>
<li>Cmd+T tab focus flash</li>
<li>Cmd+X with no selection now cuts the line (#1075)</li>
<li>Cmd+A on a query with a trailing newline (#1075)</li>
<li>Editor window size, position, and zoom across launches</li>
<li>Personal Apple Developer team builds (#1020)</li>
<li>SSH auth-failure alerts labelled the wrong cause (#1005)</li>
<li>TOTP codes rejected across rotation boundary</li>
<li>SSH Password against keyboard-interactive-only servers (#1005)</li>
<li>SSH Password + Google Authenticator (#1005)</li>
<li>Up/Down arrow at end-of-document caret</li>
<li>Caret line-number color in the gutter</li>
<li>Cmd+Left/Right at end of a line without a trailing newline (#1007)</li>
<li>Multi-window tab persistence dropped all but one tab on relaunch</li>
<li>Filter autocomplete focus on Full Keyboard Access</li>
<li>Toolbar database name on relaunch</li>
<li>Cmd+K database switch reverted in Cmd+T and other paths (#1043)</li>
<li>AI provider Test Connection showed `unsupported URL` on draft endpoint</li>
<li>Connection Form coordinator rebuilt on every parent re-render (#1102)</li>
<li>MongoDB SRV connection strings include the port (#1101)</li>
<li>AI Chat composer: IME, scroll bar, Shift+Return (#1100)</li>
<li>AI Chat tool roundtrip limit raised 5 → 10 (#1096)</li>
<li>AI Chat per-connection rules CloudKit sync (#1098)</li>
<li>AI Chat Retry button on non-recoverable errors</li>
<li>AI Chat code blocks without a language tag</li>
<li>AI Chat Insert button focus</li>
<li>MCP errors surface readable messages (#1095)</li>
<li>Data grid column header inset</li>
<li>Toolbar connection status left inset</li>
</ul>
]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.39.0/TablePro-0.39.0-x86_64.zip" length="22174999" type="application/octet-stream" sparkle:edSignature="Ck5JGZi6jCONtZ08+op2J/+YmGQkDOUotQbHOb8Hf2SKjYp8KSJ486zG284BPW148aT2zeqmStnXgB9WVbGdAw=="/>
        </item>
        <item>
            <title>0.38.0</title>
            <pubDate>Mon, 04 May 2026 12:24:33 +0000</pubDate>
            <sparkle:version>77</sparkle:version>
            <sparkle:shortVersionString>0.38.0</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <sparkle:hardwareRequirements>arm64</sparkle:hardwareRequirements>
            <description><![CDATA[<h3>Added</h3>
<ul>
<li>Welcome window: "Check for Updates" link next to the version number</li>
<li>Window menu: dedicated Integrations Activity window for the MCP activity log and connected clients. Sidebar, native search, filter, refresh, export. Position remembered across launches</li>
<li>Sample database (Chinook) bundled. Open from welcome screen with one click; reset via File menu</li>
<li>Connection string detection: paste a `postgres://`, `mysql://`, `redis://`, or `mongodb://` URL to auto-fill the form</li>
<li>MCP: protocol versions `2025-06-18` and `2025-11-25` (in addition to `2025-03-26`). Includes structured tool output (`structuredContent`), tool annotations (`readOnlyHint`, `destructiveHint`, etc.), `completions` capability, and streaming progress notifications via `notifications/progress`</li>
<li>MCP: pairing redirect carries `error=denied` when the user clicks Deny</li>
<li>MCP: re-pairing the same client name revokes the previous token</li>
<li>Oracle 10G password verifier auth, matching DBeaver/JDBC/sqlplus (#483)</li>
<li>Oracle Test Connection: diagnostic sheet on auth failure with copy-able info, suggested actions, and an issue link</li>
<li>Oracle connection negotiation matches python-oracledb 23ai (TTC4 boundary, TTC5 token/pipelining/sessionless, OCI3 sync, dequeue selectors, sparse vectors)</li>
<li>SSH tunnel resolves `~/.ssh/config` host aliases at connection time, with full `ssh_config(5)` semantics: glob `Host` patterns, all `Match` types, `ProxyJump`, hostname canonicalization, `Include`. Live (no app restart). Applies to primary host and jump hosts (#977)</li>
</ul>
<h3>Changed</h3>
<ul>
<li>Welcome window aligned to macOS HIG: subtle drop shadow on the app icon (no accent glow), dynamic text styles, "Sponsor" button removed, "Create connection" uses the bordered style, toolbar `+` / new-group buttons gain a hover background, native window vibrancy via `NSVisualEffectView`</li>
<li>Settings > Integrations is a flat preferences pane per macOS HIG. Activity log and connected-clients moved to the new Integrations Activity window; setup snippets to a "Connect a Client…" sheet</li>
<li>MCP: idle session timeout 5 → 15 minutes</li>
<li>MCP: server, stdio bridge, and protocol dispatcher rewritten for spec compliance. Public API of `MCPServerManager` and the on-disk handshake format unchanged; clients do not need to re-pair</li>
<li>Security: non-syncing keychain items use `kSecAttrAccessibleAfterFirstUnlockThisDeviceOnly`. Keeps local-only secrets out of unencrypted device backups. Existing items keep their accessibility class until you re-save</li>
<li>Settings > Sync > Passwords: caption clarifies the toggle only affects new saves</li>
</ul>
<h3>Removed</h3>
<ul>
<li>SSH `useSSHConfig` per-connection toggle. `~/.ssh/config` is always consulted now; explicit form values still take precedence</li>
<li>Legacy-keychain migration and password-sync-state migration. Both violated Apple's Data Protection keychain contract on sandboxed macOS and corrupted credentials. Stale items in the legacy keychain can be cleaned via Keychain Access</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>Welcome / connection form / feedback panel now remember position and size across launches (frame autosave was missing on the underlying `NSWindow`/`NSPanel`)</li>
<li>Saved connection passwords no longer disappear after relaunch. The legacy-keychain migration was deleting the only copy on sandboxed macOS; removed entirely</li>
<li>Cmd+Z after editing a cell now clears the yellow "modified" highlight (the coordinator's `dataTabDelegate` was being nilled too eagerly)</li>
<li>Tab switching: rapid Cmd+Number no longer leaves a tail of tab transitions after key release. AppKit switches now apply synchronously via `NSAnimationContext` with `duration = 0`</li>
<li>Oracle: TIMESTAMP variants, INTERVAL DAY TO SECOND, INTERVAL YEAR TO MONTH, DATE, RAW, and BLOB render through typed decoders. INTERVAL YEAR TO MONTH and BFILE no longer crash on row fetch. Unknown types show `<unsupported: type>` instead of crashing (#965)</li>
<li>Oracle: 23ai cloud and container handshakes no longer fail with `uncleanShutdown`. OOB urgent-byte send now requires `TNS_ACCEPT_FLAG_CHECK_OOB` advertisement (#483)</li>
<li>Plugin install prompt reopens when connecting to a downloadable database type whose plugin is disabled or uninstalled (#975)</li>
<li>Redshift: schema switcher no longer empty for non-admin users. Reads from `pg_namespace` filtered by `has_schema_privilege` instead of `information_schema.schemata` (#971)</li>
<li>MCP: GET `/mcp` opens a real SSE notification stream</li>
<li>MCP: concurrent tool calls no longer serialize at the dispatcher loop</li>
<li>MCP: server validates `protocolVersion` and `MCP-Protocol-Version`; rejects unknown versions with `-32600 invalid_request`</li>
<li>MCP: 429 responses include a real `Retry-After` header from the rate-limiter lockout</li>
<li>MCP: token revocation cancels in-flight requests and terminates sessions</li>
<li>MCP: CORS reflects the request `Origin` against an allowlist (`localhost`, `127.0.0.1`, `claude.ai`, `app.cursor.com`)</li>
<li>MCP: stale `Mcp-Session-Id` after idle timeout returns JSON-RPC `-32001 "Session not found"` with HTTP 404, letting clients re-initialize cleanly instead of hanging until a 4-minute client timeout</li>
<li>MCP: stdio bridge uses `FileHandle.bytes` AsyncBytes (no more silent exit on briefly empty stdin)</li>
<li>MCP: SSE responses stream incrementally instead of buffering</li>
<li>MCP: rate limiter keys on `(client_address, principal_fingerprint)` to close localhost auth-DoS</li>
<li>MCP: in-app setup snippets use the stdio command form for `tablepro-mcp` (Claude Desktop rejected the URL form)</li>
<li>MCP: duplicate `initialize` returns `invalid_request` instead of overwriting `clientInfo`</li>
<li>MCP: `xcodebuild test` no longer leaves an orphan `TablePro.app` running</li>
<li>MCP: server start cleans stale handshake file from a crashed previous PID</li>
<li>MCP: activity log auto-refreshes when new audit entries are written</li>
</ul>
]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.38.0/TablePro-0.38.0-arm64.zip" length="20506287" type="application/octet-stream" sparkle:edSignature="byglgocg/PGA0o4pZ13mWsGCqpkItAL+v84oJiOdDzU26qHlbQq/qflETQfYujrCO9eQXbwbw0iiZbjxXFpXAQ=="/>
        </item>
        <item>
            <title>0.38.0</title>
            <pubDate>Mon, 04 May 2026 12:24:34 +0000</pubDate>
            <sparkle:version>77</sparkle:version>
            <sparkle:shortVersionString>0.38.0</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <description><![CDATA[<h3>Added</h3>
<ul>
<li>Welcome window: "Check for Updates" link next to the version number</li>
<li>Window menu: dedicated Integrations Activity window for the MCP activity log and connected clients. Sidebar, native search, filter, refresh, export. Position remembered across launches</li>
<li>Sample database (Chinook) bundled. Open from welcome screen with one click; reset via File menu</li>
<li>Connection string detection: paste a `postgres://`, `mysql://`, `redis://`, or `mongodb://` URL to auto-fill the form</li>
<li>MCP: protocol versions `2025-06-18` and `2025-11-25` (in addition to `2025-03-26`). Includes structured tool output (`structuredContent`), tool annotations (`readOnlyHint`, `destructiveHint`, etc.), `completions` capability, and streaming progress notifications via `notifications/progress`</li>
<li>MCP: pairing redirect carries `error=denied` when the user clicks Deny</li>
<li>MCP: re-pairing the same client name revokes the previous token</li>
<li>Oracle 10G password verifier auth, matching DBeaver/JDBC/sqlplus (#483)</li>
<li>Oracle Test Connection: diagnostic sheet on auth failure with copy-able info, suggested actions, and an issue link</li>
<li>Oracle connection negotiation matches python-oracledb 23ai (TTC4 boundary, TTC5 token/pipelining/sessionless, OCI3 sync, dequeue selectors, sparse vectors)</li>
<li>SSH tunnel resolves `~/.ssh/config` host aliases at connection time, with full `ssh_config(5)` semantics: glob `Host` patterns, all `Match` types, `ProxyJump`, hostname canonicalization, `Include`. Live (no app restart). Applies to primary host and jump hosts (#977)</li>
</ul>
<h3>Changed</h3>
<ul>
<li>Welcome window aligned to macOS HIG: subtle drop shadow on the app icon (no accent glow), dynamic text styles, "Sponsor" button removed, "Create connection" uses the bordered style, toolbar `+` / new-group buttons gain a hover background, native window vibrancy via `NSVisualEffectView`</li>
<li>Settings > Integrations is a flat preferences pane per macOS HIG. Activity log and connected-clients moved to the new Integrations Activity window; setup snippets to a "Connect a Client…" sheet</li>
<li>MCP: idle session timeout 5 → 15 minutes</li>
<li>MCP: server, stdio bridge, and protocol dispatcher rewritten for spec compliance. Public API of `MCPServerManager` and the on-disk handshake format unchanged; clients do not need to re-pair</li>
<li>Security: non-syncing keychain items use `kSecAttrAccessibleAfterFirstUnlockThisDeviceOnly`. Keeps local-only secrets out of unencrypted device backups. Existing items keep their accessibility class until you re-save</li>
<li>Settings > Sync > Passwords: caption clarifies the toggle only affects new saves</li>
</ul>
<h3>Removed</h3>
<ul>
<li>SSH `useSSHConfig` per-connection toggle. `~/.ssh/config` is always consulted now; explicit form values still take precedence</li>
<li>Legacy-keychain migration and password-sync-state migration. Both violated Apple's Data Protection keychain contract on sandboxed macOS and corrupted credentials. Stale items in the legacy keychain can be cleaned via Keychain Access</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>Welcome / connection form / feedback panel now remember position and size across launches (frame autosave was missing on the underlying `NSWindow`/`NSPanel`)</li>
<li>Saved connection passwords no longer disappear after relaunch. The legacy-keychain migration was deleting the only copy on sandboxed macOS; removed entirely</li>
<li>Cmd+Z after editing a cell now clears the yellow "modified" highlight (the coordinator's `dataTabDelegate` was being nilled too eagerly)</li>
<li>Tab switching: rapid Cmd+Number no longer leaves a tail of tab transitions after key release. AppKit switches now apply synchronously via `NSAnimationContext` with `duration = 0`</li>
<li>Oracle: TIMESTAMP variants, INTERVAL DAY TO SECOND, INTERVAL YEAR TO MONTH, DATE, RAW, and BLOB render through typed decoders. INTERVAL YEAR TO MONTH and BFILE no longer crash on row fetch. Unknown types show `<unsupported: type>` instead of crashing (#965)</li>
<li>Oracle: 23ai cloud and container handshakes no longer fail with `uncleanShutdown`. OOB urgent-byte send now requires `TNS_ACCEPT_FLAG_CHECK_OOB` advertisement (#483)</li>
<li>Plugin install prompt reopens when connecting to a downloadable database type whose plugin is disabled or uninstalled (#975)</li>
<li>Redshift: schema switcher no longer empty for non-admin users. Reads from `pg_namespace` filtered by `has_schema_privilege` instead of `information_schema.schemata` (#971)</li>
<li>MCP: GET `/mcp` opens a real SSE notification stream</li>
<li>MCP: concurrent tool calls no longer serialize at the dispatcher loop</li>
<li>MCP: server validates `protocolVersion` and `MCP-Protocol-Version`; rejects unknown versions with `-32600 invalid_request`</li>
<li>MCP: 429 responses include a real `Retry-After` header from the rate-limiter lockout</li>
<li>MCP: token revocation cancels in-flight requests and terminates sessions</li>
<li>MCP: CORS reflects the request `Origin` against an allowlist (`localhost`, `127.0.0.1`, `claude.ai`, `app.cursor.com`)</li>
<li>MCP: stale `Mcp-Session-Id` after idle timeout returns JSON-RPC `-32001 "Session not found"` with HTTP 404, letting clients re-initialize cleanly instead of hanging until a 4-minute client timeout</li>
<li>MCP: stdio bridge uses `FileHandle.bytes` AsyncBytes (no more silent exit on briefly empty stdin)</li>
<li>MCP: SSE responses stream incrementally instead of buffering</li>
<li>MCP: rate limiter keys on `(client_address, principal_fingerprint)` to close localhost auth-DoS</li>
<li>MCP: in-app setup snippets use the stdio command form for `tablepro-mcp` (Claude Desktop rejected the URL form)</li>
<li>MCP: duplicate `initialize` returns `invalid_request` instead of overwriting `clientInfo`</li>
<li>MCP: `xcodebuild test` no longer leaves an orphan `TablePro.app` running</li>
<li>MCP: server start cleans stale handshake file from a crashed previous PID</li>
<li>MCP: activity log auto-refreshes when new audit entries are written</li>
</ul>
]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.38.0/TablePro-0.38.0-x86_64.zip" length="21704979" type="application/octet-stream" sparkle:edSignature="NnYMUtR9fjSDRyorl9ERRqQO/aSayTvJpb+VpVm+2kqOvuh2+eUx1e7Cx34/36OBAtU+411B+C4ZW+6ErtuwCQ=="/>
        </item>
        <item>
            <title>0.37.0</title>
            <pubDate>Fri, 01 May 2026 16:10:07 +0000</pubDate>
            <sparkle:version>76</sparkle:version>
            <sparkle:shortVersionString>0.37.0</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <sparkle:hardwareRequirements>arm64</sparkle:hardwareRequirements>
            <description><![CDATA[<h3>Added</h3>
<ul>
<li>External API for Raycast, Cursor, Claude Desktop, and other MCP clients. New Integrations panel with token-based pairing (PKCE), per-connection access control, and a 90-day activity log</li>
<li>New MCP tools: <code>list_recent_tabs</code>, <code>search_query_history</code>, <code>open_connection_window</code>, <code>open_table_tab</code>, <code>focus_query_tab</code></li>
<li>Per-connection External Access setting (<code>blocked</code> / <code>readOnly</code> / <code>readWrite</code>); effective scope is the minimum of token scope and connection level</li>
<li>PostgreSQL ICU collation provider in Create Database (PG 15+)</li>
<li>Connection URL parsing supports SSH <code>user:password@host</code>, multi-host, MongoDB auth params, and Redis database index</li>
<li>SSH Private Key auth auto-resolves keys from <code>~/.ssh/config</code> and default locations (<code>id_ed25519</code>, <code>id_rsa</code>, <code>id_ecdsa</code>)</li>
<li>Single-click cell editing in the data grid (no more double-click)</li>
<li>Multi-cell paste from TSV clipboard data, grouped as one undo</li>
<li>Shift+Tab navigates to the previous cell</li>
<li>Copy rows in TSV, HTML table, and plain text for richer paste in spreadsheet apps</li>
<li>AI provider settings allow manually entering a model name when the provider does not return one</li>
<li>VoiceOver: column headers announce sort direction and priority; cells expose row and column index ranges</li>
</ul>
<h3>Changed</h3>
<ul>
<li>Result safety cap is enforced after the query runs, not by rewriting your SQL. When a result is capped, the status bar shows "Showing N rows (truncated)" with a Fetch All button. Load More on user-query tabs is removed; table-tab pagination is unchanged</li>
<li>MCP server lazy-starts on first external request; manual enable is gone</li>
<li>Settings tab renamed from "MCP" to "Integrations" with sections for connected clients, activity log, and pairing</li>
<li>Activity log gained an Export button that writes the current filtered list to CSV</li>
<li>Connection Advanced settings: AI Policy and External Clients share a single External Access section with a segmented control</li>
<li>Create Database is driver-driven; engines without creation support hide the Create button instead of failing on click</li>
<li>Data grid: persistent column reuse pool, SF Symbol sort indicators that respect light and dark mode, header divider taps trigger resize instead of sort, focus ring follows system accent</li>
<li>Data grid undo/redo uses the window's UndoManager, unifying Cmd+Z across editor and grid</li>
<li>Right-click during cell editing shows the native text context menu instead of the row menu</li>
<li>OpenSSL shared as dylib across app and plugins, reducing bundle size by ~15MB</li>
</ul>
<h3>Removed (BREAKING)</h3>
<ul>
<li>Old name-based deep links (<code>tablepro://connect/{name}/...</code>) are gone. Use UUID-keyed paths from "Copy Connection Deep Link" in the sidebar context menu; saved bookmarks must be regenerated</li>
<li>MCP server data directory moved from <code>~/Library/Application Support/com.TablePro/</code> to <code>~/Library/Application Support/TablePro/</code>. Re-pair external clients after upgrading. Delete the old directory with <code>rm -rf ~/Library/Application\ Support/com.TablePro</code></li>
<li>Separately distributed plugins (Oracle, DuckDB, MSSQL, MongoDB, BigQuery, LibSQL, Cassandra, Etcd, Cloudflare D1, DynamoDB) require update before use. PluginKit ABI bumped to 9</li>
<li>Settings renamed: <code>enforceQueryResultLimit</code> is now <code>truncateQueryResults</code>, <code>queryResultLimit</code> is now <code>queryResultRowCap</code>. Custom values revert to defaults on first launch</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>SELECT queries with a user-written LIMIT now return the requested row count. The query engine no longer strips your LIMIT and substitutes its own cap, so <code>LIMIT 10</code> returns 10 rows. Affected SQLite, DuckDB, LibSQL, ClickHouse, Redshift, Cloudflare D1, and the MCP query path. MSSQL and Oracle no longer silently inject <code>ORDER BY 1</code> either (#956)</li>
<li>Crash on macOS 26 when opening SQL Preview</li>
<li>File associations for <code>.sql</code>, <code>.sqlite</code>, <code>.duckdb</code> now appear in Finder's Open With menu</li>
<li>New tab from the empty state replaces the placeholder instead of opening a side-by-side tab</li>
<li>PostgreSQL Create Database collation errors on glibc-initialized servers (#927)</li>
<li>Redshift Create Database emits valid <code>COLLATE { CASE_SENSITIVE | CASE_INSENSITIVE }</code> instead of PostgreSQL <code>LC_COLLATE</code> syntax</li>
<li>SSH agent and <code>IdentityAgent</code> socket paths now expand <code>~</code> so 1Password and similar agents work</li>
<li>Connection form <code>usePrivateKey=true</code> from URL no longer disables Test and Create buttons</li>
<li>Transient connections from URL clean up keychain entries on connection failure</li>
<li>Native Search Field focus regression when clearing text</li>
<li>Group and connection deletions persist before firing the sync notification, fixing a race that could re-upload deleted records to iCloud</li>
<li>MCP <code>execute_query</code>: trailing semicolons no longer break appended LIMIT/OFFSET</li>
<li>Pairing approval: 5-minute countdown timer, searchable connection list, can no longer grant via Return key, requires explicit Approve click</li>
<li>Token deletion and client disconnect now require confirmation</li>
<li>Activity log: searchable across action, token, connection, and details; connection name shown instead of UUID prefix; single scroll owner</li>
<li>Token, audit, and pairing sheets respect Dynamic Type and dark mode; warning banner stays visible in dark mode</li>
<li>Token list switched to a native list with keyboard navigation, multi-select, and a context menu (Revoke, Copy ID, Delete)</li>
<li>"Last used" timestamps use RelativeDateTimeFormatter for correct localization</li>
<li>Refuse to generate SQL when the database dialect cannot be resolved, instead of silently emitting unquoted identifiers</li>
</ul>]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.37.0/TablePro-0.37.0-arm64.zip" length="19727545" type="application/octet-stream" sparkle:edSignature="j/gGEPpZMLQZhARtinQxAVwZzR2hgZ+6n65DWXv4O323IerB673WNWXbuwdbb9sDTz6WiT1nk5y7LJqLKU0mAQ=="/>
        </item>
        <item>
            <title>0.37.0</title>
            <pubDate>Fri, 01 May 2026 16:10:08 +0000</pubDate>
            <sparkle:version>76</sparkle:version>
            <sparkle:shortVersionString>0.37.0</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <description><![CDATA[<h3>Added</h3>
<ul>
<li>External API for Raycast, Cursor, Claude Desktop, and other MCP clients. New Integrations panel with token-based pairing (PKCE), per-connection access control, and a 90-day activity log</li>
<li>New MCP tools: <code>list_recent_tabs</code>, <code>search_query_history</code>, <code>open_connection_window</code>, <code>open_table_tab</code>, <code>focus_query_tab</code></li>
<li>Per-connection External Access setting (<code>blocked</code> / <code>readOnly</code> / <code>readWrite</code>); effective scope is the minimum of token scope and connection level</li>
<li>PostgreSQL ICU collation provider in Create Database (PG 15+)</li>
<li>Connection URL parsing supports SSH <code>user:password@host</code>, multi-host, MongoDB auth params, and Redis database index</li>
<li>SSH Private Key auth auto-resolves keys from <code>~/.ssh/config</code> and default locations (<code>id_ed25519</code>, <code>id_rsa</code>, <code>id_ecdsa</code>)</li>
<li>Single-click cell editing in the data grid (no more double-click)</li>
<li>Multi-cell paste from TSV clipboard data, grouped as one undo</li>
<li>Shift+Tab navigates to the previous cell</li>
<li>Copy rows in TSV, HTML table, and plain text for richer paste in spreadsheet apps</li>
<li>AI provider settings allow manually entering a model name when the provider does not return one</li>
<li>VoiceOver: column headers announce sort direction and priority; cells expose row and column index ranges</li>
</ul>
<h3>Changed</h3>
<ul>
<li>Result safety cap is enforced after the query runs, not by rewriting your SQL. When a result is capped, the status bar shows "Showing N rows (truncated)" with a Fetch All button. Load More on user-query tabs is removed; table-tab pagination is unchanged</li>
<li>MCP server lazy-starts on first external request; manual enable is gone</li>
<li>Settings tab renamed from "MCP" to "Integrations" with sections for connected clients, activity log, and pairing</li>
<li>Activity log gained an Export button that writes the current filtered list to CSV</li>
<li>Connection Advanced settings: AI Policy and External Clients share a single External Access section with a segmented control</li>
<li>Create Database is driver-driven; engines without creation support hide the Create button instead of failing on click</li>
<li>Data grid: persistent column reuse pool, SF Symbol sort indicators that respect light and dark mode, header divider taps trigger resize instead of sort, focus ring follows system accent</li>
<li>Data grid undo/redo uses the window's UndoManager, unifying Cmd+Z across editor and grid</li>
<li>Right-click during cell editing shows the native text context menu instead of the row menu</li>
<li>OpenSSL shared as dylib across app and plugins, reducing bundle size by ~15MB</li>
</ul>
<h3>Removed (BREAKING)</h3>
<ul>
<li>Old name-based deep links (<code>tablepro://connect/{name}/...</code>) are gone. Use UUID-keyed paths from "Copy Connection Deep Link" in the sidebar context menu; saved bookmarks must be regenerated</li>
<li>MCP server data directory moved from <code>~/Library/Application Support/com.TablePro/</code> to <code>~/Library/Application Support/TablePro/</code>. Re-pair external clients after upgrading. Delete the old directory with <code>rm -rf ~/Library/Application\ Support/com.TablePro</code></li>
<li>Separately distributed plugins (Oracle, DuckDB, MSSQL, MongoDB, BigQuery, LibSQL, Cassandra, Etcd, Cloudflare D1, DynamoDB) require update before use. PluginKit ABI bumped to 9</li>
<li>Settings renamed: <code>enforceQueryResultLimit</code> is now <code>truncateQueryResults</code>, <code>queryResultLimit</code> is now <code>queryResultRowCap</code>. Custom values revert to defaults on first launch</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>SELECT queries with a user-written LIMIT now return the requested row count. The query engine no longer strips your LIMIT and substitutes its own cap, so <code>LIMIT 10</code> returns 10 rows. Affected SQLite, DuckDB, LibSQL, ClickHouse, Redshift, Cloudflare D1, and the MCP query path. MSSQL and Oracle no longer silently inject <code>ORDER BY 1</code> either (#956)</li>
<li>Crash on macOS 26 when opening SQL Preview</li>
<li>File associations for <code>.sql</code>, <code>.sqlite</code>, <code>.duckdb</code> now appear in Finder's Open With menu</li>
<li>New tab from the empty state replaces the placeholder instead of opening a side-by-side tab</li>
<li>PostgreSQL Create Database collation errors on glibc-initialized servers (#927)</li>
<li>Redshift Create Database emits valid <code>COLLATE { CASE_SENSITIVE | CASE_INSENSITIVE }</code> instead of PostgreSQL <code>LC_COLLATE</code> syntax</li>
<li>SSH agent and <code>IdentityAgent</code> socket paths now expand <code>~</code> so 1Password and similar agents work</li>
<li>Connection form <code>usePrivateKey=true</code> from URL no longer disables Test and Create buttons</li>
<li>Transient connections from URL clean up keychain entries on connection failure</li>
<li>Native Search Field focus regression when clearing text</li>
<li>Group and connection deletions persist before firing the sync notification, fixing a race that could re-upload deleted records to iCloud</li>
<li>MCP <code>execute_query</code>: trailing semicolons no longer break appended LIMIT/OFFSET</li>
<li>Pairing approval: 5-minute countdown timer, searchable connection list, can no longer grant via Return key, requires explicit Approve click</li>
<li>Token deletion and client disconnect now require confirmation</li>
<li>Activity log: searchable across action, token, connection, and details; connection name shown instead of UUID prefix; single scroll owner</li>
<li>Token, audit, and pairing sheets respect Dynamic Type and dark mode; warning banner stays visible in dark mode</li>
<li>Token list switched to a native list with keyboard navigation, multi-select, and a context menu (Revoke, Copy ID, Delete)</li>
<li>"Last used" timestamps use RelativeDateTimeFormatter for correct localization</li>
<li>Refuse to generate SQL when the database dialect cannot be resolved, instead of silently emitting unquoted identifiers</li>
</ul>]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.37.0/TablePro-0.37.0-x86_64.zip" length="20899073" type="application/octet-stream" sparkle:edSignature="FTJ69uTQb/cwbf+QmXIM2KclsAYxRzigg941kKqo5yl7ithR/JcCI+IDjJRbCYu4JQdwP2Eoc50+2GrR+RgMCg=="/>
        </item>
        <item>
            <title>0.36.0</title>
            <pubDate>Mon, 27 Apr 2026 04:27:41 +0000</pubDate>
            <sparkle:version>75</sparkle:version>
            <sparkle:shortVersionString>0.36.0</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <sparkle:hardwareRequirements>arm64</sparkle:hardwareRequirements>
            <description><![CDATA[<h3>Added</h3>
<ul>
<li>GitHub Copilot: inline suggestions, chat, OAuth sign-in, schema context</li>
<li>Query parameters: `:name` placeholders in SQL with inline value panel and native prepared statement binding</li>
<li>Plugin auto-update at launch and one-click update in Settings</li>
<li>Connection sharing: Copy Connection String, Copy TablePro Link, Copy as JSON via Share menu</li>
<li>MCP server: token auth with permission tiers, TLS, remote access, rate limiting, stdio bridge, one-click setup for Claude Code/Desktop/Cursor</li>
<li>Edit > Find menu item (Cmd+F)</li>
</ul>
<h3>Changed</h3>
<ul>
<li>AI settings rewritten as single tab with one active provider, per-provider config sheets</li>
<li>Filter value field uses native SwiftUI suggestion dropdown</li>
<li>MCP bridge pins TLS certificate fingerprint</li>
<li>Native NSSearchField in keyboard shortcuts, database switcher, quick switcher</li>
<li>About window uses standard macOS panel</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>Plugin ABI mismatch guard for user-installed plugins</li>
<li>SQL parameter escaping for control characters and edge-case formats</li>
<li>Query parameter conversion for Bool, Date, Data, non-finite numbers</li>
<li>Filter preset duplicate name overwrite</li>
<li>Raw SQL filter injection and destructive statement validation</li>
<li>IME input (Chinese, Japanese, Korean) in filter value field</li>
<li>MCP server shutdown on app quit and access policy enforcement</li>
<li>Foreign app import: SSL/SSH parsing for TablePlus, DBeaver, Sequel Ace</li>
<li>Export race condition, missing confirmation dialog, empty state</li>
<li>Window position restore, connection error display, list selection clicks</li>
<li>Localization for error messages, connection labels, filter options</li>
</ul>
]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.36.0/TablePro-0.36.0-arm64.zip" length="25509856" type="application/octet-stream" sparkle:edSignature="kmajEYE5ukmZKAEyW0aMKj9YfiIz5+D1p0XcujmAGfrca9MdKBEX4uP36P1FX51ARX0JuIbCr+6109cgjBahAQ=="/>
        </item>
        <item>
            <title>0.36.0</title>
            <pubDate>Mon, 27 Apr 2026 04:56:06 +0000</pubDate>
            <sparkle:version>75</sparkle:version>
            <sparkle:shortVersionString>0.36.0</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <description><![CDATA[<h3>Added</h3>
<ul>
<li>GitHub Copilot: inline suggestions, chat, OAuth sign-in, schema context</li>
<li>Query parameters: `:name` placeholders in SQL with inline value panel and native prepared statement binding</li>
<li>Plugin auto-update at launch and one-click update in Settings</li>
<li>Connection sharing: Copy Connection String, Copy TablePro Link, Copy as JSON via Share menu</li>
<li>MCP server: token auth with permission tiers, TLS, remote access, rate limiting, stdio bridge, one-click setup for Claude Code/Desktop/Cursor</li>
<li>Edit > Find menu item (Cmd+F)</li>
</ul>
<h3>Changed</h3>
<ul>
<li>AI settings rewritten as single tab with one active provider, per-provider config sheets</li>
<li>Filter value field uses native SwiftUI suggestion dropdown</li>
<li>MCP bridge pins TLS certificate fingerprint</li>
<li>Native NSSearchField in keyboard shortcuts, database switcher, quick switcher</li>
<li>About window uses standard macOS panel</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>Plugin ABI mismatch guard for user-installed plugins</li>
<li>SQL parameter escaping for control characters and edge-case formats</li>
<li>Query parameter conversion for Bool, Date, Data, non-finite numbers</li>
<li>Filter preset duplicate name overwrite</li>
<li>Raw SQL filter injection and destructive statement validation</li>
<li>IME input (Chinese, Japanese, Korean) in filter value field</li>
<li>MCP server shutdown on app quit and access policy enforcement</li>
<li>Foreign app import: SSL/SSH parsing for TablePlus, DBeaver, Sequel Ace</li>
<li>Export race condition, missing confirmation dialog, empty state</li>
<li>Window position restore, connection error display, list selection clicks</li>
<li>Localization for error messages, connection labels, filter options</li>
</ul>
]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.36.0/TablePro-0.36.0-x86_64.zip" length="25876370" type="application/octet-stream" sparkle:edSignature="JMkm/ILxbhUJzC+4pGEPnW53WDXhAnfj2IlfwrJDcuHRpsZxVVUU6zt55u3SSMqoHxZacUSpushG49uWUcJrBw=="/>
        </item>
        <item>
            <title>0.35.0</title>
            <pubDate>Sat, 25 Apr 2026 11:04:54 +0000</pubDate>
            <sparkle:version>74</sparkle:version>
            <sparkle:shortVersionString>0.35.0</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <sparkle:hardwareRequirements>arm64</sparkle:hardwareRequirements>
            <description><![CDATA[<h3>Added</h3>
<ul>
<li>MongoDB multi-host connections for replica sets</li>
<li>JSON results view mode with Data/Structure/JSON toggle in status bar</li>
<li>JSON viewer: "Open in Window" action for resizing and fullscreen</li>
<li>Import URL: dynamic placeholder, parsed preview, clipboard auto-paste, libSQL/D1/Oracle/ClickHouse/etcd support</li>
<li>In-app feedback form via Help > Report an Issue</li>
<li>Per-connection "Local only" option to exclude from iCloud sync</li>
<li>Filter operator picker shows SQL symbols alongside names</li>
<li>SQL autocomplete suggests columns before FROM using cached schema</li>
<li>MCP query safety: server-side confirmation for write and destructive queries</li>
</ul>
<h3>Changed</h3>
<ul>
<li>Native macOS UI: menu pickers, native alerts, native List selection, NSSearchField, borderless toolbar buttons</li>
<li>Quit dialog defaults to Cancel on Return key</li>
<li>Connection form delete button moved to far left</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>Connection form overflow with SSH jump hosts and TOTP fields</li>
<li>Missing confirmation on group deletion</li>
<li>Plugin principalClass resolved off main thread</li>
<li>Crash when scrolling AI Chat during streaming on macOS 15.x</li>
<li>Connection failure on PostgreSQL-compatible databases without `SET statement_timeout`</li>
<li>Schema-qualified table names resolve correctly in autocomplete</li>
<li>Alert dialogs use sheet attachment instead of bare modal</li>
</ul>
]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.35.0/TablePro-0.35.0-arm64.zip" length="26914507" type="application/octet-stream" sparkle:edSignature="H77kkFlZ6sfSRbB5lZeHr8bO9p4vBg6ChQ95hB50EUf9I8yIU+y9kyAFnYl5VE3R1JavCnCeIMlx5PrazsnsDg=="/>
        </item>
        <item>
            <title>0.35.0</title>
            <pubDate>Sat, 25 Apr 2026 11:04:54 +0000</pubDate>
            <sparkle:version>74</sparkle:version>
            <sparkle:shortVersionString>0.35.0</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <description><![CDATA[<h3>Added</h3>
<ul>
<li>MongoDB multi-host connections for replica sets</li>
<li>JSON results view mode with Data/Structure/JSON toggle in status bar</li>
<li>JSON viewer: "Open in Window" action for resizing and fullscreen</li>
<li>Import URL: dynamic placeholder, parsed preview, clipboard auto-paste, libSQL/D1/Oracle/ClickHouse/etcd support</li>
<li>In-app feedback form via Help > Report an Issue</li>
<li>Per-connection "Local only" option to exclude from iCloud sync</li>
<li>Filter operator picker shows SQL symbols alongside names</li>
<li>SQL autocomplete suggests columns before FROM using cached schema</li>
<li>MCP query safety: server-side confirmation for write and destructive queries</li>
</ul>
<h3>Changed</h3>
<ul>
<li>Native macOS UI: menu pickers, native alerts, native List selection, NSSearchField, borderless toolbar buttons</li>
<li>Quit dialog defaults to Cancel on Return key</li>
<li>Connection form delete button moved to far left</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>Connection form overflow with SSH jump hosts and TOTP fields</li>
<li>Missing confirmation on group deletion</li>
<li>Plugin principalClass resolved off main thread</li>
<li>Crash when scrolling AI Chat during streaming on macOS 15.x</li>
<li>Connection failure on PostgreSQL-compatible databases without `SET statement_timeout`</li>
<li>Schema-qualified table names resolve correctly in autocomplete</li>
<li>Alert dialogs use sheet attachment instead of bare modal</li>
</ul>
]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.35.0/TablePro-0.35.0-x86_64.zip" length="26864312" type="application/octet-stream" sparkle:edSignature="KJyjIyi/BXsbFTcuxsUJlpcpDCdHbb8OtaUlaHD90B9yBqdKO/NHO/kD6cyL9MQg6QwWvzr55vEBP4DB5jEDBA=="/>
        </item>
        <item>
            <title>0.34.0</title>
            <pubDate>Wed, 22 Apr 2026 13:10:28 +0000</pubDate>
            <sparkle:version>73</sparkle:version>
            <sparkle:shortVersionString>0.34.0</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <sparkle:hardwareRequirements>arm64</sparkle:hardwareRequirements>
            <description><![CDATA[<h3>Added</h3>
<ul>
<li>libSQL / Turso plugin</li>
<li>JSON viewer with text/tree toggle</li>
<li>MCP server with client list and status menu</li>
<li>Import connections from TablePlus, Sequel Ace, DBeaver</li>
<li>Database CLI terminal (`Ctrl+Cmd+\``)</li>
<li>Structure tab: alter columns, indexes, foreign keys, primary keys</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>SQL formatter preserving original case, UNION and parentheses spacing</li>
</ul>
<h3>Changed</h3>
<ul>
<li>Sidebar toggle uses Xcode-style navigator buttons</li>
<li>Sidebar and inspector use native split view controls</li>
<li>Theme colors follow system appearance and accent color. Removed Layout tab, font sizes use system text styles.</li>
</ul>
]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.34.0/TablePro-0.34.0-arm64.zip" length="26774339" type="application/octet-stream" sparkle:edSignature="nm5Li9gow5/zeYcpG2RW8RZ7Tp/h0RqxAzk8ZxOlJwetJFmTILJ7Mpu233N2yN+BOVxF4ksbD63q8WLQznj0Aw=="/>
        </item>
        <item>
            <title>0.34.0</title>
            <pubDate>Wed, 22 Apr 2026 13:10:29 +0000</pubDate>
            <sparkle:version>73</sparkle:version>
            <sparkle:shortVersionString>0.34.0</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <description><![CDATA[<h3>Added</h3>
<ul>
<li>libSQL / Turso plugin</li>
<li>JSON viewer with text/tree toggle</li>
<li>MCP server with client list and status menu</li>
<li>Import connections from TablePlus, Sequel Ace, DBeaver</li>
<li>Database CLI terminal (`Ctrl+Cmd+\``)</li>
<li>Structure tab: alter columns, indexes, foreign keys, primary keys</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>SQL formatter preserving original case, UNION and parentheses spacing</li>
</ul>
<h3>Changed</h3>
<ul>
<li>Sidebar toggle uses Xcode-style navigator buttons</li>
<li>Sidebar and inspector use native split view controls</li>
<li>Theme colors follow system appearance and accent color. Removed Layout tab, font sizes use system text styles.</li>
</ul>
]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.34.0/TablePro-0.34.0-x86_64.zip" length="26708578" type="application/octet-stream" sparkle:edSignature="hH8SJAVuC4alprt0WnRemP55g+/QigHuCATLVFyGKAgL7pPcVms0+9YPhRWhPmV/pwI+hpHkLt/ZXKzLHSzpCg=="/>
        </item>
        <item>
            <title>0.33.0</title>
            <pubDate>Sun, 19 Apr 2026 17:38:33 +0000</pubDate>
            <sparkle:version>72</sparkle:version>
            <sparkle:shortVersionString>0.33.0</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <sparkle:hardwareRequirements>arm64</sparkle:hardwareRequirements>
            <description><![CDATA[<h3>Added</h3>
<ul>
<li>Cancel running query from toolbar or `Cmd+.`</li>
<li>Execute All Statements shortcut (Cmd+Shift+Enter) (#770)</li>
<li>Drop database from the database switcher (context menu, toolbar button, Delete key)</li>
<li>Query result limit setting in Data Grid preferences</li>
<li>Structure tab: search, sort, count badges, PK column, DDL view with highlighting, Copy As (CSV/JSON/SQL), dropdown pickers, destructive change confirmation</li>
<li>Structure tab: charset/collation (MySQL), index prefix length, partial indexes (PostgreSQL), cross-schema FK, schema changes in query history</li>
<li>ClickHouse: parts tab actions (optimize table, drop/detach partition)</li>
<li>Streaming export for query results with partial loading (no memory limit)</li>
<li>Import error handling modes: Stop and Rollback, Stop and Commit, Skip and Continue</li>
<li>Handoff via NSUserActivity</li>
</ul>
<h3>Changed</h3>
<ul>
<li>Query tabs load rows progressively (default 10,000) with Load More and Fetch All in status bar</li>
<li>Main editor window rewritten on AppKit (`NSWindowController` + `NSToolbar`) for faster tab opens and correct lifecycle</li>
<li>Toolbar layout follows Apple HIG (sidebar left, connection center, view actions right)</li>
<li>Export engine rewritten: streaming row fetch, macOS system progress, atomic file writes</li>
<li>SQL import parser rewritten: DELIMITER support, MySQL conditional/hash comments, chunk boundary handling, single-pass async decompression, error surfacing</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>Selection highlight not covering the last line on Cmd+A (#770)</li>
<li>Cmd+W closing the connection window instead of clearing to empty state</li>
<li>ER Diagram and Server Dashboard replacing the current tab instead of opening a new one</li>
<li>Welcome window stealing focus on connect, disabling Cmd+T until manual click</li>
<li>Toolbar empty on second tab, menu shortcuts disabled after toolbar click</li>
<li>AI chat freeze when large queries or results are in the system prompt (#774)</li>
<li>AI chat panel not updating when switching database connections</li>
<li>Schema restored on reconnect for PostgreSQL, Redshift, and BigQuery (#777)</li>
<li>Database restored after auto-reconnect (was lost when connection dropped)</li>
<li>Database switch no longer closes windows before confirming success</li>
<li>Redis database selection persisted across sessions</li>
<li>SSH jumphost lost after disconnect or app restart (#790)</li>
<li>Password appears missing when Keychain is locked after reboot (#780)</li>
<li>Import: correct rollback reporting, FK checks restored after failure, decompressed-size progress</li>
<li>JSON export no longer coerces leading-zero strings to integers</li>
<li>XLSX export auto-splits tables exceeding 1,048,576 rows into multiple sheets</li>
<li>CSV formula injection guard corrected to OWASP-standard prefixes only</li>
<li>MQL export validates JSON values before passthrough</li>
<li>SQL export gzip compression is now async and cancellable</li>
<li>Export progress bar reliably reaches 100%</li>
</ul>
]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.33.0/TablePro-0.33.0-arm64.zip" length="26128408" type="application/octet-stream" sparkle:edSignature="QAPaxj7RP5Bw1WTOP0vK6dWc7XKW6Q0yrUSw186ZTgjONeRoZm692T3KH3/qYQjUi65iYTC9lBKRJ+fz3WB2AQ=="/>
        </item>
        <item>
            <title>0.33.0</title>
            <pubDate>Sun, 19 Apr 2026 17:38:34 +0000</pubDate>
            <sparkle:version>72</sparkle:version>
            <sparkle:shortVersionString>0.33.0</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <description><![CDATA[<h3>Added</h3>
<ul>
<li>Cancel running query from toolbar or `Cmd+.`</li>
<li>Execute All Statements shortcut (Cmd+Shift+Enter) (#770)</li>
<li>Drop database from the database switcher (context menu, toolbar button, Delete key)</li>
<li>Query result limit setting in Data Grid preferences</li>
<li>Structure tab: search, sort, count badges, PK column, DDL view with highlighting, Copy As (CSV/JSON/SQL), dropdown pickers, destructive change confirmation</li>
<li>Structure tab: charset/collation (MySQL), index prefix length, partial indexes (PostgreSQL), cross-schema FK, schema changes in query history</li>
<li>ClickHouse: parts tab actions (optimize table, drop/detach partition)</li>
<li>Streaming export for query results with partial loading (no memory limit)</li>
<li>Import error handling modes: Stop and Rollback, Stop and Commit, Skip and Continue</li>
<li>Handoff via NSUserActivity</li>
</ul>
<h3>Changed</h3>
<ul>
<li>Query tabs load rows progressively (default 10,000) with Load More and Fetch All in status bar</li>
<li>Main editor window rewritten on AppKit (`NSWindowController` + `NSToolbar`) for faster tab opens and correct lifecycle</li>
<li>Toolbar layout follows Apple HIG (sidebar left, connection center, view actions right)</li>
<li>Export engine rewritten: streaming row fetch, macOS system progress, atomic file writes</li>
<li>SQL import parser rewritten: DELIMITER support, MySQL conditional/hash comments, chunk boundary handling, single-pass async decompression, error surfacing</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>Selection highlight not covering the last line on Cmd+A (#770)</li>
<li>Cmd+W closing the connection window instead of clearing to empty state</li>
<li>ER Diagram and Server Dashboard replacing the current tab instead of opening a new one</li>
<li>Welcome window stealing focus on connect, disabling Cmd+T until manual click</li>
<li>Toolbar empty on second tab, menu shortcuts disabled after toolbar click</li>
<li>AI chat freeze when large queries or results are in the system prompt (#774)</li>
<li>AI chat panel not updating when switching database connections</li>
<li>Schema restored on reconnect for PostgreSQL, Redshift, and BigQuery (#777)</li>
<li>Database restored after auto-reconnect (was lost when connection dropped)</li>
<li>Database switch no longer closes windows before confirming success</li>
<li>Redis database selection persisted across sessions</li>
<li>SSH jumphost lost after disconnect or app restart (#790)</li>
<li>Password appears missing when Keychain is locked after reboot (#780)</li>
<li>Import: correct rollback reporting, FK checks restored after failure, decompressed-size progress</li>
<li>JSON export no longer coerces leading-zero strings to integers</li>
<li>XLSX export auto-splits tables exceeding 1,048,576 rows into multiple sheets</li>
<li>CSV formula injection guard corrected to OWASP-standard prefixes only</li>
<li>MQL export validates JSON values before passthrough</li>
<li>SQL export gzip compression is now async and cancellable</li>
<li>Export progress bar reliably reaches 100%</li>
</ul>
]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.33.0/TablePro-0.33.0-x86_64.zip" length="25440328" type="application/octet-stream" sparkle:edSignature="VWW/+r1/6m9xNTFVZLd7F3S52sY6C+z3HKcbaLZOCCaVpXeot8BF8ZCWR9/UcAbcZYvD4YWgc8tn3H9tVp/YCg=="/>
        </item>
        <item>
            <title>0.32.1</title>
            <pubDate>Fri, 17 Apr 2026 00:51:02 +0000</pubDate>
            <sparkle:version>71</sparkle:version>
            <sparkle:shortVersionString>0.32.1</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <sparkle:hardwareRequirements>arm64</sparkle:hardwareRequirements>
            <description><![CDATA[<h3>Changed</h3>
<ul>
<li>Revert in-app tab bar refactor to restore native macOS window tabs (stability)</li>
</ul>
]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.32.1/TablePro-0.32.1-arm64.zip" length="24668630" type="application/octet-stream" sparkle:edSignature="6q8j4WtM4TboUSfO+PTnEttsUKcZ4DVghGaQUO1u9TBCik4+0/YscUgrysJeMglcneh6i9nC21ezOkCT7eRwAQ=="/>
        </item>
        <item>
            <title>0.32.1</title>
            <pubDate>Fri, 17 Apr 2026 00:51:03 +0000</pubDate>
            <sparkle:version>71</sparkle:version>
            <sparkle:shortVersionString>0.32.1</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <description><![CDATA[<h3>Changed</h3>
<ul>
<li>Revert in-app tab bar refactor to restore native macOS window tabs (stability)</li>
</ul>
]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.32.1/TablePro-0.32.1-x86_64.zip" length="23958562" type="application/octet-stream" sparkle:edSignature="5oLLaPr5sdAClrKXsFhGIfbGedvkbnAsa2+8gJl/0INV9J1PlQG6oIn0Z/u5nEOKKkh9W7uC9/sx05O9H6j3CA=="/>
        </item>
        <item>
            <title>0.32.0</title>
            <pubDate>Thu, 16 Apr 2026 16:08:08 +0000</pubDate>
            <sparkle:version>70</sparkle:version>
            <sparkle:shortVersionString>0.32.0</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <sparkle:hardwareRequirements>arm64</sparkle:hardwareRequirements>
            <description><![CDATA[<h3>Added</h3>
<ul>
<li>In-app tab bar with instant switching, drag reorder, pinned tabs, and dirty indicators</li>
<li>Reopen closed tab (Cmd+Shift+T), MRU tab selection on close</li>
<li>Deeplinks and Handoff route to in-app tabs instead of creating duplicate windows</li>
</ul>
<h3>Changed</h3>
<ul>
<li>Replace native macOS window tabs with in-app tab bar (600ms+ → instant)</li>
<li>Tab content preserved across switches (no view destruction/recreation)</li>
<li>Connection state persisted incrementally (survives force quit)</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>Raw SQL injection via external URL scheme deeplinks — now requires user confirmation</li>
<li>MySQL prepared statements silently truncating columns larger than 64KB</li>
<li>MSSQL error messages misattributed when multiple connections open simultaneously</li>
<li>BigQuery filter injection via unescaped column names and unvalidated operators</li>
<li>App quitting without warning when tabs have unsaved edits</li>
<li>Connection list corruption risk from non-atomic UserDefaults writes</li>
<li>Stale user-installed plugins silently rejected with no UI feedback</li>
<li>SSL mode picker showing misleading "Required" instead of "Required (skip verify)"</li>
<li>Plugin load blocking main thread on first connection after launch</li>
</ul>
<h3>Changed</h3>
<ul>
<li>OpenSSL updated to 3.4.3 (CVE-2025-9230, CVE-2025-9231)</li>
<li>SHA-256 checksum verification added to FreeTDS, Cassandra, and DuckDB build scripts</li>
<li>Memory pressure monitoring now reactive via DispatchSource</li>
</ul>
]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.32.0/TablePro-0.32.0-arm64.zip" length="24704327" type="application/octet-stream" sparkle:edSignature="ihWO20+mBSr9xYMsoCRHZNCK5GI0xZOvFvPJKXpGeXT4oBvnQft846c7uYcRSuP1/X7AtZcxpCV1nRk+/Ab5AQ=="/>
        </item>
        <item>
            <title>0.32.0</title>
            <pubDate>Thu, 16 Apr 2026 16:08:09 +0000</pubDate>
            <sparkle:version>70</sparkle:version>
            <sparkle:shortVersionString>0.32.0</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <description><![CDATA[<h3>Added</h3>
<ul>
<li>In-app tab bar with instant switching, drag reorder, pinned tabs, and dirty indicators</li>
<li>Reopen closed tab (Cmd+Shift+T), MRU tab selection on close</li>
<li>Deeplinks and Handoff route to in-app tabs instead of creating duplicate windows</li>
</ul>
<h3>Changed</h3>
<ul>
<li>Replace native macOS window tabs with in-app tab bar (600ms+ → instant)</li>
<li>Tab content preserved across switches (no view destruction/recreation)</li>
<li>Connection state persisted incrementally (survives force quit)</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>Raw SQL injection via external URL scheme deeplinks — now requires user confirmation</li>
<li>MySQL prepared statements silently truncating columns larger than 64KB</li>
<li>MSSQL error messages misattributed when multiple connections open simultaneously</li>
<li>BigQuery filter injection via unescaped column names and unvalidated operators</li>
<li>App quitting without warning when tabs have unsaved edits</li>
<li>Connection list corruption risk from non-atomic UserDefaults writes</li>
<li>Stale user-installed plugins silently rejected with no UI feedback</li>
<li>SSL mode picker showing misleading "Required" instead of "Required (skip verify)"</li>
<li>Plugin load blocking main thread on first connection after launch</li>
</ul>
<h3>Changed</h3>
<ul>
<li>OpenSSL updated to 3.4.3 (CVE-2025-9230, CVE-2025-9231)</li>
<li>SHA-256 checksum verification added to FreeTDS, Cassandra, and DuckDB build scripts</li>
<li>Memory pressure monitoring now reactive via DispatchSource</li>
</ul>
]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.32.0/TablePro-0.32.0-x86_64.zip" length="23999293" type="application/octet-stream" sparkle:edSignature="oVxL9JzZ/MO5NZM5JimNCB3jaq8lsu+7Y16Ui8QGFpkgd9Qnc4jS3cBy8SYzhlmrC1GqYjQvrFqEldERqNelDA=="/>
        </item>
        <item>
            <title>0.31.5</title>
            <pubDate>Tue, 14 Apr 2026 06:46:28 +0000</pubDate>
            <sparkle:version>69</sparkle:version>
            <sparkle:shortVersionString>0.31.5</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <sparkle:hardwareRequirements>arm64</sparkle:hardwareRequirements>
            <description><![CDATA[<h3>Fixed</h3>
<ul>
<li>Fix AI chat hanging the app during streaming, schema fetch, and conversation loading (#735)</li>
<li>SSH Agent auth: fall back to key file from `~/.ssh/config` or default paths when agent has no loaded identities (#729)</li>
<li>Wire AI Explain (⌘L), Optimize (⌘⌥L), and Toggle Sidebar (⌘0) shortcuts to menu bar commands</li>
<li>Keyboard shortcuts follow macOS HIG — remap Quick Switcher to ⌘⇧O, Format Query to ⌘⇧L, fix stale tooltip hints</li>
<li>SSH-tunneled connections failing to reconnect after idle/sleep — health monitor now rebuilds the tunnel, OS-level TCP keepalive detects dead NAT mappings, and wake-from-sleep triggers immediate validation (#736)</li>
<li>Composite primary key tables: editing or deleting a row affects all rows sharing the first PK value instead of just the target row</li>
<li>Structure view saves bypass safe mode on read-only connections</li>
</ul>
]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.31.5/TablePro-0.31.5-arm64.zip" length="24632288" type="application/octet-stream" sparkle:edSignature="E1lEYWrHva/J/ilZPz9n6+3aFk54sA2tdu5GiqXFzM3Yz0uMKT7y76NRFOccp1cbXQxndEAoF4etQEBnwHAACQ=="/>
        </item>
        <item>
            <title>0.31.5</title>
            <pubDate>Tue, 14 Apr 2026 06:46:29 +0000</pubDate>
            <sparkle:version>69</sparkle:version>
            <sparkle:shortVersionString>0.31.5</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <description><![CDATA[<h3>Fixed</h3>
<ul>
<li>Fix AI chat hanging the app during streaming, schema fetch, and conversation loading (#735)</li>
<li>SSH Agent auth: fall back to key file from `~/.ssh/config` or default paths when agent has no loaded identities (#729)</li>
<li>Wire AI Explain (⌘L), Optimize (⌘⌥L), and Toggle Sidebar (⌘0) shortcuts to menu bar commands</li>
<li>Keyboard shortcuts follow macOS HIG — remap Quick Switcher to ⌘⇧O, Format Query to ⌘⇧L, fix stale tooltip hints</li>
<li>SSH-tunneled connections failing to reconnect after idle/sleep — health monitor now rebuilds the tunnel, OS-level TCP keepalive detects dead NAT mappings, and wake-from-sleep triggers immediate validation (#736)</li>
<li>Composite primary key tables: editing or deleting a row affects all rows sharing the first PK value instead of just the target row</li>
<li>Structure view saves bypass safe mode on read-only connections</li>
</ul>
]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.31.5/TablePro-0.31.5-x86_64.zip" length="23927529" type="application/octet-stream" sparkle:edSignature="p1CfGGWSQ8z/0ZMzVJ7ale/YFP53ty2A+tFlUCqMfKqJ8mWj4YjjlI6aPxWDNFU4UhMfemqBV39hzSjQf7a8CQ=="/>
        </item>
        <item>
            <title>0.31.4</title>
            <pubDate>Mon, 13 Apr 2026 18:53:27 +0000</pubDate>
            <sparkle:version>68</sparkle:version>
            <sparkle:shortVersionString>0.31.4</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <sparkle:hardwareRequirements>arm64</sparkle:hardwareRequirements>
            <description><![CDATA[<h3>Added</h3>
<ul>
<li>iOS: database brand icons instead of SF Symbols (#733)</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>Native tab bar "+" button always creates "Query 1" instead of incrementing (#727)</li>
<li>Sidebar gap inconsistent when switching tabs (#728)</li>
<li>SSH Agent auth failing when SSH_AUTH_SOCK not in process env (#729)</li>
<li>iOS: SSH private key import file not working during test connection (#730)</li>
<li>iOS: SQLite file picker not updating after file selection (#732)</li>
<li>Default shortcut mismatch with toast in toggle inspector (#726)</li>
</ul>
]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.31.4/TablePro-0.31.4-arm64.zip" length="24608164" type="application/octet-stream" sparkle:edSignature="vdn6ufZscFuHal4NPo90GyKhn7grfvQqasGPJiArg6OqhOgSZLAuPwrXR+TTdIi0fRKVu3RSACofsQrFbG4iDw=="/>
        </item>
        <item>
            <title>0.31.4</title>
            <pubDate>Mon, 13 Apr 2026 18:53:28 +0000</pubDate>
            <sparkle:version>68</sparkle:version>
            <sparkle:shortVersionString>0.31.4</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <description><![CDATA[<h3>Added</h3>
<ul>
<li>iOS: database brand icons instead of SF Symbols (#733)</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>Native tab bar "+" button always creates "Query 1" instead of incrementing (#727)</li>
<li>Sidebar gap inconsistent when switching tabs (#728)</li>
<li>SSH Agent auth failing when SSH_AUTH_SOCK not in process env (#729)</li>
<li>iOS: SSH private key import file not working during test connection (#730)</li>
<li>iOS: SQLite file picker not updating after file selection (#732)</li>
<li>Default shortcut mismatch with toast in toggle inspector (#726)</li>
</ul>
]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.31.4/TablePro-0.31.4-x86_64.zip" length="23900174" type="application/octet-stream" sparkle:edSignature="pJ7eph0nksQNoKAJhlDeMn2CbAnvk8hD6p0rjmHhWumIKvxFlV3OoPYqlON9Adn32bVnfQJN/SdwbQAb9loTCw=="/>
        </item>
        <item>
            <title>0.31.3</title>
            <pubDate>Mon, 13 Apr 2026 09:46:22 +0000</pubDate>
            <sparkle:version>67</sparkle:version>
            <sparkle:shortVersionString>0.31.3</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <sparkle:hardwareRequirements>arm64</sparkle:hardwareRequirements>
            <description><![CDATA[<h3>Added</h3>
<ul>
<li>Restore all open connections and tabs after quitting the app (#703)</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>Database Switcher: auto-select first item on fast typing (#714)</li>
<li>AI settings: fix Ollama model selection and error messages (#712)</li>
<li>SQL formatter: rewrite with token-based architecture (#705)</li>
<li>Filters: `= NULL` auto-converts to `IS NULL`, BETWEEN and IN/NOT IN NULL handling (#706)</li>
<li>SQLite: auto-detect schema changes from external tools (#704)</li>
<li>UI layout stability when toggling menus, panels, and inspectors (#702)</li>
<li>Misc bug fixes: save tabs before DB switch, log rollback failures, standardize colors, fix localization, button safety, filter validation (#707)</li>
<li>Fix Ollama AI chat streaming — responses were silently discarded due to wrong stream format parsing</li>
</ul>
<h3>Changed</h3>
<ul>
<li>Keyboard shortcuts follow macOS HIG — `⌘F` is Find, `⌘⇧F` for filters, `⌘⌥I` for inspector, `⌘0` for sidebar</li>
<li>Format Query and Pagination shortcuts now customizable in Settings</li>
<li>Menu bar restructured per macOS HIG: ⌘N opens connection list (#722), new Query menu, Help search restored, duplicate items removed</li>
</ul>
]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.31.3/TablePro-0.31.3-arm64.zip" length="24605193" type="application/octet-stream" sparkle:edSignature="stfm3pT8hAfSDLWxShg/+qj8DQZhL56Heg6nUqpzodhxbOc0uUfI3+HPEcS/sHfeIymD/r1pb+RQk6rigZG6BA=="/>
        </item>
        <item>
            <title>0.31.3</title>
            <pubDate>Mon, 13 Apr 2026 09:46:23 +0000</pubDate>
            <sparkle:version>67</sparkle:version>
            <sparkle:shortVersionString>0.31.3</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <description><![CDATA[<h3>Added</h3>
<ul>
<li>Restore all open connections and tabs after quitting the app (#703)</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>Database Switcher: auto-select first item on fast typing (#714)</li>
<li>AI settings: fix Ollama model selection and error messages (#712)</li>
<li>SQL formatter: rewrite with token-based architecture (#705)</li>
<li>Filters: `= NULL` auto-converts to `IS NULL`, BETWEEN and IN/NOT IN NULL handling (#706)</li>
<li>SQLite: auto-detect schema changes from external tools (#704)</li>
<li>UI layout stability when toggling menus, panels, and inspectors (#702)</li>
<li>Misc bug fixes: save tabs before DB switch, log rollback failures, standardize colors, fix localization, button safety, filter validation (#707)</li>
<li>Fix Ollama AI chat streaming — responses were silently discarded due to wrong stream format parsing</li>
</ul>
<h3>Changed</h3>
<ul>
<li>Keyboard shortcuts follow macOS HIG — `⌘F` is Find, `⌘⇧F` for filters, `⌘⌥I` for inspector, `⌘0` for sidebar</li>
<li>Format Query and Pagination shortcuts now customizable in Settings</li>
<li>Menu bar restructured per macOS HIG: ⌘N opens connection list (#722), new Query menu, Help search restored, duplicate items removed</li>
</ul>
]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.31.3/TablePro-0.31.3-x86_64.zip" length="23896532" type="application/octet-stream" sparkle:edSignature="l3G3p2blaVoh8ok9VSkj4i/AGApyqGcvfoMF4zXWBUpHSw9TfibSAWOZLPCCycCG3SqPsh5tHALPYru5EMQiAg=="/>
        </item>
        <item>
            <title>0.31.2</title>
            <pubDate>Sun, 12 Apr 2026 18:00:14 +0000</pubDate>
            <sparkle:version>66</sparkle:version>
            <sparkle:shortVersionString>0.31.2</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <sparkle:hardwareRequirements>arm64</sparkle:hardwareRequirements>
            <description><![CDATA[<h3>Fixed</h3>
<ul>
<li>Query tabs always named "Query 1" instead of incrementing (#695)</li>
<li>Sidebar empty in new or restored window tabs (#694)</li>
<li>Tab titles, order, and persistence lost on quit/restore</li>
<li>PostgreSQL version display for v10+ (#698)</li>
<li>License activation metadata and deactivation error handling</li>
</ul>
]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.31.2/TablePro-0.31.2-arm64.zip" length="24576297" type="application/octet-stream" sparkle:edSignature="09fczpRZtRZ7ywU1CS7FdFhBHe71CSSEAyT5B07NKN+6bi9ccXukusNx2VLxYrLFtHtUNSZNC4xHwmGlxHovAA=="/>
        </item>
        <item>
            <title>0.31.2</title>
            <pubDate>Sun, 12 Apr 2026 18:00:15 +0000</pubDate>
            <sparkle:version>66</sparkle:version>
            <sparkle:shortVersionString>0.31.2</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <description><![CDATA[<h3>Fixed</h3>
<ul>
<li>Query tabs always named "Query 1" instead of incrementing (#695)</li>
<li>Sidebar empty in new or restored window tabs (#694)</li>
<li>Tab titles, order, and persistence lost on quit/restore</li>
<li>PostgreSQL version display for v10+ (#698)</li>
<li>License activation metadata and deactivation error handling</li>
</ul>
]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.31.2/TablePro-0.31.2-x86_64.zip" length="23865033" type="application/octet-stream" sparkle:edSignature="4qe/RwHL7GemOpooISJkOOZgPOm7DsIQy1sKpmnaDWMIfX4CWbgJbQhUN1FNWCozmAf1k+1XRrAf5dIONE29Ag=="/>
        </item>
        <item>
            <title>0.31.1</title>
            <pubDate>Sat, 11 Apr 2026 20:35:05 +0000</pubDate>
            <sparkle:version>65</sparkle:version>
            <sparkle:shortVersionString>0.31.1</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <sparkle:hardwareRequirements>arm64</sparkle:hardwareRequirements>
            <description><![CDATA[<h3>Fixed</h3>
<ul>
<li>iCloud Sync not working on TestFlight/App Store builds (CloudKit environment set to Production)</li>
</ul>
]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.31.1/TablePro-0.31.1-arm64.zip" length="24569693" type="application/octet-stream" sparkle:edSignature="vNUxLYZXP8Z58CT/EtpVchc9qXkThxZOk+TSp1amiRE8KBuHepsX0cM3uu86g1mRf2EkQjHd+hNyvDucsmqkAA=="/>
        </item>
        <item>
            <title>0.31.1</title>
            <pubDate>Sat, 11 Apr 2026 20:35:06 +0000</pubDate>
            <sparkle:version>65</sparkle:version>
            <sparkle:shortVersionString>0.31.1</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <description><![CDATA[<h3>Fixed</h3>
<ul>
<li>iCloud Sync not working on TestFlight/App Store builds (CloudKit environment set to Production)</li>
</ul>
]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.31.1/TablePro-0.31.1-x86_64.zip" length="23859176" type="application/octet-stream" sparkle:edSignature="iAZNjmodIgb84s42zSCI0HTnPgED5Pm+V3gpGN2KGyopdHxnv4E/OqN4VWCr1jmetrDf38Uys5/0I9PJYoVnDw=="/>
        </item>
        <item>
            <title>0.31.0</title>
            <pubDate>Sat, 11 Apr 2026 19:31:15 +0000</pubDate>
            <sparkle:version>64</sparkle:version>
            <sparkle:shortVersionString>0.31.0</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <sparkle:hardwareRequirements>arm64</sparkle:hardwareRequirements>
            <description><![CDATA[<h3>Added</h3>
<ul>
<li>Server Dashboard: active sessions, metrics, slow queries (PostgreSQL, MySQL, MSSQL, ClickHouse, DuckDB, SQLite)</li>
<li>Handoff support between iOS and macOS</li>
<li>iOS: full-text search in data browser, state restoration, iPad keyboard shortcuts</li>
</ul>
<h3>Changed</h3>
<ul>
<li>Sidebar table loading refactored: single source of truth, explicit loading states, no race conditions on database switch</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>Create Database dialog now shows correct options per database type (encoding/LC_COLLATE for PostgreSQL, hidden for Redis/etcd)</li>
<li>SSH tunnel with `~/.ssh/config` profiles (#672): `Include` directives, token expansion, multi-word `Host` filtering</li>
</ul>
]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.31.0/TablePro-0.31.0-arm64.zip" length="24570827" type="application/octet-stream" sparkle:edSignature="Sveav1OEos56x3fDbWyF82eWXk1y+YlyeT1ZLqdp1chXcm/tisrnh8BbNMXCpmJS+YAtKKC6DewqhA9G2tyQCg=="/>
        </item>
        <item>
            <title>0.31.0</title>
            <pubDate>Sat, 11 Apr 2026 19:31:16 +0000</pubDate>
            <sparkle:version>64</sparkle:version>
            <sparkle:shortVersionString>0.31.0</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <description><![CDATA[<h3>Added</h3>
<ul>
<li>Server Dashboard: active sessions, metrics, slow queries (PostgreSQL, MySQL, MSSQL, ClickHouse, DuckDB, SQLite)</li>
<li>Handoff support between iOS and macOS</li>
<li>iOS: full-text search in data browser, state restoration, iPad keyboard shortcuts</li>
</ul>
<h3>Changed</h3>
<ul>
<li>Sidebar table loading refactored: single source of truth, explicit loading states, no race conditions on database switch</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>Create Database dialog now shows correct options per database type (encoding/LC_COLLATE for PostgreSQL, hidden for Redis/etcd)</li>
<li>SSH tunnel with `~/.ssh/config` profiles (#672): `Include` directives, token expansion, multi-word `Host` filtering</li>
</ul>
]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.31.0/TablePro-0.31.0-x86_64.zip" length="23858408" type="application/octet-stream" sparkle:edSignature="gTmBvk1aTnLCro+ycypscx5d+i4wYwk7CUK4pt14dcvgVFJwU9KEx8pYgTryA6HBCHQe6pu8MUd86d4Y5IBJDA=="/>
        </item>
        <item>
            <title>0.30.1</title>
            <pubDate>Fri, 10 Apr 2026 15:57:55 +0000</pubDate>
            <sparkle:version>63</sparkle:version>
            <sparkle:shortVersionString>0.30.1</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <sparkle:hardwareRequirements>arm64</sparkle:hardwareRequirements>
            <description><![CDATA[<h3>Added</h3>
<ul>
<li>Auto-uppercase SQL keywords setting (#660)</li>
<li>Unified cell editor chevrons for boolean, enum, date, JSON, blob columns (#665)</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>MSSQL connection failing on Docker/fresh SQL Server (#661)</li>
<li>Context menu Format SQL not working (#659)</li>
</ul>
]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.30.1/TablePro-0.30.1-arm64.zip" length="24409036" type="application/octet-stream" sparkle:edSignature="NZX0BOJQULErdn9coFRVSc3WkJ+0QR5MMv3Hlysm9HqEnYfONYgXxnivCm/M8H0BekBf+uu658PnXGRd4wSFDA=="/>
        </item>
        <item>
            <title>0.30.1</title>
            <pubDate>Fri, 10 Apr 2026 15:57:56 +0000</pubDate>
            <sparkle:version>63</sparkle:version>
            <sparkle:shortVersionString>0.30.1</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <description><![CDATA[<h3>Added</h3>
<ul>
<li>Auto-uppercase SQL keywords setting (#660)</li>
<li>Unified cell editor chevrons for boolean, enum, date, JSON, blob columns (#665)</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>MSSQL connection failing on Docker/fresh SQL Server (#661)</li>
<li>Context menu Format SQL not working (#659)</li>
</ul>
]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.30.1/TablePro-0.30.1-x86_64.zip" length="23686461" type="application/octet-stream" sparkle:edSignature="hu/DIx54SI8/0yrhKml8WP11e+dj3dn4yaObZPbP0ryYHRnw9Ws6Yo7kYNY8jiCB4glLamKEXvPdi2aOCx5oBw=="/>
        </item>
        <item>
            <title>0.30.0</title>
            <pubDate>Fri, 10 Apr 2026 02:35:56 +0000</pubDate>
            <sparkle:version>62</sparkle:version>
            <sparkle:shortVersionString>0.30.0</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <sparkle:hardwareRequirements>arm64</sparkle:hardwareRequirements>
            <description><![CDATA[<h3>Added</h3>
<ul>
<li>ER diagram with interactive layout, crow's foot notation, and PNG export (#186)</li>
<li>Space key toggles FK preview popover (#648)</li>
<li>Connection drag-to-reorder in iOS app with iCloud sync (#652)</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>Fix export dialog doing nothing on macOS Tahoe due to incorrect window reference for save panel (#654)</li>
<li>Fix column visibility popover and hex editor alignment — left-align per macOS HIG (#653)</li>
<li>Accept SQLAlchemy-style connection URLs with driver hints (#642)</li>
</ul>
]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.30.0/TablePro-0.30.0-arm64.zip" length="24402188" type="application/octet-stream" sparkle:edSignature="dBDkIOCmMYqhK0D89+blhrkeQkSCxi4jhxxgRuGydW7MRsuqD/IWBpuvotOtMrOSwWCGQWBVq+SeBdOIoRIADA=="/>
        </item>
        <item>
            <title>0.30.0</title>
            <pubDate>Fri, 10 Apr 2026 02:35:57 +0000</pubDate>
            <sparkle:version>62</sparkle:version>
            <sparkle:shortVersionString>0.30.0</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <description><![CDATA[<h3>Added</h3>
<ul>
<li>ER diagram with interactive layout, crow's foot notation, and PNG export (#186)</li>
<li>Space key toggles FK preview popover (#648)</li>
<li>Connection drag-to-reorder in iOS app with iCloud sync (#652)</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>Fix export dialog doing nothing on macOS Tahoe due to incorrect window reference for save panel (#654)</li>
<li>Fix column visibility popover and hex editor alignment — left-align per macOS HIG (#653)</li>
<li>Accept SQLAlchemy-style connection URLs with driver hints (#642)</li>
</ul>
]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.30.0/TablePro-0.30.0-x86_64.zip" length="23678906" type="application/octet-stream" sparkle:edSignature="eavSaXCsLmdDFsrPcrngqtquNWefEgntGl3JEey942PMoxh74o6y0UHmx+KjZKs/LpVylfxpagOuDZERA5e0DQ=="/>
        </item>
        <item>
            <title>0.29.0</title>
            <pubDate>Thu, 09 Apr 2026 05:47:23 +0000</pubDate>
            <sparkle:version>61</sparkle:version>
            <sparkle:shortVersionString>0.29.0</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <sparkle:hardwareRequirements>arm64</sparkle:hardwareRequirements>
            <description><![CDATA[<h3>Added</h3>
<ul>
<li>Maintenance tools via table context menu (VACUUM, ANALYZE, OPTIMIZE, REINDEX, CHECK TABLE, etc.)</li>
<li>EXPLAIN plan visualization with diagram, tree, and raw views (PostgreSQL, MySQL)</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>Fix cross-schema foreign key preview, edit, and navigation for PostgreSQL and MySQL (#644)</li>
<li>Fix macOS HIG compliance: system colors, accessibility labels, theme tokens, localization</li>
<li>Fix idle ping spin loop caused by exhausted AsyncStream iterator (#618)</li>
<li>Skip exact row count for large tables — use database statistics estimate (#519)</li>
</ul>
<h3>Changed</h3>
<ul>
<li>Theme font pickers now list installed monospaced fonts dynamically instead of a fixed built-in list</li>
</ul>
]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.29.0/TablePro-0.29.0-arm64.zip" length="24274200" type="application/octet-stream" sparkle:edSignature="0ohKG1IxTYJwYGXR+2w7meXiSUPfBhA8LLTkkjUn8YckuwFjHek85U3YLJpg7jJopiPsrUxELOmP381hPHZGAg=="/>
        </item>
        <item>
            <title>0.29.0</title>
            <pubDate>Thu, 09 Apr 2026 05:47:24 +0000</pubDate>
            <sparkle:version>61</sparkle:version>
            <sparkle:shortVersionString>0.29.0</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <description><![CDATA[<h3>Added</h3>
<ul>
<li>Maintenance tools via table context menu (VACUUM, ANALYZE, OPTIMIZE, REINDEX, CHECK TABLE, etc.)</li>
<li>EXPLAIN plan visualization with diagram, tree, and raw views (PostgreSQL, MySQL)</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>Fix cross-schema foreign key preview, edit, and navigation for PostgreSQL and MySQL (#644)</li>
<li>Fix macOS HIG compliance: system colors, accessibility labels, theme tokens, localization</li>
<li>Fix idle ping spin loop caused by exhausted AsyncStream iterator (#618)</li>
<li>Skip exact row count for large tables — use database statistics estimate (#519)</li>
</ul>
<h3>Changed</h3>
<ul>
<li>Theme font pickers now list installed monospaced fonts dynamically instead of a fixed built-in list</li>
</ul>
]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.29.0/TablePro-0.29.0-x86_64.zip" length="23543204" type="application/octet-stream" sparkle:edSignature="qGg4YTsFzKzDfeXudzqr8so0UTuE5PX9cHrhN+6pmUS2oNlqOdlSuQr7T9hVRLbCZWpNfj4+WcILifIXQoOhDA=="/>
        </item>
        <item>
            <title>0.28.0</title>
            <pubDate>Tue, 07 Apr 2026 16:16:27 +0000</pubDate>
            <sparkle:version>60</sparkle:version>
            <sparkle:shortVersionString>0.28.0</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <sparkle:hardwareRequirements>arm64</sparkle:hardwareRequirements>
            <description><![CDATA[<h3>Added</h3>
<ul>
<li>Smart value detection for UUIDs in BINARY(16) and timestamps in integer columns</li>
<li>Per-column "Display As" override via column header context menu</li>
<li>iOS: safe mode, FK navigation, syntax highlighting</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>Fix excessive idle ping traffic from orphaned monitor tasks</li>
<li>Fix Cmd+W save not persisting data grid changes</li>
<li>Fix window sizing, selection highlight, and connection switcher errors</li>
<li>Move file loading off main thread, replace timing hacks with signals</li>
</ul>
]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.28.0/TablePro-0.28.0-arm64.zip" length="24110638" type="application/octet-stream" sparkle:edSignature="wOCkVuhLaM+rRhsMPtlV8I9VTKmVuPztxP6jimoulQ2R+D9xE0biSUhnvPa1lq7EFlMAd7Qhhje55EDxLnl0CQ=="/>
        </item>
        <item>
            <title>0.28.0</title>
            <pubDate>Tue, 07 Apr 2026 16:16:28 +0000</pubDate>
            <sparkle:version>60</sparkle:version>
            <sparkle:shortVersionString>0.28.0</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <description><![CDATA[<h3>Added</h3>
<ul>
<li>Smart value detection for UUIDs in BINARY(16) and timestamps in integer columns</li>
<li>Per-column "Display As" override via column header context menu</li>
<li>iOS: safe mode, FK navigation, syntax highlighting</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>Fix excessive idle ping traffic from orphaned monitor tasks</li>
<li>Fix Cmd+W save not persisting data grid changes</li>
<li>Fix window sizing, selection highlight, and connection switcher errors</li>
<li>Move file loading off main thread, replace timing hacks with signals</li>
</ul>
]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.28.0/TablePro-0.28.0-x86_64.zip" length="23376061" type="application/octet-stream" sparkle:edSignature="Ag8PZE6I+5CAOmfPn0O7OBd/U2QSjx0GwRcAseDsNinexycNHZhXw6z0xi+HqcZmJ7qzyNjMo8yo5X40/NIDCw=="/>
        </item>
        <item>
            <title>0.27.5</title>
            <pubDate>Mon, 06 Apr 2026 12:39:06 +0000</pubDate>
            <sparkle:version>58</sparkle:version>
            <sparkle:shortVersionString>0.27.5</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <sparkle:hardwareRequirements>arm64</sparkle:hardwareRequirements>
            <description><![CDATA[<h3>Added</h3>
<ul>
<li>iOS: groups, tags, filter, sort, pagination, query history, export to clipboard, Spotlight, Siri Shortcuts, Home Screen widget</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>Fix crashes in SSH tunnel, export dialog, and jump host removal</li>
<li>Fix data races in storage layers (MainActor isolation)</li>
<li>Use native sheet presentation for all dialogs and file pickers</li>
<li>Replace event monitors and timing hacks with native SwiftUI APIs</li>
</ul>
<h3>Changed</h3>
<ul>
<li>Migrate undo system to NSUndoManager</li>
</ul>
]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.27.5/TablePro-0.27.5-arm64.zip" length="24073866" type="application/octet-stream" sparkle:edSignature="equHcgY2wXNf+vXsE8FbmL+PlYT4wlkiTALrcBLapijvc3symZX37kGqHSYdVOI4uP7ViHofymimdNccYU+4AA=="/>
        </item>
        <item>
            <title>0.27.5</title>
            <pubDate>Mon, 06 Apr 2026 12:39:07 +0000</pubDate>
            <sparkle:version>58</sparkle:version>
            <sparkle:shortVersionString>0.27.5</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <description><![CDATA[<h3>Added</h3>
<ul>
<li>iOS: groups, tags, filter, sort, pagination, query history, export to clipboard, Spotlight, Siri Shortcuts, Home Screen widget</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>Fix crashes in SSH tunnel, export dialog, and jump host removal</li>
<li>Fix data races in storage layers (MainActor isolation)</li>
<li>Use native sheet presentation for all dialogs and file pickers</li>
<li>Replace event monitors and timing hacks with native SwiftUI APIs</li>
</ul>
<h3>Changed</h3>
<ul>
<li>Migrate undo system to NSUndoManager</li>
</ul>
]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.27.5/TablePro-0.27.5-x86_64.zip" length="23331564" type="application/octet-stream" sparkle:edSignature="FwpHHzgCvSNNetNDIt1MeazlFme3/X0pCZtVVhxTNtfq3F203pnQ7fw+tvetbR7BtJYSRqowObyr226TteyCBQ=="/>
        </item>
        <item>
            <title>0.27.4</title>
            <pubDate>Sun, 05 Apr 2026 07:56:07 +0000</pubDate>
            <sparkle:version>57</sparkle:version>
            <sparkle:shortVersionString>0.27.4</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <sparkle:hardwareRequirements>arm64</sparkle:hardwareRequirements>
            <description><![CDATA[<h3>Added</h3>
<ul>
<li>Cloudflare D1: batch query execution via REST API for multi-statement SQL</li>
<li>Cloudflare D1: schema editing — CREATE TABLE, ADD/DROP COLUMN, CREATE/DROP INDEX</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>Multi-statement SQL execution fails on Cloudflare D1, ClickHouse, and other drivers that don't support transactions</li>
</ul>
<h3>Changed</h3>
<ul>
<li>Use Apple-standard `xcodebuild archive` + `exportArchive` build pipeline with dSYM collection</li>
</ul>
]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.27.4/TablePro-0.27.4-arm64.zip" length="26667354" type="application/octet-stream" sparkle:edSignature="LkenPCX2aTpVt7QXBIbKJokeYye8iTdsrY8tdZxjHP7zkjbVFrELr2Nt+hQbOQSDNRsVSepBQzFtP4nsaOgbAw=="/>
        </item>
        <item>
            <title>0.27.4</title>
            <pubDate>Sun, 05 Apr 2026 08:19:42 +0000</pubDate>
            <sparkle:version>57</sparkle:version>
            <sparkle:shortVersionString>0.27.4</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <description><![CDATA[<h3>Added</h3>
<ul>
<li>Cloudflare D1: batch query execution via REST API for multi-statement SQL</li>
<li>Cloudflare D1: schema editing — CREATE TABLE, ADD/DROP COLUMN, CREATE/DROP INDEX</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>Multi-statement SQL execution fails on Cloudflare D1, ClickHouse, and other drivers that don't support transactions</li>
</ul>
<h3>Changed</h3>
<ul>
<li>Use Apple-standard `xcodebuild archive` + `exportArchive` build pipeline with dSYM collection</li>
</ul>
]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.27.4/TablePro-0.27.4-x86_64.zip" length="23603235" type="application/octet-stream" sparkle:edSignature="779ujlI6psGE8rY1TPTIGIqvXK1fvohw4vym9nbmtfwQyBs4/1AKiDy6C/e7aVnvTIQ4uxuy1kOm0T8hAjYNBw=="/>
        </item>
        <item>
            <title>0.27.4</title>
            <pubDate>Sun, 05 Apr 2026 07:56:08 +0000</pubDate>
            <sparkle:version>57</sparkle:version>
            <sparkle:shortVersionString>0.27.4</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <description><![CDATA[<h3>Added</h3>
<ul>
<li>Cloudflare D1: batch query execution via REST API for multi-statement SQL</li>
<li>Cloudflare D1: schema editing — CREATE TABLE, ADD/DROP COLUMN, CREATE/DROP INDEX</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>Multi-statement SQL execution fails on Cloudflare D1, ClickHouse, and other drivers that don't support transactions</li>
</ul>
<h3>Changed</h3>
<ul>
<li>Use Apple-standard `xcodebuild archive` + `exportArchive` build pipeline with dSYM collection</li>
</ul>
]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.27.4/TablePro-0.27.4-x86_64.zip" length="23603249" type="application/octet-stream" sparkle:edSignature="4tWv+H9YFeK2ojIDaYMPLK0MKKAzwpVnlLh9FE6xvIdUX9lOFhY5Fbwdj+feYbFIqRlOzvwrUWsobAwFpsyIBg=="/>
        </item>
        <item>
            <title>0.27.3</title>
            <pubDate>Fri, 03 Apr 2026 14:07:56 +0000</pubDate>
            <sparkle:version>56</sparkle:version>
            <sparkle:shortVersionString>0.27.3</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <sparkle:hardwareRequirements>arm64</sparkle:hardwareRequirements>
            <description><![CDATA[<h3>Added</h3>
<ul>
<li>Structure tab context menu with Copy Name, Copy Definition (SQL), Duplicate, and Delete for columns, indexes, and foreign keys</li>
<li>Foreign key preview: press Cmd+Enter on a FK cell to see the referenced row in a popover</li>
<li>Column header: sort ascending/descending and show all hidden columns in context menu</li>
<li>Data grid: preview and navigate FK references from right-click context menu</li>
<li>Data grid: add row from right-click on empty space</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>Oracle: crash when opening views caused by OracleNIO state-machine corruption from concurrent queries, LONG column types, and DBMS_METADATA errors</li>
</ul>
]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.27.3/TablePro-0.27.3-arm64.zip" length="26429702" type="application/octet-stream" sparkle:edSignature="IWjkCtYm6gB1CVISohfedNVOmtTiywA5+vqgxDULQtEZCiASkC8wITfCMPZwnxOC6W+hLKF/nzYEmm6FQFk+Dw=="/>
        </item>
        <item>
            <title>0.27.3</title>
            <pubDate>Fri, 03 Apr 2026 14:07:57 +0000</pubDate>
            <sparkle:version>56</sparkle:version>
            <sparkle:shortVersionString>0.27.3</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <description><![CDATA[<h3>Added</h3>
<ul>
<li>Structure tab context menu with Copy Name, Copy Definition (SQL), Duplicate, and Delete for columns, indexes, and foreign keys</li>
<li>Foreign key preview: press Cmd+Enter on a FK cell to see the referenced row in a popover</li>
<li>Column header: sort ascending/descending and show all hidden columns in context menu</li>
<li>Data grid: preview and navigate FK references from right-click context menu</li>
<li>Data grid: add row from right-click on empty space</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>Oracle: crash when opening views caused by OracleNIO state-machine corruption from concurrent queries, LONG column types, and DBMS_METADATA errors</li>
</ul>
]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.27.3/TablePro-0.27.3-x86_64.zip" length="23361650" type="application/octet-stream" sparkle:edSignature="d4njTAJZ4CpnMajA86yNvpT+Q6ntyF/InURI2Od9Q6D5gRJeQJfyalcMZRrv0qW8DpahncNLRtAoH5onFEsTBg=="/>
        </item>
        <item>
            <title>0.27.2</title>
            <pubDate>Thu, 02 Apr 2026 06:21:44 +0000</pubDate>
            <sparkle:version>55</sparkle:version>
            <sparkle:shortVersionString>0.27.2</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <sparkle:hardwareRequirements>arm64</sparkle:hardwareRequirements>
            <description><![CDATA[<h3>Added</h3>
<ul>
<li>Option to group all connection tabs in one window instead of separate windows per connection</li>
</ul>
<h3>Changed</h3>
<ul>
<li>Separate preferred themes for Light and Dark appearance modes, with automatic switching in Auto mode</li>
</ul>
]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.27.2/TablePro-0.27.2-arm64.zip" length="26384688" type="application/octet-stream" sparkle:edSignature="9Q/8sA4sCcrAFxF29MX02af+BOaGUh0YtQeWBQ4N3kMd7erTYL06fzfRuPfBINKom9F+4C+p/+UFWPWSvP8HCg=="/>
        </item>
        <item>
            <title>0.27.2</title>
            <pubDate>Thu, 02 Apr 2026 06:21:45 +0000</pubDate>
            <sparkle:version>55</sparkle:version>
            <sparkle:shortVersionString>0.27.2</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <description><![CDATA[<h3>Added</h3>
<ul>
<li>Option to group all connection tabs in one window instead of separate windows per connection</li>
</ul>
<h3>Changed</h3>
<ul>
<li>Separate preferred themes for Light and Dark appearance modes, with automatic switching in Auto mode</li>
</ul>
]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.27.2/TablePro-0.27.2-x86_64.zip" length="23317588" type="application/octet-stream" sparkle:edSignature="PdQKj619//HV3pB9EYSILbm4VgMFh0qFqcSt+HBAN2rTNSZtUzpLm04oTd7LUdwuHs4aSHhmdjRmfUr9WkSfAg=="/>
        </item>
        <item>
            <title>0.27.1</title>
            <pubDate>Wed, 01 Apr 2026 02:06:09 +0000</pubDate>
            <sparkle:version>54</sparkle:version>
            <sparkle:shortVersionString>0.27.1</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <sparkle:hardwareRequirements>arm64</sparkle:hardwareRequirements>
            <description><![CDATA[<body style="font-family: -apple-system, sans-serif; font-size: 13px; padding: 8px;"><h3>Fixed</h3>
<ul>
<li>Table queries incorrectly prefixed with connection username as schema name on non-schema databases (MySQL, MariaDB, ClickHouse, Redis, etc.), causing "Table 'username.table' doesn't exist" errors when opening a second table tab</li>
</ul></body>]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.27.1/TablePro-0.27.1-arm64.zip" length="26377697" type="application/octet-stream" sparkle:edSignature="y2dHsqx7Osx7VKogYzQ/DA2dhUnmd+si1UzLLbmyo/V9u1O05AWboycXvJueUoAnDN6LQvZ3UNnVE1kFyR9tDw=="/>
        </item>
        <item>
            <title>0.27.1</title>
            <pubDate>Wed, 01 Apr 2026 02:06:09 +0000</pubDate>
            <sparkle:version>54</sparkle:version>
            <sparkle:shortVersionString>0.27.1</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <description><![CDATA[<body style="font-family: -apple-system, sans-serif; font-size: 13px; padding: 8px;"><h3>Fixed</h3>
<ul>
<li>Table queries incorrectly prefixed with connection username as schema name on non-schema databases (MySQL, MariaDB, ClickHouse, Redis, etc.), causing "Table 'username.table' doesn't exist" errors when opening a second table tab</li>
</ul></body>]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.27.1/TablePro-0.27.1-x86_64.zip" length="23306298" type="application/octet-stream" sparkle:edSignature="sNCHw0MYMo2ytKD2sujeoK30S1fXvPCLXoqjGqx6HG78x0fo0cB/6eAnirx6yQ0YW/geo3RoJ3+B/gkVqv6FCQ=="/>
        </item>
        <item>
            <title>0.27.0</title>
            <pubDate>Tue, 31 Mar 2026 16:33:58 +0000</pubDate>
            <sparkle:version>53</sparkle:version>
            <sparkle:shortVersionString>0.27.0</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <sparkle:hardwareRequirements>arm64</sparkle:hardwareRequirements>
            <description><![CDATA[<body style="font-family: -apple-system, sans-serif; font-size: 13px; padding: 8px;"><h3>Added</h3>
<ul>
<li>Option to prompt for database password on every connection instead of saving to Keychain</li>
<li>Autocompletion for filter fields: column names and SQL keywords suggested as you type (Raw SQL and Value fields)</li>
<li>Multi-line support for Raw SQL filter field (Option+Enter for newline)</li>
<li>Visual Create Table UI with multi-database support (sidebar → "Create New Table...")</li>
<li>Auto-fit column width: double-click column divider or right-click → "Size to Fit"</li>
<li>Collapsible results panel (`Cmd+Opt+R`), multiple result tabs for multi-statement queries, result pinning</li>
<li>Inline error banner for query errors</li>
<li>JSON syntax highlighting and brace matching in Details sidebar and JSON editor popover</li>
<li>Database-aware SQL functions in field menu (MySQL, PostgreSQL, SQLite, SQL Server, ClickHouse)</li>
</ul>
<h3>Changed</h3>
<ul>
<li>Replace GCD dispatch patterns with Swift structured concurrency</li>
<li>Refactor Details sidebar into modular field editor architecture with extracted editor components</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>PostgreSQL: Schema name lost after app restart, causing "relation does not exist" errors for non-public schemas</li>
<li>Error dialog OK button not dismissing when a SwiftUI sheet is active, making the app unusable</li>
<li>SQL Server: Unicode characters (Thai, CJK, etc.) in nvarchar/nchar/ntext columns displaying as question marks</li>
<li>Globe+F (fn+F) fullscreen shortcut not working in SwiftUI lifecycle app</li>
</ul></body>]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.27.0/TablePro-0.27.0-arm64.zip" length="26378023" type="application/octet-stream" sparkle:edSignature="n53O8RIe3dSnxsDNkM8gfwPGKiu/vhp1G75fHA0DKZxA2m+zx4HU7Y7gSnZhMMksmtHf34SQoOOIEzoeIhKfDQ=="/>
        </item>
        <item>
            <title>0.27.0</title>
            <pubDate>Tue, 31 Mar 2026 16:33:58 +0000</pubDate>
            <sparkle:version>53</sparkle:version>
            <sparkle:shortVersionString>0.27.0</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <description><![CDATA[<body style="font-family: -apple-system, sans-serif; font-size: 13px; padding: 8px;"><h3>Added</h3>
<ul>
<li>Option to prompt for database password on every connection instead of saving to Keychain</li>
<li>Autocompletion for filter fields: column names and SQL keywords suggested as you type (Raw SQL and Value fields)</li>
<li>Multi-line support for Raw SQL filter field (Option+Enter for newline)</li>
<li>Visual Create Table UI with multi-database support (sidebar → "Create New Table...")</li>
<li>Auto-fit column width: double-click column divider or right-click → "Size to Fit"</li>
<li>Collapsible results panel (`Cmd+Opt+R`), multiple result tabs for multi-statement queries, result pinning</li>
<li>Inline error banner for query errors</li>
<li>JSON syntax highlighting and brace matching in Details sidebar and JSON editor popover</li>
<li>Database-aware SQL functions in field menu (MySQL, PostgreSQL, SQLite, SQL Server, ClickHouse)</li>
</ul>
<h3>Changed</h3>
<ul>
<li>Replace GCD dispatch patterns with Swift structured concurrency</li>
<li>Refactor Details sidebar into modular field editor architecture with extracted editor components</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>PostgreSQL: Schema name lost after app restart, causing "relation does not exist" errors for non-public schemas</li>
<li>Error dialog OK button not dismissing when a SwiftUI sheet is active, making the app unusable</li>
<li>SQL Server: Unicode characters (Thai, CJK, etc.) in nvarchar/nchar/ntext columns displaying as question marks</li>
<li>Globe+F (fn+F) fullscreen shortcut not working in SwiftUI lifecycle app</li>
</ul></body>]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.27.0/TablePro-0.27.0-x86_64.zip" length="23306571" type="application/octet-stream" sparkle:edSignature="6VU+4T6agdBHgCCNiJIafAQtVlQaOxctK81qfsXOQU7Bhxh5eF3wpHLYKioDlKOgxSJb7R+EjoLenZZWmMNwCg=="/>
        </item>
        <item>
            <title>0.26.0</title>
            <pubDate>Sun, 29 Mar 2026 15:52:19 +0000</pubDate>
            <sparkle:version>52</sparkle:version>
            <sparkle:shortVersionString>0.26.0</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <sparkle:hardwareRequirements>arm64</sparkle:hardwareRequirements>
            <description><![CDATA[<body style="font-family: -apple-system, sans-serif; font-size: 13px; padding: 8px;"><h3>Added</h3>
<ul>
<li>Global toggle to disable all AI features (Settings > AI)</li>
<li>Drag to reorder columns in the Structure tab (MySQL/MariaDB)</li>
<li>Nested hierarchical groups for connection list (up to 3 levels deep)</li>
<li>Confirmation dialogs for deep link queries, connection imports, and pre-connect scripts</li>
<li>JSON fields in Row Details sidebar now display in a scrollable monospaced text area</li>
<li>Open, save, and save-as for SQL files with native macOS title bar integration (#475)</li>
<li>BigQuery plugin support (Google BigQuery analytics via REST API)</li>
</ul>
<h3>Changed</h3>
<ul>
<li>Removed query history sync from iCloud Sync (connections, groups, settings, and SSH profiles still sync)</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>SQL editor not auto-focused on new tab and cursor missing after tab switch</li>
<li>Long lines not scrollable horizontally in the SQL editor</li>
<li>Home and End keys not moving cursor in the SQL editor (#448)</li>
<li>SSH profile lost after app restart when iCloud Sync enabled</li>
<li>MariaDB JSON columns showing as hex dumps instead of JSON text</li>
<li>MongoDB Atlas TLS certificate verification failure</li>
<li>ENUM/SET dropdown chevron buttons not showing on first table open</li>
</ul></body>]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.26.0/TablePro-0.26.0-arm64.zip" length="26177098" type="application/octet-stream" sparkle:edSignature="diNKGmXTDnmind3UW/mRVDYgBIoYUiADz5O3MAHjcb0Y0/Zzo3PQKd65QNoCbX3jC5MbT1ptts8K24JuoPGoBg=="/>
        </item>
        <item>
            <title>0.26.0</title>
            <pubDate>Sun, 29 Mar 2026 15:52:19 +0000</pubDate>
            <sparkle:version>52</sparkle:version>
            <sparkle:shortVersionString>0.26.0</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <description><![CDATA[<body style="font-family: -apple-system, sans-serif; font-size: 13px; padding: 8px;"><h3>Added</h3>
<ul>
<li>Global toggle to disable all AI features (Settings > AI)</li>
<li>Drag to reorder columns in the Structure tab (MySQL/MariaDB)</li>
<li>Nested hierarchical groups for connection list (up to 3 levels deep)</li>
<li>Confirmation dialogs for deep link queries, connection imports, and pre-connect scripts</li>
<li>JSON fields in Row Details sidebar now display in a scrollable monospaced text area</li>
<li>Open, save, and save-as for SQL files with native macOS title bar integration (#475)</li>
<li>BigQuery plugin support (Google BigQuery analytics via REST API)</li>
</ul>
<h3>Changed</h3>
<ul>
<li>Removed query history sync from iCloud Sync (connections, groups, settings, and SSH profiles still sync)</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>SQL editor not auto-focused on new tab and cursor missing after tab switch</li>
<li>Long lines not scrollable horizontally in the SQL editor</li>
<li>Home and End keys not moving cursor in the SQL editor (#448)</li>
<li>SSH profile lost after app restart when iCloud Sync enabled</li>
<li>MariaDB JSON columns showing as hex dumps instead of JSON text</li>
<li>MongoDB Atlas TLS certificate verification failure</li>
<li>ENUM/SET dropdown chevron buttons not showing on first table open</li>
</ul></body>]]></description>
            <enclosure url="https://github.com/TableProApp/TablePro/releases/download/v0.26.0/TablePro-0.26.0-x86_64.zip" length="23103948" type="application/octet-stream" sparkle:edSignature="0SKMK5+lQyDLfMr1zHfzeH+h3QjGtrz5rhRmEFAayER09mNaQhNN9shycatOIOoTVHcroYGW2exw4C27uZpMCg=="/>
        </item>
    </channel>
</rss>