The {name} driver is not in the app. Picking {name} in the **Choose a Database** sheet offers the
download before the form opens, and opening a saved {name} connection installs it without asking.
**Settings > Plugins > Browse > {plugin}** installs it up front. See [Plugins](/features/plugins).
